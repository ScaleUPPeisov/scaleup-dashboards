--- a/src/ScheduleOS.tsx
+++ b/src/ScheduleOS.tsx
@@ -6,6 +6,7 @@
 import {beginManualYoutubeQuotaProbe,isYoutubeQuotaError,markYoutubeQuotaExceeded,planYoutubeQuota,releaseYoutubeQuotaReservation,reserveYoutubeQuota,youtubeOperationActualCost,youtubeQuotaMessage,youtubeQuotaState} from './youtubeQuota';
 import {markYoutubeCache} from './youtubeCache';
 import {sortChannelsAlphabetically} from './channelSort';
+import {loadActivePublishChannel,saveActivePublishChannel} from './publishWorkspaceState';
 import {buildScheduleOnlyPlan,clearScheduleRecovery,futureScheduledVideos,inferScheduleSummary,kratTime,loadScheduleRecovery,privateNoScheduleVideos,recoveryCounts,recoveryFromPlan,saveScheduleRecovery,type ScheduleOnlyMode,type ScheduleOnlyRecovery,type ScheduleOnlyStartMode} from './scheduleOnly';
 import './scheduleOnly.css';
 
@@ -14,12 +15,12 @@
 export const scheduleChannelSwitchReset=(channelId:string)=>({channelId,videos:[] as YoutubeExistingVideo[],cacheUpdatedAt:'',selectedIds:[] as string[],mode:'daily' as ScheduleOnlyMode,startMode:'preserveFirst' as ScheduleOnlyStartMode,manualStartDate:'',time:'04:00',intervalDays:2,publishDays:3,pauseDays:1,selectFrom:'',confirm:false,recovery:undefined as ScheduleOnlyRecovery|undefined,lastActualCost:null as number|null,selectedOnly:false});
 export function ScheduleOS(){
  const channels=useApp(s=>s.channels),toast=useApp(s=>s.toast);
- const [channelId,setChannelId]=useState(channels[0]?.id||''),[videos,setVideos]=useState<YoutubeExistingVideo[]>([]),[cacheUpdatedAt,setCacheUpdatedAt]=useState(''),[busy,setBusy]=useState(false),[selectedIds,setSelectedIds]=useState<string[]>([]),[mode,setMode]=useState<ScheduleOnlyMode>('daily'),[startMode,setStartMode]=useState<ScheduleOnlyStartMode>('preserveFirst'),[manualStartDate,setManualStartDate]=useState(''),[time,setTime]=useState('04:00'),[intervalDays,setIntervalDays]=useState(2),[publishDays,setPublishDays]=useState(3),[pauseDays,setPauseDays]=useState(1),[selectFrom,setSelectFrom]=useState(''),[confirm,setConfirm]=useState(false),[recovery,setRecovery]=useState<ScheduleOnlyRecovery|undefined>(),[lastActualCost,setLastActualCost]=useState<number|null>(null),[selectedOnly,setSelectedOnly]=useState(false);
+ const [channelId,setChannelId]=useState(()=>{const saved=loadActivePublishChannel();return channels.length?(channels.some(c=>c.id===saved)?saved:channels[0].id):saved}),[videos,setVideos]=useState<YoutubeExistingVideo[]>([]),[cacheUpdatedAt,setCacheUpdatedAt]=useState(''),[busy,setBusy]=useState(false),[selectedIds,setSelectedIds]=useState<string[]>([]),[mode,setMode]=useState<ScheduleOnlyMode>('daily'),[startMode,setStartMode]=useState<ScheduleOnlyStartMode>('preserveFirst'),[manualStartDate,setManualStartDate]=useState(''),[time,setTime]=useState('04:00'),[intervalDays,setIntervalDays]=useState(2),[publishDays,setPublishDays]=useState(3),[pauseDays,setPauseDays]=useState(1),[selectFrom,setSelectFrom]=useState(''),[confirm,setConfirm]=useState(false),[recovery,setRecovery]=useState<ScheduleOnlyRecovery|undefined>(),[lastActualCost,setLastActualCost]=useState<number|null>(null),[selectedOnly,setSelectedOnly]=useState(false);
  const channel=channels.find(c=>c.id===channelId),profileId=channel?.youtubeProfileId||'';
  function loadLocal(id:string){if(!id){setVideos([]);setCacheUpdatedAt('');setSelectedIds([]);setRecovery(undefined);return}const cache=readExistingCache(id);const rows=(cache?.videos||[]).map(v=>({...v,tags:[...(v.tags||[])]}));setVideos(rows);setCacheUpdatedAt(cache?.updatedAt||'');setSelectedIds([]);const summary=inferScheduleSummary(rows);if(summary.time!=='—'&&summary.time!=='разное')setTime(summary.time);setRecovery(loadScheduleRecovery(id));}
- function switchChannel(nextId:string){if(nextId===channelId)return;const reset=scheduleChannelSwitchReset(nextId);setVideos(reset.videos);setCacheUpdatedAt(reset.cacheUpdatedAt);setSelectedIds(reset.selectedIds);setMode(reset.mode);setStartMode(reset.startMode);setManualStartDate(reset.manualStartDate);setTime(reset.time);setIntervalDays(reset.intervalDays);setPublishDays(reset.publishDays);setPauseDays(reset.pauseDays);setSelectFrom(reset.selectFrom);setConfirm(reset.confirm);setRecovery(reset.recovery);setLastActualCost(reset.lastActualCost);setSelectedOnly(reset.selectedOnly);setChannelId(reset.channelId)}
+ function switchChannel(nextId:string){if(nextId===channelId)return;if(nextId&&!channels.some(c=>c.id===nextId))return;const reset=scheduleChannelSwitchReset(nextId);setVideos(reset.videos);setCacheUpdatedAt(reset.cacheUpdatedAt);setSelectedIds(reset.selectedIds);setMode(reset.mode);setStartMode(reset.startMode);setManualStartDate(reset.manualStartDate);setTime(reset.time);setIntervalDays(reset.intervalDays);setPublishDays(reset.publishDays);setPauseDays(reset.pauseDays);setSelectFrom(reset.selectFrom);setConfirm(reset.confirm);setRecovery(reset.recovery);setLastActualCost(reset.lastActualCost);setSelectedOnly(reset.selectedOnly);if(reset.channelId)saveActivePublishChannel(reset.channelId);setChannelId(reset.channelId)}
  const channelIdsKey=channels.map(c=>c.id).join('\u0000');
- useEffect(()=>{if(!channels.length){if(channelId)switchChannel('');else loadLocal('');return}if(!channels.some(c=>c.id===channelId)){switchChannel(channels[0].id);return}loadLocal(channelId)},[channelId,channelIdsKey]);
+ useEffect(()=>{if(!channels.length){loadLocal('');return}if(!channels.some(c=>c.id===channelId)){switchChannel(channels[0].id);return}if(loadActivePublishChannel()!==channelId)saveActivePublishChannel(channelId);loadLocal(channelId)},[channelId,channelIdsKey]);
  const future=useMemo(()=>futureScheduledVideos(videos),[videos]),noSchedule=useMemo(()=>privateNoScheduleVideos(videos),[videos]),summary=useMemo(()=>inferScheduleSummary(videos),[videos]);
  const cacheAge=cacheUpdatedAt?Date.now()-Date.parse(cacheUpdatedAt):Number.POSITIVE_INFINITY,cacheStale=videos.length>0&&(!Number.isFinite(cacheAge)||cacheAge>CACHE_FRESH_MS);
  const plan=useMemo(()=>buildScheduleOnlyPlan({channelId,videos,selectedIds,mode,startMode,manualStartDate,time,intervalDays,publishDays,pauseDays}),[channelId,videos,selectedIds,mode,startMode,manualStartDate,time,intervalDays,publishDays,pauseDays]);
--- a/src/scheduleChannelSelector.test.ts
+++ b/src/scheduleChannelSelector.test.ts
@@ -7,7 +7,9 @@
 import {readFileSync} from 'node:fs';
 
 const storeFixture=vi.hoisted(()=>({channels:[] as Channel[],toast:(_message:string)=>{}}));
+const activeChannelFixture=vi.hoisted(()=>({id:'',saved:[] as string[]}));
 vi.mock('./store',()=>({useApp:(selector:(state:typeof storeFixture)=>unknown)=>selector(storeFixture)}));
+vi.mock('./publishWorkspaceState',()=>({loadActivePublishChannel:()=>activeChannelFixture.id,saveActivePublishChannel:(channelId:string)=>{activeChannelFixture.id=channelId;activeChannelFixture.saved.push(channelId)}}));
 
 const ui=readFileSync(decodeURIComponent(new URL('./ScheduleOS.tsx',import.meta.url).pathname),'utf8');
 const channel=(id:string,name=`Channel ${id}`,profile=`profile-${id}`):Channel=>({id,name,slug:id,cadenceDays:2,targetBufferDays:60,publishHour:4,publishMinute:0,language:'EN',genre:'Music',country:'US',minTracks:10,targetDurationMin:120,enabled:true,youtubeProfileId:profile,seo:{titlePatterns:[],descriptionTemplate:'',tags:[],banned:[]}});
@@ -15,7 +17,7 @@
 function render(channels:Channel[]){storeFixture.channels=channels;return renderToStaticMarkup(React.createElement(ScheduleOS))}
 
 describe('Schedule channel selector isolation',()=>{
- beforeEach(()=>{vi.restoreAllMocks();storeFixture.channels=[]});
+ beforeEach(()=>{vi.restoreAllMocks();storeFixture.channels=[];activeChannelFixture.id='';activeChannelFixture.saved=[]});
  it('TEST 1 — renders an explicit channel selector when multiple channels exist',()=>{const html=render([channel('a','Channel A'),channel('b','Channel B')]);expect(html).toContain('aria-label="Канал"');expect(html).toContain('<select');});
  it('TEST 2 — exposes all 31 channels as options',()=>{const channels=Array.from({length:31},(_,i)=>channel(`c${i+1}`,`Channel ${String(i+1).padStart(2,'0')}`));const html=render(channels);expect((html.match(/<option/g)||[])).toHaveLength(31);for(const c of channels)expect(html).toContain(`value="${c.id}"`)});
  it('TEST 3 — switching A → B resets transient A data and changes the plan context to B',()=>{const reset=scheduleChannelSwitchReset('b');expect(reset.channelId).toBe('b');expect(reset.videos).toEqual([]);const b=[video('b1','2026-09-20T21:00:00.000Z')];const plan=buildScheduleOnlyPlan({channelId:reset.channelId,videos:b,selectedIds:['b1'],mode:'daily',startMode:'preserveFirst',time:'04:00',intervalDays:2,publishDays:3,pauseDays:1,now:new Date('2026-09-13T00:00:00Z')});expect(plan.channelId).toBe('b');expect(plan.rows.every(r=>r.videoId.startsWith('b'))).toBe(true)});
@@ -24,7 +26,12 @@
  it('TEST 6 — cache loading is keyed only by current channelId and stale UI cache is cleared before load',()=>{expect(ui).toContain('readExistingCache(id)');expect(ui).toContain("setCacheUpdatedAt(reset.cacheUpdatedAt)");expect(ui).toContain('loadLocal(channelId)')});
  it('TEST 7 — sync resolves OAuth profile from selected channel and writes cache under selected channelId',()=>{expect(ui).toContain("profileId=channel?.youtubeProfileId||''");expect(ui).toContain('api.youtubeListExisting(profileId,5000)');expect(ui).toContain('replaceExistingCacheFromSync(channelId,rows,r)');expect(ui).toContain("onChange={e=>switchChannel(e.target.value)}")});
  it('TEST 8 — A → B → A starts each context with an empty selection and no leaked draft/confirmation state',()=>{for(const id of ['b','a']){const x=scheduleChannelSwitchReset(id);expect(x.channelId).toBe(id);expect(x.selectedIds).toEqual([]);expect(x.confirm).toBe(false);expect(x.recovery).toBeUndefined();expect(x.selectedOnly).toBe(false);expect(x.manualStartDate).toBe('')}});
- it('TEST 9 — zero channels renders a safe empty state',()=>{const html=render([]);expect(html).toContain('Нет подключённых каналов');expect(html).not.toContain('<option')});
- it('TEST 10 — one channel remains selectable and normal',()=>{const html=render([channel('only','Only Channel')]);expect((html.match(/<option/g)||[])).toHaveLength(1);expect(html).toContain('Only Channel');expect(html).toContain('СИНХРОНИЗИРОВАТЬ С YOUTUBE')});
- it('preserves ZERO DOCX / schedule-only write contract',()=>{expect(ui).toContain('SCHEDULE-ONLY • ZERO DOCX');expect(ui).toContain('youtubeUpdateExistingSchedule');expect(ui).toContain('Metadata writes <b>0</b>');expect(ui).toContain('Thumbnail writes <b>0</b>');expect(ui).toContain('Playlist writes <b>0</b>');expect(ui).toContain('videos.insert <b>0</b>')});
+ it('TEST 9 — persisted Channel B is restored when it exists',()=>{activeChannelFixture.id='b';const html=render([channel('a','Channel A'),channel('b','Channel B')]);expect(html).toContain('<option value="b" selected="">Channel B</option>');expect(ui).toContain('loadActivePublishChannel()')});
+ it('TEST 10 — switch persistence only accepts a resolved channel and records A → B → A',()=>{expect(ui).toContain('if(nextId&&!channels.some(c=>c.id===nextId))return');expect(ui).toContain('saveActivePublishChannel(reset.channelId)');for(const id of ['b','a']){const x=scheduleChannelSwitchReset(id);expect(x.channelId).toBe(id)}expect(ui).toContain("onChange={e=>switchChannel(e.target.value)}")});
+ it('TEST 11 — remount persistence restores B from the existing application context',()=>{activeChannelFixture.id='b';const channels=[channel('a','Channel A'),channel('b','Channel B')];expect(render(channels)).toContain('<option value="b" selected="">Channel B</option>');expect(render(channels)).toContain('<option value="b" selected="">Channel B</option>')});
+ it('TEST 12 — invalid persisted channel falls back safely to the first connected channel',()=>{activeChannelFixture.id='missing';const html=render([channel('a','Channel A'),channel('b','Channel B')]);expect(html).toContain('<option value="a" selected="">Channel A</option>');expect(html).not.toContain('value="missing"')});
+ it('TEST 13 — zero channels renders a safe empty state',()=>{activeChannelFixture.id='b';const html=render([]);expect(html).toContain('Нет подключённых каналов');expect(html).not.toContain('<option')});
+ it('TEST 14 — one channel remains selectable and normal',()=>{activeChannelFixture.id='missing';const html=render([channel('only','Only Channel')]);expect((html.match(/<option/g)||[])).toHaveLength(1);expect(html).toContain('Only Channel');expect(html).toContain('СИНХРОНИЗИРОВАТЬ С YOUTUBE')});
+ it('TEST 15 — restored B keeps selection, cache, sync and apply isolated to B',()=>{activeChannelFixture.id='b';const html=render([channel('a','Channel A'),channel('b','Channel B','profile-b')]);expect(html).toContain('<option value="b" selected="">Channel B</option>');expect(scheduleChannelSwitchReset('b').selectedIds).toEqual([]);expect(ui).toContain('readExistingCache(id)');expect(ui).toContain("profileId=channel?.youtubeProfileId||''");expect(ui).toContain('api.youtubeListExisting(profileId,5000)');expect(ui).toContain('replaceExistingCacheFromSync(channelId,rows,r)');expect(ui).toContain('const rec=recoveryFromPlan(plan)');expect(ui).toContain('mergeExistingCacheVideos(channelId,[updated])')});
+ it('TEST 16 — preserves ZERO DOCX / schedule-only write contract',()=>{expect(ui).toContain('SCHEDULE-ONLY • ZERO DOCX');expect(ui).toContain('youtubeUpdateExistingSchedule');expect(ui).toContain('Metadata writes <b>0</b>');expect(ui).toContain('Thumbnail writes <b>0</b>');expect(ui).toContain('Playlist writes <b>0</b>');expect(ui).toContain('videos.insert <b>0</b>')});
 });
