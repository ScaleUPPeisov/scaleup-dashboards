name: Retired ReelsFactory v0.2.3 publisher

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'ReelsFactory v0.2.3 is already published; use the current release pipeline.'
