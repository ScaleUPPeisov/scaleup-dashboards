name: VYRON 1.0.7 Downloads Hotfix Release

on:
  push:
    branches: [vyron-v107-downloads-hotfix]
    paths: ['vyron-v107-release-trigger.txt']
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: vyron-v107-downloads-hotfix-release
  cancel-in-progress: false

jobs:
  release-macos-arm64:
    runs-on: macos-latest
    timeout-minutes: 90
    env:
      TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
      TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
      TAURI_UPDATER_PUBLIC_KEY: ${{ secrets.TAURI_UPDATER_PUBLIC_KEY }}
      APPLE_SIGNING_IDENTITY: "-"
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout VYRON 1.0.7 hotfix branch
        uses: actions/checkout@v4

      - name: Verify branch and current live 1.0.6
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "vyron-v107-downloads-hotfix"
          curl -fsSL 'https://raw.githubusercontent.com/ScaleUPPeisov/scaleup-dashboards/main/vyron-updates/latest.json' -o /tmp/current-live.json
          python3 - <<'PY'
          import json
          x=json.load(open('/tmp/current-live.json'))
          assert x['version']=='1.0.6', f"Expected live 1.0.6 before hotfix, got {x['version']}"
          print('Current live updater 1.0.6: PASS')
          PY

      - name: Normalize updater keys using existing VYRON trust
        shell: bash
        run: |
          set -euo pipefail
          test -n "${TAURI_SIGNING_PRIVATE_KEY:-}"
          test -n "${TAURI_UPDATER_PUBLIC_KEY:-}"
          python3 - <<'PY'
          import base64,os
          def dec(v):
              try:return base64.b64decode(v.strip(),validate=True).decode()
              except Exception:return None
          def norm(name,kind):
              raw=os.environ[name].strip();once=dec(raw)
              if once and once.startswith('untrusted comment:'): out,box=raw,once
              else:
                  if not once: raise SystemExit(f'{name}: invalid encoding')
                  out=once.strip();box=dec(out)
                  if not box or not box.startswith('untrusted comment:'): raise SystemExit(f'{name}: not a Tauri key')
              if kind not in box.splitlines()[0].lower(): raise SystemExit(f'{name}: wrong key type')
              return out
          sk=norm('TAURI_SIGNING_PRIVATE_KEY','secret key');pk=norm('TAURI_UPDATER_PUBLIC_KEY','public key')
          print(f'::add-mask::{sk}')
          with open(os.environ['GITHUB_ENV'],'a') as f:
              f.write(f'TAURI_SIGNING_PRIVATE_KEY={sk}\nTAURI_UPDATER_PUBLIC_KEY={pk}\n')
          print('Updater key normalization: PASS')
          PY

      - name: Restore exact released VYRON 1.0.6 source
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .vyron-v107 /tmp/vyron-v106-assets
          mkdir -p .vyron-v107 /tmp/vyron-v106-assets
          gh release download v1.0.6 --repo ScaleUPPeisov/scaleup-dashboards --pattern 'VYRON-1.0.6-source.tar.gz' --pattern 'SOURCE_SHA256.txt' --dir /tmp/vyron-v106-assets
          test -s /tmp/vyron-v106-assets/VYRON-1.0.6-source.tar.gz
          test -s /tmp/vyron-v106-assets/SOURCE_SHA256.txt
          EXPECTED="$(awk '{print $1; exit}' /tmp/vyron-v106-assets/SOURCE_SHA256.txt)"
          GOT="$(shasum -a 256 /tmp/vyron-v106-assets/VYRON-1.0.6-source.tar.gz | awk '{print $1}')"
          test "$EXPECTED" = "$GOT"
          tar -xzf /tmp/vyron-v106-assets/VYRON-1.0.6-source.tar.gz -C .vyron-v107
          grep -q '"version": "1.0.6"' .vyron-v107/package.json
          python3 - <<'PY'
          import hashlib,json
          from pathlib import Path
          root=Path('.vyron-v107')
          c=json.loads((root/'src-tauri/tauri.conf.json').read_text())
          Path('/tmp/v106-identifier').write_text(c['identifier'])
          Path('/tmp/v106-pubkey').write_text(c['plugins']['updater']['pubkey'])
          Path('/tmp/v106-endpoints').write_text(json.dumps(c['plugins']['updater']['endpoints']))
          protected=[
            'src-tauri/src/youtube.rs','src-tauri/src/storage.rs','src-tauri/src/files.rs',
            'src/api.ts','src/store.ts','src/types.ts','src/YouTubeCenter.tsx',
            'src/AnalyticsPage.tsx','src/CompetitorsPage.tsx','src/SettingsOS.tsx',
            'src/ProductionOS.tsx','src/ProductionWorkspace.tsx','src/ProductionManager.tsx',
            'src/productionManagerApi.ts','src/productionPrefs.ts'
          ]
          Path('/tmp/v106-protected.json').write_text(json.dumps({p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in protected},sort_keys=True))
          print('Exact 1.0.6 release source restored: PASS')
          PY

      - name: Apply only VYRON 1.0.7 Downloads collector hotfix
        run: python3 vyron-v100/apply_v107.py .vyron-v107

      - name: Verify scope identity updater and Zero Quota
        shell: bash
        run: |
          set -euo pipefail
          cd .vyron-v107
          python3 - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          p=json.loads(Path('package.json').read_text());c=json.loads(Path('src-tauri/tauri.conf.json').read_text())
          assert p['version']=='1.0.7' and c['version']=='1.0.7'
          assert c['identifier']==Path('/tmp/v106-identifier').read_text()=='studio.channelflow.desktop'
          assert c['plugins']['updater']['pubkey']==Path('/tmp/v106-pubkey').read_text()
          assert c['plugins']['updater']['pubkey'].strip()==os.environ['TAURI_UPDATER_PUBLIC_KEY'].strip()
          assert json.dumps(c['plugins']['updater']['endpoints'])==Path('/tmp/v106-endpoints').read_text()
          assert c['plugins']['updater']['endpoints']==['https://raw.githubusercontent.com/ScaleUPPeisov/scaleup-dashboards/main/vyron-updates/latest.json']
          assert c['bundle']['createUpdaterArtifacts'] is True
          protected=json.loads(Path('/tmp/v106-protected.json').read_text())
          for f,h in protected.items():
              got=hashlib.sha256(Path(f).read_bytes()).hexdigest()
              assert got==h, f'OUT-OF-SCOPE FILE CHANGED: {f}'
          print('Identity/updater/out-of-scope protection: PASS')
          PY
          grep -Fq 'collector_seen_at_start(&session,&baseline)' src-tauri/src/production_manager.rs
          ! grep -Fq 'let mut seen=baseline;' src-tauri/src/production_manager.rs
          grep -Fq 'recursive_images(&downloads)' src-tauri/src/production_manager.rs
          grep -Fq 'if !import_candidate_ready(&src,&mut pending){continue;}' src-tauri/src/production_manager.rs
          grep -Fq 'fs::copy(&src,&tmp)' src-tauri/src/production_manager.rs
          grep -Fq 'acceptance_existing_downloads_are_collectible_on_start' src-tauri/src/production_manager_tests.rs
          ! grep -RniE 'youtube_upload_video|youtube_list_existing_videos|youtube_update_existing_video|youtube_channel_analytics|youtube_channel_stats|youtube_oauth_profile_health|ytInvoke|googleapis\.com' src/ProductionManager.tsx src/productionManagerApi.ts src/productionPrefs.ts src-tauri/src/production_manager.rs
          echo '1.0.7 Downloads collector + Zero Quota contracts: PASS'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Rust ARM64
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: .vyron-v107/src-tauri
          cache-on-failure: true

      - name: Install JS dependencies
        working-directory: .vyron-v107
        run: npm install --no-audit --no-fund

      - name: Frontend regression tests
        working-directory: .vyron-v107
        run: npm test

      - name: TypeScript production build
        working-directory: .vyron-v107
        run: npm run build

      - name: Rust Production acceptance tests
        working-directory: .vyron-v107
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib -- --nocapture

      - name: Cargo check ARM64
        working-directory: .vyron-v107
        run: cargo check --manifest-path src-tauri/Cargo.toml --target aarch64-apple-darwin

      - name: Prevent updater private key leakage
        working-directory: .vyron-v107
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          from pathlib import Path
          secret=os.environ['TAURI_SIGNING_PRIVATE_KEY'].strip().encode()
          for root in [Path('src'),Path('dist'),Path('src-tauri/src')]:
              if not root.exists(): continue
              for p in root.rglob('*'):
                  if p.is_file() and secret and secret in p.read_bytes():
                      raise SystemExit(f'PRIVATE UPDATER KEY LEAK: {p}')
          print('Private updater key leak scan: PASS')
          PY

      - name: Build ARM64 app DMG and signed updater
        working-directory: .vyron-v107
        run: npx tauri build --target aarch64-apple-darwin --bundles app,dmg

      - name: Verify updater signature against existing trust key
        working-directory: .vyron-v107
        shell: bash
        run: |
          set -euo pipefail
          UPDATER=$(find src-tauri/target/aarch64-apple-darwin/release/bundle -name '*.app.tar.gz' -print -quit)
          SIG=$(find src-tauri/target/aarch64-apple-darwin/release/bundle -name '*.app.tar.gz.sig' -print -quit)
          test -n "$UPDATER" && test -s "$UPDATER" && test -n "$SIG" && test -s "$SIG"
          command -v minisign >/dev/null 2>&1 || brew install minisign
          base64 -D -i /tmp/v106-pubkey -o /tmp/vyron-existing.pub
          base64 -D -i "$SIG" -o /tmp/vyron-v107.minisig
          minisign -Vm "$UPDATER" -p /tmp/vyron-existing.pub -x /tmp/vyron-v107.minisig
          echo 'Updater signature verification: PASS'

      - name: Verify app DMG and package release
        working-directory: .vyron-v107
        shell: bash
        run: |
          set -euo pipefail
          APP=$(find src-tauri/target/aarch64-apple-darwin/release/bundle/macos -maxdepth 1 -name 'VYRON.app' -print -quit)
          BIN=$(find "$APP/Contents/MacOS" -maxdepth 1 -type f -perm -111 -print -quit)
          BUILT_DMG=$(find src-tauri/target/aarch64-apple-darwin/release/bundle/dmg -name '*.dmg' -print -quit)
          UPDATER=$(find src-tauri/target/aarch64-apple-darwin/release/bundle -name '*.app.tar.gz' -print -quit)
          SIG=$(find src-tauri/target/aarch64-apple-darwin/release/bundle -name '*.app.tar.gz.sig' -print -quit)
          test -n "$APP" && test -d "$APP/Contents" && test -n "$BIN" && test -s "$BIN"
          test -n "$BUILT_DMG" && test -s "$BUILT_DMG" && test -n "$UPDATER" && test -s "$UPDATER" && test -n "$SIG" && test -s "$SIG"
          test "$(/usr/bin/plutil -extract CFBundleIdentifier raw -o - "$APP/Contents/Info.plist")" = "$(cat /tmp/v106-identifier)"
          test "$(/usr/bin/plutil -extract CFBundleShortVersionString raw -o - "$APP/Contents/Info.plist")" = "1.0.7"
          file "$BIN" | tee /tmp/vyron-v107-file.txt
          grep -E 'arm64|Mach-O 64-bit executable arm64' /tmp/vyron-v107-file.txt
          codesign --verify --deep --strict --verbose=2 "$APP"
          hdiutil verify "$BUILT_DMG"
          rm -rf release && mkdir release
          cp "$BUILT_DMG" release/VYRON-1.0.7-macOS-AppleSilicon.dmg
          cp "$UPDATER" release/VYRON.app.tar.gz
          cp "$SIG" release/VYRON.app.tar.gz.sig
          cp ../vyron-v100/RELEASE_NOTES_1.0.7.md release/RELEASE_NOTES.md
          shasum -a 256 release/VYRON-1.0.7-macOS-AppleSilicon.dmg > release/SHA256.txt
          shasum -a 256 release/VYRON.app.tar.gz > release/UPDATER_SHA256.txt
          tar -czf release/VYRON-1.0.7-source.tar.gz --exclude='./node_modules' --exclude='./dist' --exclude='./src-tauri/target' --exclude='./release' .
          shasum -a 256 release/VYRON-1.0.7-source.tar.gz > release/SOURCE_SHA256.txt
          python3 - <<'PY'
          import datetime,json
          from pathlib import Path
          sig=Path('release/VYRON.app.tar.gz.sig').read_text().strip()
          if not sig: raise SystemExit('empty updater signature')
          data={
            'version':'1.0.7',
            'notes':Path('release/RELEASE_NOTES.md').read_text(),
            'pub_date':datetime.datetime.now(datetime.timezone.utc).replace(microsecond=0).isoformat().replace('+00:00','Z'),
            'platforms':{'darwin-aarch64':{
              'url':'https://github.com/ScaleUPPeisov/scaleup-dashboards/releases/download/v1.0.7/VYRON.app.tar.gz',
              'signature':sig
            }}
          }
          Path('release/latest.json').write_text(json.dumps(data,ensure_ascii=False,indent=2)+'\n')
          PY

      - name: Publish GitHub release v1.0.7
        working-directory: .vyron-v107
        shell: bash
        run: |
          set -euo pipefail
          if gh release view v1.0.7 --repo ScaleUPPeisov/scaleup-dashboards >/dev/null 2>&1; then
            gh release upload v1.0.7 release/* --repo ScaleUPPeisov/scaleup-dashboards --clobber
            gh release edit v1.0.7 --repo ScaleUPPeisov/scaleup-dashboards --title 'VYRON 1.0.7 — Downloads Image Collector Hotfix' --notes-file release/RELEASE_NOTES.md
          else
            gh release create v1.0.7 release/* --repo ScaleUPPeisov/scaleup-dashboards --target "$GITHUB_SHA" --title 'VYRON 1.0.7 — Downloads Image Collector Hotfix' --notes-file release/RELEASE_NOTES.md
          fi

      - name: Publish signed live updater feed
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          rm -rf /tmp/vyron-main-v107
          git worktree add /tmp/vyron-main-v107 origin/main
          mkdir -p /tmp/vyron-main-v107/vyron-updates
          cp .vyron-v107/release/latest.json /tmp/vyron-main-v107/vyron-updates/latest.json
          cd /tmp/vyron-main-v107
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add vyron-updates/latest.json
          if ! git diff --cached --quiet; then
            git commit -m 'release(vyron): publish 1.0.7 updater feed'
            git push origin HEAD:main
          fi

      - name: Verify published release and live updater
        shell: bash
        run: |
          set -euo pipefail
          gh release view v1.0.7 --repo ScaleUPPeisov/scaleup-dashboards --json tagName,name,assets > /tmp/release-v107.json
          curl -fsSL 'https://raw.githubusercontent.com/ScaleUPPeisov/scaleup-dashboards/main/vyron-updates/latest.json' -o /tmp/live-v107.json
          python3 - <<'PY'
          import json,time,urllib.request
          for _ in range(12):
              try:
                  x=json.load(urllib.request.urlopen('https://raw.githubusercontent.com/ScaleUPPeisov/scaleup-dashboards/main/vyron-updates/latest.json'))
                  if x.get('version')=='1.0.7':
                      assert 'darwin-aarch64' in x['platforms']
                      print('Live updater 1.0.7: PASS')
                      break
              except Exception as e:
                  print('retry live updater:',e)
              time.sleep(5)
          else: raise SystemExit('live updater did not reach 1.0.7')
          PY
          echo 'VYRON 1.0.7 RELEASED: PASS'
