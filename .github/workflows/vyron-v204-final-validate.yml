name: VYRON YT PEISOV 2.0.4 Final Validate
on:
  push:
    branches: [vyron-yt-peisov-2.0.0-final]
    paths: ['vyron-v204-validate-trigger.txt']
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate-macos-arm64:
    runs-on: macos-latest
    timeout-minutes: 180
    env:
      GH_TOKEN: ${{ github.token }}
      VITE_BUILD_DATE: 2026-09-05
    steps:
      - uses: actions/checkout@v4
        with:
          ref: vyron-yt-peisov-2.0.0-final
          fetch-depth: 0
      - name: Restore exact released VYRON 1.2.0
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/v204-work /tmp/v204-assets
          mkdir -p /tmp/v204-work /tmp/v204-assets
          gh release download v1.2.0 --repo ScaleUPPeisov/scaleup-dashboards --pattern 'VYRON-1.2.0-source.tar.gz' --pattern 'SOURCE_SHA256.txt' --dir /tmp/v204-assets
          expected="$(awk '{print $1;exit}' /tmp/v204-assets/SOURCE_SHA256.txt)"
          got="$(shasum -a 256 /tmp/v204-assets/VYRON-1.2.0-source.tar.gz | awk '{print $1}')"
          test "$expected" = "$got"
          tar -xzf /tmp/v204-assets/VYRON-1.2.0-source.tar.gz -C /tmp/v204-work
          python3 - <<'PY'
          import json
          from pathlib import Path
          r=Path('/tmp/v204-work');c=json.loads((r/'src-tauri/tauri.conf.json').read_text())
          Path('/tmp/v204-id').write_text(c['identifier']);Path('/tmp/v204-product').write_text(c.get('productName',''));Path('/tmp/v204-pub').write_text(c['plugins']['updater']['pubkey']);Path('/tmp/v204-endpoints').write_text(json.dumps(c['plugins']['updater']['endpoints']))
          PY
      - name: Apply VYRON 2.0.4 patch chain
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/v204-work
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_persistence_recovery.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_intelligence_errors.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_autopilot_attention.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_settings_importfix.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_rust_fingerprintfix.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v200/apply_v200_version.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v201/apply_v201_quota_rollover_fix.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v201/apply_v201_version.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v202/apply_v202_studio_draft_bridge.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v202/apply_v202_version.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v203/apply_v203_oauth_discovery.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v203/apply_v203_version.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v204/apply_v204_selectall_crash_fix.py" .
          python3 "$GITHUB_WORKSPACE/vyron-v204/apply_v204_version.py" .
      - name: 2.0.4 acceptance contracts
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/v204-work
          python3 - <<'PY'
          import json
          from pathlib import Path
          p=json.loads(Path('package.json').read_text());c=json.loads(Path('src-tauri/tauri.conf.json').read_text())
          assert p['version']=='2.0.4' and c['version']=='2.0.4'
          assert c['identifier']==Path('/tmp/v204-id').read_text();assert c.get('productName','')==Path('/tmp/v204-product').read_text();assert c['plugins']['updater']['pubkey']==Path('/tmp/v204-pub').read_text();assert json.dumps(c['plugins']['updater']['endpoints'])==Path('/tmp/v204-endpoints').read_text()
          y=Path('src/youtubeExisting.ts').read_text();m=Path('src/MetadataPage.tsx').read_text();yt=Path('src-tauri/src/youtube.rs').read_text()
          assert 'meta:(ImportedMetadata|undefined)[]=[]' in y and 'meta.find(x=>x?.number===i+1)' in y
          assert Path('src/youtubeExisting.selectAll.test.ts').exists()
          assert 'const selectVisible=' in m and '("forMine","true")' in yt
          print('VYRON 2.0.4 select-all crash contracts: PASS')
          PY
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin
      - name: Frontend tests and production build
        shell: bash
        run: |
          set -euo pipefail
          cd /tmp/v204-work
          npm install --no-audit --no-fund
          npm test
          npm run build
      - name: Rust regression suite
        shell: bash
        run: cd /tmp/v204-work && cargo test --manifest-path src-tauri/Cargo.toml --lib -- --nocapture
      - name: Apple Silicon compile gate
        shell: bash
        run: cd /tmp/v204-work && cargo check --manifest-path src-tauri/Cargo.toml --target aarch64-apple-darwin
      - name: Final summary
        run: echo 'VYRON YT PEISOV 2.0.4 FINAL VALIDATION PASS'
