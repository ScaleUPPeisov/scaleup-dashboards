_inspect_v1014/src/AnalyticsPage.tsx:16: const [channelId,setChannelId]=useState(channels[0]?.id||''),[period,setPeriod]=useState<Period>('28');
_inspect_v1014/src/AnalyticsPage.tsx:17: const c=channels.find(x=>x.id===channelId),a=c?.analytics,days=Number(period),rows=useMemo(()=>slice(a,days),[a,days]);
_inspect_v1014/src/AnalyticsPage.tsx:19: return <><div className="pageHeader"><div><small>ANALYTICS • LOCAL CACHE</small><h1>Аналитика</h1><p>Никаких запросов к YouTube на этом экране. Показаны только данные последней ручной синхронизации из YouTube workspace.</p></div><div className="headerActions"><button className="primary" onClick={()=>setPage('youtube')}>Обновить в YouTube</button></div></div><div className="cacheNotice"><b>Последняя синхронизация</b><span>{a?.updatedAt?new Date(a.updatedAt).toLocaleString('ru-RU'):'Нет данных'}</span></div><div className="analyticsToolbar"><select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><div className="periodTabs analyticsPeriods">{(['7','28','90'] as Period[]).map(x=><button key={x} className={period===x?'active':''} onClick={()=>setPeriod(x)}>{x} дней</button>)}</div></div>{!a?<div className="empty"><b>Аналитика ещё не синхронизирована</b><p>Открой YouTube → Данные YouTube и нажми «Обновить аналитику».</p></div>:<><div className="channelMetricsGrid analyticsKpis"><span><small>Views</small><b>{views==null?'—':formatNumber(views)}</b></span><span><small>Watch Time</small><b>{watch==null?'—':`${num(watch/60,1)} ч`}</b></span><span><small>Revenue</small><b>{revKnown?money(revenue):'—'}</b></span><span><small>RPM</small><b>{rpm==null?'—':`$${rpm.toFixed(2)}`}</b></span><span><small>Avg View Duration</small><b>{a.periodDays===days?dur(a.averageViewDuration):'—'}</b></span><span><small>Avg %</small><b>{a.periodDays===days?`${num(a.averageViewPercentage,1)}%`:'—'}</b></span><span><small>Subscribers</small><b>{c?.stats?.subscribers!=null?formatNumber(c.stats.subscribers):'—'}</b></span><span><small>CTR</small><b>{a.impressionCtr==null?'—':`${num(a.impressionCtr,2)}%`}</b></span></div><section className="panel analyticsWide"><div className="panelHead"><div><small>CACHED DYNAMICS</small><h3>Просмотры</h3></div><span>{rows.length} точек</span></div>{rows.length?<><Line rows={rows}/><div className="chartFoot"><span>{rows[0]?.date}</span><span>{rows.at(-1)?.date}</span></div></>:<p>Для этого периода нет сохранённых daily points.</p>}</section><section className="panel"><div className="panelHead"><div><small>TOP VIDEOS • CACHE</small><h3>Сохранённые метрики видео</h3></div></div>{a.topVideos?.length?<div className="topVideoList rich">{a.topVideos.slice(0,10).map(v=><div key={v.id}>{v.thumbnail?<img src={v.thumbnail} loading="lazy"/>:<i/>}<span><strong>{v.title}</strong><small>{formatNumber(v.views)} views • {num(v.averageViewPercentage,1)}% • {money(v.estimatedRevenue)}</small></span></div>)}</div>:<p>Нет данных.</p>}</section></>}</>
_inspect_v1014/src/MetadataPage.tsx:24:const draftKey=(channelId:string)=>`vyron:metadata-draft:v1:${channelId}`;
_inspect_v1014/src/MetadataPage.tsx:25:function readDraft(channelId:string):MetadataDraft|undefined{if(!channelId)return;try{const x=JSON.parse(localStorage.getItem(draftKey(channelId))||'null');return x?.version===1?x:undefined}catch{return}}
_inspect_v1014/src/MetadataPage.tsx:26:function writeDraft(channelId:string,d:MetadataDraft){if(!channelId)return;try{localStorage.setItem(draftKey(channelId),JSON.stringify(d))}catch{}}
_inspect_v1014/src/MetadataPage.tsx:32: const [channelId,setChannelId]=useState(channels[0]?.id||''),[target,setTarget]=useState<Target>('youtube'),[rows,setRows]=useState<ImportedMetadata[]>([]),[paste,setPaste]=useState(''),[yt,setYt]=useState<YoutubeExistingVideo[]>([]),[busy,setBusy]=useState(false),[order,setOrder]=useState<'oldest'|'newest'>('newest'),[limit,setLimit]=useState(1000),[filter,setFilter]=useState<ExistingFilter>('private'),[docxStrict,setDocxStrict]=useState(false),[start,setStart]=useState(()=>{const d=new Date();d.setDate(d.getDate()+1);d.setHours(18,0,0,0);return local(d.toISOString())}),[cadence,setCadence]=useState(2),[scheduleMode,setScheduleMode]=useState<'auto'|'manual'>('auto'),[scheduleRevision,setScheduleRevision]=useState(0),[syncStatus,setSyncStatus]=useState(''),[applyProgress,setApplyProgress]=useState(''),[applyReport,setApplyReport]=useState<ApplyReport|null>(null),[history,setHistory]=useState<MetadataHistory[]>([]),[draftSavedAt,setDraftSavedAt]=useState('');const file=useRef<HTMLInputElement>(null),restoring=useRef(false);
_inspect_v1014/src/MetadataPage.tsx:33: const channel=channels.find(c=>c.id===channelId),profileId=channel?.youtubeProfileId||'';
_inspect_v1014/src/MetadataPage.tsx:34: useEffect(()=>{restoring.current=true;const d=readDraft(channelId);setCadence(d?.cadence||channel?.cadenceDays||2);setScheduleMode(d?.scheduleMode||'auto');setTarget(d?.target||'youtube');setScheduleRevision(x=>x+1);setRows(d?.rows||[]);setPaste(d?.paste||'');setYt((d?.selectedVideos||[]).map(v=>({...v,selected:true})));setOrder(d?.order||'newest');setFilter(d?.filter||'private');setDocxStrict(Boolean(d?.docxStrict));if(d?.start)setStart(d.start);setSyncStatus(d?`↻ Черновик восстановлен • ${new Date(d.updatedAt).toLocaleString('ru-RU')}`:'');setHistory(d?.history||[]);setDraftSavedAt(d?.updatedAt||'');setApplyProgress('');setApplyReport(null);queueMicrotask(()=>{restoring.current=false})},[channelId]);
_inspect_v1014/src/MetadataPage.tsx:35: useEffect(()=>{if(restoring.current||!channelId)return;const updatedAt=new Date().toISOString();const d:MetadataDraft={version:1,updatedAt,target,rows,paste,order,filter,docxStrict,start,cadence,scheduleMode,selectedVideos:compactSelected(yt),syncStatus,history};const t=window.setTimeout(()=>{writeDraft(channelId,d);setDraftSavedAt(updatedAt)},180);return()=>window.clearTimeout(t)},[channelId,target,rows,paste,order,filter,docxStrict,start,cadence,scheduleMode,yt,syncStatus,history]);
_inspect_v1014/src/MetadataPage.tsx:38: async function loadYoutube(){if(!profileId){setSyncStatus('OAuth не подключён');return}if(youtubeQuotaState().blocked){setSyncStatus(youtubeQuotaMessage());return}setBusy(true);try{const selectedIds=new Set(yt.filter(v=>v.selected).map(v=>v.id));const r=await api.youtubeListExisting(profileId,Math.max(1,Math.min(5000,limit)));const videos=(r.videos||[]).map(v=>({...v,selected:selectedIds.has(v.id)}));setYt(videos);replaceExistingCacheFromSync(channelId,videos,r);setScheduleRevision(x=>x+1);setSyncStatus(r.complete?`✓ ${r.received}/${r.youtubeFound} получено точно`:`⚠ ${r.received}/${Math.min(r.youtubeFound,r.requested)} — синхронизация неполная`);notifySuccess('Данные YouTube обновлены',`Получено ${r.received}/${Math.min(r.youtubeFound,r.requested)} видео.`,{operationId:`youtube-sync:${channelId}:${Date.now()}`})}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setSyncStatus(youtubeQuotaMessage());notifyWarning('YouTube quota исчерпана','Синхронизация поставлена на паузу.')}else{setSyncStatus(String(e));notifyError('Не удалось обновить данные YouTube',String(e))}}finally{setBusy(false)}}
_inspect_v1014/src/MetadataPage.tsx:39: const future=useMemo(()=>jobs.filter(j=>j.channelId===channelId&&j.status!=='SCHEDULED').sort((a,b)=>a.number-b.number),[jobs,channelId]);
_inspect_v1014/src/MetadataPage.tsx:41: const scheduleState=useMemo(()=>channel?getChannelScheduleState(channelId,channel):undefined,[channelId,channel?.id,channel?.cadenceDays,channel?.publishHour,channel?.publishMinute,scheduleRevision,yt.length]);
_inspect_v1014/src/MetadataPage.tsx:57: async function applyFuture(){setBusy(true);let ok=0;try{for(const m of matches){const j=m.job,r=m.row;if(!j)continue;const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}notifySuccess('Метаданные сохранены',`${ok} из ${rows.length} будущих видео обновлены.`,{operationId:`future-metadata:${channelId}:${Date.now()}`})}catch(e){toast(String(e))}finally{setBusy(false)}}
_inspect_v1014/src/MetadataPage.tsx:71:    if(cacheUpdates.length){mergeExistingCacheVideos(channelId,cacheUpdates);const byId=new Map(cacheUpdates.map(v=>[v.id,v]));setYt(prev=>prev.map(v=>byId.get(v.id)?{...byId.get(v.id)!,selected:v.selected}:v));setScheduleRevision(x=>x+1)}
_inspect_v1014/src/MetadataPage.tsx:75:    else if(metadataOk===matched&&scheduleOk===scheduleTotal&&failed===0){const after=channel?getChannelScheduleState(channelId,channel):undefined;notifySuccess(scheduleTotal?'Метаданные и расписание применены':'Метаданные применены',`${metadataOk} из ${matched} видео успешно обновлены.${after?.scheduledUntil?` Канал запланирован до ${scheduleDateLabel(after.lastScheduledAt)}.`:''}`,{operationId:`metadata-apply:${channelId}:${report.at}`})}
_inspect_v1014/src/MetadataPage.tsx:76:    else notifyWarning('Выполнено частично',`Обновлено ${metadataOk} из ${matched}. Не удалось: ${Math.max(failed,matched-metadataOk)}.`,{operationId:`metadata-partial:${channelId}:${report.at}`});
_inspect_v1014/src/MetadataPage.tsx:89:   <section className="panel metadataTargetPanel"><div className="panelHead"><div><small>02 • НАЗНАЧЕНИЕ</small><h3>Куда применить</h3></div></div><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><div className="metadataTargetSwitch"><button className={target==='future'?'active':''} onClick={()=>setTarget('future')}><b>Будущие VYRON</b><small>проекты до загрузки</small></button><button className={target==='youtube'?'active':''} onClick={()=>setTarget('youtube')}><b>Уже на YouTube</b><small>выбранные private/scheduled</small></button></div>{target==='future'?<div className="metadataTargetInfo"><strong>{future.length}</strong><span>будущих проектов</span></div>:<><div className={`profileAutoBadge ${profileId?'good':'warn'}`}><small>OAuth</small><b>{profileId?'✓ привязан автоматически':'не подключён'}</b></div><div className="metadataYoutubeLoad"><label>Лимит<input type="number" min="1" max="5000" value={limit} onChange={e=>setLimit(+e.target.value||1000)}/></label><label>Порядок<select value={order} onChange={e=>setOrder(e.target.value as any)}><option value="newest">новые → старые</option><option value="oldest">старые → новые</option></select></label><button disabled={busy||!profileId} onClick={loadYoutube}>↻ Синхронизировать</button></div><div className="metadataTargetInfo"><strong>{selectedYt.length}</strong><span>выбрано из {yt.length}</span><small>{syncStatus||'YouTube будет загружен автоматически'}</small></div></>}</section>
_inspect_v1014/src/ChannelRunwayScheduler.tsx:7:  const signature=channels.map(c=>`${c.id}:${c.name}:${c.enabled}:${c.cadenceDays}:${c.youtubeProfileId||''}`).join('|');
_inspect_v1014/src/YouTubeCenter.tsx:17: return <><div className="youtubeCenterHead"><div><small>YOUTUBE CENTER • API ZONE</small><h1>YouTube</h1><p>Единственная зона VYRON, где разрешены YouTube API-вызовы. Синхронизация и обновления запускаются только твоим действием.</p></div><div className="youtubeTabs">{tabs.map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div></div><QuotaMeter compact defaultChannels={Math.max(1,channels.length)}/>{tab!=='command'&&<div className="youtubePolicyNotice"><b>ZERO HIDDEN QUOTA</b><span>Нет startup sync, polling, background refresh или API-запросов при возврате на вкладку.</span></div>}{tab==='uploaded'&&<ExistingVideos/>}{tab==='metadata'&&<MetadataPage/>}{tab==='queue'&&<PublisherOS/>}{tab==='command'&&<CommandCenter/>}{tab==='runway'&&<ChannelRunway/>}{tab==='data'&&<YouTubeDataTools/>}{tab==='accounts'&&<AccountsPage/>}{tab==='calendar'&&<section className="panel calendarPanel"><div className="panelHead"><div><small>ALL CHANNELS</small><h3>Календарь публикаций</h3><p>Локальные даты из project state / YouTube queue.</p></div><span>{calendar.length} публикаций</span></div>{calendar.length===0?<div className="empty"><b>Публикаций пока нет</b><p>Когда у видео появится publishAt, оно будет показано здесь.</p></div>:<div className="calendarList">{calendar.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="calendarRow" key={j.id}><time>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})}</time><span><b>{c?.name||'Канал удалён'}</b><small>VIDEO_{String(j.number).padStart(3,'0')} • {j.title||'Без названия'}</small></span><em className={`publishState ${j.status.toLowerCase()}`}>{j.status}</em>{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div>})}</div>}</section>}</>
_inspect_v1014/src/PublisherOS.tsx:15: async function apply(){const used=new Set<string>();let ok=0,miss=0;for(let i=0;i<rows.length;i++){const r=rows[i],named=r.channel?channels.find(c=>c.name.toLowerCase()===r.channel?.toLowerCase()||c.id===r.channel):undefined,channelId=named?.id||metaChannel;const pool=useApp.getState().jobs.filter(j=>(!channelId||j.channelId===channelId)&&!used.has(j.id)).sort((a,b)=>a.number-b.number);const j=r.number?pool.find(x=>x.number===r.number):pool[0];if(!j){miss++;continue}used.add(j.id);const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}toast(`Применено: ${ok}${miss?` • не найдено ${miss}`:''}`)}
_inspect_v1014/src/PublisherOS.tsx:16: async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);if(!r.videoId)throw new Error('YouTube не вернул video ID');patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}
_inspect_v1014/src/PublisherOS.tsx:22:  {files.length>0&&<section className="panel metadataInline"><div><b>Metadata Inbox</b><p>{files.length} файлов • {rows.length} записей</p></div><select value={metaChannel} onChange={e=>setMetaChannel(e.target.value)}><option value="">Все каналы</option>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="primary" disabled={!rows.length} onClick={apply}>Применить</button><button onClick={()=>{setRows([]);setFiles([])}}>Очистить</button></section>}
_inspect_v1014/src/PublisherOS.tsx:23:  {ready.length===0?<div className="empty"><b>Нет готовых MP4</b><p>После появления final.mp4 ролик автоматически попадёт сюда.</p></div>:<div className="publishList compactPublish">{ready.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <article className="publishCard compact" key={j.id}><div className="thumb"><span>▶</span><small>VIDEO_{String(j.number).padStart(3,'0')}</small></div><div className="publishMeta"><small>{c?.name} • {j.metadataSource||'template'}</small><input value={j.title} maxLength={100} onChange={e=>patchJob(j.id,{title:e.target.value,metadataLocked:true})}/><details><summary>Описание и теги</summary><textarea value={j.description} maxLength={5000} onChange={e=>patchJob(j.id,{description:e.target.value,metadataLocked:true})}/><input value={j.tags.join(', ')} onChange={e=>patchJob(j.id,{tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean),metadataLocked:true})}/></details>{j.status==='UPLOADING'&&<div className="progress"><i style={{width:`${j.uploadProgress||0}%`}}/><span>{Math.round(j.uploadProgress||0)}%</span></div>}{j.error&&<div className="errorBox">{j.error}</div>}</div><div className="publishSide"><label>Дата<input type="datetime-local" value={j.publishAt?new Date(new Date(j.publishAt).getTime()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):''} onChange={e=>patchJob(j.id,{publishAt:e.target.value?new Date(e.target.value).toISOString():undefined,metadataLocked:true})}/></label><span className={`publishState ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</span>{j.status==='READY_UPLOAD'&&<button className="primary" onClick={()=>upload(j)}>Загрузить</button>}{j.status==='ERROR'&&<button onClick={()=>patchJob(j.id,{status:j.finalPath?'READY_UPLOAD':'ERROR',error:undefined})}>Повторить</button>}{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div></article>})}</div>}
_inspect_v1014/src/ProductionWorkspace.tsx:6:type State={version:1;selectedChannelId?:string;byChannel:Record<string,Pref>};
_inspect_v1014/src/ProductionWorkspace.tsx:15: const selected=channels.find(c=>c.id===prefs.selectedChannelId)||channels.find(c=>c.id===state.selectedChannelId)||channels[0];
_inspect_v1014/src/ProductionWorkspace.tsx:16: useEffect(()=>{if(!selected)return;if(prefs.selectedChannelId!==selected.id)patchPrefs({selectedChannelId:selected.id});if(state.selectedChannelId===selected.id&&state.byChannel[selected.id])return;const next:State={...state,selectedChannelId:selected.id,byChannel:{...state.byChannel,[selected.id]:state.byChannel[selected.id]||{project:month(),target:30,reviewed:false,updatedAt:new Date().toISOString()}}};setState(next);write(next)},[selected?.id]);
_inspect_v1014/src/ProductionWorkspace.tsx:17: const pref:Pref|undefined=selected?state.byChannel[selected.id]:undefined;const rows=useMemo(()=>selected?jobs.filter(j=>j.channelId===selected.id):[],[jobs,selected?.id]);
_inspect_v1014/src/ProductionWorkspace.tsx:21: const patch=(p:Partial<Pref>)=>{const next:State={...state,selectedChannelId:selected.id,byChannel:{...state.byChannel,[selected.id]:{...pref,...p,updatedAt:new Date().toISOString()}}};setState(next);write(next)};
_inspect_v1014/src/ProductionWorkspace.tsx:25:  <div className="workspaceControls"><label>Канал<select value={selected.id} onChange={e=>{const id=e.target.value;patchPrefs({selectedChannelId:id});const next:State={...state,selectedChannelId:id,byChannel:{...state.byChannel,[id]:state.byChannel[id]||{project:month(),target:30,reviewed:false,updatedAt:new Date().toISOString()}}};setState(next);write(next)}}>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Период<input value={pref.project} onChange={e=>patch({project:e.target.value})}/></label><label>План проектов<input type="number" min="1" max="10000" value={target} onChange={e=>patch({target:Math.max(1,Math.min(10000,+e.target.value||1)),reviewed:false})}/></label></div>
_inspect_v1014/src/YouTubeDataTools.tsx:9: const [channelId,setChannelId]=useState(channels.find(c=>c.youtubeProfileId)?.id||channels[0]?.id||''),[busy,setBusy]=useState(''),[note,setNote]=useState('');
_inspect_v1014/src/YouTubeDataTools.tsx:10: const channel=channels.find(c=>c.id===channelId),list=useMemo(()=>competitors.filter(c=>c.channelId===channelId),[competitors,channelId]);
_inspect_v1014/src/YouTubeDataTools.tsx:13: async function discover(){if(!channel?.youtubeProfileId){toast('Сначала подключи YouTube OAuth');return}if(!guard())return;setBusy('discover');try{const found=await api.youtubeDiscoverCompetitors(channel.youtubeProfileId,Math.max(10,Math.min(50,useApp.getState().settings.competitorPoolSize||30)));const existing=new Set(list.map(c=>c.youtubeChannelId).filter(Boolean));let added=0;for(const x of found){if(existing.has(x.channelId)||x.channelId===channel.youtubeChannelId)continue;add({id:crypto.randomUUID(),channelId,name:x.name,url:x.url,youtubeChannelId:x.channelId,thumbnail:x.thumbnail,subscribers:x.subscribers,views:x.views,videos:x.videos,history:[],similarity:x.similarity,source:'auto'});existing.add(x.channelId);added++}setNote(`✓ Найдено ${found.length}, добавлено новых ${added}`);toast(`Конкуренты: +${added}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setNote(youtubeQuotaMessage())}else setNote(String(e));toast(isYoutubeQuotaError(e)?youtubeQuotaMessage():String(e))}finally{setBusy('')}}
_inspect_v1014/src/YouTubeDataTools.tsx:15: return <section className="panel youtubeDataTools"><div className="panelHead"><div><small>EXPLICIT API ACTIONS</small><h3>Данные YouTube</h3><p>API вызывается только после нажатия кнопки на этом экране. Другие разделы используют сохранённый локальный кэш.</p></div><span className="localOnlyBadge">YOUTUBE ZONE</span></div><div className="youtubeDataToolbar"><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><div className="youtubeActionGrid"><button disabled={!!busy||!channel?.youtubeProfileId} onClick={analytics}><b>{busy==='analytics'?'Обновляю…':'Обновить аналитику'}</b><small>90 дней → локальный cache</small></button><button disabled={!!busy||!channel?.youtubeProfileId} onClick={discover}><b>{busy==='discover'?'Ищу…':'Найти конкурентов'}</b><small>Только по явному действию</small></button><button disabled={!!busy||!list.length} onClick={refreshCompetitors}><b>{busy==='competitors'?'Обновляю…':'Обновить конкурентов'}</b><small>{list.length} сохранено локально</small></button></div>{note&&<div className="youtubeActionNote">{note}</div>}</section>
_inspect_v1014/src/ProductionStatusBridge.tsx:31:  },[booted,workspace,channels.map(c=>c.id).join('|')]);
_inspect_v1014/src/ProductionOS.tsx:17: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [prefs,patchPrefs]=useProductionPrefs();const tab=(prefs.tab||'queue') as Tab;const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';const setTab=(v:Tab)=>patchPrefs({tab:v});const setChannelId=(id:string)=>patchPrefs({selectedChannelId:id});const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false),[inbox,setInbox]=useState<any>(),[planOpen,setPlanOpen]=useState(false),[planCount,setPlanCount]=useState(30),[planScope,setPlanScope]=useState<'one'|'all'>('one');const [section,setSection]=useState<'overview'|'materials'|'builder'|'render'|'publish'>(()=>tab==='materials'?'materials':tab==='manager'?'builder':'overview');const scopedJobs=section==='render'?jobs.filter(j=>['READY_RENDER','RENDERING','ERROR'].includes(j.status)):section==='publish'?jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)):jobs;const filtered=scopedJobs.filter(j=>filter==='all'||j.channelId===filter||j.status===filter).sort((a,b)=>a.number-b.number);const c=channels.find(x=>x.id===channelId);const selectedJobIds=(prefs.selectedJobIds||[]).filter(id=>jobs.some(j=>j.id===id));const projectRoot=resolveProductionRootFromPrefs(prefs,channelId,settings.workspace);const [projectStorage,setProjectStorage]=useState<ProductionStorageStatus|null>(null);
_inspect_v1014/src/ProductionOS.tsx:18: useEffect(()=>{if(!prefs.selectedChannelId&&channels[0])patchPrefs({selectedChannelId:channels[0].id})},[channels.length,prefs.selectedChannelId]);
_inspect_v1014/src/ProductionOS.tsx:24: async function deleteJobs(ids:string[],label:string){const unique=[...new Set(ids)].filter(id=>useApp.getState().jobs.some(j=>j.id===id));if(!unique.length)return;if(!confirm(label))return;setBusy(true);try{const rows=useApp.getState().jobs.filter(j=>unique.includes(j.id));for(const j of rows){if(!j.folder)continue;const roots=[resolveProductionRootFromPrefs(prefs,j.channelId,settings.workspace),inferJobRoot(j.folder),settings.workspace].filter((x,i,a)=>x&&a.indexOf(x)===i);let done=false,last:unknown;for(const root of roots){try{await productionManagerApi.deleteJobFolder(root,j.folder);done=true;break}catch(e){last=e}}if(!done)throw last||new Error('Не удалось определить хранилище проекта')}setJobs(useApp.getState().jobs.filter(j=>!unique.includes(j.id)));patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!unique.includes(id))});toast(`Удалено проектов: ${unique.length}`)}catch(e){toast(`Не удалось удалить проекты: ${String(e)}`)}finally{setBusy(false)}}
_inspect_v1014/src/ProductionOS.tsx:25: async function refreshAll(){setBusy(true);try{for(const j of useApp.getState().jobs){const ch=channels.find(x=>x.id===j.channelId);if(!ch||!j.folder||j.status==='SCHEDULED')continue;try{patchJob(j.id,await api.refreshJob(j.folder,ch.minTracks))}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}toast('Статусы производства обновлены')}finally{setBusy(false)}}
_inspect_v1014/src/ProductionOS.tsx:28: const preview=useMemo(()=>planTargets().map(ch=>{const existing=jobs.filter(j=>j.channelId===ch.id);const max=Math.max(0,...existing.map(j=>j.number));return {ch,from:max+1,to:max+planCount,count:planCount}}),[planScope,channelId,planCount,channels.length,jobs.length]);
_inspect_v1014/src/ProductionOS.tsx:31: async function importImages(){if(!c)return;let root=projectRoot;if(!root){root=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:root})}const storage=await productionManagerApi.storageStatus(root);if(!storage.exists||!storage.writable){toast(storage.error||`Папка проектов недоступна: ${root}`);return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);if(!nums.length){toast('Нет проектов, ожидающих изображение');return}const imported=await api.importImages(root,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено по VIDEO_${String(nums[0]).padStart(3,'0')}…`)}
_inspect_v1014/src/ProductionOS.tsx:36:  {tab==='queue'&&<>{section==='overview'&&<ProductionWorkspace/>}<div className="productionPipeline">{stageOrder.map(st=><button key={st} className={st==='ERROR'&&jobs.some(j=>j.status==='ERROR')?'warn':''} onClick={()=>setFilter(st)}><small>{statusLabel[st]}</small><b>{jobs.filter(j=>j.status===st).length}</b></button>)}</div><div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы / статусы</option>{channels.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}{stageOrder.map(st=><option key={st} value={st}>{statusLabel[st]}</option>)}</select><span>{filtered.length} задач</span></div>{filtered.length===0?<div className="empty"><b>Нет задач в этом фильтре</b><p>Создай точное число проектов или включи поддержание буфера.</p><button className="primary" onClick={()=>setPlanOpen(true)}>Создать проекты</button></div>:<div className="jobs virtualReady"><div className="pmBulkBar"><span>Выбрано <b>{selectedJobIds.filter(id=>visibleJobIds.includes(id)).length}</b> / {visibleJobIds.length}</span><button onClick={()=>patchPrefs({selectedJobIds:[...new Set([...selectedJobIds,...visibleJobIds])]})}>Выбрать всё</button><button onClick={()=>patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!visibleJobIds.includes(id))})}>Снять выделение</button><button className="danger" disabled={!selectedJobIds.some(id=>visibleJobIds.includes(id))} onClick={()=>void deleteJobs(selectedJobIds.filter(id=>visibleJobIds.includes(id)),'Удалить выбранные проекты?')}>Удалить выбранные</button><button className="danger" onClick={()=>void deleteJobs(visibleJobIds,'Удалить все проекты в текущем списке?')}>Удалить все</button></div>{filtered.map(j=>{const ch=channels.find(x=>x.id===j.channelId);return <article className="job productionJob" key={j.id}><label className="pmJobCheck"><input type="checkbox" checked={selectedJobIds.includes(j.id)} onChange={()=>toggleJob(j.id)}/></label><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{ch?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не готовы'}</b><span>Изображение {j.coverPath?'✓':'—'} • треков {j.tracksCount}/{j.minTracks} • final.mp4 {j.finalPath?'✓':'—'} • metadata {j.title&&j.description?'✓':'—'}</span><span>{j.publishAt?`Публикация: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата публикации не назначена'}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>Папка</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,ch?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ Аудио</button>}{j.status==='READY_RENDER'&&j.folder&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в ENDLUME RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('youtube')}>YouTube</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,ch?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>Повторить</button>}</div></article>})}</div>}</>}
_inspect_v1014/src/ProductionOS.tsx:37:  {tab==='materials'&&<><section className="panel materialToolbar"><select value={channelId} onChange={e=>{setChannelId(e.target.value);setInbox(undefined)}}><option value="">— канал —</option>{channels.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="primary" disabled={!c} onClick={importImages}>+ Импортировать изображения</button></section><ProductionManager view="materials"/>{inbox&&<div className="panel"><b>Папка материалов готова</b><p>{inbox.root}</p></div>}</>}
_inspect_v1014/src/ProductionOS.tsx:39:  {planOpen&&<div className="modalBackdrop" onMouseDown={()=>setPlanOpen(false)}><section className="confirmModal planModal" onMouseDown={e=>e.stopPropagation()}><small>AUTOPILOT PLANNER</small><h2>Сколько проектов создать?</h2><div className="presetRow">{[10,30,50,100].map(n=><button className={planCount===n?'active':''} key={n} onClick={()=>setPlanCount(n)}>{n}</button>)}<label>Своё число<input type="number" min="1" max="1000" value={planCount} onChange={e=>setPlanCount(Math.max(1,Math.min(1000,+e.target.value||1)))}/></label></div><div className="metadataTargetSwitch"><button className={planScope==='one'?'active':''} onClick={()=>setPlanScope('one')}><b>Один канал</b><small>{c?.name||'выбери канал'}</small></button><button className={planScope==='all'?'active':''} onClick={()=>setPlanScope('all')}><b>Все активные</b><small>{planCount} на каждый канал</small></button></div>{planScope==='one'&&<label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>}<div className="planPreview">{preview.map(x=><div key={x.ch.id}><b>{x.ch.name}</b><span>{x.count} проектов • VIDEO_{String(x.from).padStart(3,'0')} → VIDEO_{String(x.to).padStart(3,'0')}</span><small>по {x.ch.cadenceDays} дн. • {x.ch.minTracks} треков/проект</small></div>)}</div><footer><button onClick={()=>setPlanOpen(false)}>Отмена</button><button className="primary" onClick={createExact}>Создать {preview.reduce((n,x)=>n+x.count,0)} проектов</button></footer></section></div>}
_inspect_v1014/src/ChannelsOS.tsx:8:function health(c:Channel,jobs:VideoJob[]){let n=100;if(!c.youtubeProfileId)n-=25;if(!c.analytics)n-=15;if(jobs.some(j=>j.channelId===c.id&&j.status==='ERROR'))n-=20;const b=bufferDays(c,jobs);if(b<7)n-=25;else if(b<14)n-=12;return Math.max(0,n)}
_inspect_v1014/src/ChannelsOS.tsx:17:  {channels.length===0?<div className="empty"><b>Каналов пока нет</b><p>Добавь локальный канал или подключи реальный аккаунт во вкладке YouTube.</p></div>:<div className="channelOSList">{channels.map(c=>{
_inspect_v1014/src/ChannelsOS.tsx:20:    <header><div className="channelAvatarOS">{a?.channelThumbnail?<img src={a.channelThumbnail} loading="lazy"/>:c.name.slice(0,2).toUpperCase()}</div><span className="channelIdentity"><b>{c.name}</b><small>{c.youtubeChannelId||'YouTube Channel ID —'}</small><em className={c.youtubeProfileId?'good':'warn'}>{c.youtubeProfileId?'OAuth сохранён локально':'YouTube не привязан'}</em></span><span className="channelHealth"><small>HEALTH</small><b>{health(c,jobs)}</b><em>Reserve {bufferDays(c,jobs)} days</em></span><button onClick={()=>setEditing(editing===c.id?null:c.id)}>✎</button></header>
_inspect_v1014/src/ChannelsOS.tsx:22:    <div className="channelMetricsGrid"><span><small>Subscribers</small><b>{c.stats?.subscribers!=null?formatNumber(c.stats.subscribers):'—'}</b></span><span><small>Total Views</small><b>{c.stats?.views!=null?formatNumber(c.stats.views):'—'}</b></span><span><small>Views 28d</small><b>{a?.periodDays===28?formatNumber(a.views):'—'}</b></span><span><small>Revenue 28d</small><b>{money(rev)}</b></span><span><small>RPM 28d</small><b>{a?.periodDays===28?rpm(rev,a.views):'—'}</b></span><span><small>Scheduled local</small><b>{jobs.filter(j=>j.channelId===c.id&&j.status==='SCHEDULED').length}</b></span></div>
_inspect_v1014/src/CommandCenter.tsx:22: function openProduction(channelId?:string){if(channelId)patchPrefs({selectedChannelId:channelId});setPage('production')}
_inspect_v1014/src/DashboardOS.tsx:15:  <div className="morningGrid"><section className="panel morningAttention"><div className="panelHead"><div><small>ATTENTION INBOX</small><h3>{issues.length?`${issues.length} пунктов`:'Ничего срочного'}</h3></div></div>{issues.length?issues.map((x,i)=><button key={i} onClick={()=>setPage(x.page)}><i>!</i><span><small>{x.type}</small><b>{x.text}</b></span><em>→</em></button>):<div className="successBox">OAuth, очередь и основные проверки без критических ошибок.</div>}</section><section className="panel nextPublications"><div className="panelHead"><div><small>NEXT 5</small><h3>Ближайшие публикации</h3></div><button onClick={()=>setPage('youtube')}>Календарь</button></div>{next.length?next.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div key={j.id}><span><b>{c?.name||'Канал'} • VIDEO_{String(j.number).padStart(3,'0')}</b><small>{j.title||'без названия'}</small></span><em>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})}</em></div>}):<p>Запланированных будущих публикаций нет.</p>}</section></div>
_inspect_v1014/src/ChannelRunway.tsx:69:  const signature=channels.map(c=>`${c.id}:${c.name}:${c.enabled}:${c.cadenceDays}:${c.youtubeProfileId||''}`).join('|');
_inspect_v1014/src/AccountsPage.tsx:12: function bindProfile(p:YoutubeProfile){if(!p.channelId)return;const state=useApp.getState();const c=state.channels.find(x=>x.youtubeChannelId===p.channelId);if(c){state.updateChannel(c.id,{youtubeProfileId:p.id,youtubeChannelId:p.channelId,name:p.channelTitle||c.name});return c}return state.addChannel({name:p.channelTitle||'YouTube канал',youtubeProfileId:p.id,youtubeChannelId:p.channelId})}
_inspect_v1014/src/AccountsPage.tsx:17: async function connect(){setBrowserOpen(false);setBusy(true);try{localStorage.setItem('vyron:oauth-browser',browser);const p=await api.youtubeConnectGlobal(browser);bindProfile(p);await refresh();toast(`✓ ${p.channelTitle||p.channelId||'YouTube канал'} подключён и привязан автоматически`)}catch(e){toast(String(e))}finally{setBusy(false)}}
_inspect_v1014/src/AccountsPage.tsx:20:  <div className="pageHeader"><div><small>ACCOUNT CENTER</small><h1>Аккаунты YouTube</h1><p>Один Google OAuth Client обслуживает все каналы. При подключении VYRON сам определяет YouTube channelId и привязывает канал — ручное связывание не требуется.</p></div><div className="headerActions"><button disabled={checking||!profiles.length} onClick={checkAll}>{checking?'Проверяю…':'↻ Проверить'}</button><button className="primary compactAction" disabled={busy||(!config?.configured&&!profiles.length)} onClick={askBrowser}>+ Добавить канал</button></div></div>
_inspect_v1014/src/AccountsPage.tsx:23:  <section className="panel accountsPanel"><div className="panelHead"><div><small>YOUTUBE ACCOUNTS</small><h3>{profiles.length?`${profiles.length} подключено`:'Аккаунтов пока нет'}</h3></div></div>{!profiles.length?<div className="empty"><b>Подключи первый YouTube-канал</b><p>VYRON спросит браузер, откроет Google OAuth и после подтверждения сам создаст карточку реального канала.</p></div>:<div className="accountList">{profiles.map(p=>{const h=health[p.id];const bound=channels.find(c=>c.youtubeProfileId===p.id||c.youtubeChannelId===p.channelId);return <article className="accountRow" key={p.id}>{h?.thumbnail?<img src={h.thumbnail} loading="lazy"/>:<div className="accountAvatar">YT</div>}<div className="accountMain"><b>{h?.channelTitle||p.channelTitle||'YouTube канал'}</b><small>{p.channelId||'Channel ID ещё не определён'}</small><div className="accountBadges"><span className={h?.ok?'good':'warn'}>{h?.ok?'● OAuth healthy':h?'● Требуется внимание':'● Проверяется'}</span><span>{bound?`VYRON: ${bound.name}`:'Автопривязка…'}</span>{p.preferredBrowser&&<span>Браузер: {p.preferredBrowser}</span>}</div></div><div className="accountMeta"><small>Подключён</small><b>{ago(p.connectedAt)}</b><small>Token</small><b>{h?.status||'—'}</b></div><div className="accountActions"><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refresh()}}>Отключить</button></div></article>})}</div>}</section>
_inspect_v1014/src/CompetitorsPage.tsx:13: const [channelId,setChannelId]=useState(channels[0]?.id||''),[url,setUrl]=useState(''),[name,setName]=useState('');const list=competitors.filter(c=>!channelId||c.channelId===channelId);
_inspect_v1014/src/CompetitorsPage.tsx:14: return <><div className="pageHeader"><div><small>COMPETITOR RADAR • LOCAL CACHE</small><h1>Конкуренты</h1><p>Экран читает только сохранённые публичные метрики. Поиск и обновление через YouTube API запускаются вручную только во вкладке YouTube.</p></div><div className="headerActions"><select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="primary" onClick={()=>setPage('youtube')}>Обновить через YouTube</button></div></div><div className="cacheNotice"><b>ZERO QUOTA</b><span>Автопоиск и фоновые snapshots отключены.</span></div><details className="panel advancedPanel"><summary>Ручное добавление без API</summary><div className="competitorAdd"><input placeholder="Название конкурента" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="YouTube URL / channel ID" value={url} onChange={e=>setUrl(e.target.value)}/><button disabled={!channelId||!name||!url} onClick={()=>{add({id:crypto.randomUUID(),channelId,name,url,history:[],source:'manual'});setName('');setUrl('')}}>Добавить</button></div></details>{list.length===0?<div className="empty"><b>Конкурентов в кэше нет</b><p>Открой YouTube → Данные YouTube → Найти конкурентов.</p></div>:<div className="competitorOSList">{list.map(c=>{const vv=velocity(c,'views'),sv=velocity(c,'subscribers');return <article className="competitorOSCard" key={c.id}><header>{c.thumbnail?<img src={c.thumbnail} loading="lazy"/>:<i className="compAvatar">YT</i>}<span><b>{c.name}</b><small>{c.youtubeChannelId||c.url}</small>{c.similarity!=null&&<em className="similarityBadge">Similarity {c.similarity}%</em>}</span><button className="danger mini" onClick={()=>remove(c.id)}>×</button></header><div className="competitorMetrics"><span><small>Subscribers</small><b>{c.subscribers==null?'—':formatNumber(c.subscribers)}</b></span><span><small>Total Views</small><b>{c.views==null?'—':formatNumber(c.views)}</b></span><span><small>Video Count</small><b>{c.videos==null?'—':formatNumber(c.videos)}</b></span><span><small>Recent Avg Views</small><b>{c.recentAverageViews==null?'—':formatNumber(c.recentAverageViews)}</b></span><span><small>Subscriber velocity</small><b>{signed(sv)}</b></span><span><small>View velocity</small><b>{signed(vv)}</b></span><span><small>Last upload</small><b>{miniDate(c.lastVideoAt)}</b></span><span><small>Last sync</small><b>{c.updatedAt?new Date(c.updatedAt).toLocaleString('ru-RU'):'—'}</b></span></div>{c.latestVideos?.length?<div className="competitorVideos os">{c.latestVideos.slice(0,5).map(v=><button key={v.id} onClick={()=>api.openWeb(`https://youtube.com/watch?v=${v.id}`)}>{v.thumbnail?<img src={v.thumbnail} loading="lazy"/>:<i/>}<span><b>{v.title}</b><small>{formatNumber(v.views)} views • {miniDate(v.publishedAt)}</small></span></button>)}</div>:<div className="competitorPlaceholder">Нет сохранённого snapshot</div>}<footer><small>Revenue / RPM / CTR / retention конкурента не показываются: YouTube не раскрывает private analytics.</small><button onClick={()=>api.openWeb(c.url)}>Открыть YouTube</button></footer></article>})}</div>}</>
_inspect_v1014/src/ProductionManager.tsx:27:  const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';
_inspect_v1014/src/ProductionManager.tsx:28:  const channelPrefs={...defaultChannelProductionPrefs(),...(prefs.byChannel[channelId]||{})};
_inspect_v1014/src/ProductionManager.tsx:30:  const setProjectCount=(v:number|((n:number)=>number))=>patchChannelProductionPrefs(channelId,{projectCount:clamp(typeof v==='function'?v(projectCount):v,1,10000)});
_inspect_v1014/src/ProductionManager.tsx:31:  const setTracksPerProject=(v:number)=>patchChannelProductionPrefs(channelId,{tracksPerProject:clamp(v,1,100)});
_inspect_v1014/src/ProductionManager.tsx:32:  const setMode=(v:DistributionMode)=>patchChannelProductionPrefs(channelId,{mode:v});
_inspect_v1014/src/ProductionManager.tsx:33:  const setAllowImageReuse=(v:boolean)=>patchChannelProductionPrefs(channelId,{allowImageReuse:v});
_inspect_v1014/src/ProductionManager.tsx:46:  const channel=channels.find(c=>c.id===channelId);
_inspect_v1014/src/ProductionManager.tsx:54:  useEffect(()=>{if(!prefs.selectedChannelId&&channels[0])patchPrefs({selectedChannelId:channels[0].id})},[prefs.selectedChannelId,channels.length]);
_inspect_v1014/src/ProductionManager.tsx:57:    if(!workspace||!channelId){setSession(null);setMusic(null);setBatches([]);return}
_inspect_v1014/src/ProductionManager.tsx:60:      const state:ChannelProductionState=await productionManagerApi.state(workspace,channelId);
_inspect_v1014/src/ProductionManager.tsx:63:      try{base=await productionManagerApi.batches(workspace,channelId)}catch{}
_inspect_v1014/src/ProductionManager.tsx:65:    if(productionRoot&&productionRoot!==workspace){try{base=[...base,...await productionManagerApi.batches(productionRoot,channelId)]}catch{}}
_inspect_v1014/src/ProductionManager.tsx:69:  useEffect(()=>{void refreshState();setResult(null);setValidation(null);setBatchStatus(null)},[workspace,productionRoot,channelId]);
_inspect_v1014/src/ProductionManager.tsx:76:    offs.push(productionManagerApi.onImportProgress(p=>{if(live&&p.channelId===channelId)void refreshState()}));
_inspect_v1014/src/ProductionManager.tsx:77:    offs.push(productionManagerApi.onImportError(p=>{if(live&&p.channelId===channelId)setImportError(p.message||'Ошибка сбора изображений')}));
_inspect_v1014/src/ProductionManager.tsx:84:  },[workspace,channelId]);
_inspect_v1014/src/ProductionManager.tsx:87:    if(!workspace||!channelId||!session?.active)return;
_inspect_v1014/src/ProductionManager.tsx:88:    const timer=window.setInterval(()=>{productionManagerApi.importStatus(workspace,channelId).then(next=>{setSession(next);if(!next.active)setImportError('')}).catch(e=>setImportError(String(e)))},1200);
_inspect_v1014/src/ProductionManager.tsx:90:  },[workspace,channelId,session?.active]);
_inspect_v1014/src/ProductionManager.tsx:92:  const jobLinks=useMemo(()=>jobs.filter(j=>j.channelId===channelId).sort((a,b)=>a.number-b.number),[jobs,channelId]);
_inspect_v1014/src/ProductionManager.tsx:117:    if(!workspace||!channelId)return;
_inspect_v1014/src/ProductionManager.tsx:119:    try{const indexed=await productionManagerApi.indexMusic(workspace,channelId);setMusic(indexed);notifySuccess('Музыкальная библиотека обновлена',`${indexed.tracks.toLocaleString('ru-RU')} треков доступно.`,{operationId:`music-reindex:${channelId}:${indexed.indexedAt}`})}
_inspect_v1014/src/ProductionManager.tsx:126:    const existing=current.filter(j=>j.channelId===channel.id).sort((a,b)=>a.number-b.number);
_inspect_v1014/src/ProductionManager.tsx:144:      const built=await productionManagerApi.build({requestId:crypto.randomUUID(),workspace,outputWorkspace:productionRoot||workspace,channelId:channel.id,channelName:channel.name,projectCount,tracksPerProject,mode,allowImageReuse:reuse,jobLinks:links.map(j=>({jobId:j.id,number:j.number}))});
_inspect_v1014/src/ProductionManager.tsx:174:  const setSelectedProjects=(ids:string[])=>patchChannelProductionPrefs(channelId,{selectedProjectIds:[...new Set(ids)]});
_inspect_v1014/src/ProductionManager.tsx:178:    setBusy('delete');try{const r=await productionManagerApi.deleteBatchProjects(batch.manifestPath,ids);if(r.deletedJobIds.length)setJobs(useApp.getState().jobs.filter(j=>!r.deletedJobIds.includes(j.id)));setSelectedProjects([]);await refreshState();if(r.batch){setResult({status:'ready',availableImages:collected,requestedProjects:r.batch.projectCount,batch:r.batch});patchChannelProductionPrefs(channelId,{lastBatchId:r.batch.batchId});setBatchStatus(await productionManagerApi.status(r.batch.manifestPath))}else{setResult(null);setBatchStatus(null);patchChannelProductionPrefs(channelId,{lastBatchId:undefined})}toast(`Удалено проектов: ${r.deletedProjectIds.length}`)}catch(e){toast(`Не удалось удалить проекты: ${String(e)}`)}finally{setBusy('')}
_inspect_v1014/src/ProductionManager.tsx:185:      setResult({status:'ready',availableImages:collected,requestedProjects:resumed.projectCount,batch:resumed});patchChannelProductionPrefs(channelId,{lastBatchId:resumed.batchId});
_inspect_v1014/src/ProductionManager.tsx:195:    if(scope==='global')patchPrefs({productionRoot:path});else patchChannelProductionPrefs(channelId,{productionRoot:path});setStorageStatus(status);notifySuccess('Папка проектов изменена',path,{operationId:`storage:${scope}:${channelId}:${path}`});
_inspect_v1014/src/ProductionManager.tsx:197:  function resetChannelProductionRoot(){patchChannelProductionPrefs(channelId,{productionRoot:undefined});toast('Канал снова использует основное хранилище Production')}
_inspect_v1014/src/ProductionManager.tsx:219:    <section className="panel pmHistory"><div className="pmHistoryHead"><div><small>ИСТОРИЯ СБОРОК</small><h3>{channel?.name||'Канал'}</h3></div><button onClick={()=>void refreshState()}>↻ ОБНОВИТЬ</button></div>{batches.length?<div className="pmBatchRows">{batches.slice(0,20).map(batch=><button key={batch.batchId} className={result?.batch?.batchId===batch.batchId?'active':''} onClick={()=>{setResult({status:'ready',availableImages:collected,requestedProjects:batch.projectCount,batch});patchChannelProductionPrefs(channelId,{lastBatchId:batch.batchId,selectedProjectIds:[]});void productionManagerApi.status(batch.manifestPath).then(setBatchStatus).catch(()=>setBatchStatus(null))}}><span><b>{batch.batchId}</b><small>{new Date(batch.createdAt).toLocaleString('ru-RU')}</small></span><em>{batch.projectCount} проектов</em><em>{batch.completedProjects}/{batch.projectCount}</em><small>{batch.status}</small><strong onClick={e=>{e.stopPropagation();void resume(batch)}}>{busy===batch.batchId?'…':'ОТКРЫТЬ'}</strong></button>)}</div>:<div className="pmEmpty">Для этого канала ещё нет Production batches.</div>}</section></>}
_inspect_v1014/src/ExistingVideos.tsx:31: const [channelId,setChannelId]=useState(channels[0]?.id||''),[profileId,setProfileId]=useState(channels[0]?.youtubeProfileId||''),[videos,setVideos]=useState<YoutubeExistingVideo[]>([]),[baseline,setBaseline]=useState<Record<string,YoutubeExistingVideo>>({}),[lastUndo,setLastUndo]=useState<YoutubeExistingVideo[]>([]),[busy,setBusy]=useState(false),[limit,setLimit]=useState(1000),[order,setOrder]=useState<'oldest'|'newest'>('oldest'),[start,setStart]=useState(()=>{const d=new Date();d.setDate(d.getDate()+1);d.setHours(18,0,0,0);return local(d.toISOString())}),[cadence,setCadence]=useState(2),[meta,setMeta]=useState<ImportedMetadata[]>([]),[expanded,setExpanded]=useState<Set<string>>(new Set()),[filter,setFilter]=useState<ExistingFilter>('private'),[selectedOnly,setSelectedOnly]=useState(false),[sortMode,setSortMode]=useState<'dateAsc'|'dateDesc'|'youtubeOldest'|'youtubeNewest'|'privateFirst'>('dateAsc'),[syncInfo,setSyncInfo]=useState<ExistingVideoSyncResult|null>(null),[thumbs,setThumbs]=useState<Record<string,ThumbState>>({}),[confirm,setConfirm]=useState(false),[backupPath,setBackupPath]=useState('');
_inspect_v1014/src/ExistingVideos.tsx:33: useEffect(()=>{const c=channels.find(x=>x.id===channelId),cached=readExistingCache(channelId);setProfileId(c?.youtubeProfileId||'');if(c)setCadence(c.cadenceDays||2);setVideos(cached?.videos||[]);setBaseline(cached?.baseline||{});setLastUndo(cached?.lastUndo||[]);setSyncInfo(cached?.syncInfo||null);setThumbs({});setMeta([]);setSelectedOnly(false);},[channelId,channels.find(x=>x.id===channelId)?.youtubeProfileId]);
_inspect_v1014/src/ExistingVideos.tsx:35: useEffect(()=>{if(!channelId)return;const t=window.setTimeout(()=>writeExistingCache(channelId,{version:1,updatedAt:new Date().toISOString(),videos,baseline,lastUndo,syncInfo}),220);return()=>window.clearTimeout(t)},[channelId,videos,baseline,lastUndo,syncInfo]);
_inspect_v1014/src/ExistingVideos.tsx:36: async function sync(){if(!profileId){toast('Сначала привяжи YouTube OAuth к каналу');return}if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}setBusy(true);try{const selectedIds=new Set(videos.filter(v=>v.selected).map(v=>v.id));const r=await api.youtubeListExisting(profileId,limit);const next=(r.videos||[]).map((x:any)=>({...x,selected:selectedIds.has(x.id),applyState:'idle'}));setVideos(next);setBaseline(Object.fromEntries(next.map(v=>[v.id,{...v,tags:[...v.tags]}])));setLastUndo([]);setBackupPath('');setThumbs({});setSyncInfo(r);writeExistingCache(channelId,{version:1,updatedAt:new Date().toISOString(),videos:next,baseline:Object.fromEntries(next.map(v=>[v.id,{...v,tags:[...v.tags]}])),lastUndo:[],syncInfo:r});toast(`YouTube: получено ${r.received}/${Math.min(r.youtubeFound||r.received,r.requested||limit)} видео`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);toast('⏸ YouTube quota: синхронизация поставлена на паузу')}else toast(String(e))}finally{setBusy(false)}}
_inspect_v1014/src/ExistingVideos.tsx:90: async function apply(){setConfirm(false);if(!selected.length||!profileId)return;if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}const backup=selected.map(v=>baseline[v.id]).filter(Boolean).map(v=>({...v,tags:[...v.tags]}));setBusy(true);let ok=0;const verifiedIds=new Set<string>();try{const saved=await api.youtubeBackupExisting(profileId,backup);setBackupPath(saved.path||'');for(let i=0;i<selected.length;i++){const v=selected[i];setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'saving',error:undefined}:z));try{const result=await api.youtubeUpdateExisting(profileId,v.id,v.title,v.description,v.tags,v.publishAt,v.privacyStatus);if(result?.metadataAccepted===false)throw new Error(`YouTube не принял metadata ${v.id}`);if(result.scheduleRequested&&result.scheduleAccepted===false){const err=result.scheduleError||'расписание не принято';if(isYoutubeQuotaError(err)){markYoutubeQuotaExceeded(err);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: метаданные сохранены; расписание: ${err}`}:z));ok++;verifiedIds.add(v.id);continue}ok++;verifiedIds.add(v.id);setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'done',verified:true,error:result.metadataVerifyPending||result.scheduleVerifyPending?'YouTube принял запись; контрольное чтение ещё догоняет':undefined}:z))}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: ${String(e)}`}:z))}}if(ok){setLastUndo(backup.filter(v=>verifiedIds.has(v.id)));setBaseline(prev=>{const n={...prev};for(const v of selected.filter(x=>verifiedIds.has(x.id)))n[v.id]={...v,tags:[...v.tags]};return n})}if(!youtubeQuotaState().blocked)toast(ok===selected.length?`✓ ${ok} видео приняты YouTube`:`YouTube принял ${ok}/${selected.length}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);toast('⏸ YouTube quota: пачка сохранена для продолжения')}else toast(`Backup/Update: ${String(e)}`)}finally{setBusy(false)}}
_inspect_v1014/src/ExistingVideos.tsx:104:  <div className="existingToolbar"><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{channels.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><div className={`profileAutoBadge ${profileId?'good':'warn'}`}><small>OAuth</small><b>{profileId?'✓ выбран автоматически':'не подключён'}</b></div><label>Видео<select value={limit} onChange={e=>setLimit(+e.target.value)}><option>30</option><option>50</option><option>100</option><option>200</option><option>500</option><option>1000</option><option>5000</option></select></label><label>SEO порядок<select value={order} onChange={e=>setOrder(e.target.value as any)}><option value="oldest">старые → новые</option><option value="newest">новые → старые</option></select></label><label>Сортировка<select value={sortMode} onChange={e=>setSortMode(e.target.value as any)}><option value="dateAsc">дата ↑</option><option value="dateDesc">дата ↓</option><option value="youtubeOldest">YouTube: старые → новые</option><option value="youtubeNewest">YouTube: новые → старые</option><option value="privateFirst">сначала Private</option></select></label><label>Первая дата<input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Каждые N дней<input type="number" min="1" max="30" value={cadence} onChange={e=>setCadence(+e.target.value||1)}/></label><button disabled={!selected.length} onClick={schedule}>Расставить даты • {selected.length}</button><button disabled={!selected.length} onClick={()=>file.current?.click()}>⬆ Загрузить SEO DOCX • {selected.length}</button></div>
_inspect_v1014/src/ExistingVideos.tsx:108:  {videos.length===0?<div className="empty"><b>Видео ещё не синхронизированы</b><p>Выбери канал и нажми «Синхронизировать».</p></div>:<div className="existingList compact">{shown.map((v,i)=>{const open=expanded.has(v.id);return <article className={`existingCard compact ${open?'open':''} ${v.selected?'selected':''} ${v.applyState||''} ${v.publishAt&&dateCounts[v.publishAt]>1?'dateConflictCard':''}`} key={v.id} onClick={e=>{if((e.target as HTMLElement).closest('button,input,select,textarea,label,a'))return;toggleSelected(v.id)}}><label className="existingCheckHit" onClick={e=>e.stopPropagation()}><input className="existingCheck" type="checkbox" checked={v.selected} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,selected:e.target.checked}:z))}/><i/></label><button className="existingPreview" onClick={e=>{e.stopPropagation();toggleSelected(v.id)}}><VideoThumb video={v} onState={(id,s)=>setThumbs(x=>x[id]===s?x:{...x,[id]:s})}/><em>{v.privacyStatus}</em></button><div className="existingSummary"><small>#{i+1} • {v.id}</small><b>{v.title}</b><p>{short(v.description||'Описание отсутствует')}</p><div className="tagChips">{v.tags.slice(0,5).map(t=><i key={t}>{t}</i>)}{v.tags.length>5&&<i>+{v.tags.length-5}</i>}</div></div><div className="existingSchedule"><input type="datetime-local" value={local(v.publishAt)} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,publishAt:e.target.value?new Date(e.target.value).toISOString():undefined}:z))}/>{v.publishAt&&dateCounts[v.publishAt]>1&&<span className="dateConflict">⚠ Совпадает дата публикации</span>}<b className={v.applyState==='error'?'bad':v.applyState==='done'?'ok':''}>{v.applyState==='done'?'✓ Подтверждено YouTube':v.applyState==='saving'?'Отправляю + проверяю…':v.applyState==='error'?'⚠ Не подтверждено':'К изменению'}</b><button className="expandBtn" onClick={()=>toggle(v.id)}>{open?'Свернуть':'Редактировать'}</button></div>{open&&<div className="existingEditor"><label>Название<input value={v.title} maxLength={100} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,title:e.target.value}:z))}/></label><label>Описание<textarea value={v.description} maxLength={5000} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,description:e.target.value}:z))}/></label><label>Теги<input value={v.tags.join(', ')} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,tags:e.target.value.split(',').map(a=>a.trim()).filter(Boolean)}:z))}/></label><label>Privacy<select value={v.privacyStatus} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,privacyStatus:e.target.value}:z))}><option value="private">PRIVATE</option><option value="unlisted">UNLISTED</option><option value="public">PUBLIC</option></select></label>{v.error&&<div className="errorBox">{v.error}</div>}</div>}</article>})}</div>}
_inspect_v1014/src/App.tsx:79:{channels.length===0?<Empty title="Добавь первый канал" text="Задай график публикаций — дальше VYRON сам рассчитает запас на 60 дней." action="ДОБАВИТЬ КАНАЛ" onClick={()=>setPage('channels')}/>:<div className="panel"><div className="panelHead"><div><small>ПРИОРИТЕТЫ</small><h3>Запас каналов</h3></div></div><div className="table"><div className="tr th"><span>Канал</span><span>Буфер</span><span>Нужно</span><span>Картинки</span><span>Музыка</span><span>MP4</span><span>YouTube</span></div>{rows.map(({c,days,need})=>{const cj=jobs.filter(j=>j.channelId===c.id),p=priorityFor(days);return <button className="tr" key={c.id} onClick={()=>setPage('production')}><span className="channelCell"><i className={`dot ${p}`}/><b>{c.name}</b><small>раз в {c.cadenceDays} дн.</small></span><span className={p}>{days} дн</span><span>{need||'—'}</span><span>{cj.filter(j=>j.coverPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.tracksCount>=c.minTracks).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.finalPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.status==='SCHEDULED').length}/{requiredVideos(c)}</span></button>})}</div></div>}
_inspect_v1014/src/App.tsx:89:<div className="panel"><div className="panelHead"><div><small>INBOX</small><h3>Куда должны попадать файлы</h3></div></div>{channels.length===0?<p>Сначала добавь каналы.</p>:channels.map(c=>{const x=inboxes[c.id];return <div className="inboxRow" key={c.id}><div><b>{c.name}</b><small>{x?.root||`${settings.workspace}/Inbox/${c.name}`}</small></div><span>🎵 {x?.music.length||0}</span><span>🖼 {x?.images.length||0}</span><button onClick={async()=>{try{const v=await api.ensureChannelInbox(settings.workspace,c.name);await api.openLocal(v.root)}catch(e){toast(String(e))}}}>ОТКРЫТЬ INBOX</button></div>})}</div>
_inspect_v1014/src/App.tsx:95:{channels.length===0?<Empty title="Каналов пока нет" text="Добавь канал. По умолчанию публикация — через день, запас — 60 дней." action="СОЗДАТЬ ПЕРВЫЙ" onClick={()=>{const c=addChannel({name:'Новый канал'});setEditing(c.id)}}/>:<div className="channelGrid">{channels.map(c=><div className={`channelCard ${editing===c.id?'editing':''}`} key={c.id}><div className="channelTop"><div><i className="channelAvatar">{c.name.slice(0,2).toUpperCase()}</i><div><b>{c.name}</b><small>{c.genre} • {c.language} • {c.youtubeProfileId?'YouTube ✓':'YouTube не привязан'}</small></div></div><button className="iconBtn" onClick={()=>setEditing(editing===c.id?null:c.id)}>✎</button></div><div className="channelStats"><span><small>ПУБЛИКАЦИЯ</small><b>раз в {c.cadenceDays} дн.</b></span><span><small>ЗАПАС</small><b>{c.targetBufferDays} дней</b></span><span><small>ТРЕКОВ</small><b>{c.minTracks}</b></span><span><small>ДЛИТЕЛЬНОСТЬ</small><b>{c.targetDurationMin} мин</b></span></div>{editing===c.id&&<div className="editGrid"><label>Название<input value={c.name} onChange={e=>updateChannel(c.id,{name:e.target.value})}/></label><label>Жанр<input value={c.genre} onChange={e=>updateChannel(c.id,{genre:e.target.value})}/></label><label>Язык<input value={c.language} onChange={e=>updateChannel(c.id,{language:e.target.value})}/></label><label>Страна<input value={c.country} onChange={e=>updateChannel(c.id,{country:e.target.value})}/></label><label>Раз в N дней<input type="number" min="1" max="30" value={c.cadenceDays} onChange={e=>updateChannel(c.id,{cadenceDays:+e.target.value||1})}/></label><label>Запас, дней<input type="number" min="7" max="365" value={c.targetBufferDays} onChange={e=>updateChannel(c.id,{targetBufferDays:+e.target.value||60})}/></label><label>Треков на видео<input type="number" min="1" max="100" value={c.minTracks} onChange={e=>updateChannel(c.id,{minTracks:+e.target.value||10})}/></label><label>Длительность, мин<input type="number" min="10" max="720" value={c.targetDurationMin} onChange={e=>updateChannel(c.id,{targetDurationMin:+e.target.value||120})}/></label><label>Время публикации<input type="time" value={`${String(c.publishHour).padStart(2,'0')}:${String(c.publishMinute).padStart(2,'0')}`} onChange={e=>{const [h,m]=e.target.value.split(':').map(Number);updateChannel(c.id,{publishHour:h,publishMinute:m})}}/></label><label>YouTube OAuth<select value={c.youtubeProfileId||''} onChange={e=>{const p=profiles.find(x=>x.id===e.target.value);updateChannel(c.id,{youtubeProfileId:e.target.value||undefined,youtubeChannelId:p?.channelId})}}><option value="">— не привязан —</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.channelTitle||p.channelId||p.id}</option>)}</select></label><label className="wide">Формулы названий<textarea value={c.seo.titlePatterns.join('\n')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,titlePatterns:e.target.value.split('\n').filter(Boolean)}})}/></label><label className="wide">Шаблон описания<textarea value={c.seo.descriptionTemplate} onChange={e=>updateChannel(c.id,{seo:{...c.seo,descriptionTemplate:e.target.value}})}/></label><label className="wide">Теги<input value={c.seo.tags.join(', ')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)}})}/></label><label className="wide">Инструкция AI<textarea value={c.seo.aiPrompt||''} placeholder="Например: премиальные французские deep house заголовки, без повторов…" onChange={e=>updateChannel(c.id,{seo:{...c.seo,aiPrompt:e.target.value}})}/></label><div className="editActions"><button className="danger" onClick={()=>{if(confirm(`Удалить ${c.name}? Файлы на диске останутся.`)){removeChannel(c.id);setEditing(null)}}}>УДАЛИТЬ ИЗ VYRON</button><button className="primary" onClick={()=>setEditing(null)}>ГОТОВО</button></div></div>}</div>)}</div>}</>}
_inspect_v1014/src/App.tsx:97:function Production(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false);const filtered=jobs.filter(j=>filter==='all'||j.channelId===filter).sort((a,b)=>a.number-b.number);async function refreshAll(){setBusy(true);try{for(const j of useApp.getState().jobs){const c=channels.find(x=>x.id===j.channelId);if(!c||!j.folder||j.status==='SCHEDULED')continue;try{patchJob(j.id,await api.refreshJob(j.folder,c.minTracks))}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}toast('Статусы обновлены')}finally{setBusy(false)}}async function createPlan(){if(!settings.workspace){const ws=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:ws})}let added=0;let next=[...useApp.getState().jobs];for(const c of channels.filter(c=>c.enabled)){const created=createMissingJobs(c,next);next=[...next,...created];added+=created.length}setJobs(next);await runAutopilotCycle(true);toast(added?`Создано задач: ${added}`:'Дефицита нет')}return <>
_inspect_v1014/src/App.tsx:99:<div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы</option>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><span>{filtered.length} задач</span></div>
_inspect_v1014/src/App.tsx:100:{filtered.length===0?<Empty title="Очередь пустая" text="VYRON рассчитает нужное число видео по графику каждого канала." action="СОЗДАТЬ ПЛАН" onClick={createPlan}/>:<div className="jobs">{filtered.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="job" key={j.id}><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{c?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не созданы'}</b><span>{j.publishAt?`План: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата не назначена'} • треков {j.tracksCount}/{j.minTracks}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>ПАПКА</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,c?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ МУЗЫКА</button>}{j.status==='READY_RENDER'&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('publisher')}>YOUTUBE</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,c?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>ПОВТОРИТЬ</button>}</div></div>})}</div>}</>}
_inspect_v1014/src/App.tsx:102:function Content(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [channelId,setChannelId]=useState(channels[0]?.id||''),[inbox,setInbox]=useState<any>();useEffect(()=>{if(!channelId&&channels[0])setChannelId(channels[0].id)},[channels]);const c=channels.find(x=>x.id===channelId);async function ensure(){if(!c)return;const r=await api.ensureChannelInbox(settings.workspace,c.name);setInbox(r);return r}async function importImages(){if(!c){setPage('channels');return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);const imported=await api.importImages(settings.workspace,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено`)}return <>
_inspect_v1014/src/App.tsx:104:<div className="panel addLine"><select value={channelId} onChange={e=>{setChannelId(e.target.value);setInbox(undefined)}}><option value="">— канал —</option>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button onClick={async()=>{const r=await ensure();if(r)await api.openLocal(r.music)}}>ОТКРЫТЬ MUSIC INBOX</button><button onClick={async()=>{const r=await ensure();if(r)await api.openLocal(r.images)}}>ОТКРЫТЬ IMAGE INBOX</button></div>
_inspect_v1014/src/App.tsx:108:function Competitors(){const channels=useApp(s=>s.channels),competitors=useApp(s=>s.competitors),add=useApp(s=>s.addCompetitor),patch=useApp(s=>s.patchCompetitor),remove=useApp(s=>s.removeCompetitor),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [channelId,setChannelId]=useState(channels[0]?.id||''),[url,setUrl]=useState(''),[name,setName]=useState('');const list=competitors.filter(c=>!channelId||c.channelId===channelId);async function refresh(c:Competitor){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key в Настройках');return}const m=c.url.match(/(?:channel\/)?(UC[\w-]{20,})/);if(!m){toast('Нужен URL с Channel ID UC…');return}try{const r=await api.youtubeStats(settings.youtubeApiKey,m[1]);patch(c.id,{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString(),name:r.title||c.name})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="COMPETITOR RADAR" title="Конкуренты" text="Публичные показатели конкурентов для дальнейшего AI-анализа контентных решений."/><div className="panel addLine"><select value={channelId} onChange={e=>setChannelId(e.target.value)}><option value="">К какому каналу</option>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input placeholder="Название" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="https://youtube.com/channel/UC…" value={url} onChange={e=>setUrl(e.target.value)}/><button className="primary" disabled={!channelId||!name||!url} onClick={()=>{add({id:crypto.randomUUID(),channelId,name,url});setName('');setUrl('')}}>ДОБАВИТЬ</button></div>{list.length===0?<Empty title="Конкурентов нет" text="Добавь каналы для наблюдения."/>:<div className="competitorGrid">{list.map(c=><div className="competitorCard" key={c.id}><div><i className="compAvatar">YT</i><span><b>{c.name}</b><small>{c.url}</small></span></div><div className="compStats"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.views)}</b></span><span><small>ВИДЕО</small><b>{formatNumber(c.videos)}</b></span></div><div className="cardActions"><button onClick={()=>api.openWeb(c.url)}>ОТКРЫТЬ</button><button className="primary small" onClick={()=>refresh(c)}>↻ ОБНОВИТЬ</button><button className="danger mini" onClick={()=>remove(c.id)}>×</button></div></div>)}</div>}</>}
_inspect_v1014/src/App.tsx:110:function Analytics(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),update=useApp(s=>s.updateChannel),toast=useApp(s=>s.toast);async function refresh(c:Channel){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key');return}const id=c.youtubeChannelId||prompt(`Channel ID для ${c.name} (UC…)`)||'';if(!id)return;try{const r=await api.youtubeStats(settings.youtubeApiKey,id);update(c.id,{youtubeChannelId:id,stats:{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString()}})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="ANALYTICS" title="Состояние сети" text="YouTube + производственный запас по каждому каналу."/><div className="analyticsGrid">{channels.map(c=>{const cj=jobs.filter(j=>j.channelId===c.id),days=bufferDays(c,jobs);return <div className="analyticCard" key={c.id}><div className="analyticHead"><div><i>{c.name.slice(0,2).toUpperCase()}</i><span><b>{c.name}</b><small>{c.stats?.updatedAt?new Date(c.stats.updatedAt).toLocaleString('ru-RU'):'YouTube не обновлялся'}</small></span></div><button onClick={()=>refresh(c)}>↻</button></div><div className="analyticNums"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.stats?.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.stats?.views)}</b></span><span><small>ВИДЕО YT</small><b>{formatNumber(c.stats?.videos)}</b></span></div><div className="bufferBar"><div><span>Запас</span><b className={priorityFor(days)}>{days} / {c.targetBufferDays} дн</b></div><i><em style={{width:`${Math.min(100,days/c.targetBufferDays*100)}%`}}/></i></div><div className="analyticFoot"><span>В системе <b>{cj.length}</b></span><span>MP4 <b>{cj.filter(j=>j.finalPath).length}</b></span><span>Scheduled <b>{cj.filter(j=>j.status==='SCHEDULED').length}</b></span></div></div>})}</div></>}
_inspect_v1014/src/App.tsx:112:function Publisher(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [rows,setRows]=useState<ImportedMetadata[]>([]),[files,setFiles]=useState<string[]>([]),[metaChannel,setMetaChannel]=useState(channels[0]?.id||''),[busy,setBusy]=useState(false);const input=useRef<HTMLInputElement>(null);const ready=jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED'].includes(j.status));async function read(fl:FileList|null){if(!fl?.length)return;const out:ImportedMetadata[]=[],names:string[]=[];for(const f of Array.from(fl)){out.push(...parseMetadataFile(f.name,await f.text()));names.push(f.name)}setRows(out);setFiles(names);toast(`Метаданные: ${out.length} записей`)}async function apply(){const used=new Set<string>();let ok=0,miss=0;for(let i=0;i<rows.length;i++){const r=rows[i],named=r.channel?channels.find(c=>c.name.toLowerCase()===r.channel?.toLowerCase()||c.id===r.channel):undefined,channelId=named?.id||metaChannel;const pool=useApp.getState().jobs.filter(j=>(!channelId||j.channelId===channelId)&&!used.has(j.id)).sort((a,b)=>a.number-b.number);const j=r.number?pool.find(x=>x.number===r.number):pool[0];if(!j){miss++;continue}used.add(j.id);const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}toast(`Применено: ${ok}${miss?` • не найдено ${miss}`:''}`)}async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}async function uploadAll(){setBusy(true);let n=0;try{for(const j of useApp.getState().jobs.filter(j=>j.status==='READY_UPLOAD'&&j.finalPath).sort((a,b)=>(a.publishAt||'').localeCompare(b.publishAt||''))){if(await upload(j))n++}toast(`YouTube: загружено ${n}`)}finally{setBusy(false)}}return <>
_inspect_v1014/src/App.tsx:115:<div className="metadataPanel"><div className="metadataHead"><div><small>METADATA INBOX</small><h3>Импорт старого сценария GPT/Word</h3><p>JSON / TXT / CSV можно использовать как переходный вариант. В автопилоте эти поля генерируются и сохраняются без копирования.</p></div><div className="metadataControls"><select value={metaChannel} onChange={e=>setMetaChannel(e.target.value)}><option value="">Все каналы</option>{channels.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button onClick={()=>input.current?.click()}>ВЫБРАТЬ</button><button className="primary" disabled={!rows.length} onClick={apply}>ПРИМЕНИТЬ</button></div></div>{files.length>0&&<div className="metadataSummary"><span>Файлов <b>{files.length}</b></span><span>Записей <b>{rows.length}</b></span><span>Video_XXX <b>{rows.filter(r=>r.number).length}</b></span><button onClick={()=>{setRows([]);setFiles([])}}>ОЧИСТИТЬ</button></div>}{rows.slice(0,5).map((r,i)=><div className="metaRow" key={i}><span><b>{r.number?`VIDEO_${String(r.number).padStart(3,'0')}`:`СТРОКА ${i+1}`}</b><small>{r.source}</small></span><strong>{r.title||'Без названия'}</strong><em>{r.tags?.length||0} тегов</em></div>)}</div>
_inspect_v1014/src/App.tsx:117:{ready.length===0?<Empty title="Нет готовых MP4" text="После появления final.mp4 ролик автоматически попадёт сюда."/>:<div className="publishList">{ready.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="publishCard" key={j.id}><div className="thumb"><span>▶</span><small>VIDEO_{String(j.number).padStart(3,'0')}</small></div><div className="publishMeta"><small>{c?.name} • {j.metadataSource||'template'}</small><input value={j.title} maxLength={100} onChange={e=>patchJob(j.id,{title:e.target.value,metadataLocked:true})}/><textarea value={j.description} onChange={e=>patchJob(j.id,{description:e.target.value,metadataLocked:true})}/><input value={j.tags.join(', ')} onChange={e=>patchJob(j.id,{tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean),metadataLocked:true})}/>{j.status==='UPLOADING'&&<div className="progress"><i style={{width:`${j.uploadProgress||0}%`}}/><span>{Math.round(j.uploadProgress||0)}%</span></div>}</div><div className="publishSide"><label>Дата<input type="datetime-local" value={j.publishAt?new Date(new Date(j.publishAt).getTime()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):''} onChange={e=>patchJob(j.id,{publishAt:new Date(e.target.value).toISOString(),metadataLocked:true})}/></label><span className={`publishState ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</span>{j.status==='READY_UPLOAD'&&<button className="primary" onClick={()=>upload(j)}>ЗАГРУЗИТЬ</button>}{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div></div>})}</div>}</>}
_inspect_v1014/src/App.tsx:124:{tab==='integrations'&&<div className="settingsStack"><div className="settingsCard"><h3>ENDLUME Studio</h3><p>VYRON отправляет готовые проекты в RenderQueue.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>ВЫБРАТЬ</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>ОТКРЫТЬ</button>}</div><small className="note">Полностью нулевой ручной рендер требует companion-update ENDLUME, который будет читать RenderQueue.</small></div><div className="settingsCard"><h3>OpenAI API — опционально</h3><p>Не требуется для бесплатного режима. Оставь пустым и используй ChatGPT Plus → Metadata Inbox. API нужен только если позже захочешь полностью фоновую генерацию без открытия ChatGPT.</p><input className="wideInput" type="password" placeholder="sk-…" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/><div className="pathLine"><input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/><button onClick={()=>patch({openaiModel:''})}>ОЧИСТИТЬ</button></div></div><div className="settingsCard"><h3>YouTube OAuth</h3><p>Desktop OAuth открывает Google в браузере один раз. После этого токен обновляется локально автоматически.</p><input className="wideInput" placeholder="Google OAuth Client ID …apps.googleusercontent.com" value={s.youtubeOAuthClientId} onChange={e=>patch({youtubeOAuthClientId:e.target.value.trim()})}/><input className="wideInput" type="password" autoComplete="off" placeholder="Google OAuth Client Secret — вводится один раз" value={oauthSecret} onChange={e=>setOauthSecret(e.target.value.trim())}/><small className="note">Client Secret не сохраняется в общих настройках и не выводится в логах. После успешного OAuth он хранится только внутри локального OAuth-профиля.</small><div className="cardActions"><button className="primary" disabled={connecting||!s.youtubeOAuthClientId||!oauthSecret} onClick={async()=>{setConnecting(true);setOauthError(false);setOauthMessage('OAuth: ожидаю подтверждение Google…');try{const p=await api.youtubeConnect(s.youtubeOAuthClientId,oauthSecret);setOauthSecret('');const m=`YouTube подключён: ${p.channelTitle||p.channelId||'канал'}`;setOauthMessage(m);toast(m);log(m);await refreshProfiles()}catch(e){const m=`OAuth: ${String(e)}`;setOauthError(true);setOauthMessage(m);toast(m);log(m,'error')}finally{setConnecting(false)}}}>{connecting?'ЖДУ GOOGLE…':'ПОДКЛЮЧИТЬ YOUTUBE'}</button><button onClick={async()=>{try{const p=await refreshProfiles();toast(p.length?`OAuth профилей: ${p.length}`:'OAuth профили не найдены')}catch(e){toast(String(e))}}}>↻ ОБНОВИТЬ</button></div>{oauthMessage&&<div className={oauthError?'errorBox':profiles.length?'successBox':'publisherNotice'}>{oauthMessage}</div>}{profiles.map(p=><div className="oauthProfile" key={p.id}><span><b>{p.channelTitle||'YouTube канал'}</b><small>{p.channelId||p.clientIdMasked}</small></span><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refreshProfiles()}}>ОТКЛЮЧИТЬ</button></div>)}<label className="compactLabel">YouTube category ID<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div><div className="settingsCard"><h3>YouTube Intelligence</h3><p>Автоматически обновляет приватную аналитику своих каналов и публичные показатели конкурентов, пока VYRON запущен.</p><Toggle label="Автообновление аналитики" text="Обновлять подключённые каналы и список конкурентов без ручной кнопки." value={s.youtubeIntelligenceAutoRefresh} onChange={v=>patch({youtubeIntelligenceAutoRefresh:v})}/><label className="compactLabel">Интервал, минут<input type="number" min="15" max="1440" value={s.youtubeIntelligenceRefreshMin} onChange={e=>patch({youtubeIntelligenceRefreshMin:Math.max(15,+e.target.value||30)})}/></label><small className="note">Для приватной аналитики включи YouTube Analytics API в Google Cloud и один раз переподключи OAuth после установки 0.6.0. Конкуренты используют только публичные данные.</small></div><div className="settingsCard"><h3>YouTube Data API Key</h3><p>Только публичная статистика своих каналов и конкурентов.</p><input className="wideInput" type="password" placeholder="AIza…" value={s.youtubeApiKey} onChange={e=>patch({youtubeApiKey:e.target.value.trim()})}/></div><div className="settingsCard"><h3>MusicVault AI</h3><p>VYRON принимает музыку через Inbox каждого канала. Следующий шаг — кнопка «Отправить в VYRON» прямо в MusicVault AI, чтобы не переносить файлы вручную.</p></div></div>}
