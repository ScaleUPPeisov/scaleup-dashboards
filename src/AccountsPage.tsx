import React,{useEffect,useRef,useState} from 'react';
import {api,type GoogleConfigStatus,type YoutubeProfileHealth} from './api';
import {useApp} from './store';
import type {YoutubeProfile} from './types';
import {findFutureChannelMatch} from './channelIdentity';

type BrowserOption={id:string;label:string;available:boolean};
const ago=(iso?:string)=>{if(!iso)return '—';const ms=Date.now()-new Date(iso).getTime();const m=Math.max(0,Math.round(ms/60000));return m<1?'только что':m<60?`${m} мин. назад`:m<1440?`${Math.round(m/60)} ч. назад`:`${Math.round(m/1440)} дн. назад`};

export function AccountsPage(){
 const channels=useApp(s=>s.channels),addChannel=useApp(s=>s.addChannel),updateChannel=useApp(s=>s.updateChannel),toast=useApp(s=>s.toast),settings=useApp(s=>s.settings),patchSettings=useApp(s=>s.patchSettings);
 const [profiles,setProfiles]=useState<YoutubeProfile[]>([]),[health,setHealth]=useState<Record<string,YoutubeProfileHealth>>({}),[config,setConfig]=useState<GoogleConfigStatus|null>(null),[busy,setBusy]=useState(false),[checking,setChecking]=useState(false),[browserOpen,setBrowserOpen]=useState(false),[browsers,setBrowsers]=useState<BrowserOption[]>([]),[browser,setBrowser]=useState(localStorage.getItem('vyron:oauth-browser')||'default');const file=useRef<HTMLInputElement>(null);
 function bindProfile(p:YoutubeProfile){
  if(!p.channelId)return;
  const state=useApp.getState();
  const exact=state.channels.find(x=>x.youtubeChannelId===p.channelId||x.youtubeProfileId===p.id);
  if(exact){state.updateChannel(exact.id,{youtubeProfileId:p.id,youtubeChannelId:p.channelId});return{channel:exact,mode:'existing' as const}}
  const future=findFutureChannelMatch(state.channels,p.channelTitle);
  if(future){state.updateChannel(future.id,{youtubeProfileId:p.id,youtubeChannelId:p.channelId});return{channel:future,mode:'future' as const}}
  const created=state.addChannel({name:p.channelTitle||'YouTube канал',youtubeProfileId:p.id,youtubeChannelId:p.channelId});return{channel:created,mode:'created' as const}
 }
 async function refresh(){const [p,c]=await Promise.all([api.youtubeProfiles(),api.youtubeGoogleConfig()]);setProfiles(p);setConfig(c);for(const profile of p)bindProfile(profile);return p}
 useEffect(()=>{void refresh().catch(e=>toast(String(e)))},[]);
 async function importCredentials(fl:FileList|null){const f=fl?.[0];if(!f)return;setBusy(true);try{const c=await api.youtubeImportGoogleConfig(await f.text(),settings.youtubeApiKey||'');setConfig(c);toast('Google Credentials сохранены глобально')}catch(e){toast(String(e))}finally{setBusy(false)}}
 async function askBrowser(){if(!config?.readyForOAuth){toast('Сначала один раз импортируй credentials.json текущего VYRON OAuth Client. После этого каждый канал подключается через выбор браузера.');file.current?.click();return}setBusy(true);try{const rows=await api.youtubeOauthBrowsers();const available=rows.filter(x=>x.available);setBrowsers(available.length?available:[{id:'default',label:'Браузер по умолчанию',available:true}]);if(!available.some(x=>x.id===browser))setBrowser('default');setBrowserOpen(true)}catch{setBrowsers([{id:'default',label:'Браузер по умолчанию',available:true}]);setBrowserOpen(true)}finally{setBusy(false)}}
 async function connect(){setBrowserOpen(false);setBusy(true);try{localStorage.setItem('vyron:oauth-browser',browser);const p=await api.youtubeConnectGlobal(browser);const binding=bindProfile(p);await refresh();toast(binding?.mode==='future'?`✓ ${p.channelTitle||p.channelId||'YouTube канал'} привязан к будущему каналу ${binding.channel.name}. Все готовые проекты сохранены.`:`✓ ${p.channelTitle||p.channelId||'YouTube канал'} подключён и привязан автоматически`)}catch(e){toast(String(e))}finally{setBusy(false)}}
 async function checkAll(){setChecking(true);const next:Record<string,YoutubeProfileHealth>={};for(const p of profiles){try{next[p.id]=await api.youtubeProfileHealth(p.id)}catch(e){next[p.id]={ok:false,status:'RECONNECT_REQUIRED',error:String(e)}}}setHealth(next);setChecking(false)}
 return <>
  <div className="pageHeader"><div><small>ACCOUNT CENTER</small><h1>Аккаунты YouTube</h1><p>Один Google OAuth Client обслуживает все каналы. Можно заранее создать будущий канал и выпускать для него проекты без YouTube. При подключении VYRON YT PEISOV сначала ищет будущий канал с тем же названием и привязывает OAuth к нему.</p></div><div className="headerActions"><button disabled={checking||!profiles.length} onClick={checkAll}>{checking?'Проверяю…':'↻ Проверить'}</button><button className="primary compactAction" disabled={busy} onClick={askBrowser}>{config?.readyForOAuth?'+ Добавить канал':'Сначала настроить OAuth Client'}</button></div></div>
  <input ref={file} hidden type="file" accept=".json,application/json" onChange={e=>{void importCredentials(e.target.files);e.currentTarget.value=''}}/>
  <section className="panel googleConfigCard"><div><small>GLOBAL GOOGLE CONFIG</small><h3>Одна конфигурация для всех каналов</h3><p>credentials.json нужен один раз для самого VYRON OAuth Client, а не для каждого канала. После этого нажимаешь «Добавить канал», выбираешь нужный браузер и нужный Google/YouTube аккаунт.</p>{!config?.readyForOAuth&&<div className="publisherNotice"><b>OAuth Client ещё не готов</b><span>Client ID без Client Secret недостаточен для token exchange. Импортируй credentials.json один раз — затем каналы переподключаются только через браузер.</span></div>}</div><div className="configChecks"><span className={config?.configured?'good':''}>OAuth Client <b>{config?.configured?'✓':'—'}</b></span><span className={config?.projectId?'good':''}>Project <b>{config?.projectId||'—'}</b></span><span className={config?.hasSecret?'good':'warn'}>Client Secret <b>{config?.hasSecret?'✓':'НУЖЕН 1 РАЗ'}</b></span><span className={config?.readyForOAuth?'good':'warn'}>OAuth готов <b>{config?.readyForOAuth?'✓':'НЕТ'}</b></span><span className={settings.youtubeApiKey?'good':''}>Public API Key <b>{settings.youtubeApiKey?'✓':'не нужен для OAuth'}</b></span></div><div className="googleConfigActions"><button disabled={busy} onClick={()=>file.current?.click()}>Импортировать credentials.json</button><label>Public API Key<input type="password" placeholder="опционально" value={settings.youtubeApiKey} onChange={e=>patchSettings({youtubeApiKey:e.target.value.trim()})}/></label></div></section>
  <section className="panel accountsPanel"><div className="panelHead"><div><small>YOUTUBE ACCOUNTS</small><h3>{profiles.length?`${profiles.length} подключено`:'Аккаунтов пока нет'}</h3></div></div>{!profiles.length?<div className="empty"><b>Подключи первый YouTube-канал</b><p>VYRON YT PEISOV спросит браузер и после OAuth привяжет реальный канал к одноимённому будущему каналу. Если такого канала нет — создаст новый.</p></div>:<div className="accountList">{profiles.map(p=>{const h=health[p.id];const bound=channels.find(c=>c.youtubeProfileId===p.id||c.youtubeChannelId===p.channelId);return <article className="accountRow" key={p.id}>{h?.thumbnail?<img src={h.thumbnail} loading="lazy"/>:<div className="accountAvatar">YT</div>}<div className="accountMain"><b>{h?.channelTitle||p.channelTitle||'YouTube канал'}</b><small>{p.channelId||'Channel ID ещё не определён'}</small><div className="accountBadges"><span className={h?.ok?'good':'warn'}>{h?.ok?'● OAuth healthy':h?'● Требуется внимание':'● Проверяется'}</span><span>{bound?`VYRON YT PEISOV: ${bound.name}`:'Автопривязка…'}</span>{p.preferredBrowser&&<span>Браузер: {p.preferredBrowser}</span>}</div></div><div className="accountMeta"><small>Подключён</small><b>{ago(p.connectedAt)}</b><small>Token</small><b>{h?.status||'—'}</b></div><div className="accountActions"><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refresh()}}>Отключить</button></div></article>})}</div>}</section>
  {browserOpen&&<div className="modalBackdrop" onMouseDown={()=>setBrowserOpen(false)}><section className="confirmModal browserPicker" onMouseDown={e=>e.stopPropagation()}><small>GOOGLE OAUTH</small><h2>Через какой браузер открыть Google?</h2><p>Выбери браузер, где уже открыт нужный Gmail. VYRON YT PEISOV откроет OAuth именно там.</p><div className="browserGrid">{browsers.map(x=><button key={x.id} className={browser===x.id?'active':''} onClick={()=>setBrowser(x.id)}><b>{x.label}</b><small>{x.id==='default'?'Использовать системный браузер':'Открыть этот браузер'}</small></button>)}</div><footer><button onClick={()=>setBrowserOpen(false)}>Отмена</button><button className="primary" onClick={connect}>Продолжить через {browsers.find(x=>x.id===browser)?.label||'браузер'}</button></footer></section></div>}
 </>
}
