# VYRON 1.0.15 exact release source audit

## Identity / versions
- package version: 1.0.15
- tauri version: 1.0.15
- productName: VYRON
- identifier: studio.channelflow.desktop
- updater endpoints: ['https://raw.githubusercontent.com/ScaleUPPeisov/scaleup-dashboards/main/vyron-updates/latest.json']

## src file tree
- `src/AccountsPage.tsx`
- `src/AnalyticsPage.tsx`
- `src/api.ts`
- `src/App.tsx`
- `src/autopilotCore.ts`
- `src/autopilotRuntime.ts`
- `src/channelRunway.test.ts`
- `src/ChannelRunway.tsx`
- `src/channelRunwayCore.ts`
- `src/ChannelRunwayScheduler.tsx`
- `src/channelRunwayStore.ts`
- `src/channelSchedule.test.ts`
- `src/channelSchedule.ts`
- `src/channelSort.test.ts`
- `src/channelSort.ts`
- `src/ChannelsOS.tsx`
- `src/commandCenter.test.ts`
- `src/CommandCenter.tsx`
- `src/commandCenterCore.ts`
- `src/commandCenterStore.ts`
- `src/CompetitorsPage.tsx`
- `src/core.ts`
- `src/DashboardOS.tsx`
- `src/ExistingVideos.tsx`
- `src/main.tsx`
- `src/metadata.ts`
- `src/metadataCoverage.test.ts`
- `src/MetadataPage.schedule.test.ts`
- `src/MetadataPage.tsx`
- `src/notificationCenter.ts`
- `src/NotificationStack.tsx`
- `src/production-manager.css`
- `src/ProductionManager.tsx`
- `src/productionManagerApi.ts`
- `src/ProductionOS.tsx`
- `src/productionPrefs.test.ts`
- `src/productionPrefs.ts`
- `src/ProductionStatusBridge.tsx`
- `src/ProductionWorkspace.tsx`
- `src/PublisherOS.tsx`
- `src/QuotaMeter.tsx`
- `src/RecoveryGate.tsx`
- `src/SettingsOS.tsx`
- `src/store.ts`
- `src/styles.css`
- `src/types.ts`
- `src/v060.css`
- `src/v070.css`
- `src/v080.css`
- `src/v081.css`
- `src/v090.css`
- `src/v095.css`
- `src/YouTubeCenter.tsx`
- `src/YouTubeDataTools.tsx`
- `src/youtubeExisting.ts`
- `src/youtubeIntelligence.ts`
- `src/youtubeQuota.test.ts`
- `src/youtubeQuota.ts`
- `src/youtubeQuotaPlanner.test.ts`
- `src/youtubeWorkflow.ts`

## Rust source tree
- `src-tauri/src/ai.rs`
- `src-tauri/src/files.rs`
- `src-tauri/src/lib.rs`
- `src-tauri/src/license.rs`
- `src-tauri/src/main.rs`
- `src-tauri/src/production_manager_tests.rs`
- `src-tauri/src/production_manager.rs`
- `src-tauri/src/storage.rs`
- `src-tauri/src/system.rs`
- `src-tauri/src/updater_bridge.rs`
- `src-tauri/src/youtube_intelligence.rs`
- `src-tauri/src/youtube.rs`

## Branding / navigation / settings / dashboard symbols
.vyron-v110-audit/src/CommandCenter.tsx:25:  {next?<div className="commandToday"><div><small>СЕГОДНЯ</small><h4>{next.channel.name}</h4><p>{next.record?.runwayDays===undefined?'Нет свежих данных о запасе публикаций.':`Видео в запасе примерно на ${next.record.runwayDays} дн.`}</p></div><div className="commandTodayFacts"><span>Запас<b>{next.record?.runwayDays===undefined?'—':`${next.record.runwayDays} дней`}</b></span><span>Готовность{nextReady?<div className="readinessCompact"><b><strong>{nextReady.readyToPublish}</strong><i>/</i><em>{nextReady.targetProjects}</em></b><small>{nextReady.percent}% готово</small><span><i style={{width:`${nextReady.percent}%`}}/></span></div>:<b>—</b>}</span><span>Рекомендация<b>{next.record?.runwayDays!==undefined&&next.record.runwayDays<=14?'Готовить в первую очередь':next.record?.runwayDays!==undefined&&next.record.runwayDays<=45?'Продолжить производство':'Запас пока достаточный'}</b></span><span>График<b>{scheduleDescription(next.channel)}</b></span></div><button className="primary" onClick={()=>openProduction(next.channel.id)}>Открыть производство</button></div>:<div className="commandEmpty"><b>Нет активных каналов</b><span>Включите канал, чтобы VYRON мог показать производственный приоритет.</span></div>}
.vyron-v110-audit/src/channelSchedule.ts:7:const EVENT='vyron-channel-schedule-changed';
.vyron-v110-audit/src/channelSchedule.ts:8:export const existingCacheKey=(channelId:string)=>`vyron:existing-cache:v1:${channelId}`;
.vyron-v110-audit/src/RecoveryGate.tsx:5:import {notifyError,notifyInfo,notifySuccess} from './notificationCenter';
.vyron-v110-audit/src/RecoveryGate.tsx:8:  const booted=useApp(s=>s.booted),workspace=useApp(s=>s.settings.workspace),channels=useApp(s=>s.channels),setPage=useApp(s=>s.setPage);
.vyron-v110-audit/src/RecoveryGate.tsx:20:    <small>ВОССТАНОВЛЕНИЕ VYRON</small><h2>Производство было прервано</h2><h3>{recovery.channelName}</h3>
.vyron-v110-audit/src/RecoveryGate.tsx:22:    {!confirmRestart?<><p>Можно продолжить с последнего подтверждённого checkpoint. Готовые проекты не будут пересоздаваться.</p><div className="recoveryCountdown"><small>Автоматическое продолжение через</small><b>{seconds}</b><span>сек.</span></div><footer><button className="primary" disabled={busy} onClick={()=>void resumeNow()}>{busy?'ВОССТАНАВЛИВАЮ…':'ПРОДОЛЖИТЬ'}</button><button disabled={busy} onClick={requestRestart}>НАЧАТЬ ЗАНОВО</button></footer></>:<><div className="recoveryWarning"><b>Начать новую сборку?</b><p>Уже создано {recovery.completedProjects} проектов. Они не будут удалены. VYRON создаст новый batchId и сохранит старую незавершённую сборку.</p></div><footer><button className="danger" disabled={busy} onClick={()=>void restartNow()}>{busy?'СОЗДАЮ…':'НАЧАТЬ НОВУЮ СБОРКУ'}</button><button disabled={busy} onClick={()=>{setConfirmRestart(false);decided.current=false;setDeadline(Date.now()+60_000)}}>ОТМЕНА</button></footer></>}
.vyron-v110-audit/src/App.tsx:7:import type { AutopilotSummary, Channel, Competitor, InboxScan, JobStatus, LicenseStatus, Page, VideoJob, YoutubeProfile } from './types';
.vyron-v110-audit/src/App.tsx:14:import { DashboardOS } from './DashboardOS';
.vyron-v110-audit/src/App.tsx:20:import { SettingsOS } from './SettingsOS';
.vyron-v110-audit/src/App.tsx:21:import { isPermissionGranted, requestPermission, sendNotification } from '@tauri-apps/plugin-notification';
.vyron-v110-audit/src/App.tsx:22:import {NotificationCenter} from './NotificationStack';
.vyron-v110-audit/src/App.tsx:23:import {notifyError,notifyInfo,notifySuccess} from './notificationCenter';
.vyron-v110-audit/src/App.tsx:28:  const notifiedKey='vyron:update-notified-version';
.vyron-v110-audit/src/App.tsx:32:    if(!granted&&localStorage.getItem('vyron:notification-permission-requested')!=='1'){
.vyron-v110-audit/src/App.tsx:33:      localStorage.setItem('vyron:notification-permission-requested','1');
.vyron-v110-audit/src/App.tsx:37:      sendNotification({title:`VYRON ${version} доступен`,body:'Новая версия готова. Открой VYRON и нажми «Установить и перезапустить».'});
.vyron-v110-audit/src/App.tsx:44:  {page:'dashboard',icon:'⌂',label:'Главная'},
.vyron-v110-audit/src/App.tsx:50:  {page:'settings',icon:'⚙',label:'Настройки'}
.vyron-v110-audit/src/App.tsx:55:  const booted=useApp(s=>s.booted),hydrate=useApp(s=>s.hydrate),settings=useApp(s=>s.settings),page=useApp(s=>s.page),setPage=useApp(s=>s.setPage),log=useApp(s=>s.log),patchJob=useApp(s=>s.patchJob);
.vyron-v110-audit/src/App.tsx:56:  const [license,setLicense]=useState<LicenseStatus|null>(null),[update,setUpdate]=useState<any>(null);
.vyron-v110-audit/src/App.tsx:57:  useEffect(()=>{api.loadState().then(hydrate).catch(e=>{hydrate(EMPTY_STATE);useApp.getState().log(`Не удалось загрузить состояние: ${String(e)}`,'error')});api.license().then(setLicense).catch(()=>setLicense({valid:false}))},[]);
.vyron-v110-audit/src/App.tsx:58:  useEffect(()=>{if(booted&&!settings.workspace){api.defaultWorkspace().then(workspace=>{useApp.getState().patchSettings({workspace});useApp.getState().log(`Workspace: ${workspace}`)}).catch(e=>log(`Workspace: ${String(e)}`,'error'))}},[booted,settings.workspace]);
.vyron-v110-audit/src/App.tsx:59:  useEffect(()=>{if(!booted||!settings.autoCheckUpdates)return;let live=true;const run=()=>api.checkUpdate().then(u=>{if(live&&u?.version){setUpdate(u);notifyInfo(`Доступно обновление VYRON ${u.version}`,'Новая версия готова к установке.',{operationId:`update-available:${u.version}`});void notifyUpdateAvailable(String(u.version))}}).catch(()=>{});void run();const once=window.setTimeout(()=>void run(),30_000);const recurring=window.setInterval(()=>void run(),6*60*60_000);return()=>{live=false;window.clearTimeout(once);window.clearInterval(recurring)}},[booted,settings.autoCheckUpdates]);
.vyron-v110-audit/src/App.tsx:60:  useEffect(()=>{if(!booted)return;void api.appVersion().then(v=>{const expected=localStorage.getItem('vyron:update-installing-version');if(expected&&expected===v){localStorage.removeItem('vyron:update-installing-version');notifySuccess('Обновление установлено',`VYRON обновлён до версии ${v}.`,{operationId:`update-installed:${v}`})}})},[booted]);
.vyron-v110-audit/src/App.tsx:61:  useEffect(()=>{document.documentElement.classList.toggle('reduceMotion',settings.reduceMotion)},[settings.reduceMotion]);
.vyron-v110-audit/src/App.tsx:63:  useEffect(()=>{if(!booted||!settings.autopilotEnabled)return;void runAutopilotCycle();const ms=Math.max(10,settings.autopilotIntervalSec||30)*1000;const id=window.setInterval(()=>void runAutopilotCycle(),ms);return()=>window.clearInterval(id)},[booted,settings.autopilotEnabled,settings.autopilotIntervalSec]);
.vyron-v110-audit/src/App.tsx:64:  if(!booted||!license)return <Boot/>;
.vyron-v110-audit/src/App.tsx:65:  if(!license.valid)return <Activation onActivated={setLicense}/>;
.vyron-v110-audit/src/App.tsx:66:  const screen=page==='dashboard'||page==='autopilot'?<DashboardOS/>:page==='accounts'?<SettingsOS license={license}/>:page==='channels'?<ChannelsOS/>:page==='production'||page==='content'?<ProductionOS/>:page==='youtube'?<YouTubeCenter/>:page==='competitors'?<CompetitorsPage/>:page==='analytics'?<AnalyticsPage/>:page==='metadata'?<YouTubeCenter initialTab='metadata'/>:page==='existing'?<YouTubeCenter initialTab='uploaded'/>:page==='publisher'?<YouTubeCenter initialTab='queue'/>:<SettingsOS license={license}/>;
.vyron-v110-audit/src/App.tsx:67:  return <div className="appShell"><ChannelRunwayScheduler/><ProductionStatusBridge/><RecoveryGate/><Sidebar page={page} setPage={setPage}/><main className="main"><Topbar/><div className="pageWrap" key={page}>{screen}</div></main>{settings.fpsMonitor&&<FpsMonitor/>}<NotificationCenter/>{update&&<UpdateNotice update={update} onLater={()=>setUpdate(null)}/>}<div className="bgGlow a"/><div className="bgGlow b"/></div>
.vyron-v110-audit/src/App.tsx:70:function Boot(){return <div className="boot"><div className="logoMark"><span>▶</span></div><b>VYRON</b><small>AUTONOMOUS CONTENT OS</small><i/></div>}
.vyron-v110-audit/src/App.tsx:71:function Activation({onActivated}:{onActivated:(x:LicenseStatus)=>void}){const version=useRuntimeVersion();const [key,setKey]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');return <div className="activation"><div className="activationCard"><div className="brand big"><div className="brandMark">▶</div><div><b>VYRON</b><small>CONTENT OS</small></div></div><h1>Активация VYRON</h1><p>Используется совместимая лицензия владельца ENDLUME Studio.</p><input autoFocus value={key} placeholder="Ключ ENDLUME / VYRON" onChange={e=>setKey(e.target.value)} onKeyDown={e=>e.key==='Enter'&&document.getElementById('activate')?.click()}/>{error&&<div className="errorBox">{error}</div>}<button id="activate" className="primary full" disabled={busy||!key.trim()} onClick={async()=>{setBusy(true);setError('');try{onActivated(await api.activate(key))}catch(e){setError(String(e))}finally{setBusy(false)}}}>{busy?'ПРОВЕРЯЮ…':'АКТИВИРОВАТЬ →'}</button><small>VYRON {version||'…'} • macOS Apple Silicon</small></div></div>}
.vyron-v110-audit/src/App.tsx:72:function Sidebar({page,setPage}:{page:Page;setPage:(p:Page)=>void}){const auto=useApp(s=>s.settings.autopilotEnabled);return <aside className="sidebar"><div className="brand"><div className="brandMark">▶</div><div><b>VYRON</b><small>AUTONOMOUS OS</small></div></div><nav>{nav.map(n=><button key={n.page} className={page===n.page?'active':''} onClick={()=>setPage(n.page)}><span>{n.icon}</span>{n.label}{n.page==='autopilot'&&<i className={`navPulse ${auto?'on':''}`}/>}</button>)}</nav><div className="sideFoot"><i className={auto?'autoOn':''}/><div><b>{auto?'AUTOPILOT ON':'LOCAL CORE'}</b><small>{auto?'очередь работает':'состояние сохраняется'}</small></div></div></aside>}
.vyron-v110-audit/src/App.tsx:73:function Topbar(){const version=useRuntimeVersion();const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),auto=useApp(s=>s.settings.autopilotEnabled);const errors=jobs.filter(j=>j.status==='ERROR').length;return <header className="topbar"><div><span className="crumb">VYRON {version||'…'}</span><b>{new Intl.DateTimeFormat('ru-RU',{weekday:'long',day:'2-digit',month:'long'}).format(new Date())}</b></div><div className="topStatus"><span><i className={auto?'ok':'idle'}/> {auto?'автопилот':'ручной режим'}</span><span><i className="ok"/> {channels.length} каналов</span><span className={errors?'warn':''}>{errors?`⚠ ${errors} ошибок`:'✓ система готова'}</span></div></header>}
.vyron-v110-audit/src/App.tsx:77:function Dashboard(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setPage=useApp(s=>s.setPage),auto=useApp(s=>s.settings.autopilotEnabled);const now=new Date();const rows=channels.filter(c=>c.enabled).map(c=>({c,days:bufferDays(c,jobs,now),need:deficit(c,jobs,now)})).sort((a,b)=>a.days-b.days);const urgent=rows.filter(r=>r.days<14).length;return <>
.vyron-v110-audit/src/App.tsx:78:<PageHeader eyebrow="CONTROL CENTER" title="Что происходит сейчас" text={auto?'Автопилот сам поддерживает запас, распределяет входящие файлы и двигает готовые видео дальше.':'Включи Автопилот — VYRON начнёт обслуживать производственную очередь автоматически.'} actions={<button className="primary" onClick={()=>setPage('autopilot')}>{auto?'ОТКРЫТЬ АВТОПИЛОТ':'НАСТРОИТЬ АВТОПИЛОТ'}</button>}/>
.vyron-v110-audit/src/App.tsx:80:{channels.length===0?<Empty title="Добавь первый канал" text="Задай график публикаций — дальше VYRON сам рассчитает запас на 60 дней." action="ДОБАВИТЬ КАНАЛ" onClick={()=>setPage('channels')}/>:<div className="panel"><div className="panelHead"><div><small>ПРИОРИТЕТЫ</small><h3>Запас каналов</h3></div></div><div className="table"><div className="tr th"><span>Канал</span><span>Буфер</span><span>Нужно</span><span>Картинки</span><span>Музыка</span><span>MP4</span><span>YouTube</span></div>{rows.map(({c,days,need})=>{const cj=jobs.filter(j=>j.channelId===c.id),p=priorityFor(days);return <button className="tr" key={c.id} onClick={()=>setPage('production')}><span className="channelCell"><i className={`dot ${p}`}/><b>{c.name}</b><small>раз в {c.cadenceDays} дн.</small></span><span className={p}>{days} дн</span><span>{need||'—'}</span><span>{cj.filter(j=>j.coverPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.tracksCount>=c.minTracks).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.finalPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.status==='SCHEDULED').length}/{requiredVideos(c)}</span></button>})}</div></div>}
.vyron-v110-audit/src/App.tsx:82:function SystemCard(){const s=useApp(x=>x.settings),channels=useApp(x=>x.channels);return <div className="panel systemCard"><div className="panelHead"><div><small>СИСТЕМА</small><h3>Автономный контур</h3></div></div><div className="flow"><b>SUNO</b><i>→</i><b>VYRON</b><i>→</i><b>ENDLUME</b><i>→</i><b>YouTube</b></div><div className="sysRows"><span>Workspace <b>{s.workspace?'готов':'нет'}</b></span><span>Метаданные <b>{s.openaiApiKey?'AI API подключён':'ChatGPT / Inbox'}</b></span><span>ENDLUME <b>{s.endlumePath?'подключён':'не выбран'}</b></span><span>YouTube OAuth <b>{channels.some(c=>c.youtubeProfileId)?'привязан':'не привязан'}</b></span></div></div>}
.vyron-v110-audit/src/App.tsx:84:function Autopilot(){const settings=useApp(s=>s.settings),patch=useApp(s=>s.patchSettings),channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [busy,setBusy]=useState(false),[last,setLast]=useState<AutopilotSummary|null>(null),[inboxes,setInboxes]=useState<Record<string,InboxScan>>({});async function run(){setBusy(true);try{const r=await runAutopilotCycle(true);setLast(r);toast(r.errors?`Цикл завершён: ошибок ${r.errors}`:'Автопилот: цикл завершён')}finally{setBusy(false)}}async function loadInboxes(){if(!settings.workspace)return;const next:Record<string,InboxScan>={};for(const c of channels){try{next[c.id]=await api.scanChannelInbox(settings.workspace,c.name)}catch{}}setInboxes(next)}useEffect(()=>{void loadInboxes()},[settings.workspace,channels.length]);return <>
.vyron-v110-audit/src/App.tsx:86:<div className={`autopilotHero ${settings.autopilotEnabled?'running':''}`}><div><i className="autoOrb">◉</i><span><small>СОСТОЯНИЕ</small><h2>{settings.autopilotEnabled?'АВТОПИЛОТ ВКЛЮЧЁН':'АВТОПИЛОТ ВЫКЛЮЧЕН'}</h2><p>Проверка каждые {Math.max(10,settings.autopilotIntervalSec)} сек. Состояние очереди хранится на диске.</p></span></div><button className={`power ${settings.autopilotEnabled?'on':''}`} onClick={()=>patch({autopilotEnabled:!settings.autopilotEnabled})}>{settings.autopilotEnabled?'ВЫКЛЮЧИТЬ':'ВКЛЮЧИТЬ'}</button></div>
.vyron-v110-audit/src/App.tsx:87:<div className="metrics six"><Metric label="НУЖНА КАРТИНКА" value={jobs.filter(j=>j.status==='NEED_IMAGE').length} note="Inbox/Images"/><Metric label="ЖДЁТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note={`${settings.tracksPerVideo} треков`} tone="orange"/><Metric label="РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="RenderQueue" tone="purple"/><Metric label="MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="готово" tone="green"/><Metric label="UPLOAD" value={jobs.filter(j=>j.status==='UPLOADING').length} note="resumable" tone="blue"/><Metric label="ОШИБКИ" value={jobs.filter(j=>j.status==='ERROR').length} note="не блокируют очередь" tone="red"/></div>
.vyron-v110-audit/src/App.tsx:88:<div className="autoSteps">{[['01','План 60 дней',settings.autoCreatePlan,'autoCreatePlan'],['02','Музыка → по проектам',settings.autoAssignMusic,'autoAssignMusic'],['03','Изображение → 1 проект',settings.autoAssignImages,'autoAssignImages'],['04','AI title/description/tags',settings.autoGenerateMetadata,'autoGenerateMetadata'],['05','Очередь ENDLUME',settings.autoQueueRender,'autoQueueRender'],['06','YouTube upload + schedule',settings.autoUploadYoutube,'autoUploadYoutube']].map(([n,label,val,key]:any)=><button key={key} className={`autoStep ${val?'active':''}`} onClick={()=>patch({[key]:!val} as any)}><span>{n}</span><b>{label}</b><em>{val?'AUTO':'OFF'}</em></button>)}</div>
.vyron-v110-audit/src/App.tsx:90:<div className="panel"><div className="panelHead"><div><small>INBOX</small><h3>Куда должны попадать файлы</h3></div></div>{channels.length===0?<p>Сначала добавь каналы.</p>:channels.map(c=>{const x=inboxes[c.id];return <div className="inboxRow" key={c.id}><div><b>{c.name}</b><small>{x?.root||`${settings.workspace}/Inbox/${c.name}`}</small></div><span>🎵 {x?.music.length||0}</span><span>🖼 {x?.images.length||0}</span><button onClick={async()=>{try{const v=await api.ensureChannelInbox(settings.workspace,c.name);await api.openLocal(v.root)}catch(e){toast(String(e))}}}>ОТКРЫТЬ INBOX</button></div>})}</div>
.vyron-v110-audit/src/App.tsx:91:<div className="panel warningPanel"><b>Что ещё требуется один раз</b><p>Для полностью автоматической публикации: подключить Google OAuth в Настройках и привязать профиль к каналу. Метаданные можно бесплатно импортировать из обычного ChatGPT Plus через Metadata Inbox; платный OpenAI API не обязателен. Для нулевого ручного рендера ENDLUME должен читать RenderQueue — VYRON уже создаёт эту очередь.</p><button onClick={()=>setPage('settings')}>ОТКРЫТЬ НАСТРОЙКИ</button></div>
.vyron-v110-audit/src/App.tsx:95:<PageHeader eyebrow="CHANNELS" title="Мои каналы" text="График каждого канала определяет, сколько проектов VYRON автоматически держит в запасе." actions={<button className="primary" onClick={()=>{const c=addChannel({name:`Канал ${channels.length+1}`});setEditing(c.id);toast('Канал добавлен')}}>+ ДОБАВИТЬ КАНАЛ</button>}/>
.vyron-v110-audit/src/App.tsx:96:{channels.length===0?<Empty title="Каналов пока нет" text="Добавь канал. По умолчанию публикация — через день, запас — 60 дней." action="СОЗДАТЬ ПЕРВЫЙ" onClick={()=>{const c=addChannel({name:'Новый канал'});setEditing(c.id)}}/>:<div className="channelGrid">{channels.map(c=><div className={`channelCard ${editing===c.id?'editing':''}`} key={c.id}><div className="channelTop"><div><i className="channelAvatar">{c.name.slice(0,2).toUpperCase()}</i><div><b>{c.name}</b><small>{c.genre} • {c.language} • {c.youtubeProfileId?'YouTube ✓':'YouTube не привязан'}</small></div></div><button className="iconBtn" onClick={()=>setEditing(editing===c.id?null:c.id)}>✎</button></div><div className="channelStats"><span><small>ПУБЛИКАЦИЯ</small><b>раз в {c.cadenceDays} дн.</b></span><span><small>ЗАПАС</small><b>{c.targetBufferDays} дней</b></span><span><small>ТРЕКОВ</small><b>{c.minTracks}</b></span><span><small>ДЛИТЕЛЬНОСТЬ</small><b>{c.targetDurationMin} мин</b></span></div>{editing===c.id&&<div className="editGrid"><label>Название<input value={c.name} onChange={e=>updateChannel(c.id,{name:e.target.value})}/></label><label>Жанр<input value={c.genre} onChange={e=>updateChannel(c.id,{genre:e.target.value})}/></label><label>Язык<input value={c.language} onChange={e=>updateChannel(c.id,{language:e.target.value})}/></label><label>Страна<input value={c.country} onChange={e=>updateChannel(c.id,{country:e.target.value})}/></label><label>Раз в N дней<input type="number" min="1" max="30" value={c.cadenceDays} onChange={e=>updateChannel(c.id,{cadenceDays:+e.target.value||1})}/></label><label>Запас, дней<input type="number" min="7" max="365" value={c.targetBufferDays} onChange={e=>updateChannel(c.id,{targetBufferDays:+e.target.value||60})}/></label><label>Треков на видео<input type="number" min="1" max="100" value={c.minTracks} onChange={e=>updateChannel(c.id,{minTracks:+e.target.value||10})}/></label><label>Длительность, мин<input type="number" min="10" max="720" value={c.targetDurationMin} onChange={e=>updateChannel(c.id,{targetDurationMin:+e.target.value||120})}/></label><label>Время публикации<input type="time" value={`${String(c.publishHour).padStart(2,'0')}:${String(c.publishMinute).padStart(2,'0')}`} onChange={e=>{const [h,m]=e.target.value.split(':').map(Number);updateChannel(c.id,{publishHour:h,publishMinute:m})}}/></label><label>YouTube OAuth<select value={c.youtubeProfileId||''} onChange={e=>{const p=profiles.find(x=>x.id===e.target.value);updateChannel(c.id,{youtubeProfileId:e.target.value||undefined,youtubeChannelId:p?.channelId})}}><option value="">— не привязан —</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.channelTitle||p.channelId||p.id}</option>)}</select></label><label className="wide">Формулы названий<textarea value={c.seo.titlePatterns.join('\n')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,titlePatterns:e.target.value.split('\n').filter(Boolean)}})}/></label><label className="wide">Шаблон описания<textarea value={c.seo.descriptionTemplate} onChange={e=>updateChannel(c.id,{seo:{...c.seo,descriptionTemplate:e.target.value}})}/></label><label className="wide">Теги<input value={c.seo.tags.join(', ')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)}})}/></label><label className="wide">Инструкция AI<textarea value={c.seo.aiPrompt||''} placeholder="Например: премиальные французские deep house заголовки, без повторов…" onChange={e=>updateChannel(c.id,{seo:{...c.seo,aiPrompt:e.target.value}})}/></label><div className="editActions"><button className="danger" onClick={()=>{if(confirm(`Удалить ${c.name}? Файлы на диске останутся.`)){removeChannel(c.id);setEditing(null)}}}>УДАЛИТЬ ИЗ VYRON</button><button className="primary" onClick={()=>setEditing(null)}>ГОТОВО</button></div></div>}</div>)}</div>}</>}
.vyron-v110-audit/src/App.tsx:98:function Production(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false);const filtered=jobs.filter(j=>filter==='all'||j.channelId===filter).sort((a,b)=>a.number-b.number);async function refreshAll(){setBusy(true);try{for(const j of useApp.getState().jobs){const c=channels.find(x=>x.id===j.channelId);if(!c||!j.folder||j.status==='SCHEDULED')continue;try{patchJob(j.id,await api.refreshJob(j.folder,c.minTracks))}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}toast('Статусы обновлены')}finally{setBusy(false)}}async function createPlan(){if(!settings.workspace){const ws=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:ws})}let added=0;let next=[...useApp.getState().jobs];for(const c of channels.filter(c=>c.enabled)){const created=createMissingJobs(c,next);next=[...next,...created];added+=created.length}setJobs(next);await runAutopilotCycle(true);toast(added?`Создано задач: ${added}`:'Дефицита нет')}return <>
.vyron-v110-audit/src/App.tsx:101:{filtered.length===0?<Empty title="Очередь пустая" text="VYRON рассчитает нужное число видео по графику каждого канала." action="СОЗДАТЬ ПЛАН" onClick={createPlan}/>:<div className="jobs">{filtered.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="job" key={j.id}><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{c?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не созданы'}</b><span>{j.publishAt?`План: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата не назначена'} • треков {j.tracksCount}/{j.minTracks}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>ПАПКА</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,c?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ МУЗЫКА</button>}{j.status==='READY_RENDER'&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('publisher')}>YOUTUBE</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,c?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>ПОВТОРИТЬ</button>}</div></div>})}</div>}</>}
.vyron-v110-audit/src/App.tsx:103:function Content(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [channelId,setChannelId]=useState(channels[0]?.id||''),[inbox,setInbox]=useState<any>();useEffect(()=>{if(!channelId&&channels[0])setChannelId(channels[0].id)},[channels]);const c=channels.find(x=>x.id===channelId);async function ensure(){if(!c)return;const r=await api.ensureChannelInbox(settings.workspace,c.name);setInbox(r);return r}async function importImages(){if(!c){setPage('channels');return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);const imported=await api.importImages(settings.workspace,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено`)}return <>
.vyron-v110-audit/src/App.tsx:104:<PageHeader eyebrow="CONTENT INBOX" title="Входящие файлы" text="Для автономного режима скачивай/клади музыку и изображения в Inbox канала. VYRON сам заберёт их в следующие Video_XXX." actions={<button className="primary" onClick={importImages}>+ ИМПОРТИРОВАТЬ ИЗОБРАЖЕНИЯ</button>}/>
.vyron-v110-audit/src/App.tsx:106:<div className="contentLayout"><div className="panel importPanel"><div className="stepNum">01</div><h3>Музыка</h3><p>Автопилот берёт файлы по времени появления и распределяет по {c?.minTracks||settings.tracksPerVideo} треков на Video_XXX.</p><div className="bigCount"><strong>{jobs.filter(j=>j.status==='WAITING_MUSIC').length}</strong><span>ждут музыку</span></div></div><div className="panel importPanel"><div className="stepNum green">02</div><h3>Изображения</h3><p>Одна картинка автоматически закрепляется за одним проектом. Порядок — по номеру Video_XXX.</p><div className="bigCount"><strong>{jobs.filter(j=>j.status==='NEED_IMAGE').length}</strong><span>ждут изображение</span></div></div><div className="panel importPanel"><div className="stepNum purple">03</div><h3>GPT / AI</h3><p>В автономном режиме metadata генерируется через OpenAI API. Ручной ChatGPT остаётся доступен как запасной сценарий.</p><button className="ghost full" onClick={()=>api.openWeb('https://chatgpt.com')}>ОТКРЫТЬ CHATGPT</button></div></div>
.vyron-v110-audit/src/App.tsx:109:function Competitors(){const channels=useApp(s=>s.channels),competitors=useApp(s=>s.competitors),add=useApp(s=>s.addCompetitor),patch=useApp(s=>s.patchCompetitor),remove=useApp(s=>s.removeCompetitor),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [channelId,setChannelId]=useState(channels[0]?.id||''),[url,setUrl]=useState(''),[name,setName]=useState('');const list=competitors.filter(c=>!channelId||c.channelId===channelId);async function refresh(c:Competitor){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key в Настройках');return}const m=c.url.match(/(?:channel\/)?(UC[\w-]{20,})/);if(!m){toast('Нужен URL с Channel ID UC…');return}try{const r=await api.youtubeStats(settings.youtubeApiKey,m[1]);patch(c.id,{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString(),name:r.title||c.name})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="COMPETITOR RADAR" title="Конкуренты" text="Публичные показатели конкурентов для дальнейшего AI-анализа контентных решений."/><div className="panel addLine"><select value={channelId} onChange={e=>setChannelId(e.target.value)}><option value="">К какому каналу</option>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input placeholder="Название" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="https://youtube.com/channel/UC…" value={url} onChange={e=>setUrl(e.target.value)}/><button className="primary" disabled={!channelId||!name||!url} onClick={()=>{add({id:crypto.randomUUID(),channelId,name,url});setName('');setUrl('')}}>ДОБАВИТЬ</button></div>{list.length===0?<Empty title="Конкурентов нет" text="Добавь каналы для наблюдения."/>:<div className="competitorGrid">{list.map(c=><div className="competitorCard" key={c.id}><div><i className="compAvatar">YT</i><span><b>{c.name}</b><small>{c.url}</small></span></div><div className="compStats"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.views)}</b></span><span><small>ВИДЕО</small><b>{formatNumber(c.videos)}</b></span></div><div className="cardActions"><button onClick={()=>api.openWeb(c.url)}>ОТКРЫТЬ</button><button className="primary small" onClick={()=>refresh(c)}>↻ ОБНОВИТЬ</button><button className="danger mini" onClick={()=>remove(c.id)}>×</button></div></div>)}</div>}</>}
.vyron-v110-audit/src/App.tsx:111:function Analytics(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),update=useApp(s=>s.updateChannel),toast=useApp(s=>s.toast);async function refresh(c:Channel){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key');return}const id=c.youtubeChannelId||prompt(`Channel ID для ${c.name} (UC…)`)||'';if(!id)return;try{const r=await api.youtubeStats(settings.youtubeApiKey,id);update(c.id,{youtubeChannelId:id,stats:{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString()}})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="ANALYTICS" title="Состояние сети" text="YouTube + производственный запас по каждому каналу."/><div className="analyticsGrid">{channels.map(c=>{const cj=jobs.filter(j=>j.channelId===c.id),days=bufferDays(c,jobs);return <div className="analyticCard" key={c.id}><div className="analyticHead"><div><i>{c.name.slice(0,2).toUpperCase()}</i><span><b>{c.name}</b><small>{c.stats?.updatedAt?new Date(c.stats.updatedAt).toLocaleString('ru-RU'):'YouTube не обновлялся'}</small></span></div><button onClick={()=>refresh(c)}>↻</button></div><div className="analyticNums"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.stats?.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.stats?.views)}</b></span><span><small>ВИДЕО YT</small><b>{formatNumber(c.stats?.videos)}</b></span></div><div className="bufferBar"><div><span>Запас</span><b className={priorityFor(days)}>{days} / {c.targetBufferDays} дн</b></div><i><em style={{width:`${Math.min(100,days/c.targetBufferDays*100)}%`}}/></i></div><div className="analyticFoot"><span>В системе <b>{cj.length}</b></span><span>MP4 <b>{cj.filter(j=>j.finalPath).length}</b></span><span>Scheduled <b>{cj.filter(j=>j.status==='SCHEDULED').length}</b></span></div></div>})}</div></>}
.vyron-v110-audit/src/App.tsx:113:function Publisher(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [rows,setRows]=useState<ImportedMetadata[]>([]),[files,setFiles]=useState<string[]>([]),[metaChannel,setMetaChannel]=useState(channels[0]?.id||''),[busy,setBusy]=useState(false);const input=useRef<HTMLInputElement>(null);const ready=jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED'].includes(j.status));async function read(fl:FileList|null){if(!fl?.length)return;const out:ImportedMetadata[]=[],names:string[]=[];for(const f of Array.from(fl)){out.push(...parseMetadataFile(f.name,await f.text()));names.push(f.name)}setRows(out);setFiles(names);toast(`Метаданные: ${out.length} записей`)}async function apply(){const used=new Set<string>();let ok=0,miss=0;for(let i=0;i<rows.length;i++){const r=rows[i],named=r.channel?channels.find(c=>c.name.toLowerCase()===r.channel?.toLowerCase()||c.id===r.channel):undefined,channelId=named?.id||metaChannel;const pool=useApp.getState().jobs.filter(j=>(!channelId||j.channelId===channelId)&&!used.has(j.id)).sort((a,b)=>a.number-b.number);const j=r.number?pool.find(x=>x.number===r.number):pool[0];if(!j){miss++;continue}used.add(j.id);const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}toast(`Применено: ${ok}${miss?` • не найдено ${miss}`:''}`)}async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}async function uploadAll(){setBusy(true);let n=0;try{for(const j of useApp.getState().jobs.filter(j=>j.status==='READY_UPLOAD'&&j.finalPath).sort((a,b)=>(a.publishAt||'').localeCompare(b.publishAt||''))){if(await upload(j))n++}toast(`YouTube: загружено ${n}`)}finally{setBusy(false)}}return <>
.vyron-v110-audit/src/App.tsx:117:<div className="publisherNotice"><b>⚠ Google API compliance</b><p>OAuth и resumable upload встроены. Если Google API-проект новый и не прошёл YouTube compliance audit, API может разрешать загрузку только как private. VYRON покажет фактическую ошибку YouTube и не потеряет остальные задачи.</p></div>
.vyron-v110-audit/src/App.tsx:120:function Settings({license,setLicense}:{license:LicenseStatus;setLicense:(x:LicenseStatus)=>void}){const s=useApp(x=>x.settings),patch=useApp(x=>x.patchSettings),logs=useApp(x=>x.logs),toast=useApp(x=>x.toast),log=useApp(x=>x.log);const [tab,setTab]=useState<'general'|'autopilot'|'integrations'|'subscription'|'updates'|'diagnostics'|'about'>('general'),[profiles,setProfiles]=useState<YoutubeProfile[]>([]),[connecting,setConnecting]=useState(false),[oauthSecret,setOauthSecret]=useState(''),[oauthMessage,setOauthMessage]=useState(''),[oauthError,setOauthError]=useState(false),[update,setUpdate]=useState<any>(),[progress,setProgress]=useState<number|null>(null),[diag,setDiag]=useState<any>(),[key,setKey]=useState('');const refreshProfiles=async()=>{try{const p=await api.youtubeProfiles();setProfiles(p);setOauthError(false);setOauthMessage(p.length?`OAuth: подключено профилей — ${p.length}`:'OAuth: сохранённых YouTube-профилей пока нет');return p}catch(e){const m=`OAuth storage: ${String(e)}`;setProfiles([]);setOauthError(true);setOauthMessage(m);log(m,'error');throw e}};useEffect(()=>{void refreshProfiles().catch(()=>{})},[]);return <>
.vyron-v110-audit/src/App.tsx:121:<PageHeader eyebrow="SETTINGS" title="Настройки" text="Один раз подключи интеграции — дальше очередь может работать автономно."/>
.vyron-v110-audit/src/App.tsx:122:<div className="settingsTabs">{(['general','autopilot','integrations','subscription','updates','diagnostics','about'] as const).map(t=><button className={tab===t?'active':''} key={t} onClick={()=>setTab(t)}>{t==='general'?'ОБЩИЕ':t==='autopilot'?'АВТОПИЛОТ':t==='integrations'?'ИНТЕГРАЦИИ':t==='subscription'?'ПОДПИСКА':t==='updates'?'ОБНОВЛЕНИЯ':t==='diagnostics'?'ДИАГНОСТИКА':'О ПРОГРАММЕ'}</button>)}</div>
.vyron-v110-audit/src/App.tsx:123:{tab==='general'&&<div className="settingsStack"><div className="settingsCard"><h3>Workspace</h3><p>VYRON хранит Inbox, проекты Video_XXX и RenderQueue.</p><div className="pathLine"><input readOnly value={s.workspace} placeholder="создаётся автоматически"/><button onClick={async()=>{const p=await api.chooseWorkspace();if(p)patch({workspace:p})}}>ВЫБРАТЬ</button><button onClick={()=>s.workspace&&api.openLocal(s.workspace)}>ОТКРЫТЬ</button></div></div><div className="settingsCard"><div className="toggleRows"><Toggle label="FPS-монитор" text="Контроль плавности интерфейса." value={s.fpsMonitor} onChange={v=>patch({fpsMonitor:v})}/><Toggle label="Уменьшить анимации" text="Отключить декоративные переходы." value={s.reduceMotion} onChange={v=>patch({reduceMotion:v})}/></div></div></div>}
.vyron-v110-audit/src/App.tsx:124:{tab==='autopilot'&&<div className="settingsStack"><div className="settingsCard"><h3>Autonomous Pipeline</h3><div className="toggleRows"><Toggle label="Автопилот" text="Запускать цикл автоматически." value={s.autopilotEnabled} onChange={v=>patch({autopilotEnabled:v})}/><Toggle label="Поддерживать план 60 дней" text="Автоматически создавать недостающие Video_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Брать треки из Inbox/Music." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 Video_XXX." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI-метаданные (опционально)" text="Только если вручную подключён отдельный API. Для бесплатного режима используй ChatGPT Plus → Metadata Inbox." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="Очередь ENDLUME" text="Создавать RenderQueue marker для готовых проектов." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Запускать приложение при появлении новых render-задач." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/><Toggle label="YouTube Autoupload" text="Загружать по одному готовому видео за цикл и ставить publishAt." value={s.autoUploadYoutube} onChange={v=>patch({autoUploadYoutube:v})}/></div></div><div className="settingsCard"><h3>Параметры</h3><div className="editGrid"><label>Цикл, секунд<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label><label>Треков по умолчанию<input type="number" min="1" max="100" value={s.tracksPerVideo} onChange={e=>patch({tracksPerVideo:+e.target.value||10})}/></label></div></div></div>}
.vyron-v110-audit/src/App.tsx:125:{tab==='integrations'&&<div className="settingsStack"><div className="settingsCard"><h3>ENDLUME Studio</h3><p>VYRON отправляет готовые проекты в RenderQueue.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>ВЫБРАТЬ</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>ОТКРЫТЬ</button>}</div><small className="note">Полностью нулевой ручной рендер требует companion-update ENDLUME, который будет читать RenderQueue.</small></div><div className="settingsCard"><h3>OpenAI API — опционально</h3><p>Не требуется для бесплатного режима. Оставь пустым и используй ChatGPT Plus → Metadata Inbox. API нужен только если позже захочешь полностью фоновую генерацию без открытия ChatGPT.</p><input className="wideInput" type="password" placeholder="sk-…" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/><div className="pathLine"><input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/><button onClick={()=>patch({openaiModel:''})}>ОЧИСТИТЬ</button></div></div><div className="settingsCard"><h3>YouTube OAuth</h3><p>Desktop OAuth открывает Google в браузере один раз. После этого токен обновляется локально автоматически.</p><input className="wideInput" placeholder="Google OAuth Client ID …apps.googleusercontent.com" value={s.youtubeOAuthClientId} onChange={e=>patch({youtubeOAuthClientId:e.target.value.trim()})}/><input className="wideInput" type="password" autoComplete="off" placeholder="Google OAuth Client Secret — вводится один раз" value={oauthSecret} onChange={e=>setOauthSecret(e.target.value.trim())}/><small className="note">Client Secret не сохраняется в общих настройках и не выводится в логах. После успешного OAuth он хранится только внутри локального OAuth-профиля.</small><div className="cardActions"><button className="primary" disabled={connecting||!s.youtubeOAuthClientId||!oauthSecret} onClick={async()=>{setConnecting(true);setOauthError(false);setOauthMessage('OAuth: ожидаю подтверждение Google…');try{const p=await api.youtubeConnect(s.youtubeOAuthClientId,oauthSecret);setOauthSecret('');const m=`YouTube подключён: ${p.channelTitle||p.channelId||'канал'}`;setOauthMessage(m);toast(m);log(m);await refreshProfiles()}catch(e){const m=`OAuth: ${String(e)}`;setOauthError(true);setOauthMessage(m);toast(m);log(m,'error')}finally{setConnecting(false)}}}>{connecting?'ЖДУ GOOGLE…':'ПОДКЛЮЧИТЬ YOUTUBE'}</button><button onClick={async()=>{try{const p=await refreshProfiles();toast(p.length?`OAuth профилей: ${p.length}`:'OAuth профили не найдены')}catch(e){toast(String(e))}}}>↻ ОБНОВИТЬ</button></div>{oauthMessage&&<div className={oauthError?'errorBox':profiles.length?'successBox':'publisherNotice'}>{oauthMessage}</div>}{profiles.map(p=><div className="oauthProfile" key={p.id}><span><b>{p.channelTitle||'YouTube канал'}</b><small>{p.channelId||p.clientIdMasked}</small></span><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refreshProfiles()}}>ОТКЛЮЧИТЬ</button></div>)}<label className="compactLabel">YouTube category ID<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div><div className="settingsCard"><h3>YouTube Intelligence</h3><p>Автоматически обновляет приватную аналитику своих каналов и публичные показатели конкурентов, пока VYRON запущен.</p><Toggle label="Автообновление аналитики" text="Обновлять подключённые каналы и список конкурентов без ручной кнопки." value={s.youtubeIntelligenceAutoRefresh} onChange={v=>patch({youtubeIntelligenceAutoRefresh:v})}/><label className="compactLabel">Интервал, минут<input type="number" min="15" max="1440" value={s.youtubeIntelligenceRefreshMin} onChange={e=>patch({youtubeIntelligenceRefreshMin:Math.max(15,+e.target.value||30)})}/></label><small className="note">Для приватной аналитики включи YouTube Analytics API в Google Cloud и один раз переподключи OAuth после установки 0.6.0. Конкуренты используют только публичные данные.</small></div><div className="settingsCard"><h3>YouTube Data API Key</h3><p>Только публичная статистика своих каналов и конкурентов.</p><input className="wideInput" type="password" placeholder="AIza…" value={s.youtubeApiKey} onChange={e=>patch({youtubeApiKey:e.target.value.trim()})}/></div><div className="settingsCard"><h3>MusicVault AI</h3><p>VYRON принимает музыку через Inbox каждого канала. Следующий шаг — кнопка «Отправить в VYRON» прямо в MusicVault AI, чтобы не переносить файлы вручную.</p></div></div>}
.vyron-v110-audit/src/App.tsx:126:{tab==='subscription'&&<div className="settingsCard"><h3>Лицензия</h3><div className="licenseRow"><i className={license.valid?'ok':'bad'}/><div><b>{license.valid?'VYRON активирован':'Нужна активация'}</b><small>{license.type==='owner-lifetime'?'Бессрочная лицензия владельца':license.type==='monthly'?'Месячная подписка':license.type==='development'?'Development':'Нет лицензии'}</small></div></div>{!license.valid&&<div className="pathLine"><input value={key} onChange={e=>setKey(e.target.value)}/><button className="primary" onClick={async()=>{try{setLicense(await api.activate(key))}catch(e){toast(String(e))}}}>АКТИВИРОВАТЬ</button></div>}</div>}
.vyron-v110-audit/src/App.tsx:127:{tab==='updates'&&<div className="settingsCard"><h3>Update Center</h3><p>Версия 0.9.9. Проверка версии работает внутри приложения. Текущая тестовая DMG-сборка пока не является Apple-notarized release.</p><Toggle label="Проверять при запуске" text="Показывать новую версию автоматически." value={s.autoCheckUpdates} onChange={v=>patch({autoCheckUpdates:v})}/><button className="settingsAction" onClick={async()=>{try{setUpdate(await api.checkUpdate())}catch(e){setUpdate({error:String(e)})}}}>ПРОВЕРИТЬ ОБНОВЛЕНИЯ</button>{update?.none&&<div className="successBox">Установлена актуальная версия {update.current||'0.8.1'}.</div>}{update?.version&&<div className="updateFound"><b>Доступна {update.version}</b><p>{update.body}</p>{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}<button className="primary" onClick={async()=>{setProgress(0);try{await update.install((p:number)=>setProgress(p))}catch(e){setProgress(null);toast(String(e))}}}>ОБНОВИТЬ И ПЕРЕЗАПУСТИТЬ</button></div>}{update?.error&&<div className="errorBox">{update.error}</div>}<div className="changelog"><h4>31.08.2026 • 0.5.0</h4><ul><li>Новое имя приложения: VYRON — CONTENT OS.</li><li>VYRON.app / новый фирменный интерфейс и системные названия.</li><li>Автоматическая миграция Documents/ChannelFlow → Documents/VYRON без потери проектов.</li><li>Сохраняются лицензия, каналы, очередь, OAuth-профили и настройки.</li><li>Бесплатный сценарий ChatGPT Plus → Metadata Inbox теперь основной; OpenAI API отмечен как опциональный.</li><li>Подготовлен отдельный VYRON update feed с legacy fallback.</li></ul></div></div>}
.vyron-v110-audit/src/App.tsx:128:{tab==='diagnostics'&&<div className="settingsStack"><div className="settingsCard"><h3>Диагностика</h3><button className="settingsAction" onClick={async()=>{try{let ws=s.workspace;if(!ws){ws=await api.defaultWorkspace();patch({workspace:ws})}setDiag(await api.diagnostics(ws))}catch(e){setDiag({ok:false,notes:[String(e)]})}}}>ЗАПУСТИТЬ ПРОВЕРКУ</button>{diag&&<div className="diag"><span>Состояние <b className={diag.ok?'green':'red'}>{diag.ok?'OK':'Есть проблема'}</b></span><span>Workspace <b>{diag.workspaceExists?'Да':'Нет'}</b></span><span>Запись <b>{diag.workspaceWritable?'Да':'Нет'}</b></span><span>Версия <b>{diag.appVersion}</b></span>{diag.notes?.map((n:string,i:number)=><p key={i}>{n}</p>)}</div>}</div><div className="settingsCard"><h3>Последние события</h3><div className="logs">{logs.length?logs.slice(0,60).map((l,i)=><div key={i} className={l.level}><small>{new Date(l.at).toLocaleString('ru-RU')}</small><span>{l.message}</span></div>):<p>Событий пока нет.</p>}</div></div></div>}
.vyron-v110-audit/src/App.tsx:129:{tab==='about'&&<div className="settingsCard about"><div className="aboutMark">▶</div><h2>VYRON</h2><p>Autonomous Content Operating System</p><div className="aboutRows"><span>Версия <b>0.9.9</b></span><span>macOS <b>Apple Silicon M1+</b></span><span>UI <b>Tauri 2 + React</b></span><span>Core <b>Rust</b></span><span>Pipeline <b>SUNO → ENDLUME → YouTube</b></span><span>Target UI <b>60 FPS</b></span></div></div>}</>}
.vyron-v110-audit/src/App.tsx:134:function UpdateNotice({update,onLater}:{update:any;onLater:()=>void}){const [progress,setProgress]=useState<number|null>(null),[error,setError]=useState('');return <aside className="updateNotice"><div><i/><span><b>Новое обновление</b><small>VYRON {update.version}</small></span></div><p>{String(update.body||'Доступна новая версия VYRON.').split('\n').slice(0,2).join(' • ')}</p>{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}{error&&<div className="errorBox">{error}</div>}<footer><button disabled={progress!==null} onClick={onLater}>ПОЗЖЕ</button><button className="primary" disabled={progress!==null} onClick={async()=>{setProgress(0);localStorage.setItem('vyron:update-installing-version',String(update.version));try{await update.install((p:number)=>setProgress(p))}catch(e){localStorage.removeItem('vyron:update-installing-version');setProgress(null);setError(String(e));notifyError('Не удалось установить обновление',String(e))}}}>ОБНОВИТЬ</button></footer></aside>}
.vyron-v110-audit/src/ChannelRunway.tsx:86:  const quotaSettings=loadYoutubeQuotaPlan();
.vyron-v110-audit/src/ChannelRunway.tsx:87:  const batchSize=Math.max(1,quotaSettings.videosPerChannel);
.vyron-v110-audit/src/ExistingVideos.tsx:27: return <img className="vyronThumb" src={src} loading="lazy" onLoad={()=>onState(video.id,'ok')} onError={()=>{const n=fallback+1;if(remotes[n]){setFallback(n);setSrc(remotes[n]);return}setFailed(true);onState(video.id,'error')}}/>;
.vyron-v110-audit/src/ExistingVideos.tsx:102:  {syncInfo&&<div className={`syncAudit ${mismatch?'warn':''}`}><span>YouTube найдено <b>{syncInfo.youtubeFound}</b></span><span>Получено VYRON <b>{syncInfo.received}</b></span><span>Private <b>{syncInfo.privateCount}</b></span><span>Scheduled <b>{syncInfo.scheduledCount}</b></span><span>Public <b>{syncInfo.publicCount}</b></span><span>Unlisted <b>{syncInfo.unlistedCount||0}</b></span><span>Изображения <b>{thumbOk}/{videos.length}</b></span>{thumbError>0&&<span className="warn">{thumbError} ожидают retry</span>}{syncInfo.youtubeFound>limit&&<span>Лимит: {limit} из {syncInfo.youtubeFound}</span>}{mismatch&&<strong>⚠ Синхронизация неполная</strong>}</div>}
.vyron-v110-audit/src/ExistingVideos.tsx:110:  {confirm&&<div className="modalBackdrop" onMouseDown={()=>setConfirm(false)}><section className="confirmModal" onMouseDown={e=>e.stopPropagation()}><small>YOUTUBE WRITE CONFIRMATION</small><h2>Будет изменено {selected.length} видео</h2><div className="confirmCounts"><span>Название <b>{changes.title}</b></span><span>Описание <b>{changes.description}</b></span><span>Теги <b>{changes.tags}</b></span><span>Дата <b>{changes.date}</b></span><span>Privacy <b>{changes.privacy}</b></span><span className={changes.public?'dangerText':''}>Public <b>{changes.public}</b></span></div><p>Перед записью VYRON сохранит локальный backup. После каждого videos.update будет выполнен videos.list и сравнение фактических данных YouTube.</p><footer><button onClick={()=>setConfirm(false)}>ОТМЕНА</button><button className="primary" disabled={busy||mismatch} onClick={apply}>ОТПРАВИТЬ {selected.length} В YOUTUBE</button></footer></section></div>}
.vyron-v110-audit/src/ProductionWorkspace.tsx:8:const KEY='vyron:production-workspace:v1';
.vyron-v110-audit/src/productionPrefs.test.ts:22:    localStorage.setItem('vyron:production-workspace:v1',JSON.stringify({selectedChannelId:'neon'}));
.vyron-v110-audit/src/productionPrefs.test.ts:30:  it('persists channel-specific builder settings and selected projects',()=>{
.vyron-v110-audit/src/channelRunwayStore.ts:18:const EVENT='vyron-channel-runway';
.vyron-v110-audit/src/channelRunwayStore.ts:19:const existingCacheKey=(channelId:string)=>`vyron:existing-cache:v1:${channelId}`;
.vyron-v110-audit/src/SettingsOS.tsx:4:import type {AutopilotMode,LicenseStatus} from './types';
.vyron-v110-audit/src/SettingsOS.tsx:5:import {notifyError,notifyInfo,notifySuccess} from './notificationCenter';
.vyron-v110-audit/src/SettingsOS.tsx:11:export function SettingsOS({license}:{license:LicenseStatus}){
.vyron-v110-audit/src/SettingsOS.tsx:12: const s=useApp(x=>x.settings),patch=useApp(x=>x.patchSettings),logs=useApp(x=>x.logs),toast=useApp(x=>x.toast),setPage=useApp(x=>x.setPage);
.vyron-v110-audit/src/SettingsOS.tsx:17: async function runDiagnostics(){setChecking(true);try{let ws=s.workspace;if(!ws){ws=await api.defaultWorkspace();patch({workspace:ws})}const core=await api.diagnostics(ws);setDiag([{label:'Local Core',status:core.ok?'Готово':'Требует внимания',ok:core.ok},{label:'Workspace',status:core.workspaceWritable?'Доступен для записи':'Проблема записи',ok:core.workspaceWritable},{label:'ENDLUME',status:s.endlumePath?'Путь настроен':'Не выбран',ok:Boolean(s.endlumePath)},{label:'Updater',status:'Tauri signed updater подключён',ok:true},{label:'YouTube',status:'Проверяется только во вкладке YouTube',detail:'Zero Quota Policy: настройки не вызывают YouTube Data API.'}])}catch(e){setDiag([{label:'Диагностика',status:'Ошибка',ok:false,detail:String(e)}])}finally{setChecking(false)}}
.vyron-v110-audit/src/SettingsOS.tsx:18: async function checkForUpdate(){try{const next=await api.checkUpdate();setUpdate(next);if(next?.none)notifySuccess('VYRON обновлён',`Установлена актуальная версия ${next.current||installedVersion||'—'}.`,{operationId:`update-latest:${next.current||installedVersion}`});else if(next?.version)notifyInfo(`Доступно обновление VYRON ${next.version}`,'Можно установить и перезапустить приложение.',{operationId:`settings-update:${next.version}`})}catch(e){setUpdate({error:String(e)});notifyError('Не удалось проверить обновления',String(e))}}
.vyron-v110-audit/src/SettingsOS.tsx:19: async function installUpdate(){setProgress(0);setStage('Загрузка');localStorage.setItem('vyron:update-installing-version',String(update.version||''));try{await update.install((p:number)=>{setProgress(p);if(p>=100)setStage('Проверка подписи и установка')})}catch(e){localStorage.removeItem('vyron:update-installing-version');setProgress(null);setStage('Обновление не установлено. Текущая версия сохранена.');notifyError('Не удалось установить обновление',String(e))}}
.vyron-v110-audit/src/SettingsOS.tsx:21: return <><div className="pageHeader"><div><small>SETTINGS</small><h1>Настройки</h1><p>Локальная конфигурация VYRON. Ни один переход сюда не расходует YouTube API quota.</p></div></div><div className="settingsTabs osTabs">{(['general','youtube','autopilot','endlume','updates','diagnostics'] as Tab[]).map(t=><button className={tab===t?'active':''} key={t} onClick={()=>setTab(t)}>{label(t)}</button>)}</div>
.vyron-v110-audit/src/SettingsOS.tsx:22: {tab==='general'&&<div className="settingsStack"><section className="settingsCard"><h3>Workspace</h3><p>Inbox, проекты, очередь, backup и локальное состояние VYRON.</p><div className="pathLine"><input readOnly value={s.workspace} placeholder="создаётся автоматически"/><button onClick={async()=>{const p=await api.chooseWorkspace();if(p)patch({workspace:p})}}>Выбрать</button><button onClick={()=>s.workspace&&api.openLocal(s.workspace)}>Открыть</button></div></section><section className="settingsCard"><h3>Интерфейс</h3><div className="toggleRows"><Toggle label="FPS monitor" text="Показывать текущий FPS интерфейса." value={s.fpsMonitor} onChange={v=>patch({fpsMonitor:v})}/><Toggle label="Уменьшить анимации" text="Уменьшить декоративные переходы." value={s.reduceMotion} onChange={v=>patch({reduceMotion:v})}/></div></section><section className="settingsCard"><h3>Лицензия</h3><p>{license.valid?'VYRON активирован':'Требуется активация'} • {license.type||'—'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced</summary><div className="advancedInner"><label>OpenAI API — опционально<input type="password" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/></label><label>OpenAI model<input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/></label><label>YouTube category<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:23: {tab==='youtube'&&<div className="settingsStack"><section className="settingsCard youtubeMovedCard"><small>ZERO QUOTA POLICY</small><h3>YouTube вынесен в отдельную рабочую зону</h3><p>OAuth, синхронизация, аналитика, конкуренты, metadata и загрузка выполняются только во вкладке YouTube и только после явного действия пользователя.</p><button className="primary" onClick={()=>setPage('youtube')}>Открыть YouTube</button></section></div>}
.vyron-v110-audit/src/SettingsOS.tsx:24: {tab==='autopilot'&&<div className="settingsStack"><section className="settingsCard"><h3>Режим Autopilot</h3><div className="modeSwitch large"><button className={mode==='off'?'active':''} onClick={()=>setMode('off')}>OFF</button><button className={mode==='assisted'?'active':''} onClick={()=>setMode('assisted')}>ASSISTED</button><button className={mode==='full'?'active':''} onClick={()=>setMode('full')}>FULL LOCAL</button></div><p>{mode==='off'?'Автоматизация выключена.':'Autopilot обслуживает только локальный Production pipeline и ENDLUME. YouTube API всегда остаётся ручным.'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced Autopilot</summary><div className="toggleRows"><Toggle label="Поддерживать план" text="Создавать недостающие VIDEO_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Inbox/Music → проекты." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 проект." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI metadata" text="Только при подключённом OpenAI API." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="ENDLUME queue" text="Создавать render marker." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Открывать приложение при render задачах." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/></div><label>Интервал локального цикла, сек<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:25: {tab==='endlume'&&<div className="settingsStack"><section className="settingsCard"><h3>ENDLUME Studio</h3><p>Готовые локальные проекты отправляются в RenderQueue без обращения к YouTube.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>Выбрать</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>Открыть</button>}</div></section></div>}
.vyron-v110-audit/src/SettingsOS.tsx:26: {tab==='updates'&&<section className="settingsCard"><h3>Update Center</h3><p>Текущая версия: {installedVersion||'—'}</p><Toggle label="Проверять автоматически" text="Проверка updater feed не использует YouTube API." value={s.autoCheckUpdates} onChange={v=>patch({autoCheckUpdates:v})}/><button className="settingsAction" onClick={checkForUpdate}>Проверить обновления</button>{update?.none&&<div className="successBox">Установлена актуальная версия {update.current||installedVersion||'—'}.</div>}{update?.version&&<div className="updateFound"><b>Доступна: {update.version}</b><p>{update.body}</p>{stage&&<strong>{stage}</strong>}{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}<button className="primary" onClick={installUpdate}>Установить и перезапустить</button></div>}{update?.error&&<div className="errorBox">Обновление не установлено. Текущая версия сохранена.<details><summary>Technical detail</summary>{update.error}</details></div>}</section>}
.vyron-v110-audit/src/SettingsOS.tsx:27: {tab==='diagnostics'&&<div className="settingsStack"><section className="settingsCard"><div className="panelHead"><div><h3>Локальная диагностика</h3><p>YouTube health/API checks намеренно исключены отсюда.</p></div><button className="primary" disabled={checking} onClick={runDiagnostics}>{checking?'Проверяю…':'Запустить проверку'}</button></div>{diag.length>0&&<div className="diagnosticGrid">{diag.map((x,i)=><div key={i} className={x.ok===false?'bad':x.ok?'good':''}><span><b>{x.label}</b><em>{x.status}</em></span>{x.detail&&<details><summary>Подробнее</summary><p>{x.detail}</p></details>}</div>)}</div>}</section><section className="settingsCard"><h3>Последние события</h3><div className="logs">{logs.length?logs.slice(0,60).map((l,i)=><div key={i} className={l.level}><small>{new Date(l.at).toLocaleString('ru-RU')}</small><span>{l.message}</span></div>):<p>Событий пока нет.</p>}</div></section></div>}</>
.vyron-v110-audit/src/autopilotRuntime.ts:33:  const s=useApp.getState().settings;
.vyron-v110-audit/src/autopilotRuntime.ts:42:  const state=useApp.getState(),s=state.settings,workspace=s.workspace;
.vyron-v110-audit/src/autopilotRuntime.ts:100:    let s=useApp.getState().settings;
.vyron-v110-audit/src/autopilotRuntime.ts:101:    if(!s.workspace){try{const workspace=await api.defaultWorkspace();useApp.getState().patchSettings({workspace});s=useApp.getState().settings}catch(e){logError(summary,'Workspace',e);return summary}}
.vyron-v110-audit/src/channelRunwayCore.ts:3:export const CHANNEL_RUNWAY_STORAGE_KEY='vyron:channel-runway:v1';
.vyron-v110-audit/src/AccountsPage.tsx:10: const channels=useApp(s=>s.channels),addChannel=useApp(s=>s.addChannel),updateChannel=useApp(s=>s.updateChannel),toast=useApp(s=>s.toast),settings=useApp(s=>s.settings),patchSettings=useApp(s=>s.patchSettings);
.vyron-v110-audit/src/AccountsPage.tsx:11: const [profiles,setProfiles]=useState<YoutubeProfile[]>([]),[health,setHealth]=useState<Record<string,YoutubeProfileHealth>>({}),[config,setConfig]=useState<GoogleConfigStatus|null>(null),[busy,setBusy]=useState(false),[checking,setChecking]=useState(false),[browserOpen,setBrowserOpen]=useState(false),[browsers,setBrowsers]=useState<BrowserOption[]>([]),[browser,setBrowser]=useState(localStorage.getItem('vyron:oauth-browser')||'default');const file=useRef<HTMLInputElement>(null);
.vyron-v110-audit/src/AccountsPage.tsx:15: async function importCredentials(fl:FileList|null){const f=fl?.[0];if(!f)return;setBusy(true);try{const c=await api.youtubeImportGoogleConfig(await f.text(),settings.youtubeApiKey||'');setConfig(c);toast('Google Credentials сохранены глобально')}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/AccountsPage.tsx:17: async function connect(){setBrowserOpen(false);setBusy(true);try{localStorage.setItem('vyron:oauth-browser',browser);const p=await api.youtubeConnectGlobal(browser);bindProfile(p);await refresh();toast(`✓ ${p.channelTitle||p.channelId||'YouTube канал'} подключён и привязан автоматически`)}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/AccountsPage.tsx:20:  <div className="pageHeader"><div><small>ACCOUNT CENTER</small><h1>Аккаунты YouTube</h1><p>Один Google OAuth Client обслуживает все каналы. При подключении VYRON сам определяет YouTube channelId и привязывает канал — ручное связывание не требуется.</p></div><div className="headerActions"><button disabled={checking||!profiles.length} onClick={checkAll}>{checking?'Проверяю…':'↻ Проверить'}</button><button className="primary compactAction" disabled={busy||(!config?.configured&&!profiles.length)} onClick={askBrowser}>+ Добавить канал</button></div></div>
.vyron-v110-audit/src/AccountsPage.tsx:22:  <section className="panel googleConfigCard"><div><small>GLOBAL GOOGLE CONFIG</small><h3>Одна конфигурация для всех каналов</h3><p>credentials.json нужен один раз. После этого каждый новый Gmail подключается обычным Google OAuth.</p></div><div className="configChecks"><span className={config?.configured?'good':''}>OAuth Client <b>{config?.configured?'✓':'—'}</b></span><span className={config?.projectId?'good':''}>Project <b>{config?.projectId||'—'}</b></span><span className={config?.hasSecret?'good':''}>Client Secret <b>{config?.hasSecret?'✓':'—'}</b></span><span className={settings.youtubeApiKey?'good':''}>Public API Key <b>{settings.youtubeApiKey?'✓':'не нужен для OAuth'}</b></span></div><div className="googleConfigActions"><button disabled={busy} onClick={()=>file.current?.click()}>Импортировать credentials.json</button><label>Public API Key<input type="password" placeholder="опционально" value={settings.youtubeApiKey} onChange={e=>patchSettings({youtubeApiKey:e.target.value.trim()})}/></label></div></section>
.vyron-v110-audit/src/AccountsPage.tsx:23:  <section className="panel accountsPanel"><div className="panelHead"><div><small>YOUTUBE ACCOUNTS</small><h3>{profiles.length?`${profiles.length} подключено`:'Аккаунтов пока нет'}</h3></div></div>{!profiles.length?<div className="empty"><b>Подключи первый YouTube-канал</b><p>VYRON спросит браузер, откроет Google OAuth и после подтверждения сам создаст карточку реального канала.</p></div>:<div className="accountList">{profiles.map(p=>{const h=health[p.id];const bound=channels.find(c=>c.youtubeProfileId===p.id||c.youtubeChannelId===p.channelId);return <article className="accountRow" key={p.id}>{h?.thumbnail?<img src={h.thumbnail} loading="lazy"/>:<div className="accountAvatar">YT</div>}<div className="accountMain"><b>{h?.channelTitle||p.channelTitle||'YouTube канал'}</b><small>{p.channelId||'Channel ID ещё не определён'}</small><div className="accountBadges"><span className={h?.ok?'good':'warn'}>{h?.ok?'● OAuth healthy':h?'● Требуется внимание':'● Проверяется'}</span><span>{bound?`VYRON: ${bound.name}`:'Автопривязка…'}</span>{p.preferredBrowser&&<span>Браузер: {p.preferredBrowser}</span>}</div></div><div className="accountMeta"><small>Подключён</small><b>{ago(p.connectedAt)}</b><small>Token</small><b>{h?.status||'—'}</b></div><div className="accountActions"><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refresh()}}>Отключить</button></div></article>})}</div>}</section>
.vyron-v110-audit/src/AccountsPage.tsx:24:  {browserOpen&&<div className="modalBackdrop" onMouseDown={()=>setBrowserOpen(false)}><section className="confirmModal browserPicker" onMouseDown={e=>e.stopPropagation()}><small>GOOGLE OAUTH</small><h2>Через какой браузер открыть Google?</h2><p>Выбери браузер, где уже открыт нужный Gmail. VYRON откроет OAuth именно там.</p><div className="browserGrid">{browsers.map(x=><button key={x.id} className={browser===x.id?'active':''} onClick={()=>setBrowser(x.id)}><b>{x.label}</b><small>{x.id==='default'?'Использовать системный браузер':'Открыть этот браузер'}</small></button>)}</div><footer><button onClick={()=>setBrowserOpen(false)}>Отмена</button><button className="primary" onClick={connect}>Продолжить через {browsers.find(x=>x.id===browser)?.label||'браузер'}</button></footer></section></div>}
.vyron-v110-audit/src/youtubeQuota.ts:5:const KEY='vyron:youtube-quota-guard:v1';
.vyron-v110-audit/src/youtubeQuota.ts:6:const USAGE_KEY='vyron:youtube-quota-usage:v2';
.vyron-v110-audit/src/youtubeQuota.ts:7:const PLAN_KEY='vyron:youtube-quota-plan:v1';
.vyron-v110-audit/src/youtubeQuota.ts:8:const EVENT='vyron-youtube-quota';
.vyron-v110-audit/src/youtubeQuota.ts:64:export function youtubeQuotaMessage(){const r=youtubeQuotaResetLocalInfo();return `Квота YouTube Data API исчерпана. VYRON поставил YouTube-запросы на паузу до ${r.time} (${r.date}) по вашему местному времени. Базовый сброс Google: 00:00 Pacific Time. Черновики и незавершённые пачки сохранены.`}
.vyron-v110-audit/src/channelSchedule.test.ts:7:describe('VYRON calendar schedule strategies',()=>{
.vyron-v110-audit/src/notificationCenter.ts:1:export type NotificationType='success'|'info'|'warning'|'error';
.vyron-v110-audit/src/notificationCenter.ts:2:export type NotificationAction={label:string;onClick:()=>void;closeAfter?:boolean};
.vyron-v110-audit/src/notificationCenter.ts:3:export type AppNotification={id:string;operationId?:string;type:NotificationType;title:string;message?:string;durationMs:number|null;actions:NotificationAction[];createdAt:number};
.vyron-v110-audit/src/notificationCenter.ts:4:export type NotificationOptions={operationId?:string;durationMs?:number|null;actions?:NotificationAction[]};
.vyron-v110-audit/src/notificationCenter.ts:5:const EVENT='vyron:notification-center';
.vyron-v110-audit/src/notificationCenter.ts:7:const defaults:Record<NotificationType,number|null>={success:5000,info:7000,warning:9000,error:null};
.vyron-v110-audit/src/notificationCenter.ts:9:export function notify(type:NotificationType,title:string,message='',options:NotificationOptions={}){
.vyron-v110-audit/src/notificationCenter.ts:12:  const detail:AppNotification={id:crypto.randomUUID(),operationId:options.operationId,type,title,message,durationMs:options.durationMs===undefined?defaults[type]:options.durationMs,actions:options.actions||[],createdAt:now};
.vyron-v110-audit/src/notificationCenter.ts:13:  window.dispatchEvent(new CustomEvent<AppNotification>(EVENT,{detail}));
.vyron-v110-audit/src/notificationCenter.ts:15:export const notifySuccess=(title:string,message='',options:NotificationOptions={})=>notify('success',title,message,options);
.vyron-v110-audit/src/notificationCenter.ts:16:export const notifyInfo=(title:string,message='',options:NotificationOptions={})=>notify('info',title,message,options);
.vyron-v110-audit/src/notificationCenter.ts:17:export const notifyWarning=(title:string,message='',options:NotificationOptions={})=>notify('warning',title,message,options);
.vyron-v110-audit/src/notificationCenter.ts:18:export const notifyError=(title:string,message='',options:NotificationOptions={})=>notify('error',title,message,options);
.vyron-v110-audit/src/notificationCenter.ts:27:export function subscribeNotifications(cb:(n:AppNotification)=>void){const fn=(e:Event)=>cb((e as CustomEvent<AppNotification>).detail);window.addEventListener(EVENT,fn);return()=>window.removeEventListener(EVENT,fn)}
.vyron-v110-audit/src/QuotaMeter.tsx:17:  <div className="quotaMeterHead"><div><small>YOUTUBE API QUOTA</small><h3>{guard.blocked?'Пауза по квоте':'Live estimate'}</h3><p>Мгновенный расчёт запросов VYRON. Google Cloud Monitoring может запаздывать на несколько минут.</p></div><span className={guard.blocked?'bad':'good'}>{guard.blocked?'QUOTA EXCEEDED':'TRACKING'}</span></div>
.vyron-v110-audit/src/QuotaMeter.tsx:24:   <p className="quotaFinePrint">Планировщик использует консервативную оценку ~52 units на изменяемое видео: один videos.update (50) + обычный read/verify overhead. Уже совпадающие видео VYRON пропускает, поэтому фактический расход может быть ниже. Search/insert с 2026 года имеют отдельные granular buckets и здесь не смешиваются с основным write-пулом.</p>
.vyron-v110-audit/src/YouTubeCenter.tsx:17: return <><div className="youtubeCenterHead"><div><small>YOUTUBE CENTER • API ZONE</small><h1>YouTube</h1><p>Единственная зона VYRON, где разрешены YouTube API-вызовы. Синхронизация и обновления запускаются только твоим действием.</p></div><div className="youtubeTabs">{tabs.map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div></div><QuotaMeter compact defaultChannels={Math.max(1,channels.length)}/>{tab!=='command'&&<div className="youtubePolicyNotice"><b>ZERO HIDDEN QUOTA</b><span>Нет startup sync, polling, background refresh или API-запросов при возврате на вкладку.</span></div>}{tab==='uploaded'&&<ExistingVideos/>}{tab==='metadata'&&<MetadataPage/>}{tab==='queue'&&<PublisherOS/>}{tab==='command'&&<CommandCenter/>}{tab==='runway'&&<ChannelRunway/>}{tab==='data'&&<YouTubeDataTools/>}{tab==='accounts'&&<AccountsPage/>}{tab==='calendar'&&<section className="panel calendarPanel"><div className="panelHead"><div><small>ALL CHANNELS</small><h3>Календарь публикаций</h3><p>Локальные даты из project state / YouTube queue.</p></div><span>{calendar.length} публикаций</span></div>{calendar.length===0?<div className="empty"><b>Публикаций пока нет</b><p>Когда у видео появится publishAt, оно будет показано здесь.</p></div>:<div className="calendarList">{calendar.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="calendarRow" key={j.id}><time>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})}</time><span><b>{c?.name||'Канал удалён'}</b><small>VIDEO_{String(j.number).padStart(3,'0')} • {j.title||'Без названия'}</small></span><em className={`publishState ${j.status.toLowerCase()}`}>{j.status}</em>{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div>})}</div>}</section>}</>
.vyron-v110-audit/src/ProductionOS.tsx:18: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [prefs,patchPrefs]=useProductionPrefs();const tab=(prefs.tab||'queue') as Tab;const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';const setTab=(v:Tab)=>patchPrefs({tab:v});const setChannelId=(id:string)=>patchPrefs({selectedChannelId:id});const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false),[inbox,setInbox]=useState<any>(),[planOpen,setPlanOpen]=useState(false),[planCount,setPlanCount]=useState(30),[planScope,setPlanScope]=useState<'one'|'all'>('one');const [section,setSection]=useState<'overview'|'materials'|'builder'|'render'|'publish'>(()=>tab==='materials'?'materials':tab==='manager'?'builder':'overview');const scopedJobs=section==='render'?jobs.filter(j=>['READY_RENDER','RENDERING','ERROR'].includes(j.status)):section==='publish'?jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)):jobs;const filtered=scopedJobs.filter(j=>filter==='all'||j.channelId===filter||j.status===filter).sort((a,b)=>a.number-b.number);const c=channels.find(x=>x.id===channelId);const selectedJobIds=(prefs.selectedJobIds||[]).filter(id=>jobs.some(j=>j.id===id));const projectRoot=resolveProductionRootFromPrefs(prefs,channelId,settings.workspace);const [projectStorage,setProjectStorage]=useState<ProductionStorageStatus|null>(null);
.vyron-v110-audit/src/ProductionOS.tsx:22: async function chooseProjectRoot(scope:'global'|'channel'){const initial=scope==='channel'?(c?prefs.byChannel[c.id]?.productionRoot:undefined)||projectRoot:(prefs.productionRoot||settings.workspace||projectRoot);const requested=await productionManagerApi.chooseProductionRoot(initial||undefined);if(!requested)return;const st=await productionManagerApi.storageStatus(requested);if(!st.exists||!st.writable){toast(st.error||'Выбранная папка проектов недоступна для записи');return}const path=st.path||requested;if(scope==='channel'&&c){patchChannelProductionPrefs(c.id,{productionRoot:path});toast(`Отдельная папка для ${c.name} сохранена`)}else{patchPrefs({productionRoot:path});toast('Основная папка проектов сохранена')}setProjectStorage(st)}
.vyron-v110-audit/src/ProductionOS.tsx:25: async function deleteJobs(ids:string[],label:string){const unique=[...new Set(ids)].filter(id=>useApp.getState().jobs.some(j=>j.id===id));if(!unique.length)return;if(!confirm(label))return;setBusy(true);try{const rows=useApp.getState().jobs.filter(j=>unique.includes(j.id));for(const j of rows){if(!j.folder)continue;const roots=[resolveProductionRootFromPrefs(prefs,j.channelId,settings.workspace),inferJobRoot(j.folder),settings.workspace].filter((x,i,a)=>x&&a.indexOf(x)===i);let done=false,last:unknown;for(const root of roots){try{await productionManagerApi.deleteJobFolder(root,j.folder);done=true;break}catch(e){last=e}}if(!done)throw last||new Error('Не удалось определить хранилище проекта')}setJobs(useApp.getState().jobs.filter(j=>!unique.includes(j.id)));patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!unique.includes(id))});toast(`Удалено проектов: ${unique.length}`)}catch(e){toast(`Не удалось удалить проекты: ${String(e)}`)}finally{setBusy(false)}}
.vyron-v110-audit/src/ProductionOS.tsx:27: async function maintainBuffer(){if(!settings.workspace){const ws=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:ws})}let added=0;let next=[...useApp.getState().jobs];for(const ch of channels.filter(x=>x.enabled)){const created=createMissingJobs(ch,next);next=[...next,...created];added+=created.length}setJobs(next);await runAutopilotCycle(true);toast(added?`Буфер пополнен: +${added} проектов`:'Буфер уже заполнен')}
.vyron-v110-audit/src/ProductionOS.tsx:31: async function ensure(){if(!c)return;const r=await api.ensureChannelInbox(settings.workspace,c.name);setInbox(r);return r}
.vyron-v110-audit/src/ProductionOS.tsx:32: async function importImages(){if(!c)return;let root=projectRoot;if(!root){root=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:root})}const storage=await productionManagerApi.storageStatus(root);if(!storage.exists||!storage.writable){toast(storage.error||`Папка проектов недоступна: ${root}`);return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);if(!nums.length){toast('Нет проектов, ожидающих изображение');return}const imported=await api.importImages(root,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено по VIDEO_${String(nums[0]).padStart(3,'0')}…`)}
.vyron-v110-audit/src/ProductionOS.tsx:37:  {tab==='queue'&&<>{section==='overview'&&<ProductionWorkspace/>}<div className="productionPipeline">{stageOrder.map(st=><button key={st} className={st==='ERROR'&&jobs.some(j=>j.status==='ERROR')?'warn':''} onClick={()=>setFilter(st)}><small>{statusLabel[st]}</small><b>{jobs.filter(j=>j.status===st).length}</b></button>)}</div><div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы / статусы</option>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}{stageOrder.map(st=><option key={st} value={st}>{statusLabel[st]}</option>)}</select><span>{filtered.length} задач</span></div>{filtered.length===0?<div className="empty"><b>Нет задач в этом фильтре</b><p>Создай точное число проектов или включи поддержание буфера.</p><button className="primary" onClick={()=>setPlanOpen(true)}>Создать проекты</button></div>:<div className="jobs virtualReady"><div className="pmBulkBar"><span>Выбрано <b>{selectedJobIds.filter(id=>visibleJobIds.includes(id)).length}</b> / {visibleJobIds.length}</span><button onClick={()=>patchPrefs({selectedJobIds:[...new Set([...selectedJobIds,...visibleJobIds])]})}>Выбрать всё</button><button onClick={()=>patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!visibleJobIds.includes(id))})}>Снять выделение</button><button className="danger" disabled={!selectedJobIds.some(id=>visibleJobIds.includes(id))} onClick={()=>void deleteJobs(selectedJobIds.filter(id=>visibleJobIds.includes(id)),'Удалить выбранные проекты?')}>Удалить выбранные</button><button className="danger" onClick={()=>void deleteJobs(visibleJobIds,'Удалить все проекты в текущем списке?')}>Удалить все</button></div>{filtered.map(j=>{const ch=channels.find(x=>x.id===j.channelId);return <article className="job productionJob" key={j.id}><label className="pmJobCheck"><input type="checkbox" checked={selectedJobIds.includes(j.id)} onChange={()=>toggleJob(j.id)}/></label><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{ch?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не готовы'}</b><span>Изображение {j.coverPath?'✓':'—'} • треков {j.tracksCount}/{j.minTracks} • final.mp4 {j.finalPath?'✓':'—'} • metadata {j.title&&j.description?'✓':'—'}</span><span>{j.publishAt?`Публикация: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата публикации не назначена'}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>Папка</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,ch?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ Аудио</button>}{j.status==='READY_RENDER'&&j.folder&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в ENDLUME RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('youtube')}>YouTube</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,ch?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>Повторить</button>}</div></article>})}</div>}</>}
.vyron-v110-audit/src/NotificationStack.tsx:2:import {subscribeNotifications,type AppNotification} from './notificationCenter';
.vyron-v110-audit/src/NotificationStack.tsx:4:function ToastCard({item,onClose}:{item:AppNotification;onClose:()=>void}){
.vyron-v110-audit/src/NotificationStack.tsx:9:  return <article className={`vyronNotice ${item.type}`} onMouseEnter={pause} onMouseLeave={start}>
.vyron-v110-audit/src/NotificationStack.tsx:10:    <i className="vyronNoticeIcon">{item.type==='success'?'✓':item.type==='warning'?'⚠':item.type==='error'?'✕':'ℹ'}</i>
.vyron-v110-audit/src/NotificationStack.tsx:11:    <div className="vyronNoticeText"><b>{item.title}</b>{item.message&&<p>{item.message}</p>}{item.actions.length>0&&<div className="vyronNoticeActions">{item.actions.map((a,i)=><button key={i} onClick={()=>{a.onClick();if(a.closeAfter!==false)onClose()}}>{a.label}</button>)}</div>}</div>
.vyron-v110-audit/src/NotificationStack.tsx:12:    <button className="vyronNoticeClose" onClick={onClose} aria-label="Закрыть">×</button>
.vyron-v110-audit/src/NotificationStack.tsx:15:export function NotificationCenter(){
.vyron-v110-audit/src/NotificationStack.tsx:16:  const [visible,setVisible]=useState<AppNotification[]>([]);const queue=useRef<AppNotification[]>([]);
.vyron-v110-audit/src/NotificationStack.tsx:17:  const fill=(rows:AppNotification[])=>{const next=[...rows];while(next.length<MAX_VISIBLE&&queue.current.length)next.push(queue.current.shift()!);return next};
.vyron-v110-audit/src/NotificationStack.tsx:18:  useEffect(()=>subscribeNotifications(n=>setVisible(rows=>{if(rows.some(x=>x.operationId&&x.operationId===n.operationId))return rows;if(rows.length<MAX_VISIBLE)return[...rows,n];queue.current.push(n);return rows})),[]);
.vyron-v110-audit/src/NotificationStack.tsx:20:  return <aside className="vyronNotificationCenter" aria-live="polite">{visible.map(x=><ToastCard key={x.id} item={x} onClose={()=>close(x.id)}/>)}</aside>
.vyron-v110-audit/src/channelRunway.test.ts:93:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({version:1,updatedAt:'2026-09-02T08:00:00Z',videos:[],baseline:{},syncInfo:null}));
.vyron-v110-audit/src/channelRunway.test.ts:104:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({
.vyron-v110-audit/src/channelRunway.test.ts:117:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({version:1,videos:[stale],baseline:{[stale.id]:stale},syncInfo:{complete:true}}));
.vyron-v110-audit/src/api.ts:8:import type { AppState, ChannelAnalytics, Competitor, Diagnostics, InboxScan, LicenseStatus, VideoJob, YoutubeProfile } from './types';
.vyron-v110-audit/src/api.ts:22:  license:()=>invoke<LicenseStatus>('license_status'),
.vyron-v110-audit/src/api.ts:23:  activate:(key:string)=>invoke<LicenseStatus>('activate_license',{key}),
.vyron-v110-audit/src/api.ts:26:  chooseWorkspace:async()=>{const r=await open({directory:true,multiple:false,title:'Папка VYRON'});return typeof r==='string'?r:null},
.vyron-v110-audit/src/PublisherOS.tsx:12: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);
.vyron-v110-audit/src/PublisherOS.tsx:17: async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);if(!r.videoId)throw new Error('YouTube не вернул video ID');patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}
.vyron-v110-audit/src/types.ts:1:export type Page='dashboard'|'autopilot'|'accounts'|'channels'|'production'|'content'|'youtube'|'competitors'|'analytics'|'metadata'|'existing'|'publisher'|'settings';
.vyron-v110-audit/src/types.ts:34:export type Settings={
.vyron-v110-audit/src/types.ts:43:export type LicenseStatus={valid:boolean;type?:'owner-lifetime'|'monthly'|'development';expiresAt?:string|null;maskedKey?:string};
.vyron-v110-audit/src/types.ts:44:export type AppState={version:number;channels:Channel[];jobs:VideoJob[];competitors:Competitor[];settings:Settings;logs:{at:string;level:'info'|'warn'|'error';message:string}[]};
.vyron-v110-audit/src/MetadataPage.tsx:11:import {notifyError,notifySuccess,notifyWarning} from './notificationCenter';
.vyron-v110-audit/src/MetadataPage.tsx:25:const draftKey=(channelId:string)=>`vyron:metadata-draft:v1:${channelId}`;
.vyron-v110-audit/src/MetadataPage.tsx:98:   <section className="panel metadataTargetPanel"><div className="panelHead"><div><small>02 • НАЗНАЧЕНИЕ</small><h3>Куда применить</h3></div></div><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><div className="metadataTargetSwitch"><button className={target==='future'?'active':''} onClick={()=>setTarget('future')}><b>Будущие VYRON</b><small>проекты до загрузки</small></button><button className={target==='youtube'?'active':''} onClick={()=>setTarget('youtube')}><b>Уже на YouTube</b><small>выбранные private/scheduled</small></button></div>{target==='future'?<div className="metadataTargetInfo"><strong>{future.length}</strong><span>будущих проектов</span></div>:<><div className={`profileAutoBadge ${profileId?'good':'warn'}`}><small>OAuth</small><b>{profileId?'✓ привязан автоматически':'не подключён'}</b></div><div className="metadataYoutubeLoad"><label>Лимит<input type="number" min="1" max="5000" value={limit} onChange={e=>setLimit(+e.target.value||1000)}/></label><label>Порядок<select value={order} onChange={e=>setOrder(e.target.value as any)}><option value="newest">новые → старые</option><option value="oldest">старые → новые</option></select></label><button disabled={busy||!profileId} onClick={loadYoutube}>↻ Синхронизировать</button></div><div className="metadataTargetInfo"><strong>{selectedYt.length}</strong><span>выбрано из {yt.length}</span><small>{syncStatus||'YouTube будет загружен автоматически'}</small></div></>}</section>
.vyron-v110-audit/src/MetadataPage.tsx:100:  {target==='youtube'&&yt.length>0&&<section className="panel metadataVideoPicker"><div className="panelHead"><div><small>03 • ВЫБОР ВИДЕО</small><h3>Точно видно, куда пойдёт DOCX</h3></div><div className="batchScopeActions"><button onClick={selectVisible}>Выбрать все здесь</button><button onClick={selectLatest30}>Последние 30 Private</button><button onClick={selectNone}>Снять всё</button></div></div><div className="periodTabs"><button className={filter==='private'?'active':''} onClick={()=>setFilter('private')}>PRIVATE</button><button className={filter==='scheduled'?'active':''} onClick={()=>setFilter('scheduled')}>SCHEDULED</button><button className={filter==='public'?'active':''} onClick={()=>setFilter('public')}>PUBLIC</button><button className={filter==='unlisted'?'active':''} onClick={()=>setFilter('unlisted')}>UNLISTED</button></div><div className="metadataVideoRows">{shown.slice(0,500).map(v=><button key={v.id} className={v.selected?'selected':''} onClick={()=>toggle(v.id)}><i>{v.selected?'✓':''}</i><span><b>{v.title}</b><small>{existingBucket(v).toUpperCase()} • {v.id}</small></span></button>)}</div><div className="scheduleMini scheduleSmart"><div className="scheduleSmartHead"><div><b>Расписание канала</b><small>{scheduleState?.updatedAt?`Локальные данные обновлены ${new Date(scheduleState.updatedAt).toLocaleString('ru-RU')}`:'Используются сохранённые данные канала'}</small></div></div><div className="scheduleStrategy"><small>ГРАФИК ПУБЛИКАЦИЙ</small><div className="schedulePresetButtons"><button className={strategy==='interval'&&cadence===1?'active':''} onClick={()=>selectInterval(1)}>Каждый день</button><button className={strategy==='interval'&&cadence===2?'active':''} onClick={()=>selectInterval(2)}>Через день</button><button className={strategy==='pattern'&&patternPublishDays===3&&patternPauseDays===1&&!customPattern?'active':''} onClick={()=>selectPattern(3,1,false)}>3 дня / 1 пауза</button><button className={strategy==='pattern'&&customPattern?'active':''} onClick={()=>selectPattern(patternPublishDays,patternPauseDays,true)}>Настроить</button></div>{strategy==='pattern'&&<><p>{patternPublishDays} дня подряд публикуем видео, {patternPauseDays} день без публикаций, затем цикл повторяется.</p><div className="patternSettings">{customPattern&&<><label>Дней с видео<input type="number" min="1" max="30" value={patternPublishDays} onChange={e=>changePattern(+e.target.value||1,patternPauseDays)}/></label><label>Дней паузы<input type="number" min="1" max="30" value={patternPauseDays} onChange={e=>changePattern(patternPublishDays,+e.target.value||1)}/></label></>}<label>Начало цикла<input type="date" value={channel?.patternAnchorDate||''} onChange={e=>changePatternAnchor(e.target.value)}/></label></div></>}</div><div className="scheduleModeButtons"><button className={scheduleMode==='auto'?'active':''} onClick={()=>setScheduleMode('auto')}>Продолжить расписание автоматически</button><button className={scheduleMode==='manual'?'active':''} onClick={()=>setScheduleMode('manual')}>Указать первую дату вручную</button></div>{scheduleMode==='auto'?scheduleState?.lastScheduledAt?<><div className="scheduleFacts"><span><small>Запланировано до</small><b>{scheduleDateLabel(scheduleState.lastScheduledAt)}</b></span><span><small>Следующая дата</small><b>{scheduleDateLabel(scheduleState.nextAvailableAt)}</b></span><span><small>График</small><b>{channel?scheduleDescription(channel):`каждые ${cadence} дн.`}</b></span><span><small>Время</small><b>{defaultPublishTime}</b></span></div>{schedulePreview.length>0&&<div className="scheduleBatchPreview"><b>Новая партия: {scheduleDateLabel(previewFirst)} → {scheduleDateLabel(previewLast)}</b><small>{schedulePreview.length} видео • первая свободная дата выбрана автоматически</small><div>{schedulePreview.slice(0,6).map((v,i)=><span key={v.id}>{String(i+1).padStart(3,'0')} → {scheduleDateLabel(v.publishAt)}</span>)}{schedulePreview.length>6&&<span>… ещё {schedulePreview.length-6}</span>}</div></div>}{strategy==='pattern'&&patternGenerated.calendar.length>0&&<div className="patternCalendarPreview"><b>Календарь цикла</b><div>{patternGenerated.calendar.slice(0,20).map((d,i)=><span key={`${d.date}-${i}`} className={d.kind}>{dateKeyLabel(d.date)} — {d.kind==='pause'?'ПАУЗА':d.kind==='occupied'?'ЗАНЯТО':`VIDEO ${String(patternGenerated.calendar.slice(0,i+1).filter(x=>x.kind==='video').length).padStart(3,'0')}`}</span>)}{patternGenerated.calendar.length>20&&<span>… ещё {patternGenerated.calendar.length-20} дней</span>}</div></div>}</>:<div className="scheduleNoFuture"><b>На канале нет будущих отложенных публикаций</b><span>Выберите «Указать первую дату вручную», чтобы задать начало расписания.</span></div>:<label>Первая дата<input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label>}<label>Публиковать каждые N дней<input type="number" min="1" max="30" value={cadence} onChange={e=>changeCadence(+e.target.value||1)}/></label><label>Время по умолчанию<input type="time" value={defaultPublishTime} onChange={e=>changeDefaultTime(e.target.value)}/></label><small>Если DOCX содержит PUBLISH TIME, для конкретного видео используется его время. Даты продолжают расписание текущего канала.</small></div></section>}
.vyron-v110-audit/src/YouTubeDataTools.tsx:14: async function discover(){if(!channel?.youtubeProfileId){toast('Сначала подключи YouTube OAuth');return}if(!guard())return;setBusy('discover');try{const found=await api.youtubeDiscoverCompetitors(channel.youtubeProfileId,Math.max(10,Math.min(50,useApp.getState().settings.competitorPoolSize||30)));const existing=new Set(list.map(c=>c.youtubeChannelId).filter(Boolean));let added=0;for(const x of found){if(existing.has(x.channelId)||x.channelId===channel.youtubeChannelId)continue;add({id:crypto.randomUUID(),channelId,name:x.name,url:x.url,youtubeChannelId:x.channelId,thumbnail:x.thumbnail,subscribers:x.subscribers,views:x.views,videos:x.videos,history:[],similarity:x.similarity,source:'auto'});existing.add(x.channelId);added++}setNote(`✓ Найдено ${found.length}, добавлено новых ${added}`);toast(`Конкуренты: +${added}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setNote(youtubeQuotaMessage())}else setNote(String(e));toast(isYoutubeQuotaError(e)?youtubeQuotaMessage():String(e))}finally{setBusy('')}}
.vyron-v110-audit/src/productionManagerApi.ts:9:export type ChannelProductionState={settings:{musicLibrary?:string};importSession:ImportSession;music?:MusicSummary|null;batches:BatchSummary[]};
.vyron-v110-audit/src/productionManagerApi.ts:21:  chooseProductionRoot:async(defaultPath?:string)=>{const p=await open({directory:true,multiple:false,title:'Папка для проектов VYRON',defaultPath:defaultPath||undefined});return typeof p==='string'?p:'';},
.vyron-v110-audit/src/commandCenterCore.ts:5:export const COMMAND_CENTER_STORAGE_KEY='vyron:command-center:v1';
.vyron-v110-audit/src/ChannelsOS.tsx:23:    <div className="channelActions"><button onClick={()=>setPage('youtube')}>Открыть YouTube</button><button onClick={()=>setEditing(editing===c.id?null:c.id)}>{editing===c.id?'Закрыть настройки':'Настроить канал'}</button></div>
.vyron-v110-audit/src/ChannelsOS.tsx:30:     <div className="editActions"><button className="danger" onClick={()=>{if(window.confirm(`Удалить ${c.name}? Файлы на диске останутся.`)){removeChannel(c.id);setEditing(null)}}}>Удалить из VYRON</button><button className="primary" onClick={()=>setEditing(null)}>Готово</button></div>
.vyron-v110-audit/src/commandCenter.test.ts:79:  it('one channel every 3 days is about 2.3 channels per week',()=>{
.vyron-v110-audit/src/productionPrefs.ts:7:const KEY='vyron:production-manager:v2';
.vyron-v110-audit/src/productionPrefs.ts:8:const EVENT='vyron-production-prefs-changed';
.vyron-v110-audit/src/productionPrefs.ts:15:  try{const old=JSON.parse(localStorage.getItem('vyron:production-workspace:v1')||'null');if(old?.selectedChannelId)next.selectedChannelId=String(old.selectedChannelId)}catch{}
.vyron-v110-audit/src/youtubeIntelligence.ts:12: if(!force&&channel.analytics?.periodDays===days&&Number(channel.analytics?.offsetDays||0)===offsetDays&&!stale(channel.analytics?.updatedAt,s.settings.youtubeIntelligenceRefreshMin))return channel.analytics;
.vyron-v110-audit/src/youtubeIntelligence.ts:19: const s=useApp.getState(),c=s.competitors.find(x=>x.id===competitorId);if(!c)throw new Error('Конкурент не найден');const own=s.channels.find(x=>x.id===c.channelId);if(!own?.youtubeProfileId)throw new Error(`Для ${own?.name||'канала'} сначала привяжи YouTube OAuth`);if(!force&&!stale(c.updatedAt,Math.max(60,s.settings.youtubeIntelligenceRefreshMin)))return c;
.vyron-v110-audit/src/youtubeIntelligence.ts:25: const s=useApp.getState(),own=s.channels.find(c=>c.id===channelId);if(!own?.youtubeProfileId)return 0;const key=`vyron:competitor-discovery:${channelId}`,last=Number(localStorage.getItem(key)||0);if(!force&&Date.now()-last<24*3600_000)return 0;
.vyron-v110-audit/src/youtubeIntelligence.ts:26: const pool=Math.max(10,Math.min(50,s.settings.competitorPoolSize||30));const found=await api.youtubeDiscoverCompetitors(own.youtubeProfileId,Math.min(50,pool));const current=useApp.getState().competitors.filter(c=>c.channelId===channelId);const existing=new Set(current.map(c=>c.youtubeChannelId).filter(Boolean));let added=0;
.vyron-v110-audit/src/youtubeIntelligence.ts:32: if(running||youtubeQuotaState().blocked)return;running=true;const errors:string[]=[];try{const state=useApp.getState();for(const channel of state.channels.filter(c=>c.enabled&&c.youtubeProfileId)){try{await refreshChannelAnalytics(channel.id,28,force)}catch(e){errors.push(`${channel.name}: ${String(e)}`)}try{await discoverCompetitorsForChannel(channel.id,force)}catch(e){errors.push(`${channel.name} discovery: ${String(e)}`)}const candidates=useApp.getState().competitors.filter(c=>c.channelId===channel.id).sort((a,b)=>(Date.parse(a.updatedAt||'')||0)-(Date.parse(b.updatedAt||'')||0)).filter(c=>force||stale(c.updatedAt,Math.max(60,useApp.getState().settings.youtubeIntelligenceRefreshMin))).slice(0,force?10:3);for(const c of candidates){try{await refreshCompetitor(c.id,true)}catch(e){errors.push(`${c.name}: ${String(e)}`)}}}
.vyron-v110-audit/src/ProductionManager.tsx:6:import {notifyInfo,notifySuccess} from './notificationCenter';
.vyron-v110-audit/src/ProductionManager.tsx:23:  const settings=useApp(s=>s.settings);
.vyron-v110-audit/src/ProductionManager.tsx:47:  const workspace=settings.workspace||'';
.vyron-v110-audit/src/ProductionManager.tsx:50:  const endlumePath=settings.endlumePath||'';
.vyron-v110-audit/src/ProductionManager.tsx:95:    if(!workspace||!channel){toast('Сначала выберите канал и рабочую папку VYRON');return}
.vyron-v110-audit/src/ProductionManager.tsx:155:    if(!batch)return;if(!endlumePath){toast('Укажите путь к ENDLUME в настройках VYRON');return}
.vyron-v110-audit/src/ProductionManager.tsx:163:    if(!batch)return;if(!endlumePath){toast('Укажите путь к ENDLUME в настройках VYRON');return}
.vyron-v110-audit/src/ProductionManager.tsx:199:  if(!channels.length)return <div className="panel emptyManager"><div><b>Нет подключённых каналов</b><p>Production Manager использует существующий список каналов VYRON.</p></div></div>;
.vyron-v110-audit/src/ProductionManager.tsx:204:      <section className="panel pmCard"><div className="pmCardHead"><span><small>01</small><h3>Изображения</h3></span><b className={session?.active&&!importError?'live':''}>{importError?'● ОШИБКА':session?.active?(collected?'● СБОР ИДЁТ':'● ЖДУ ФАЙЛЫ'):'ГОТОВО'}</b></div><p>VYRON следит за Downloads только во время активной import-сессии выбранного канала и создаёт собственную нумерацию.</p><div className="pmFileCount"><b>{collected.toLocaleString('ru-RU')}</b><span>файлов собрано</span></div>{importError&&<div className="pmShortage"><div><b>Проблема со сбором изображений</b><span>{importError}</span></div></div>}<div className="pmActions"><button className="primary" disabled={busy==='import'} onClick={toggleImport}>{session?.active?'ЗАВЕРШИТЬ СБОР':'НАЧАТЬ СБОР'}</button></div><small className="pmHint">{session?.active?`Слежу: ${shortPath(session.downloadsPath)} • Копии: ${shortPath(session.importPath)}`:session?.importPath?shortPath(session.importPath):'После запуска скачивайте изображения из ChatGPT как обычно.'}</small></section>
.vyron-v110-audit/src/ProductionStatusBridge.tsx:6:  const booted=useApp(s=>s.booted),workspace=useApp(s=>s.settings.workspace),channels=useApp(s=>s.channels);
.vyron-v110-audit/src/DashboardOS.tsx:10:export function DashboardOS(){
.vyron-v110-audit/src/DashboardOS.tsx:11: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),competitors=useApp(s=>s.competitors),settings=useApp(s=>s.settings),setPage=useApp(s=>s.setPage),logs=useApp(s=>s.logs);const enabled=channels.filter(c=>c.enabled),errors=jobs.filter(j=>j.status==='ERROR'),needImage=jobs.filter(j=>j.status==='NEED_IMAGE'),needMusic=jobs.filter(j=>j.status==='WAITING_MUSIC');const analytics=enabled.map(c=>c.analytics).filter(Boolean);const todayViews=analytics.reduce((n,a)=>n+(a?.daily?.at(-1)?.views||0),0);const rev28=enabled.reduce((n,c)=>n+(c.analytics?.periodDays===28?Number(c.analytics.estimatedRevenue||0):0),0);const revenueKnown=enabled.some(c=>c.analytics?.periodDays===28&&c.analytics.estimatedRevenue!=null);const netSubs=enabled.reduce((n,c)=>n+(c.analytics?.periodDays===28?(c.analytics.subscribersGained-c.analytics.subscribersLost):0),0);const next=[...jobs].filter(j=>j.publishAt&&Date.parse(j.publishAt)>Date.now()).sort((a,b)=>Date.parse(a.publishAt!)-Date.parse(b.publishAt!)).slice(0,5);const issues=[...enabled.filter(c=>!c.youtubeProfileId).map(c=>({type:'OAuth',text:`${c.name}: YouTube не подключён`,page:'settings' as const})),...enabled.filter(c=>c.youtubeProfileId&&!c.analytics).map(c=>({type:'Analytics',text:`${c.name}: ждёт первую аналитику`,page:'analytics' as const})),...errors.slice(0,6).map(j=>({type:'Ошибка',text:`VIDEO_${String(j.number).padStart(3,'0')}: ${j.error||'ошибка производства'}`,page:'production' as const})),...(needImage.length?[{type:'Материалы',text:`${needImage.length} проектов ждут изображения`,page:'production' as const}]:[]),...(needMusic.length?[{type:'Материалы',text:`${needMusic.length} проектов ждут аудио`,page:'production' as const}]:[])].slice(0,10);const competitorPulse=[...competitors].map(c=>({c,v:velocity(c)})).filter(x=>x.v!=null).sort((a,b)=>(b.v||0)-(a.v||0)).slice(0,4);const systemOk=!errors.length&&enabled.every(c=>c.youtubeProfileId);
.vyron-v110-audit/src/DashboardOS.tsx:14:  <div className="morningMetrics"><span><small>VIEWS TODAY*</small><b>{analytics.length?formatNumber(todayViews):'—'}</b><em>*последняя доступная строка YouTube</em></span><span><small>REVENUE 28D</small><b>{revenueKnown?money(rev28):'—'}</b><em>{revenueKnown?'реальные monetary analytics':'нет monetary data'}</em></span><span><small>NET SUBS 28D</small><b>{analytics.length?`${netSubs>=0?'+':''}${formatNumber(netSubs)}`:'—'}</b><em>{enabled.length} каналов</em></span><span><small>NEXT PUBLISH</small><b>{next[0]?.publishAt?new Date(next[0].publishAt).toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'}):'—'}</b><em>{next[0]?.publishAt?new Date(next[0].publishAt).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}):'нет в плане'}</em></span><span><small>AUTOPILOT</small><b>{settings.autopilotMode.toUpperCase()}</b><em>{settings.autopilotEnabled?'цикл активен':'ручной режим'}</em></span></div>
.vyron-v110-audit/src/DashboardOS.tsx:17:  <div className="morningGrid"><section className="panel competitorPulse"><div className="panelHead"><div><small>COMPETITOR PULSE</small><h3>Кто ускорился</h3></div><button onClick={()=>setPage('competitors')}>Radar</button></div>{competitorPulse.length?competitorPulse.map(({c,v})=><div key={c.id}><b>{c.name}</b><span>+{Math.max(0,Math.round(v||0)).toLocaleString('ru-RU')} views/д</span></div>):<p>Нужно минимум два snapshot конкурентов, чтобы увидеть скорость роста.</p>}</section><section className="panel autopilotActivity"><div className="panelHead"><div><small>AUTOPILOT ACTIVITY</small><h3>Последние действия</h3></div><button onClick={()=>setPage('settings')}>Настроить</button></div>{logs.length?logs.slice(0,6).map((l,i)=><div key={i}><small>{new Date(l.at).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}</small><span>{l.message}</span></div>):<p>Автоматических действий пока нет.</p>}</section></div>
.vyron-v110-audit/src/commandCenterStore.ts:5:const EVENT='vyron-command-center';
.vyron-v110-audit/src/store.ts:3:import type { AppState, Channel, Competitor, Page, Settings, VideoJob } from './types';
.vyron-v110-audit/src/store.ts:5:import {notifyLegacy} from './notificationCenter';
.vyron-v110-audit/src/store.ts:7:export const DEFAULT_SETTINGS:Settings={
.vyron-v110-audit/src/store.ts:17:export const EMPTY_STATE:AppState={version:7,channels:[],jobs:[],competitors:[],settings:DEFAULT_SETTINGS,logs:[]};
.vyron-v110-audit/src/store.ts:25:  patchSettings:(p:Partial<Settings>)=>void; log:(message:string,level?:'info'|'warn'|'error')=>void; toast:(message:string)=>void;
.vyron-v110-audit/src/store.ts:34:  ...EMPTY_STATE,page:'dashboard',booted:false,
.vyron-v110-audit/src/store.ts:35:  hydrate:s=>set({...EMPTY_STATE,...s,version:7,channels:(s.channels||[]).map(normalizeChannel),jobs:(s.jobs||[]).map(normalizeJob),competitors:s.competitors||[],settings:{...DEFAULT_SETTINGS,...s.settings,autoUploadYoutube:false,youtubeIntelligenceAutoRefresh:false},logs:s.logs||[],booted:true}),
.vyron-v110-audit/src/store.ts:37:  persist:async()=>{const s=get();const state:AppState={version:7,channels:s.channels,jobs:s.jobs,competitors:s.competitors,settings:s.settings,logs:s.logs};await api.saveState(state)},
.vyron-v110-audit/src/store.ts:39:    const id=crypto.randomUUID();const name=(p.name||'Новый канал').trim();const defaultTracks=get().settings.tracksPerVideo||10;
.vyron-v110-audit/src/store.ts:40:    const channel:Channel={id,name,slug:slugify(name),cadenceDays:p.cadenceDays||2,targetBufferDays:p.targetBufferDays||60,publishHour:p.publishHour??18,publishMinute:p.publishMinute??0,language:p.language||'RU',genre:p.genre||'Music',country:p.country||'Россия',minTracks:p.minTracks||defaultTracks,targetDurationMin:p.targetDurationMin||get().settings.endlumeTargetDurationMin||120,enabled:p.enabled??true,youtubeProfileId:p.youtubeProfileId,youtubeChannelId:p.youtubeChannelId,seo:p.seo||{titlePatterns:['{topic} • Session {number}','{genre} — {topic} | Mix {number}'],descriptionTemplate:'{title}\n\nНовая подборка в стиле {genre}.',tags:[p.genre||'music','mix','playlist'],banned:[],aiPrompt:''}};
.vyron-v110-audit/src/store.ts:51:  patchSettings:p=>{set(s=>({settings:{...s.settings,...p}}));scheduleSave()},

## YouTube / quota / metadata / publish symbols
.vyron-v110-audit/src/CommandCenter.tsx:7:import {getChannelScheduleState,scheduleDateLabel,scheduleDescription} from './channelSchedule';
.vyron-v110-audit/src/CommandCenter.tsx:21: const totalReady=active.reduce((n,c)=>n+productionReadiness(c,jobs,targetFor(c.id)).readyToYoutube,0);
.vyron-v110-audit/src/CommandCenter.tsx:25:  {next?<div className="commandToday"><div><small>СЕГОДНЯ</small><h4>{next.channel.name}</h4><p>{next.record?.runwayDays===undefined?'Нет свежих данных о запасе публикаций.':`Видео в запасе примерно на ${next.record.runwayDays} дн.`}</p></div><div className="commandTodayFacts"><span>Запас<b>{next.record?.runwayDays===undefined?'—':`${next.record.runwayDays} дней`}</b></span><span>Готовность{nextReady?<div className="readinessCompact"><b><strong>{nextReady.readyToPublish}</strong><i>/</i><em>{nextReady.targetProjects}</em></b><small>{nextReady.percent}% готово</small><span><i style={{width:`${nextReady.percent}%`}}/></span></div>:<b>—</b>}</span><span>Рекомендация<b>{next.record?.runwayDays!==undefined&&next.record.runwayDays<=14?'Готовить в первую очередь':next.record?.runwayDays!==undefined&&next.record.runwayDays<=45?'Продолжить производство':'Запас пока достаточный'}</b></span><span>График<b>{scheduleDescription(next.channel)}</b></span></div><button className="primary" onClick={()=>openProduction(next.channel.id)}>Открыть производство</button></div>:<div className="commandEmpty"><b>Нет активных каналов</b><span>Включите канал, чтобы VYRON мог показать производственный приоритет.</span></div>}
.vyron-v110-audit/src/CommandCenter.tsx:27:  <div className="commandBlock networkPlan"><div className="commandBlockHead"><div><small>КАНАЛЫ</small><h4>Очередь производства</h4></div><span>{rows.length}</span></div><div className="commandTable"><div className="commandRow commandTh"><span>Канал</span><span>Запас видео</span><span>Следующее производство</span><span>Готовность</span><span>Статус</span></div>{rows.map(({channel,record})=>{const ready=getChannelProductionReadiness(channel,jobs,targetFor(channel.id));const runwayDays=record?.runwayDays;const status:ChannelRunwayRecord['status']=record?.status||'no-data';const localSchedule=getChannelScheduleState(channel.id,channel);return <button className="commandRow commandChannelRow" key={channel.id} onClick={()=>openProduction(channel.id)}><span><b>{channel.name}</b><small>{localSchedule.lastScheduledAt?`Запланировано до ${scheduleDateLabel(localSchedule.lastScheduledAt)}`:record?.scheduledUntil?`Запланировано до ${dateLabel(record.scheduledUntil)}`:'Расписание ещё не подтверждено'}</small><small>График: {scheduleDescription(channel)}</small></span><span className={`commandRunway ${status}`}>{runwayDays===undefined?'—':`${runwayDays} дн.`}</span><span>{runwayDays===undefined?'—':runwayDays<=45?'Сейчас':dateLabel(record?.nextProductionDate)}</span><span><div className="readinessCompact tableReady"><b><strong>{ready.readyToPublish}</strong><i>/</i><em>{ready.targetProjects}</em></b><small>{ready.percent}% • готово к публикации</small><span><i style={{width:`${ready.percent}%`}}/></span></div></span><span>{statusLabel(status)}</span></button>})}</div></div>
.vyron-v110-audit/src/CommandCenter.tsx:28:  <div className="commandFootNote">Данные этого экрана берутся из локально сохранённого расписания и Production. Переход на экран сам по себе не запускает синхронизацию YouTube.</div>
.vyron-v110-audit/src/channelSchedule.ts:1:import type {Channel,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/channelSchedule.ts:2:export type ExistingCache={version:1;updatedAt:string;videos:YoutubeExistingVideo[];baseline:Record<string,YoutubeExistingVideo>;lastUndo:YoutubeExistingVideo[];syncInfo:any};
.vyron-v110-audit/src/channelSchedule.ts:3:export type ScheduleMode='interval'|'pattern';
.vyron-v110-audit/src/channelSchedule.ts:4:export type SchedulePattern={publishDays:number;pauseDays:number;anchorDate:string};
.vyron-v110-audit/src/channelSchedule.ts:5:export type ChannelScheduleState={channelId:string;lastPublishedAt?:string;lastScheduledAt?:string;nextAvailableAt?:string;scheduledUntil?:string;scheduleMode:ScheduleMode;publishIntervalDays:number;publishDays:number;pauseDays:number;patternAnchorDate?:string;defaultPublishTime:string;scheduledCount:number;updatedAt?:string};
.vyron-v110-audit/src/channelSchedule.ts:7:const EVENT='vyron-channel-schedule-changed';
.vyron-v110-audit/src/channelSchedule.ts:11:const cloneVideo=(v:YoutubeExistingVideo)=>({...v,tags:[...(v.tags||[])]});
.vyron-v110-audit/src/channelSchedule.ts:12:export function replaceExistingCacheFromSync(channelId:string,videos:YoutubeExistingVideo[],syncInfo:any){const rows=videos.map(cloneVideo),baseline=Object.fromEntries(rows.map(v=>[v.id,cloneVideo(v)]));writeExistingCache(channelId,{version:1,updatedAt:new Date().toISOString(),videos:rows,baseline,lastUndo:[],syncInfo})}
.vyron-v110-audit/src/channelSchedule.ts:13:export function mergeExistingCacheVideos(channelId:string,updates:YoutubeExistingVideo[]){const prev=readExistingCache(channelId);const map=new Map((prev?.videos||[]).map(v=>[v.id,cloneVideo(v)]));const base={...(prev?.baseline||{})};for(const u of updates){map.set(u.id,cloneVideo(u));base[u.id]=cloneVideo(u)}writeExistingCache(channelId,{version:1,updatedAt:new Date().toISOString(),videos:[...map.values()],baseline:base,lastUndo:prev?.lastUndo||[],syncInfo:prev?.syncInfo||null})}
.vyron-v110-audit/src/channelSchedule.ts:18:export function scheduleDateLabel(iso?:string){if(!iso)return'—';const d=new Date(iso);if(Number.isNaN(d.getTime()))return'—';return new Intl.DateTimeFormat('ru-RU',{timeZone:'Asia/Krasnoyarsk',day:'2-digit',month:'2-digit',year:'numeric'}).format(d)}
.vyron-v110-audit/src/channelSchedule.ts:23:export function scheduleModeFor(channel:Channel):ScheduleMode{return channel.scheduleMode==='pattern'?'pattern':'interval'}
.vyron-v110-audit/src/channelSchedule.ts:25:export function patternFor(channel:Channel):SchedulePattern|undefined{if(scheduleModeFor(channel)!=='pattern')return;const anchor=channel.patternAnchorDate;return anchor?{publishDays:Math.max(1,Math.floor(channel.publishDays||3)),pauseDays:Math.max(1,Math.floor(channel.pauseDays||1)),anchorDate:anchor}:undefined}
.vyron-v110-audit/src/channelSchedule.ts:26:export function scheduleLabel(channel:Channel){return scheduleModeFor(channel)==='pattern'?`${Math.max(1,Math.floor(channel.publishDays||3))} / ${Math.max(1,Math.floor(channel.pauseDays||1))}`:intervalDaysFor(channel)===1?'каждый день':intervalDaysFor(channel)===2?'через день':`каждые ${intervalDaysFor(channel)} дн.`}
.vyron-v110-audit/src/channelSchedule.ts:27:export function scheduleDescription(channel:Channel){return scheduleModeFor(channel)==='pattern'?`${Math.max(1,Math.floor(channel.publishDays||3))} дня публикаций / ${Math.max(1,Math.floor(channel.pauseDays||1))} день пауза`:scheduleLabel(channel)}
.vyron-v110-audit/src/channelSchedule.ts:28:export function scheduleAverageIntervalDays(channel:Channel){if(scheduleModeFor(channel)==='pattern'){const p=Math.max(1,Math.floor(channel.publishDays||3)),q=Math.max(1,Math.floor(channel.pauseDays||1));return(p+q)/p}return intervalDaysFor(channel)}
.vyron-v110-audit/src/channelSchedule.ts:29:export function isPatternPublishDate(key:string,pattern:SchedulePattern){const cycle=Math.max(2,pattern.publishDays+pattern.pauseDays);const delta=diffDays(pattern.anchorDate,key);if(delta<0)return false;const pos=((delta%cycle)+cycle)%cycle;return pos<pattern.publishDays}
.vyron-v110-audit/src/channelSchedule.ts:31:function occupiedDates(videos:YoutubeExistingVideo[],excludeIds:string[]=[]){const excluded=new Set(excludeIds);return new Set(videos.filter(v=>!excluded.has(v.id)).map(v=>krasDateKey(v.publishAt)).filter(Boolean) as string[])}
.vyron-v110-audit/src/channelSchedule.ts:32:export function deriveChannelScheduleState(channel:Channel,videos:YoutubeExistingVideo[],updatedAt?:string,excludeIds:string[]=[]):ChannelScheduleState{
.vyron-v110-audit/src/channelSchedule.ts:33:  const mode=scheduleModeFor(channel),interval=intervalDaysFor(channel),publishDays=Math.max(1,Math.floor(channel.publishDays||3)),pauseDays=Math.max(1,Math.floor(channel.pauseDays||1));const defaultTime=`${pad(channel.publishHour||0)}:${pad(channel.publishMinute||0)}`;
.vyron-v110-audit/src/channelSchedule.ts:34:  const excluded=new Set(excludeIds),visible=videos.filter(v=>!excluded.has(v.id));const scheduled=visible.map(v=>v.publishAt).filter((x):x is string=>Boolean(x)&&Number.isFinite(Date.parse(x!))).sort((a,b)=>Date.parse(a)-Date.parse(b));const published=visible.map(v=>v.publishedAt).filter((x):x is string=>Boolean(x)&&Number.isFinite(Date.parse(x!))).sort((a,b)=>Date.parse(a)-Date.parse(b));
.vyron-v110-audit/src/channelSchedule.ts:35:  const lastScheduledAt=scheduled.length?scheduled[scheduled.length-1]:undefined,lastPublishedAt=published.length?published[published.length-1]:undefined,scheduledUntil=krasDateKey(lastScheduledAt),occupied=occupiedDates(visible);
.vyron-v110-audit/src/channelSchedule.ts:39:    if(anchor){let k=scheduledUntil?addCalendarDays(scheduledUntil,1):anchor;let guard=0;const pattern={publishDays,pauseDays,anchorDate:anchor};while(guard++<20000&&(!isPatternPublishDate(k,pattern)||occupied.has(k)))k=addCalendarDays(k,1);nextKey=k}
.vyron-v110-audit/src/channelSchedule.ts:40:  }else if(scheduledUntil){let k=addCalendarDays(scheduledUntil,interval),guard=0;while(guard++<10000&&occupied.has(k))k=addCalendarDays(k,interval);nextKey=k}
.vyron-v110-audit/src/channelSchedule.ts:41:  return{channelId:channel.id,lastPublishedAt,lastScheduledAt,nextAvailableAt:nextKey?toKratIso(nextKey,defaultTime):undefined,scheduledUntil,scheduleMode:mode,publishIntervalDays:interval,publishDays,pauseDays,patternAnchorDate:channel.patternAnchorDate,defaultPublishTime:defaultTime,scheduledCount:scheduled.length,updatedAt};
.vyron-v110-audit/src/channelSchedule.ts:43:export function getChannelScheduleState(channelId:string,channel:Channel,excludeIds:string[]=[]){const cache=readExistingCache(channelId);return deriveChannelScheduleState(channel,cache?.videos||[],cache?.updatedAt,excludeIds)}
.vyron-v110-audit/src/channelSchedule.ts:44:export function generatePatternSchedule(channel:Channel,videos:YoutubeExistingVideo[],count:number,excludeIds:string[]=[]){
.vyron-v110-audit/src/channelSchedule.ts:45:  const pattern=patternFor(channel);if(!pattern)return{dates:[] as string[],calendar:[] as PatternCalendarDay[]};const cacheVideos=readExistingCache(channel.id)?.videos||videos,occupied=occupiedDates(cacheVideos,excludeIds),state=deriveChannelScheduleState(channel,cacheVideos,undefined,excludeIds),time=state.defaultPublishTime;let k=state.nextAvailableAt?krasDateKey(state.nextAvailableAt):pattern.anchorDate;if(!k)return{dates:[],calendar:[]};
.vyron-v110-audit/src/channelSchedule.ts:50:export function generateIntervalSchedule(channel:Channel,videos:YoutubeExistingVideo[],count:number,excludeIds:string[]=[]){const state=deriveChannelScheduleState(channel,videos,undefined,excludeIds);let k=state.nextAvailableAt?krasDateKey(state.nextAvailableAt):undefined;if(!k)return[];const occupied=occupiedDates(videos,excludeIds),days=intervalDaysFor(channel),time=state.defaultPublishTime,out:string[]=[];let guard=0;while(out.length<count&&guard++<20000){if(!occupied.has(k)){out.push(toKratIso(k,time));occupied.add(k)}k=addCalendarDays(k,days)}return out}
.vyron-v110-audit/src/channelSchedule.ts:51:export function subscribeChannelSchedule(cb:(channelId:string)=>void){const fn=(e:Event)=>cb(String((e as CustomEvent<any>).detail?.channelId||''));window.addEventListener(EVENT,fn);return()=>window.removeEventListener(EVENT,fn)}
.vyron-v110-audit/src/App.tsx:5:import { parseMetadataFile,type ImportedMetadata } from './metadata';
.vyron-v110-audit/src/App.tsx:7:import type { AutopilotSummary, Channel, Competitor, InboxScan, JobStatus, LicenseStatus, Page, VideoJob, YoutubeProfile } from './types';
.vyron-v110-audit/src/App.tsx:11:import { MetadataPage } from './MetadataPage';
.vyron-v110-audit/src/App.tsx:17:import { YouTubeCenter } from './YouTubeCenter';
.vyron-v110-audit/src/App.tsx:18:import { ChannelRunwayScheduler } from './ChannelRunwayScheduler';
.vyron-v110-audit/src/App.tsx:47:  {page:'youtube',icon:'▶',label:'YouTube'},
.vyron-v110-audit/src/App.tsx:52:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужна картинка',WAITING_MUSIC:'Ждёт музыку',READY_RENDER:'Готов к рендеру',RENDERING:'Рендерится',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загружается',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/App.tsx:62:  useEffect(()=>{let unlisten:(()=>void)|undefined;void api.onYoutubeProgress(({jobId,progress})=>patchJob(jobId,{uploadProgress:progress,status:'UPLOADING'})).then(fn=>unlisten=fn);return()=>unlisten?.()},[]);
.vyron-v110-audit/src/App.tsx:66:  const screen=page==='dashboard'||page==='autopilot'?<DashboardOS/>:page==='accounts'?<SettingsOS license={license}/>:page==='channels'?<ChannelsOS/>:page==='production'||page==='content'?<ProductionOS/>:page==='youtube'?<YouTubeCenter/>:page==='competitors'?<CompetitorsPage/>:page==='analytics'?<AnalyticsPage/>:page==='metadata'?<YouTubeCenter initialTab='metadata'/>:page==='existing'?<YouTubeCenter initialTab='uploaded'/>:page==='publisher'?<YouTubeCenter initialTab='queue'/>:<SettingsOS license={license}/>;
.vyron-v110-audit/src/App.tsx:67:  return <div className="appShell"><ChannelRunwayScheduler/><ProductionStatusBridge/><RecoveryGate/><Sidebar page={page} setPage={setPage}/><main className="main"><Topbar/><div className="pageWrap" key={page}>{screen}</div></main>{settings.fpsMonitor&&<FpsMonitor/>}<NotificationCenter/>{update&&<UpdateNotice update={update} onLater={()=>setUpdate(null)}/>}<div className="bgGlow a"/><div className="bgGlow b"/></div>
.vyron-v110-audit/src/App.tsx:79:<div className="metrics six"><Metric label="КАНАЛОВ" value={channels.length} note="в системе"/><Metric label="СРОЧНО" value={urgent} note="буфер < 14 дней" tone="red"/><Metric label="ЖДУТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note="папки готовы" tone="orange"/><Metric label="ГОТОВО РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="ENDLUME queue" tone="purple"/><Metric label="ГОТОВО MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="YouTube queue" tone="green"/><Metric label="YOUTUBE" value={jobs.filter(j=>j.status==='SCHEDULED').length} note="запланировано" tone="blue"/></div>
.vyron-v110-audit/src/App.tsx:80:{channels.length===0?<Empty title="Добавь первый канал" text="Задай график публикаций — дальше VYRON сам рассчитает запас на 60 дней." action="ДОБАВИТЬ КАНАЛ" onClick={()=>setPage('channels')}/>:<div className="panel"><div className="panelHead"><div><small>ПРИОРИТЕТЫ</small><h3>Запас каналов</h3></div></div><div className="table"><div className="tr th"><span>Канал</span><span>Буфер</span><span>Нужно</span><span>Картинки</span><span>Музыка</span><span>MP4</span><span>YouTube</span></div>{rows.map(({c,days,need})=>{const cj=jobs.filter(j=>j.channelId===c.id),p=priorityFor(days);return <button className="tr" key={c.id} onClick={()=>setPage('production')}><span className="channelCell"><i className={`dot ${p}`}/><b>{c.name}</b><small>раз в {c.cadenceDays} дн.</small></span><span className={p}>{days} дн</span><span>{need||'—'}</span><span>{cj.filter(j=>j.coverPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.tracksCount>=c.minTracks).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.finalPath).length}/{requiredVideos(c)}</span><span>{cj.filter(j=>j.status==='SCHEDULED').length}/{requiredVideos(c)}</span></button>})}</div></div>}
.vyron-v110-audit/src/App.tsx:82:function SystemCard(){const s=useApp(x=>x.settings),channels=useApp(x=>x.channels);return <div className="panel systemCard"><div className="panelHead"><div><small>СИСТЕМА</small><h3>Автономный контур</h3></div></div><div className="flow"><b>SUNO</b><i>→</i><b>VYRON</b><i>→</i><b>ENDLUME</b><i>→</i><b>YouTube</b></div><div className="sysRows"><span>Workspace <b>{s.workspace?'готов':'нет'}</b></span><span>Метаданные <b>{s.openaiApiKey?'AI API подключён':'ChatGPT / Inbox'}</b></span><span>ENDLUME <b>{s.endlumePath?'подключён':'не выбран'}</b></span><span>YouTube OAuth <b>{channels.some(c=>c.youtubeProfileId)?'привязан':'не привязан'}</b></span></div></div>}
.vyron-v110-audit/src/App.tsx:85:<PageHeader eyebrow="AUTONOMOUS PIPELINE" title="Автопилот" text="Один цикл проверяет дефицит, Inbox, проекты, рендеры и очередь YouTube. Ошибка одной задачи не останавливает остальные." actions={<><button className="ghost" onClick={()=>void loadInboxes()}>↻ INBOX</button><button className="primary" disabled={busy} onClick={run}>{busy?'ВЫПОЛНЯЮ…':'ЗАПУСТИТЬ ЦИКЛ СЕЙЧАС'}</button></>}/>
.vyron-v110-audit/src/App.tsx:87:<div className="metrics six"><Metric label="НУЖНА КАРТИНКА" value={jobs.filter(j=>j.status==='NEED_IMAGE').length} note="Inbox/Images"/><Metric label="ЖДЁТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note={`${settings.tracksPerVideo} треков`} tone="orange"/><Metric label="РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="RenderQueue" tone="purple"/><Metric label="MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="готово" tone="green"/><Metric label="UPLOAD" value={jobs.filter(j=>j.status==='UPLOADING').length} note="resumable" tone="blue"/><Metric label="ОШИБКИ" value={jobs.filter(j=>j.status==='ERROR').length} note="не блокируют очередь" tone="red"/></div>
.vyron-v110-audit/src/App.tsx:88:<div className="autoSteps">{[['01','План 60 дней',settings.autoCreatePlan,'autoCreatePlan'],['02','Музыка → по проектам',settings.autoAssignMusic,'autoAssignMusic'],['03','Изображение → 1 проект',settings.autoAssignImages,'autoAssignImages'],['04','AI title/description/tags',settings.autoGenerateMetadata,'autoGenerateMetadata'],['05','Очередь ENDLUME',settings.autoQueueRender,'autoQueueRender'],['06','YouTube upload + schedule',settings.autoUploadYoutube,'autoUploadYoutube']].map(([n,label,val,key]:any)=><button key={key} className={`autoStep ${val?'active':''}`} onClick={()=>patch({[key]:!val} as any)}><span>{n}</span><b>{label}</b><em>{val?'AUTO':'OFF'}</em></button>)}</div>
.vyron-v110-audit/src/App.tsx:89:{last&&<div className="panel autoResult"><div className="panelHead"><div><small>ПОСЛЕДНИЙ РУЧНОЙ ЦИКЛ</small><h3>Результат</h3></div></div><div className="resultGrid"><span>Проекты <b>{last.prepared}</b></span><span>Треки <b>{last.tracksMoved}</b></span><span>Картинки <b>{last.imagesMoved}</b></span><span>AI <b>{last.metadataGenerated}</b></span><span>RenderQueue <b>{last.renderQueued}</b></span><span>YouTube <b>{last.uploads}</b></span><span>Ошибки <b>{last.errors}</b></span></div>{last.notes.slice(0,8).map((n,i)=><p key={i}>{n}</p>)}</div>}
.vyron-v110-audit/src/App.tsx:91:<div className="panel warningPanel"><b>Что ещё требуется один раз</b><p>Для полностью автоматической публикации: подключить Google OAuth в Настройках и привязать профиль к каналу. Метаданные можно бесплатно импортировать из обычного ChatGPT Plus через Metadata Inbox; платный OpenAI API не обязателен. Для нулевого ручного рендера ENDLUME должен читать RenderQueue — VYRON уже создаёт эту очередь.</p><button onClick={()=>setPage('settings')}>ОТКРЫТЬ НАСТРОЙКИ</button></div>
.vyron-v110-audit/src/App.tsx:94:function Channels(){const channels=useApp(s=>s.channels),addChannel=useApp(s=>s.addChannel),updateChannel=useApp(s=>s.updateChannel),removeChannel=useApp(s=>s.removeChannel),toast=useApp(s=>s.toast);const [editing,setEditing]=useState<string|null>(null),[profiles,setProfiles]=useState<YoutubeProfile[]>([]);useEffect(()=>{void api.youtubeProfiles().then(setProfiles).catch(()=>{})},[]);return <>
.vyron-v110-audit/src/App.tsx:96:{channels.length===0?<Empty title="Каналов пока нет" text="Добавь канал. По умолчанию публикация — через день, запас — 60 дней." action="СОЗДАТЬ ПЕРВЫЙ" onClick={()=>{const c=addChannel({name:'Новый канал'});setEditing(c.id)}}/>:<div className="channelGrid">{channels.map(c=><div className={`channelCard ${editing===c.id?'editing':''}`} key={c.id}><div className="channelTop"><div><i className="channelAvatar">{c.name.slice(0,2).toUpperCase()}</i><div><b>{c.name}</b><small>{c.genre} • {c.language} • {c.youtubeProfileId?'YouTube ✓':'YouTube не привязан'}</small></div></div><button className="iconBtn" onClick={()=>setEditing(editing===c.id?null:c.id)}>✎</button></div><div className="channelStats"><span><small>ПУБЛИКАЦИЯ</small><b>раз в {c.cadenceDays} дн.</b></span><span><small>ЗАПАС</small><b>{c.targetBufferDays} дней</b></span><span><small>ТРЕКОВ</small><b>{c.minTracks}</b></span><span><small>ДЛИТЕЛЬНОСТЬ</small><b>{c.targetDurationMin} мин</b></span></div>{editing===c.id&&<div className="editGrid"><label>Название<input value={c.name} onChange={e=>updateChannel(c.id,{name:e.target.value})}/></label><label>Жанр<input value={c.genre} onChange={e=>updateChannel(c.id,{genre:e.target.value})}/></label><label>Язык<input value={c.language} onChange={e=>updateChannel(c.id,{language:e.target.value})}/></label><label>Страна<input value={c.country} onChange={e=>updateChannel(c.id,{country:e.target.value})}/></label><label>Раз в N дней<input type="number" min="1" max="30" value={c.cadenceDays} onChange={e=>updateChannel(c.id,{cadenceDays:+e.target.value||1})}/></label><label>Запас, дней<input type="number" min="7" max="365" value={c.targetBufferDays} onChange={e=>updateChannel(c.id,{targetBufferDays:+e.target.value||60})}/></label><label>Треков на видео<input type="number" min="1" max="100" value={c.minTracks} onChange={e=>updateChannel(c.id,{minTracks:+e.target.value||10})}/></label><label>Длительность, мин<input type="number" min="10" max="720" value={c.targetDurationMin} onChange={e=>updateChannel(c.id,{targetDurationMin:+e.target.value||120})}/></label><label>Время публикации<input type="time" value={`${String(c.publishHour).padStart(2,'0')}:${String(c.publishMinute).padStart(2,'0')}`} onChange={e=>{const [h,m]=e.target.value.split(':').map(Number);updateChannel(c.id,{publishHour:h,publishMinute:m})}}/></label><label>YouTube OAuth<select value={c.youtubeProfileId||''} onChange={e=>{const p=profiles.find(x=>x.id===e.target.value);updateChannel(c.id,{youtubeProfileId:e.target.value||undefined,youtubeChannelId:p?.channelId})}}><option value="">— не привязан —</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.channelTitle||p.channelId||p.id}</option>)}</select></label><label className="wide">Формулы названий<textarea value={c.seo.titlePatterns.join('\n')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,titlePatterns:e.target.value.split('\n').filter(Boolean)}})}/></label><label className="wide">Шаблон описания<textarea value={c.seo.descriptionTemplate} onChange={e=>updateChannel(c.id,{seo:{...c.seo,descriptionTemplate:e.target.value}})}/></label><label className="wide">Теги<input value={c.seo.tags.join(', ')} onChange={e=>updateChannel(c.id,{seo:{...c.seo,tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)}})}/></label><label className="wide">Инструкция AI<textarea value={c.seo.aiPrompt||''} placeholder="Например: премиальные французские deep house заголовки, без повторов…" onChange={e=>updateChannel(c.id,{seo:{...c.seo,aiPrompt:e.target.value}})}/></label><div className="editActions"><button className="danger" onClick={()=>{if(confirm(`Удалить ${c.name}? Файлы на диске останутся.`)){removeChannel(c.id);setEditing(null)}}}>УДАЛИТЬ ИЗ VYRON</button><button className="primary" onClick={()=>setEditing(null)}>ГОТОВО</button></div></div>}</div>)}</div>}</>}
.vyron-v110-audit/src/App.tsx:98:function Production(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false);const filtered=jobs.filter(j=>filter==='all'||j.channelId===filter).sort((a,b)=>a.number-b.number);async function refreshAll(){setBusy(true);try{for(const j of useApp.getState().jobs){const c=channels.find(x=>x.id===j.channelId);if(!c||!j.folder||j.status==='SCHEDULED')continue;try{patchJob(j.id,await api.refreshJob(j.folder,c.minTracks))}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}toast('Статусы обновлены')}finally{setBusy(false)}}async function createPlan(){if(!settings.workspace){const ws=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:ws})}let added=0;let next=[...useApp.getState().jobs];for(const c of channels.filter(c=>c.enabled)){const created=createMissingJobs(c,next);next=[...next,...created];added+=created.length}setJobs(next);await runAutopilotCycle(true);toast(added?`Создано задач: ${added}`:'Дефицита нет')}return <>
.vyron-v110-audit/src/App.tsx:99:<PageHeader eyebrow="PRODUCTION" title="Производственная очередь" text="Каждая карточка — один будущий ролик: 10 треков + 1 изображение + metadata + final.mp4." actions={<><button className="ghost" disabled={busy} onClick={refreshAll}>{busy?'ПРОВЕРЯЮ…':'↻ СТАТУСЫ'}</button><button className="primary" onClick={createPlan}>ЗАПОЛНИТЬ ДО 60 ДНЕЙ</button></>}/>
.vyron-v110-audit/src/App.tsx:101:{filtered.length===0?<Empty title="Очередь пустая" text="VYRON рассчитает нужное число видео по графику каждого канала." action="СОЗДАТЬ ПЛАН" onClick={createPlan}/>:<div className="jobs">{filtered.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="job" key={j.id}><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{c?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не созданы'}</b><span>{j.publishAt?`План: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата не назначена'} • треков {j.tracksCount}/{j.minTracks}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>ПАПКА</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,c?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ МУЗЫКА</button>}{j.status==='READY_RENDER'&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('publisher')}>YOUTUBE</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,c?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>ПОВТОРИТЬ</button>}</div></div>})}</div>}</>}
.vyron-v110-audit/src/App.tsx:106:<div className="contentLayout"><div className="panel importPanel"><div className="stepNum">01</div><h3>Музыка</h3><p>Автопилот берёт файлы по времени появления и распределяет по {c?.minTracks||settings.tracksPerVideo} треков на Video_XXX.</p><div className="bigCount"><strong>{jobs.filter(j=>j.status==='WAITING_MUSIC').length}</strong><span>ждут музыку</span></div></div><div className="panel importPanel"><div className="stepNum green">02</div><h3>Изображения</h3><p>Одна картинка автоматически закрепляется за одним проектом. Порядок — по номеру Video_XXX.</p><div className="bigCount"><strong>{jobs.filter(j=>j.status==='NEED_IMAGE').length}</strong><span>ждут изображение</span></div></div><div className="panel importPanel"><div className="stepNum purple">03</div><h3>GPT / AI</h3><p>В автономном режиме metadata генерируется через OpenAI API. Ручной ChatGPT остаётся доступен как запасной сценарий.</p><button className="ghost full" onClick={()=>api.openWeb('https://chatgpt.com')}>ОТКРЫТЬ CHATGPT</button></div></div>
.vyron-v110-audit/src/App.tsx:109:function Competitors(){const channels=useApp(s=>s.channels),competitors=useApp(s=>s.competitors),add=useApp(s=>s.addCompetitor),patch=useApp(s=>s.patchCompetitor),remove=useApp(s=>s.removeCompetitor),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [channelId,setChannelId]=useState(channels[0]?.id||''),[url,setUrl]=useState(''),[name,setName]=useState('');const list=competitors.filter(c=>!channelId||c.channelId===channelId);async function refresh(c:Competitor){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key в Настройках');return}const m=c.url.match(/(?:channel\/)?(UC[\w-]{20,})/);if(!m){toast('Нужен URL с Channel ID UC…');return}try{const r=await api.youtubeStats(settings.youtubeApiKey,m[1]);patch(c.id,{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString(),name:r.title||c.name})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="COMPETITOR RADAR" title="Конкуренты" text="Публичные показатели конкурентов для дальнейшего AI-анализа контентных решений."/><div className="panel addLine"><select value={channelId} onChange={e=>setChannelId(e.target.value)}><option value="">К какому каналу</option>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input placeholder="Название" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="https://youtube.com/channel/UC…" value={url} onChange={e=>setUrl(e.target.value)}/><button className="primary" disabled={!channelId||!name||!url} onClick={()=>{add({id:crypto.randomUUID(),channelId,name,url});setName('');setUrl('')}}>ДОБАВИТЬ</button></div>{list.length===0?<Empty title="Конкурентов нет" text="Добавь каналы для наблюдения."/>:<div className="competitorGrid">{list.map(c=><div className="competitorCard" key={c.id}><div><i className="compAvatar">YT</i><span><b>{c.name}</b><small>{c.url}</small></span></div><div className="compStats"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.views)}</b></span><span><small>ВИДЕО</small><b>{formatNumber(c.videos)}</b></span></div><div className="cardActions"><button onClick={()=>api.openWeb(c.url)}>ОТКРЫТЬ</button><button className="primary small" onClick={()=>refresh(c)}>↻ ОБНОВИТЬ</button><button className="danger mini" onClick={()=>remove(c.id)}>×</button></div></div>)}</div>}</>}
.vyron-v110-audit/src/App.tsx:111:function Analytics(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),update=useApp(s=>s.updateChannel),toast=useApp(s=>s.toast);async function refresh(c:Channel){if(!settings.youtubeApiKey){toast('Добавь YouTube API Key');return}const id=c.youtubeChannelId||prompt(`Channel ID для ${c.name} (UC…)`)||'';if(!id)return;try{const r=await api.youtubeStats(settings.youtubeApiKey,id);update(c.id,{youtubeChannelId:id,stats:{subscribers:r.subscribers,views:r.views,videos:r.videos,updatedAt:new Date().toISOString()}})}catch(e){toast(String(e))}}return <><PageHeader eyebrow="ANALYTICS" title="Состояние сети" text="YouTube + производственный запас по каждому каналу."/><div className="analyticsGrid">{channels.map(c=>{const cj=jobs.filter(j=>j.channelId===c.id),days=bufferDays(c,jobs);return <div className="analyticCard" key={c.id}><div className="analyticHead"><div><i>{c.name.slice(0,2).toUpperCase()}</i><span><b>{c.name}</b><small>{c.stats?.updatedAt?new Date(c.stats.updatedAt).toLocaleString('ru-RU'):'YouTube не обновлялся'}</small></span></div><button onClick={()=>refresh(c)}>↻</button></div><div className="analyticNums"><span><small>ПОДПИСЧИКИ</small><b>{formatNumber(c.stats?.subscribers)}</b></span><span><small>ПРОСМОТРЫ</small><b>{formatNumber(c.stats?.views)}</b></span><span><small>ВИДЕО YT</small><b>{formatNumber(c.stats?.videos)}</b></span></div><div className="bufferBar"><div><span>Запас</span><b className={priorityFor(days)}>{days} / {c.targetBufferDays} дн</b></div><i><em style={{width:`${Math.min(100,days/c.targetBufferDays*100)}%`}}/></i></div><div className="analyticFoot"><span>В системе <b>{cj.length}</b></span><span>MP4 <b>{cj.filter(j=>j.finalPath).length}</b></span><span>Scheduled <b>{cj.filter(j=>j.status==='SCHEDULED').length}</b></span></div></div>})}</div></>}
.vyron-v110-audit/src/App.tsx:113:function Publisher(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast);const [rows,setRows]=useState<ImportedMetadata[]>([]),[files,setFiles]=useState<string[]>([]),[metaChannel,setMetaChannel]=useState(channels[0]?.id||''),[busy,setBusy]=useState(false);const input=useRef<HTMLInputElement>(null);const ready=jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED'].includes(j.status));async function read(fl:FileList|null){if(!fl?.length)return;const out:ImportedMetadata[]=[],names:string[]=[];for(const f of Array.from(fl)){out.push(...parseMetadataFile(f.name,await f.text()));names.push(f.name)}setRows(out);setFiles(names);toast(`Метаданные: ${out.length} записей`)}async function apply(){const used=new Set<string>();let ok=0,miss=0;for(let i=0;i<rows.length;i++){const r=rows[i],named=r.channel?channels.find(c=>c.name.toLowerCase()===r.channel?.toLowerCase()||c.id===r.channel):undefined,channelId=named?.id||metaChannel;const pool=useApp.getState().jobs.filter(j=>(!channelId||j.channelId===channelId)&&!used.has(j.id)).sort((a,b)=>a.number-b.number);const j=r.number?pool.find(x=>x.number===r.number):pool[0];if(!j){miss++;continue}used.add(j.id);const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}toast(`Применено: ${ok}${miss?` • не найдено ${miss}`:''}`)}async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}async function uploadAll(){setBusy(true);let n=0;try{for(const j of useApp.getState().jobs.filter(j=>j.status==='READY_UPLOAD'&&j.finalPath).sort((a,b)=>(a.publishAt||'').localeCompare(b.publishAt||''))){if(await upload(j))n++}toast(`YouTube: загружено ${n}`)}finally{setBusy(false)}}return <>
.vyron-v110-audit/src/App.tsx:115:<PageHeader eyebrow="YOUTUBE PUBLISHER" title="Публикации" text="Video_XXX уже содержит title, description, tags и дату. YouTube upload использует OAuth и возобновляемую загрузку." actions={<><button className="ghost" onClick={()=>input.current?.click()}>+ МЕТАДАННЫЕ</button><button className="primary" disabled={busy||!jobs.some(j=>j.status==='READY_UPLOAD')} onClick={uploadAll}>{busy?'ЗАГРУЖАЮ…':'ЗАГРУЗИТЬ ВСЕ ГОТОВЫЕ'}</button></>}/>
.vyron-v110-audit/src/App.tsx:116:<div className="metadataPanel"><div className="metadataHead"><div><small>METADATA INBOX</small><h3>Импорт старого сценария GPT/Word</h3><p>JSON / TXT / CSV можно использовать как переходный вариант. В автопилоте эти поля генерируются и сохраняются без копирования.</p></div><div className="metadataControls"><select value={metaChannel} onChange={e=>setMetaChannel(e.target.value)}><option value="">Все каналы</option>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button onClick={()=>input.current?.click()}>ВЫБРАТЬ</button><button className="primary" disabled={!rows.length} onClick={apply}>ПРИМЕНИТЬ</button></div></div>{files.length>0&&<div className="metadataSummary"><span>Файлов <b>{files.length}</b></span><span>Записей <b>{rows.length}</b></span><span>Video_XXX <b>{rows.filter(r=>r.number).length}</b></span><button onClick={()=>{setRows([]);setFiles([])}}>ОЧИСТИТЬ</button></div>}{rows.slice(0,5).map((r,i)=><div className="metaRow" key={i}><span><b>{r.number?`VIDEO_${String(r.number).padStart(3,'0')}`:`СТРОКА ${i+1}`}</b><small>{r.source}</small></span><strong>{r.title||'Без названия'}</strong><em>{r.tags?.length||0} тегов</em></div>)}</div>
.vyron-v110-audit/src/App.tsx:117:<div className="publisherNotice"><b>⚠ Google API compliance</b><p>OAuth и resumable upload встроены. Если Google API-проект новый и не прошёл YouTube compliance audit, API может разрешать загрузку только как private. VYRON покажет фактическую ошибку YouTube и не потеряет остальные задачи.</p></div>
.vyron-v110-audit/src/App.tsx:118:{ready.length===0?<Empty title="Нет готовых MP4" text="После появления final.mp4 ролик автоматически попадёт сюда."/>:<div className="publishList">{ready.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="publishCard" key={j.id}><div className="thumb"><span>▶</span><small>VIDEO_{String(j.number).padStart(3,'0')}</small></div><div className="publishMeta"><small>{c?.name} • {j.metadataSource||'template'}</small><input value={j.title} maxLength={100} onChange={e=>patchJob(j.id,{title:e.target.value,metadataLocked:true})}/><textarea value={j.description} onChange={e=>patchJob(j.id,{description:e.target.value,metadataLocked:true})}/><input value={j.tags.join(', ')} onChange={e=>patchJob(j.id,{tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean),metadataLocked:true})}/>{j.status==='UPLOADING'&&<div className="progress"><i style={{width:`${j.uploadProgress||0}%`}}/><span>{Math.round(j.uploadProgress||0)}%</span></div>}</div><div className="publishSide"><label>Дата<input type="datetime-local" value={j.publishAt?new Date(new Date(j.publishAt).getTime()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):''} onChange={e=>patchJob(j.id,{publishAt:new Date(e.target.value).toISOString(),metadataLocked:true})}/></label><span className={`publishState ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</span>{j.status==='READY_UPLOAD'&&<button className="primary" onClick={()=>upload(j)}>ЗАГРУЗИТЬ</button>}{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div></div>})}</div>}</>}
.vyron-v110-audit/src/App.tsx:120:function Settings({license,setLicense}:{license:LicenseStatus;setLicense:(x:LicenseStatus)=>void}){const s=useApp(x=>x.settings),patch=useApp(x=>x.patchSettings),logs=useApp(x=>x.logs),toast=useApp(x=>x.toast),log=useApp(x=>x.log);const [tab,setTab]=useState<'general'|'autopilot'|'integrations'|'subscription'|'updates'|'diagnostics'|'about'>('general'),[profiles,setProfiles]=useState<YoutubeProfile[]>([]),[connecting,setConnecting]=useState(false),[oauthSecret,setOauthSecret]=useState(''),[oauthMessage,setOauthMessage]=useState(''),[oauthError,setOauthError]=useState(false),[update,setUpdate]=useState<any>(),[progress,setProgress]=useState<number|null>(null),[diag,setDiag]=useState<any>(),[key,setKey]=useState('');const refreshProfiles=async()=>{try{const p=await api.youtubeProfiles();setProfiles(p);setOauthError(false);setOauthMessage(p.length?`OAuth: подключено профилей — ${p.length}`:'OAuth: сохранённых YouTube-профилей пока нет');return p}catch(e){const m=`OAuth storage: ${String(e)}`;setProfiles([]);setOauthError(true);setOauthMessage(m);log(m,'error');throw e}};useEffect(()=>{void refreshProfiles().catch(()=>{})},[]);return <>
.vyron-v110-audit/src/App.tsx:124:{tab==='autopilot'&&<div className="settingsStack"><div className="settingsCard"><h3>Autonomous Pipeline</h3><div className="toggleRows"><Toggle label="Автопилот" text="Запускать цикл автоматически." value={s.autopilotEnabled} onChange={v=>patch({autopilotEnabled:v})}/><Toggle label="Поддерживать план 60 дней" text="Автоматически создавать недостающие Video_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Брать треки из Inbox/Music." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 Video_XXX." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI-метаданные (опционально)" text="Только если вручную подключён отдельный API. Для бесплатного режима используй ChatGPT Plus → Metadata Inbox." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="Очередь ENDLUME" text="Создавать RenderQueue marker для готовых проектов." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Запускать приложение при появлении новых render-задач." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/><Toggle label="YouTube Autoupload" text="Загружать по одному готовому видео за цикл и ставить publishAt." value={s.autoUploadYoutube} onChange={v=>patch({autoUploadYoutube:v})}/></div></div><div className="settingsCard"><h3>Параметры</h3><div className="editGrid"><label>Цикл, секунд<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label><label>Треков по умолчанию<input type="number" min="1" max="100" value={s.tracksPerVideo} onChange={e=>patch({tracksPerVideo:+e.target.value||10})}/></label></div></div></div>}
.vyron-v110-audit/src/App.tsx:125:{tab==='integrations'&&<div className="settingsStack"><div className="settingsCard"><h3>ENDLUME Studio</h3><p>VYRON отправляет готовые проекты в RenderQueue.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>ВЫБРАТЬ</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>ОТКРЫТЬ</button>}</div><small className="note">Полностью нулевой ручной рендер требует companion-update ENDLUME, который будет читать RenderQueue.</small></div><div className="settingsCard"><h3>OpenAI API — опционально</h3><p>Не требуется для бесплатного режима. Оставь пустым и используй ChatGPT Plus → Metadata Inbox. API нужен только если позже захочешь полностью фоновую генерацию без открытия ChatGPT.</p><input className="wideInput" type="password" placeholder="sk-…" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/><div className="pathLine"><input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/><button onClick={()=>patch({openaiModel:''})}>ОЧИСТИТЬ</button></div></div><div className="settingsCard"><h3>YouTube OAuth</h3><p>Desktop OAuth открывает Google в браузере один раз. После этого токен обновляется локально автоматически.</p><input className="wideInput" placeholder="Google OAuth Client ID …apps.googleusercontent.com" value={s.youtubeOAuthClientId} onChange={e=>patch({youtubeOAuthClientId:e.target.value.trim()})}/><input className="wideInput" type="password" autoComplete="off" placeholder="Google OAuth Client Secret — вводится один раз" value={oauthSecret} onChange={e=>setOauthSecret(e.target.value.trim())}/><small className="note">Client Secret не сохраняется в общих настройках и не выводится в логах. После успешного OAuth он хранится только внутри локального OAuth-профиля.</small><div className="cardActions"><button className="primary" disabled={connecting||!s.youtubeOAuthClientId||!oauthSecret} onClick={async()=>{setConnecting(true);setOauthError(false);setOauthMessage('OAuth: ожидаю подтверждение Google…');try{const p=await api.youtubeConnect(s.youtubeOAuthClientId,oauthSecret);setOauthSecret('');const m=`YouTube подключён: ${p.channelTitle||p.channelId||'канал'}`;setOauthMessage(m);toast(m);log(m);await refreshProfiles()}catch(e){const m=`OAuth: ${String(e)}`;setOauthError(true);setOauthMessage(m);toast(m);log(m,'error')}finally{setConnecting(false)}}}>{connecting?'ЖДУ GOOGLE…':'ПОДКЛЮЧИТЬ YOUTUBE'}</button><button onClick={async()=>{try{const p=await refreshProfiles();toast(p.length?`OAuth профилей: ${p.length}`:'OAuth профили не найдены')}catch(e){toast(String(e))}}}>↻ ОБНОВИТЬ</button></div>{oauthMessage&&<div className={oauthError?'errorBox':profiles.length?'successBox':'publisherNotice'}>{oauthMessage}</div>}{profiles.map(p=><div className="oauthProfile" key={p.id}><span><b>{p.channelTitle||'YouTube канал'}</b><small>{p.channelId||p.clientIdMasked}</small></span><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refreshProfiles()}}>ОТКЛЮЧИТЬ</button></div>)}<label className="compactLabel">YouTube category ID<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div><div className="settingsCard"><h3>YouTube Intelligence</h3><p>Автоматически обновляет приватную аналитику своих каналов и публичные показатели конкурентов, пока VYRON запущен.</p><Toggle label="Автообновление аналитики" text="Обновлять подключённые каналы и список конкурентов без ручной кнопки." value={s.youtubeIntelligenceAutoRefresh} onChange={v=>patch({youtubeIntelligenceAutoRefresh:v})}/><label className="compactLabel">Интервал, минут<input type="number" min="15" max="1440" value={s.youtubeIntelligenceRefreshMin} onChange={e=>patch({youtubeIntelligenceRefreshMin:Math.max(15,+e.target.value||30)})}/></label><small className="note">Для приватной аналитики включи YouTube Analytics API в Google Cloud и один раз переподключи OAuth после установки 0.6.0. Конкуренты используют только публичные данные.</small></div><div className="settingsCard"><h3>YouTube Data API Key</h3><p>Только публичная статистика своих каналов и конкурентов.</p><input className="wideInput" type="password" placeholder="AIza…" value={s.youtubeApiKey} onChange={e=>patch({youtubeApiKey:e.target.value.trim()})}/></div><div className="settingsCard"><h3>MusicVault AI</h3><p>VYRON принимает музыку через Inbox каждого канала. Следующий шаг — кнопка «Отправить в VYRON» прямо в MusicVault AI, чтобы не переносить файлы вручную.</p></div></div>}
.vyron-v110-audit/src/App.tsx:127:{tab==='updates'&&<div className="settingsCard"><h3>Update Center</h3><p>Версия 0.9.9. Проверка версии работает внутри приложения. Текущая тестовая DMG-сборка пока не является Apple-notarized release.</p><Toggle label="Проверять при запуске" text="Показывать новую версию автоматически." value={s.autoCheckUpdates} onChange={v=>patch({autoCheckUpdates:v})}/><button className="settingsAction" onClick={async()=>{try{setUpdate(await api.checkUpdate())}catch(e){setUpdate({error:String(e)})}}}>ПРОВЕРИТЬ ОБНОВЛЕНИЯ</button>{update?.none&&<div className="successBox">Установлена актуальная версия {update.current||'0.8.1'}.</div>}{update?.version&&<div className="updateFound"><b>Доступна {update.version}</b><p>{update.body}</p>{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}<button className="primary" onClick={async()=>{setProgress(0);try{await update.install((p:number)=>setProgress(p))}catch(e){setProgress(null);toast(String(e))}}}>ОБНОВИТЬ И ПЕРЕЗАПУСТИТЬ</button></div>}{update?.error&&<div className="errorBox">{update.error}</div>}<div className="changelog"><h4>31.08.2026 • 0.5.0</h4><ul><li>Новое имя приложения: VYRON — CONTENT OS.</li><li>VYRON.app / новый фирменный интерфейс и системные названия.</li><li>Автоматическая миграция Documents/ChannelFlow → Documents/VYRON без потери проектов.</li><li>Сохраняются лицензия, каналы, очередь, OAuth-профили и настройки.</li><li>Бесплатный сценарий ChatGPT Plus → Metadata Inbox теперь основной; OpenAI API отмечен как опциональный.</li><li>Подготовлен отдельный VYRON update feed с legacy fallback.</li></ul></div></div>}
.vyron-v110-audit/src/App.tsx:129:{tab==='about'&&<div className="settingsCard about"><div className="aboutMark">▶</div><h2>VYRON</h2><p>Autonomous Content Operating System</p><div className="aboutRows"><span>Версия <b>0.9.9</b></span><span>macOS <b>Apple Silicon M1+</b></span><span>UI <b>Tauri 2 + React</b></span><span>Core <b>Rust</b></span><span>Pipeline <b>SUNO → ENDLUME → YouTube</b></span><span>Target UI <b>60 FPS</b></span></div></div>}</>}
.vyron-v110-audit/src/ChannelRunway.tsx:8:  quotaRiskCount,
.vyron-v110-audit/src/ChannelRunway.tsx:16:  upsertChannelRunwayFromYoutube
.vyron-v110-audit/src/ChannelRunway.tsx:18:import {scheduleAverageIntervalDays,scheduleDescription} from './channelSchedule';
.vyron-v110-audit/src/ChannelRunway.tsx:20:  buildYoutubeQuotaPlan,
.vyron-v110-audit/src/ChannelRunway.tsx:21:  isYoutubeQuotaError,
.vyron-v110-audit/src/ChannelRunway.tsx:22:  loadYoutubeQuotaPlan,
.vyron-v110-audit/src/ChannelRunway.tsx:23:  subscribeYoutubeQuota,
.vyron-v110-audit/src/ChannelRunway.tsx:24:  youtubeQuotaMessage,
.vyron-v110-audit/src/ChannelRunway.tsx:25:  youtubeQuotaUsage
.vyron-v110-audit/src/ChannelRunway.tsx:26:} from './youtubeQuota';
.vyron-v110-audit/src/ChannelRunway.tsx:45:  if(!value)return'YouTube не синхронизирован';
.vyron-v110-audit/src/ChannelRunway.tsx:47:  if(Number.isNaN(d.getTime()))return'YouTube не синхронизирован';
.vyron-v110-audit/src/ChannelRunway.tsx:69:  const [busy,setBusy]=useState(''),[,setQuotaTick]=useState(0);
.vyron-v110-audit/src/ChannelRunway.tsx:70:  const signature=channels.map(c=>`${c.id}:${c.name}:${c.enabled}:${c.cadenceDays}:${c.scheduleMode||'interval'}:${c.publishIntervalDays||''}:${c.publishDays||''}:${c.pauseDays||''}:${c.patternAnchorDate||''}:${c.youtubeProfileId||''}`).join('|');
.vyron-v110-audit/src/ChannelRunway.tsx:78:  useEffect(()=>subscribeYoutubeQuota(()=>setQuotaTick(x=>x+1)),[]);
.vyron-v110-audit/src/ChannelRunway.tsx:85:  const usage=youtubeQuotaUsage();
.vyron-v110-audit/src/ChannelRunway.tsx:86:  const quotaSettings=loadYoutubeQuotaPlan();
.vyron-v110-audit/src/ChannelRunway.tsx:87:  const batchSize=Math.max(1,quotaSettings.videosPerChannel);
.vyron-v110-audit/src/ChannelRunway.tsx:88:  const capacityPlan=buildYoutubeQuotaPlan(Math.max(1,active.length),batchSize,usage);
.vyron-v110-audit/src/ChannelRunway.tsx:93:  const attentionPlan=attention.length?buildYoutubeQuotaPlan(attention.length,batchSize,usage):null;
.vyron-v110-audit/src/ChannelRunway.tsx:94:  const effectiveIntervals=rows.map(({channel,record})=>record.averagePublishIntervalDays||scheduleAverageIntervalDays(channel)).filter(x=>Number.isFinite(x)&&x>0);
.vyron-v110-audit/src/ChannelRunway.tsx:98:  const riskCount=quotaRiskCount(attention.map(x=>x.record),capacityPlan.todayChannels,capacityPlan.fullDayChannels);
.vyron-v110-audit/src/ChannelRunway.tsx:106:    if(!channel.youtubeProfileId){toast(`У ${channel.name} не привязан YouTube OAuth`);return}
.vyron-v110-audit/src/ChannelRunway.tsx:109:      const result=await api.youtubeListExisting(channel.youtubeProfileId,1000);
.vyron-v110-audit/src/ChannelRunway.tsx:110:      const nextStore=upsertChannelRunwayFromYoutube(channel,result.videos||[],new Date());
.vyron-v110-audit/src/ChannelRunway.tsx:113:      toast(`${channel.name}: расписание обновлено • ${r?.scheduledVideoCount||0} запланировано • до ${dateLabel(r?.scheduledUntil)}`);
.vyron-v110-audit/src/ChannelRunway.tsx:115:      toast(isYoutubeQuotaError(e)?youtubeQuotaMessage():`Расписание ${channel.name}: ${String(e)}`);
.vyron-v110-audit/src/ChannelRunway.tsx:122:      <span className="localOnlyBadge">LOCAL • ZERO QUOTA</span>
.vyron-v110-audit/src/ChannelRunway.tsx:148:          <span className="runwayChannel"><b>{channel.name}</b><small>{syncLabel(record.lastScheduleSync)}</small></span>
.vyron-v110-audit/src/ChannelRunway.tsx:149:          <span><b>{dateLabel(record.scheduledUntil)}</b></span>
.vyron-v110-audit/src/ChannelRunway.tsx:151:          <span>{record.runwayDays===undefined?'—':record.scheduledVideoCount}</span>
.vyron-v110-audit/src/ChannelRunway.tsx:152:          <span>{record.runwayDays===undefined?'—':scheduleDescription(channel)}</span>
.vyron-v110-audit/src/ChannelRunway.tsx:155:          <span><button disabled={busy===channel.id||!channel.youtubeProfileId} onClick={()=>void syncChannel(channel)}>{busy===channel.id?'СИНХРОНИЗАЦИЯ…':'ОБНОВИТЬ РАСПИСАНИЕ'}</button></span>
.vyron-v110-audit/src/ChannelRunway.tsx:162:      <span>Размер пачки из Quota Planner: <b>{batchSize} видео</b></span>
.vyron-v110-audit/src/ExistingVideos.tsx:6:import type {YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/ExistingVideos.tsx:7:import {countBuckets,existingBucket,latestPrivateIds,matchesExistingFilter,selectedVisibleIds,type ExistingFilter} from './youtubeWorkflow';
.vyron-v110-audit/src/ExistingVideos.tsx:8:import {parseMetadataFile,validateSequentialMetadata,type ImportedMetadata} from './metadata';
.vyron-v110-audit/src/ExistingVideos.tsx:9:import {buildExistingScheduleFromLocal,orderedExistingVideos,overlayExistingMetadata} from './youtubeExisting';
.vyron-v110-audit/src/ExistingVideos.tsx:10:import {isYoutubeQuotaError,markYoutubeQuotaExceeded,youtubeQuotaMessage,youtubeQuotaState} from './youtubeQuota';
.vyron-v110-audit/src/ExistingVideos.tsx:11:import {readExistingCache,writeExistingCache} from './channelSchedule';
.vyron-v110-audit/src/ExistingVideos.tsx:21:function VideoThumb({video,onState}:{video:YoutubeExistingVideo;onState:(id:string,s:ThumbState)=>void}){
.vyron-v110-audit/src/ExistingVideos.tsx:22: const remotes=[video.thumbnail,`https://i.ytimg.com/vi/${video.id}/hqdefault.jpg`,`https://i.ytimg.com/vi/${video.id}/mqdefault.jpg`,`https://i.ytimg.com/vi/${video.id}/default.jpg`].filter(Boolean) as string[];
.vyron-v110-audit/src/ExistingVideos.tsx:24: useEffect(()=>{let live=true;setSrc('');setFallback(0);setFailed(false);onState(video.id,'loading');api.youtubeCacheThumbnail(video.id,video.thumbnail).then(p=>{if(live){setSrc(convertFileSrc(p));onState(video.id,'ok')}}).catch(()=>{if(live)setSrc(remotes[0]||'')});return()=>{live=false}},[video.id,video.thumbnail,retry]);
.vyron-v110-audit/src/ExistingVideos.tsx:32: const [channelId,setChannelId]=useState(channels[0]?.id||''),[profileId,setProfileId]=useState(channels[0]?.youtubeProfileId||''),[videos,setVideos]=useState<YoutubeExistingVideo[]>([]),[baseline,setBaseline]=useState<Record<string,YoutubeExistingVideo>>({}),[lastUndo,setLastUndo]=useState<YoutubeExistingVideo[]>([]),[busy,setBusy]=useState(false),[limit,setLimit]=useState(1000),[order,setOrder]=useState<'oldest'|'newest'>('oldest'),[start,setStart]=useState(()=>{const d=new Date();d.setDate(d.getDate()+1);d.setHours(18,0,0,0);return local(d.toISOString())}),[cadence,setCadence]=useState(2),[meta,setMeta]=useState<ImportedMetadata[]>([]),[expanded,setExpanded]=useState<Set<string>>(new Set()),[filter,setFilter]=useState<ExistingFilter>('private'),[selectedOnly,setSelectedOnly]=useState(false),[sortMode,setSortMode]=useState<'dateAsc'|'dateDesc'|'youtubeOldest'|'youtubeNewest'|'privateFirst'>('dateAsc'),[syncInfo,setSyncInfo]=useState<ExistingVideoSyncResult|null>(null),[thumbs,setThumbs]=useState<Record<string,ThumbState>>({}),[confirm,setConfirm]=useState(false),[backupPath,setBackupPath]=useState('');
.vyron-v110-audit/src/ExistingVideos.tsx:34: useEffect(()=>{const c=channels.find(x=>x.id===channelId),cached=readExistingCache(channelId);setProfileId(c?.youtubeProfileId||'');if(c)setCadence(c.cadenceDays||2);setVideos(cached?.videos||[]);setBaseline(cached?.baseline||{});setLastUndo(cached?.lastUndo||[]);setSyncInfo(cached?.syncInfo||null);setThumbs({});setMeta([]);setSelectedOnly(false);},[channelId,channels.find(x=>x.id===channelId)?.youtubeProfileId]);
.vyron-v110-audit/src/ExistingVideos.tsx:37: async function sync(){if(!profileId){toast('Сначала привяжи YouTube OAuth к каналу');return}if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}setBusy(true);try{const selectedIds=new Set(videos.filter(v=>v.selected).map(v=>v.id));const r=await api.youtubeListExisting(profileId,limit);const next=(r.videos||[]).map((x:any)=>({...x,selected:selectedIds.has(x.id),applyState:'idle'}));setVideos(next);setBaseline(Object.fromEntries(next.map(v=>[v.id,{...v,tags:[...v.tags]}])));setLastUndo([]);setBackupPath('');setThumbs({});setSyncInfo(r);writeExistingCache(channelId,{version:1,updatedAt:new Date().toISOString(),videos:next,baseline:Object.fromEntries(next.map(v=>[v.id,{...v,tags:[...v.tags]}])),lastUndo:[],syncInfo:r});toast(`YouTube: получено ${r.received}/${Math.min(r.youtubeFound||r.received,r.requested||limit)} видео`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);toast('⏸ YouTube quota: синхронизация поставлена на паузу')}else toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/ExistingVideos.tsx:38: function schedule(){
.vyron-v110-audit/src/ExistingVideos.tsx:42:  if(!targets.length){toast('Для расписания выбери Private / Scheduled видео');return}
.vyron-v110-audit/src/ExistingVideos.tsx:44:  const scheduled=buildExistingScheduleFromLocal(targets,start,cadence,meta);
.vyron-v110-audit/src/ExistingVideos.tsx:45:  const byId=new Map(scheduled.map(v=>[v.id,v]));
.vyron-v110-audit/src/ExistingVideos.tsx:54:  const rows:ImportedMetadata[]=[];
.vyron-v110-audit/src/ExistingVideos.tsx:55:  let hasDocx=false;
.vyron-v110-audit/src/ExistingVideos.tsx:57:    const docx=f.name.toLowerCase().endsWith('.docx');
.vyron-v110-audit/src/ExistingVideos.tsx:58:    hasDocx=hasDocx||docx;
.vyron-v110-audit/src/ExistingVideos.tsx:59:    const text=docx?(await mammoth.extractRawText({arrayBuffer:await f.arrayBuffer()})).value:await f.text();
.vyron-v110-audit/src/ExistingVideos.tsx:60:    rows.push(...parseMetadataFile(f.name,text));
.vyron-v110-audit/src/ExistingVideos.tsx:63:  if(hasDocx){
.vyron-v110-audit/src/ExistingVideos.tsx:64:    const check=validateSequentialMetadata(rows,selectedNow.length);
.vyron-v110-audit/src/ExistingVideos.tsx:66:      const details=[rows.length!==selectedNow.length?`в DOCX ${rows.length}, выбрано ${selectedNow.length}`:'',check.missing.length?`нет VIDEO ${check.missing.join(', ')}`:'',check.duplicates.length?`дубли VIDEO ${check.duplicates.join(', ')}`:''].filter(Boolean).join(' • ');
.vyron-v110-audit/src/ExistingVideos.tsx:67:      toast(`SEO DOCX не применён: ${details}`);return
.vyron-v110-audit/src/ExistingVideos.tsx:70:    if(incomplete.length){toast(`SEO DOCX не применён: неполные блоки VIDEO ${incomplete.map(x=>x.number).join(', ')}`);return}
.vyron-v110-audit/src/ExistingVideos.tsx:77:    const overlaid=overlayExistingMetadata(targets,rows);
.vyron-v110-audit/src/ExistingVideos.tsx:82:  toast(hasDocx?`✓ SEO DOCX: ${rows.length}/${selectedNow.length} видео заполнено автоматически${firstTime?` • ${firstTime}${zone?` ${zone}`:''}`:''}`:`Метаданные применены: ${Math.min(rows.length,selectedNow.length)} из ${selectedNow.length}`)
.vyron-v110-audit/src/ExistingVideos.tsx:91: async function apply(){setConfirm(false);if(!selected.length||!profileId)return;if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}const backup=selected.map(v=>baseline[v.id]).filter(Boolean).map(v=>({...v,tags:[...v.tags]}));setBusy(true);let ok=0;const verifiedIds=new Set<string>();try{const saved=await api.youtubeBackupExisting(profileId,backup);setBackupPath(saved.path||'');for(let i=0;i<selected.length;i++){const v=selected[i];setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'saving',error:undefined}:z));try{const result=await api.youtubeUpdateExisting(profileId,v.id,v.title,v.description,v.tags,v.publishAt,v.privacyStatus);if(result?.metadataAccepted===false)throw new Error(`YouTube не принял metadata ${v.id}`);if(result.scheduleRequested&&result.scheduleAccepted===false){const err=result.scheduleError||'расписание не принято';if(isYoutubeQuotaError(err)){markYoutubeQuotaExceeded(err);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: метаданные сохранены; расписание: ${err}`}:z));ok++;verifiedIds.add(v.id);continue}ok++;verifiedIds.add(v.id);setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'done',verified:true,error:result.metadataVerifyPending||result.scheduleVerifyPending?'YouTube принял запись; контрольное чтение ещё догоняет':undefined}:z))}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: ${String(e)}`}:z))}}if(ok){setLastUndo(backup.filter(v=>verifiedIds.has(v.id)));setBaseline(prev=>{const n={...prev};for(const v of selected.filter(x=>verifiedIds.has(x.id)))n[v.id]={...v,tags:[...v.tags]};return n})}if(!youtubeQuotaState().blocked)toast(ok===selected.length?`✓ ${ok} видео приняты YouTube`:`YouTube принял ${ok}/${selected.length}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);toast('⏸ YouTube quota: пачка сохранена для продолжения')}else toast(`Backup/Update: ${String(e)}`)}finally{setBusy(false)}}
.vyron-v110-audit/src/ExistingVideos.tsx:92: async function undo(){if(!lastUndo.length||!profileId)return;setBusy(true);let ok=0;try{for(const v of lastUndo){try{const r=await api.youtubeUpdateExisting(profileId,v.id,v.title,v.description,v.tags,v.publishAt,v.privacyStatus);if(r?.verified)ok++}catch(e){toast(`Undo ${v.id}: ${String(e)}`)}}toast(`↶ YouTube подтвердил откат ${ok}/${lastUndo.length}`);if(ok===lastUndo.length)setLastUndo([]);await sync()}finally{setBusy(false)}}
.vyron-v110-audit/src/ExistingVideos.tsx:93: const displayDate=(v:YoutubeExistingVideo)=>v.publishAt||v.publishedAt||'';const timeOf=(v:YoutubeExistingVideo)=>{const t=Date.parse(displayDate(v));return Number.isFinite(t)?t:Number.POSITIVE_INFINITY};const youtubeTime=(v:YoutubeExistingVideo)=>{const t=Date.parse(v.publishedAt||v.publishAt||'');return Number.isFinite(t)?t:Number.POSITIVE_INFINITY};
.vyron-v110-audit/src/ExistingVideos.tsx:94: const shown=[...videos.filter(v=>matchesExistingFilter(v,filter)&&(!selectedOnly||v.selected))].sort((a,b)=>{if(sortMode==='dateAsc')return timeOf(a)-timeOf(b);if(sortMode==='dateDesc'){const ta=timeOf(a),tb=timeOf(b);if(!Number.isFinite(ta)&&!Number.isFinite(tb))return 0;if(!Number.isFinite(ta))return 1;if(!Number.isFinite(tb))return -1;return tb-ta}if(sortMode==='youtubeOldest')return youtubeTime(a)-youtubeTime(b);if(sortMode==='youtubeNewest'){const ta=youtubeTime(a),tb=youtubeTime(b);if(!Number.isFinite(ta)&&!Number.isFinite(tb))return 0;if(!Number.isFinite(ta))return 1;if(!Number.isFinite(tb))return -1;return tb-ta}return existingBucket(a).localeCompare(existingBucket(b))||timeOf(a)-timeOf(b)});
.vyron-v110-audit/src/ExistingVideos.tsx:97: const expected=syncInfo?Math.min(syncInfo.youtubeFound||syncInfo.received,syncInfo.requested||limit):0;const mismatch=Boolean(syncInfo&&(!syncInfo.complete||syncInfo.received!==expected));
.vyron-v110-audit/src/ExistingVideos.tsx:101:  <div className="pageHeader"><div><small>EXISTING VIDEOS MANAGER</small><h1>Загруженные</h1><p>Синхронизация идёт через uploads playlist с pagination. Изменение считается успешным только после повторного чтения YouTube.</p></div><div className="headerActions"><button disabled={busy} onClick={sync}>{busy?'Работаю…':'↻ Синхронизировать'}</button><button className="primary" disabled={busy||!selected.length||mismatch} onClick={()=>setConfirm(true)}>ОТПРАВИТЬ В YOUTUBE • {selected.length}</button></div></div>
.vyron-v110-audit/src/ExistingVideos.tsx:102:  {syncInfo&&<div className={`syncAudit ${mismatch?'warn':''}`}><span>YouTube найдено <b>{syncInfo.youtubeFound}</b></span><span>Получено VYRON <b>{syncInfo.received}</b></span><span>Private <b>{syncInfo.privateCount}</b></span><span>Scheduled <b>{syncInfo.scheduledCount}</b></span><span>Public <b>{syncInfo.publicCount}</b></span><span>Unlisted <b>{syncInfo.unlistedCount||0}</b></span><span>Изображения <b>{thumbOk}/{videos.length}</b></span>{thumbError>0&&<span className="warn">{thumbError} ожидают retry</span>}{syncInfo.youtubeFound>limit&&<span>Лимит: {limit} из {syncInfo.youtubeFound}</span>}{mismatch&&<strong>⚠ Синхронизация неполная</strong>}</div>}
.vyron-v110-audit/src/ExistingVideos.tsx:103:  {lastUndo.length>0&&<div className="undoBar"><span><b>✓ Последняя пачка подтверждена YouTube</b> • backup {lastUndo.length} видео{backupPath&&<small> • {backupPath}</small>}</span><button disabled={busy} onClick={undo}>↶ ОТМЕНИТЬ</button></div>}
.vyron-v110-audit/src/ExistingVideos.tsx:104:  <input ref={file} hidden type="file" multiple accept=".docx,.json,.txt,.csv" onChange={e=>{void read(e.target.files);e.currentTarget.value=''}}/>
.vyron-v110-audit/src/ExistingVideos.tsx:105:  <div className="existingToolbar"><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><div className={`profileAutoBadge ${profileId?'good':'warn'}`}><small>OAuth</small><b>{profileId?'✓ выбран автоматически':'не подключён'}</b></div><label>Видео<select value={limit} onChange={e=>setLimit(+e.target.value)}><option>30</option><option>50</option><option>100</option><option>200</option><option>500</option><option>1000</option><option>5000</option></select></label><label>SEO порядок<select value={order} onChange={e=>setOrder(e.target.value as any)}><option value="oldest">старые → новые</option><option value="newest">новые → старые</option></select></label><label>Сортировка<select value={sortMode} onChange={e=>setSortMode(e.target.value as any)}><option value="dateAsc">дата ↑</option><option value="dateDesc">дата ↓</option><option value="youtubeOldest">YouTube: старые → новые</option><option value="youtubeNewest">YouTube: новые → старые</option><option value="privateFirst">сначала Private</option></select></label><label>Первая дата<input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Каждые N дней<input type="number" min="1" max="30" value={cadence} onChange={e=>setCadence(+e.target.value||1)}/></label><button disabled={!selected.length} onClick={schedule}>Расставить даты • {selected.length}</button><button disabled={!selected.length} onClick={()=>file.current?.click()}>⬆ Загрузить SEO DOCX • {selected.length}</button></div>
.vyron-v110-audit/src/ExistingVideos.tsx:106:  {videos.length>0&&<div className="existingQuick stickyBatch"><div className="periodTabs"><button className={filter==='private'?'active':''} onClick={()=>setFilter('private')}>PRIVATE {buckets.private}</button><button className={filter==='scheduled'?'active':''} onClick={()=>setFilter('scheduled')}>SCHEDULED {buckets.scheduled}</button><button className={filter==='public'?'active':''} onClick={()=>setFilter('public')}>PUBLIC {buckets.public}</button><button className={filter==='unlisted'?'active':''} onClick={()=>setFilter('unlisted')}>UNLISTED {buckets.unlisted}</button><button className={filter==='all'?'active':''} onClick={()=>setFilter('all')}>ВСЕ {videos.length}</button></div><div className="batchScopeActions"><span className="selectionCount">Выбрано <b>{selected.length}</b></span><button onClick={selectVisible}>Выбрать все здесь</button><button onClick={()=>selectLatestPrivate(30)}>Последние 30 Private</button><button onClick={()=>setSelectedOnly(x=>!x)}>{selectedOnly?'Показать все':'Только выбранные'}</button><button onClick={selectNone}>Снять всё</button></div></div>}
.vyron-v110-audit/src/ExistingVideos.tsx:107:  {videos.length>0&&<div className="publisherNotice selectionNotice"><b>Область изменений: {selected.length?`${selected.length} выбранных видео`:'ничего не выбрано'}</b><p>Название, описание, теги, даты и отправка в YouTube применяются только к отмеченным роликам. Для всей приватной библиотеки используй «Все Private», для свежей партии — «Последние 30 Private».</p></div>}
.vyron-v110-audit/src/ExistingVideos.tsx:108:  {meta.length>0&&<div className="publisherNotice seoMappingNotice"><b>SEO Pack • {meta.length} видео • сопоставление 1:1</b><p>Название, описание и теги подставлены только выбранным роликам. {meta.some(x=>x.publishTime)&&<>Время: <b>{meta[0]?.publishTime} {meta[0]?.publishTimezone||''}</b>.</>}</p><div className="seoMapRows">{mapping.slice(0,40).map((m,i)=><span key={i}><b>VIDEO {m.row.number||i+1}</b><em>→</em><small>{m.video?`${m.video.title} • ${m.video.id}`:'НЕ СОПОСТАВЛЕНО'}</small></span>)}</div></div>}
.vyron-v110-audit/src/ExistingVideos.tsx:109:  {videos.length===0?<div className="empty"><b>Видео ещё не синхронизированы</b><p>Выбери канал и нажми «Синхронизировать».</p></div>:<div className="existingList compact">{shown.map((v,i)=>{const open=expanded.has(v.id);return <article className={`existingCard compact ${open?'open':''} ${v.selected?'selected':''} ${v.applyState||''} ${v.publishAt&&dateCounts[v.publishAt]>1?'dateConflictCard':''}`} key={v.id} onClick={e=>{if((e.target as HTMLElement).closest('button,input,select,textarea,label,a'))return;toggleSelected(v.id)}}><label className="existingCheckHit" onClick={e=>e.stopPropagation()}><input className="existingCheck" type="checkbox" checked={v.selected} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,selected:e.target.checked}:z))}/><i/></label><button className="existingPreview" onClick={e=>{e.stopPropagation();toggleSelected(v.id)}}><VideoThumb video={v} onState={(id,s)=>setThumbs(x=>x[id]===s?x:{...x,[id]:s})}/><em>{v.privacyStatus}</em></button><div className="existingSummary"><small>#{i+1} • {v.id}</small><b>{v.title}</b><p>{short(v.description||'Описание отсутствует')}</p><div className="tagChips">{v.tags.slice(0,5).map(t=><i key={t}>{t}</i>)}{v.tags.length>5&&<i>+{v.tags.length-5}</i>}</div></div><div className="existingSchedule"><input type="datetime-local" value={local(v.publishAt)} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,publishAt:e.target.value?new Date(e.target.value).toISOString():undefined}:z))}/>{v.publishAt&&dateCounts[v.publishAt]>1&&<span className="dateConflict">⚠ Совпадает дата публикации</span>}<b className={v.applyState==='error'?'bad':v.applyState==='done'?'ok':''}>{v.applyState==='done'?'✓ Подтверждено YouTube':v.applyState==='saving'?'Отправляю + проверяю…':v.applyState==='error'?'⚠ Не подтверждено':'К изменению'}</b><button className="expandBtn" onClick={()=>toggle(v.id)}>{open?'Свернуть':'Редактировать'}</button></div>{open&&<div className="existingEditor"><label>Название<input value={v.title} maxLength={100} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,title:e.target.value}:z))}/></label><label>Описание<textarea value={v.description} maxLength={5000} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,description:e.target.value}:z))}/></label><label>Теги<input value={v.tags.join(', ')} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,tags:e.target.value.split(',').map(a=>a.trim()).filter(Boolean)}:z))}/></label><label>Privacy<select value={v.privacyStatus} onChange={e=>setVideos(x=>x.map(z=>z.id===v.id?{...z,privacyStatus:e.target.value}:z))}><option value="private">PRIVATE</option><option value="unlisted">UNLISTED</option><option value="public">PUBLIC</option></select></label>{v.error&&<div className="errorBox">{v.error}</div>}</div>}</article>})}</div>}
.vyron-v110-audit/src/ExistingVideos.tsx:110:  {confirm&&<div className="modalBackdrop" onMouseDown={()=>setConfirm(false)}><section className="confirmModal" onMouseDown={e=>e.stopPropagation()}><small>YOUTUBE WRITE CONFIRMATION</small><h2>Будет изменено {selected.length} видео</h2><div className="confirmCounts"><span>Название <b>{changes.title}</b></span><span>Описание <b>{changes.description}</b></span><span>Теги <b>{changes.tags}</b></span><span>Дата <b>{changes.date}</b></span><span>Privacy <b>{changes.privacy}</b></span><span className={changes.public?'dangerText':''}>Public <b>{changes.public}</b></span></div><p>Перед записью VYRON сохранит локальный backup. После каждого videos.update будет выполнен videos.list и сравнение фактических данных YouTube.</p><footer><button onClick={()=>setConfirm(false)}>ОТМЕНА</button><button className="primary" disabled={busy||mismatch} onClick={apply}>ОТПРАВИТЬ {selected.length} В YOUTUBE</button></footer></section></div>}
.vyron-v110-audit/src/ProductionWorkspace.tsx:20: const target=Math.max(1,pref.target||30),images=rows.filter(j=>Boolean(j.coverPath)).length,music=rows.filter(j=>j.tracksCount>=j.minTracks).length,videos=rows.filter(j=>Boolean(j.finalPath)).length,seo=rows.filter(j=>Boolean(j.title&&j.description&&j.tags?.length)).length,schedule=rows.filter(j=>Boolean(j.publishAt)).length,sent=rows.filter(j=>Boolean(j.youtubeVideoId)||j.status==='SCHEDULED').length,errors=rows.filter(j=>j.status==='ERROR').length;
.vyron-v110-audit/src/ProductionWorkspace.tsx:21: const ready=[images,music,videos,seo,schedule].every(n=>n>=target)&&pref.reviewed&&!errors;
.vyron-v110-audit/src/ProductionWorkspace.tsx:27:  <div className="actualProductionCounts">{metric('Изображения',images)}{metric('Музыка готова',music)}{metric('Видео',videos)}{metric('SEO',seo)}{metric('Расписание',schedule)}</div>
.vyron-v110-audit/src/ProductionWorkspace.tsx:29:  <div className="workspacePaths"><span>План: <b>{target.toLocaleString('ru-RU')} проектов</b></span><span>Отправлено в YouTube: <b>{sent.toLocaleString('ru-RU')}</b>{errors?` • Ошибок: ${errors}`:''}</span></div>
.vyron-v110-audit/src/AnalyticsPage.tsx:20: return <><div className="pageHeader"><div><small>ANALYTICS • LOCAL CACHE</small><h1>Аналитика</h1><p>Никаких запросов к YouTube на этом экране. Показаны только данные последней ручной синхронизации из YouTube workspace.</p></div><div className="headerActions"><button className="primary" onClick={()=>setPage('youtube')}>Обновить в YouTube</button></div></div><div className="cacheNotice"><b>Последняя синхронизация</b><span>{a?.updatedAt?new Date(a.updatedAt).toLocaleString('ru-RU'):'Нет данных'}</span></div><div className="analyticsToolbar"><select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><div className="periodTabs analyticsPeriods">{(['7','28','90'] as Period[]).map(x=><button key={x} className={period===x?'active':''} onClick={()=>setPeriod(x)}>{x} дней</button>)}</div></div>{!a?<div className="empty"><b>Аналитика ещё не синхронизирована</b><p>Открой YouTube → Данные YouTube и нажми «Обновить аналитику».</p></div>:<><div className="channelMetricsGrid analyticsKpis"><span><small>Views</small><b>{views==null?'—':formatNumber(views)}</b></span><span><small>Watch Time</small><b>{watch==null?'—':`${num(watch/60,1)} ч`}</b></span><span><small>Revenue</small><b>{revKnown?money(revenue):'—'}</b></span><span><small>RPM</small><b>{rpm==null?'—':`$${rpm.toFixed(2)}`}</b></span><span><small>Avg View Duration</small><b>{a.periodDays===days?dur(a.averageViewDuration):'—'}</b></span><span><small>Avg %</small><b>{a.periodDays===days?`${num(a.averageViewPercentage,1)}%`:'—'}</b></span><span><small>Subscribers</small><b>{c?.stats?.subscribers!=null?formatNumber(c.stats.subscribers):'—'}</b></span><span><small>CTR</small><b>{a.impressionCtr==null?'—':`${num(a.impressionCtr,2)}%`}</b></span></div><section className="panel analyticsWide"><div className="panelHead"><div><small>CACHED DYNAMICS</small><h3>Просмотры</h3></div><span>{rows.length} точек</span></div>{rows.length?<><Line rows={rows}/><div className="chartFoot"><span>{rows[0]?.date}</span><span>{rows.at(-1)?.date}</span></div></>:<p>Для этого периода нет сохранённых daily points.</p>}</section><section className="panel"><div className="panelHead"><div><small>TOP VIDEOS • CACHE</small><h3>Сохранённые метрики видео</h3></div></div>{a.topVideos?.length?<div className="topVideoList rich">{a.topVideos.slice(0,10).map(v=><div key={v.id}>{v.thumbnail?<img src={v.thumbnail} loading="lazy"/>:<i/>}<span><strong>{v.title}</strong><small>{formatNumber(v.views)} views • {num(v.averageViewPercentage,1)}% • {money(v.estimatedRevenue)}</small></span></div>)}</div>:<p>Нет данных.</p>}</section></>}</>
.vyron-v110-audit/src/channelRunwayStore.ts:1:import type {Channel,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/channelRunwayStore.ts:45:  videos?:YoutubeExistingVideo[];
.vyron-v110-audit/src/channelRunwayStore.ts:46:  baseline?:Record<string,YoutubeExistingVideo>;
.vyron-v110-audit/src/channelRunwayStore.ts:64:  if(!cache)return[] as YoutubeExistingVideo[];
.vyron-v110-audit/src/channelRunwayStore.ts:81:    if(prior?.lastScheduleSync){
.vyron-v110-audit/src/channelRunwayStore.ts:86:    // Existing Videos stores drafts/selections in `videos`, so they are never used as YouTube truth here.
.vyron-v110-audit/src/channelRunwayStore.ts:104:export function upsertChannelRunwayFromYoutube(
.vyron-v110-audit/src/channelRunwayStore.ts:106:  videos:YoutubeExistingVideo[],
.vyron-v110-audit/src/SettingsOS.tsx:7:type Tab='general'|'youtube'|'autopilot'|'endlume'|'updates'|'diagnostics';
.vyron-v110-audit/src/SettingsOS.tsx:16: const setMode=(m:AutopilotMode)=>{if(m==='off')patch({autopilotMode:m,autopilotEnabled:false,autoUploadYoutube:false});else patch({autopilotMode:m,autopilotEnabled:true,autoUploadYoutube:false})};
.vyron-v110-audit/src/SettingsOS.tsx:17: async function runDiagnostics(){setChecking(true);try{let ws=s.workspace;if(!ws){ws=await api.defaultWorkspace();patch({workspace:ws})}const core=await api.diagnostics(ws);setDiag([{label:'Local Core',status:core.ok?'Готово':'Требует внимания',ok:core.ok},{label:'Workspace',status:core.workspaceWritable?'Доступен для записи':'Проблема записи',ok:core.workspaceWritable},{label:'ENDLUME',status:s.endlumePath?'Путь настроен':'Не выбран',ok:Boolean(s.endlumePath)},{label:'Updater',status:'Tauri signed updater подключён',ok:true},{label:'YouTube',status:'Проверяется только во вкладке YouTube',detail:'Zero Quota Policy: настройки не вызывают YouTube Data API.'}])}catch(e){setDiag([{label:'Диагностика',status:'Ошибка',ok:false,detail:String(e)}])}finally{setChecking(false)}}
.vyron-v110-audit/src/SettingsOS.tsx:20: const label=(t:Tab)=>t==='general'?'Общие':t==='youtube'?'YouTube':t==='autopilot'?'Автопилот':t==='endlume'?'ENDLUME':t==='updates'?'Обновления':'Диагностика';
.vyron-v110-audit/src/SettingsOS.tsx:21: return <><div className="pageHeader"><div><small>SETTINGS</small><h1>Настройки</h1><p>Локальная конфигурация VYRON. Ни один переход сюда не расходует YouTube API quota.</p></div></div><div className="settingsTabs osTabs">{(['general','youtube','autopilot','endlume','updates','diagnostics'] as Tab[]).map(t=><button className={tab===t?'active':''} key={t} onClick={()=>setTab(t)}>{label(t)}</button>)}</div>
.vyron-v110-audit/src/SettingsOS.tsx:22: {tab==='general'&&<div className="settingsStack"><section className="settingsCard"><h3>Workspace</h3><p>Inbox, проекты, очередь, backup и локальное состояние VYRON.</p><div className="pathLine"><input readOnly value={s.workspace} placeholder="создаётся автоматически"/><button onClick={async()=>{const p=await api.chooseWorkspace();if(p)patch({workspace:p})}}>Выбрать</button><button onClick={()=>s.workspace&&api.openLocal(s.workspace)}>Открыть</button></div></section><section className="settingsCard"><h3>Интерфейс</h3><div className="toggleRows"><Toggle label="FPS monitor" text="Показывать текущий FPS интерфейса." value={s.fpsMonitor} onChange={v=>patch({fpsMonitor:v})}/><Toggle label="Уменьшить анимации" text="Уменьшить декоративные переходы." value={s.reduceMotion} onChange={v=>patch({reduceMotion:v})}/></div></section><section className="settingsCard"><h3>Лицензия</h3><p>{license.valid?'VYRON активирован':'Требуется активация'} • {license.type||'—'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced</summary><div className="advancedInner"><label>OpenAI API — опционально<input type="password" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/></label><label>OpenAI model<input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/></label><label>YouTube category<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:23: {tab==='youtube'&&<div className="settingsStack"><section className="settingsCard youtubeMovedCard"><small>ZERO QUOTA POLICY</small><h3>YouTube вынесен в отдельную рабочую зону</h3><p>OAuth, синхронизация, аналитика, конкуренты, metadata и загрузка выполняются только во вкладке YouTube и только после явного действия пользователя.</p><button className="primary" onClick={()=>setPage('youtube')}>Открыть YouTube</button></section></div>}
.vyron-v110-audit/src/SettingsOS.tsx:24: {tab==='autopilot'&&<div className="settingsStack"><section className="settingsCard"><h3>Режим Autopilot</h3><div className="modeSwitch large"><button className={mode==='off'?'active':''} onClick={()=>setMode('off')}>OFF</button><button className={mode==='assisted'?'active':''} onClick={()=>setMode('assisted')}>ASSISTED</button><button className={mode==='full'?'active':''} onClick={()=>setMode('full')}>FULL LOCAL</button></div><p>{mode==='off'?'Автоматизация выключена.':'Autopilot обслуживает только локальный Production pipeline и ENDLUME. YouTube API всегда остаётся ручным.'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced Autopilot</summary><div className="toggleRows"><Toggle label="Поддерживать план" text="Создавать недостающие VIDEO_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Inbox/Music → проекты." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 проект." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI metadata" text="Только при подключённом OpenAI API." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="ENDLUME queue" text="Создавать render marker." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Открывать приложение при render задачах." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/></div><label>Интервал локального цикла, сек<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:25: {tab==='endlume'&&<div className="settingsStack"><section className="settingsCard"><h3>ENDLUME Studio</h3><p>Готовые локальные проекты отправляются в RenderQueue без обращения к YouTube.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>Выбрать</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>Открыть</button>}</div></section></div>}
.vyron-v110-audit/src/SettingsOS.tsx:26: {tab==='updates'&&<section className="settingsCard"><h3>Update Center</h3><p>Текущая версия: {installedVersion||'—'}</p><Toggle label="Проверять автоматически" text="Проверка updater feed не использует YouTube API." value={s.autoCheckUpdates} onChange={v=>patch({autoCheckUpdates:v})}/><button className="settingsAction" onClick={checkForUpdate}>Проверить обновления</button>{update?.none&&<div className="successBox">Установлена актуальная версия {update.current||installedVersion||'—'}.</div>}{update?.version&&<div className="updateFound"><b>Доступна: {update.version}</b><p>{update.body}</p>{stage&&<strong>{stage}</strong>}{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}<button className="primary" onClick={installUpdate}>Установить и перезапустить</button></div>}{update?.error&&<div className="errorBox">Обновление не установлено. Текущая версия сохранена.<details><summary>Technical detail</summary>{update.error}</details></div>}</section>}
.vyron-v110-audit/src/SettingsOS.tsx:27: {tab==='diagnostics'&&<div className="settingsStack"><section className="settingsCard"><div className="panelHead"><div><h3>Локальная диагностика</h3><p>YouTube health/API checks намеренно исключены отсюда.</p></div><button className="primary" disabled={checking} onClick={runDiagnostics}>{checking?'Проверяю…':'Запустить проверку'}</button></div>{diag.length>0&&<div className="diagnosticGrid">{diag.map((x,i)=><div key={i} className={x.ok===false?'bad':x.ok?'good':''}><span><b>{x.label}</b><em>{x.status}</em></span>{x.detail&&<details><summary>Подробнее</summary><p>{x.detail}</p></details>}</div>)}</div>}</section><section className="settingsCard"><h3>Последние события</h3><div className="logs">{logs.length?logs.slice(0,60).map((l,i)=><div key={i} className={l.level}><small>{new Date(l.at).toLocaleString('ru-RU')}</small><span>{l.message}</span></div>):<p>Событий пока нет.</p>}</div></section></div>}</>
.vyron-v110-audit/src/autopilotRuntime.ts:13:function blankSummary():AutopilotSummary{return {prepared:0,tracksMoved:0,imagesMoved:0,metadataGenerated:0,renderQueued:0,uploads:0,errors:0,notes:[]}}
.vyron-v110-audit/src/autopilotRuntime.ts:34:  if(!s.autoGenerateMetadata||!s.openaiApiKey||job.metadataLocked||job.metadataSource==='ai'||job.metadataSource==='import')return false;
.vyron-v110-audit/src/autopilotRuntime.ts:35:  const meta=await api.aiGenerateMetadata(s.openaiApiKey,s.openaiModel,channel.name,channel.genre,channel.language,channel.country,job.number,job.topic,channel.seo.aiPrompt);
.vyron-v110-audit/src/autopilotRuntime.ts:36:  useApp.getState().patchJob(job.id,{title:meta.title,description:meta.description,tags:meta.tags,metadataSource:'ai',lastAutomationAt:new Date().toISOString()});
.vyron-v110-audit/src/autopilotRuntime.ts:37:  if(job.folder)await api.writeJobMetadata(job.folder,meta.title,meta.description,meta.tags,job.publishAt,'ai');
.vyron-v110-audit/src/autopilotRuntime.ts:53:  let jobs=useApp.getState().jobs.filter(j=>j.channelId===channel.id&&j.status!=='SCHEDULED').sort((a,b)=>a.number-b.number);
.vyron-v110-audit/src/autopilotRuntime.ts:59:  jobs=useApp.getState().jobs.filter(j=>j.channelId===channel.id&&j.status!=='SCHEDULED'&&j.status!=='ERROR').sort((a,b)=>a.number-b.number);
.vyron-v110-audit/src/autopilotRuntime.ts:67:  jobs=useApp.getState().jobs.filter(j=>j.channelId===channel.id&&j.status!=='SCHEDULED'&&j.status!=='ERROR').sort((a,b)=>a.number-b.number);
.vyron-v110-audit/src/autopilotRuntime.ts:74:  jobs=useApp.getState().jobs.filter(j=>j.channelId===channel.id&&j.status!=='SCHEDULED'&&j.status!=='ERROR').sort((a,b)=>a.number-b.number);
.vyron-v110-audit/src/autopilotRuntime.ts:79:      if(aiBudget.left>0&&current.folder&&await generateAi(channel,current)){summary.metadataGenerated++;aiBudget.left--}
.vyron-v110-audit/src/autopilotRuntime.ts:81:      if(after.folder&&after.title)await api.writeJobMetadata(after.folder,after.title,after.description,after.tags,after.publishAt,after.metadataSource||'template');
.vyron-v110-audit/src/autopilotRuntime.ts:105:    const moved=summary.tracksMoved+summary.imagesMoved+summary.renderQueued+summary.metadataGenerated+summary.prepared;
.vyron-v110-audit/src/autopilotRuntime.ts:106:    if(moved||summary.errors)useApp.getState().log(`Autopilot: папки ${summary.prepared}, треки ${summary.tracksMoved}, изображения ${summary.imagesMoved}, AI ${summary.metadataGenerated}, рендер ${summary.renderQueued}, ошибок ${summary.errors}`,summary.errors?'warn':'info');
.vyron-v110-audit/src/channelRunwayCore.ts:1:import type {Channel,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/channelRunwayCore.ts:13:  scheduledUntil?:string;
.vyron-v110-audit/src/channelRunwayCore.ts:14:  scheduledVideoCount:number;
.vyron-v110-audit/src/channelRunwayCore.ts:16:  lastScheduleSync?:string;
.vyron-v110-audit/src/channelRunwayCore.ts:96:  videos:Pick<YoutubeExistingVideo,'publishAt'|'privacyStatus'>[],
.vyron-v110-audit/src/channelRunwayCore.ts:98:  lastScheduleSync?:string,
.vyron-v110-audit/src/channelRunwayCore.ts:102:  // YouTube Scheduled is represented as privacyStatus=private + future status.publishAt.
.vyron-v110-audit/src/channelRunwayCore.ts:104:  const scheduled=videos
.vyron-v110-audit/src/channelRunwayCore.ts:109:  const last=scheduled.at(-1);
.vyron-v110-audit/src/channelRunwayCore.ts:110:  const scheduledUntil=last?.publishAt?dateKeyInKrasnoyarsk(last.publishAt):undefined;
.vyron-v110-audit/src/channelRunwayCore.ts:112:  const rawDays=scheduledUntil?calendarDaysBetween(today,scheduledUntil):undefined;
.vyron-v110-audit/src/channelRunwayCore.ts:118:    scheduledUntil,
.vyron-v110-audit/src/channelRunwayCore.ts:119:    scheduledVideoCount:scheduled.length,
.vyron-v110-audit/src/channelRunwayCore.ts:120:    averagePublishIntervalDays:averageIntervalDays(scheduled),
.vyron-v110-audit/src/channelRunwayCore.ts:121:    lastScheduleSync,
.vyron-v110-audit/src/channelRunwayCore.ts:124:    nextProductionDate:scheduledUntil?subtractCalendarDays(scheduledUntil,RUNWAY_PLAN_THRESHOLD_DAYS):undefined,
.vyron-v110-audit/src/channelRunwayCore.ts:132:  const known=record.status!=='no-data'||Boolean(record.lastScheduleSync)||Boolean(record.scheduledUntil);
.vyron-v110-audit/src/channelRunwayCore.ts:133:  const raw=record.scheduledUntil?calendarDaysBetween(today,record.scheduledUntil):undefined;
.vyron-v110-audit/src/channelRunwayCore.ts:140:    nextProductionDate:record.scheduledUntil?subtractCalendarDays(record.scheduledUntil,RUNWAY_PLAN_THRESHOLD_DAYS):undefined,
.vyron-v110-audit/src/channelRunwayCore.ts:168:export function quotaRiskCount(records:ChannelRunwayRecord[],todayCapacity:number,fullDayCapacity:number){
.vyron-v110-audit/src/AccountsPage.tsx:2:import {api,type GoogleConfigStatus,type YoutubeProfileHealth} from './api';
.vyron-v110-audit/src/AccountsPage.tsx:4:import type {YoutubeProfile} from './types';
.vyron-v110-audit/src/AccountsPage.tsx:11: const [profiles,setProfiles]=useState<YoutubeProfile[]>([]),[health,setHealth]=useState<Record<string,YoutubeProfileHealth>>({}),[config,setConfig]=useState<GoogleConfigStatus|null>(null),[busy,setBusy]=useState(false),[checking,setChecking]=useState(false),[browserOpen,setBrowserOpen]=useState(false),[browsers,setBrowsers]=useState<BrowserOption[]>([]),[browser,setBrowser]=useState(localStorage.getItem('vyron:oauth-browser')||'default');const file=useRef<HTMLInputElement>(null);
.vyron-v110-audit/src/AccountsPage.tsx:12: function bindProfile(p:YoutubeProfile){if(!p.channelId)return;const state=useApp.getState();const c=state.channels.find(x=>x.youtubeChannelId===p.channelId);if(c){state.updateChannel(c.id,{youtubeProfileId:p.id,youtubeChannelId:p.channelId,name:p.channelTitle||c.name});return c}return state.addChannel({name:p.channelTitle||'YouTube канал',youtubeProfileId:p.id,youtubeChannelId:p.channelId})}
.vyron-v110-audit/src/AccountsPage.tsx:13: async function refresh(){const [p,c]=await Promise.all([api.youtubeProfiles(),api.youtubeGoogleConfig()]);setProfiles(p);setConfig(c);for(const profile of p)bindProfile(profile);return p}
.vyron-v110-audit/src/AccountsPage.tsx:15: async function importCredentials(fl:FileList|null){const f=fl?.[0];if(!f)return;setBusy(true);try{const c=await api.youtubeImportGoogleConfig(await f.text(),settings.youtubeApiKey||'');setConfig(c);toast('Google Credentials сохранены глобально')}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/AccountsPage.tsx:16: async function askBrowser(){setBusy(true);try{const rows=await api.youtubeOauthBrowsers();const available=rows.filter(x=>x.available);setBrowsers(available.length?available:[{id:'default',label:'Браузер по умолчанию',available:true}]);if(!available.some(x=>x.id===browser))setBrowser('default');setBrowserOpen(true)}catch{setBrowsers([{id:'default',label:'Браузер по умолчанию',available:true}]);setBrowserOpen(true)}finally{setBusy(false)}}
.vyron-v110-audit/src/AccountsPage.tsx:17: async function connect(){setBrowserOpen(false);setBusy(true);try{localStorage.setItem('vyron:oauth-browser',browser);const p=await api.youtubeConnectGlobal(browser);bindProfile(p);await refresh();toast(`✓ ${p.channelTitle||p.channelId||'YouTube канал'} подключён и привязан автоматически`)}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/AccountsPage.tsx:18: async function checkAll(){setChecking(true);const next:Record<string,YoutubeProfileHealth>={};for(const p of profiles){try{next[p.id]=await api.youtubeProfileHealth(p.id)}catch(e){next[p.id]={ok:false,status:'RECONNECT_REQUIRED',error:String(e)}}}setHealth(next);setChecking(false)}
.vyron-v110-audit/src/AccountsPage.tsx:20:  <div className="pageHeader"><div><small>ACCOUNT CENTER</small><h1>Аккаунты YouTube</h1><p>Один Google OAuth Client обслуживает все каналы. При подключении VYRON сам определяет YouTube channelId и привязывает канал — ручное связывание не требуется.</p></div><div className="headerActions"><button disabled={checking||!profiles.length} onClick={checkAll}>{checking?'Проверяю…':'↻ Проверить'}</button><button className="primary compactAction" disabled={busy||(!config?.configured&&!profiles.length)} onClick={askBrowser}>+ Добавить канал</button></div></div>
.vyron-v110-audit/src/AccountsPage.tsx:22:  <section className="panel googleConfigCard"><div><small>GLOBAL GOOGLE CONFIG</small><h3>Одна конфигурация для всех каналов</h3><p>credentials.json нужен один раз. После этого каждый новый Gmail подключается обычным Google OAuth.</p></div><div className="configChecks"><span className={config?.configured?'good':''}>OAuth Client <b>{config?.configured?'✓':'—'}</b></span><span className={config?.projectId?'good':''}>Project <b>{config?.projectId||'—'}</b></span><span className={config?.hasSecret?'good':''}>Client Secret <b>{config?.hasSecret?'✓':'—'}</b></span><span className={settings.youtubeApiKey?'good':''}>Public API Key <b>{settings.youtubeApiKey?'✓':'не нужен для OAuth'}</b></span></div><div className="googleConfigActions"><button disabled={busy} onClick={()=>file.current?.click()}>Импортировать credentials.json</button><label>Public API Key<input type="password" placeholder="опционально" value={settings.youtubeApiKey} onChange={e=>patchSettings({youtubeApiKey:e.target.value.trim()})}/></label></div></section>
.vyron-v110-audit/src/AccountsPage.tsx:23:  <section className="panel accountsPanel"><div className="panelHead"><div><small>YOUTUBE ACCOUNTS</small><h3>{profiles.length?`${profiles.length} подключено`:'Аккаунтов пока нет'}</h3></div></div>{!profiles.length?<div className="empty"><b>Подключи первый YouTube-канал</b><p>VYRON спросит браузер, откроет Google OAuth и после подтверждения сам создаст карточку реального канала.</p></div>:<div className="accountList">{profiles.map(p=>{const h=health[p.id];const bound=channels.find(c=>c.youtubeProfileId===p.id||c.youtubeChannelId===p.channelId);return <article className="accountRow" key={p.id}>{h?.thumbnail?<img src={h.thumbnail} loading="lazy"/>:<div className="accountAvatar">YT</div>}<div className="accountMain"><b>{h?.channelTitle||p.channelTitle||'YouTube канал'}</b><small>{p.channelId||'Channel ID ещё не определён'}</small><div className="accountBadges"><span className={h?.ok?'good':'warn'}>{h?.ok?'● OAuth healthy':h?'● Требуется внимание':'● Проверяется'}</span><span>{bound?`VYRON: ${bound.name}`:'Автопривязка…'}</span>{p.preferredBrowser&&<span>Браузер: {p.preferredBrowser}</span>}</div></div><div className="accountMeta"><small>Подключён</small><b>{ago(p.connectedAt)}</b><small>Token</small><b>{h?.status||'—'}</b></div><div className="accountActions"><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refresh()}}>Отключить</button></div></article>})}</div>}</section>
.vyron-v110-audit/src/youtubeQuota.ts:1:export type YoutubeQuotaState={blocked:boolean;ptDate?:string;detectedAt?:string;reason?:string};
.vyron-v110-audit/src/youtubeQuota.ts:2:export type YoutubeQuotaUsage={ptDate:string;limit:number;used:number;calls:number;updatedAt?:string;lastAction?:string};
.vyron-v110-audit/src/youtubeQuota.ts:3:export type YoutubeQuotaPlanState={channels:number;videosPerChannel:number};
.vyron-v110-audit/src/youtubeQuota.ts:4:export type YoutubeQuotaPlan={perChannel:number;remaining:number;todayChannels:number;fullDayChannels:number;days:number;totalUnits:number;rows:{day:number;channels:number;units:number}[]};
.vyron-v110-audit/src/youtubeQuota.ts:5:const KEY='vyron:youtube-quota-guard:v1';
.vyron-v110-audit/src/youtubeQuota.ts:6:const USAGE_KEY='vyron:youtube-quota-usage:v2';
.vyron-v110-audit/src/youtubeQuota.ts:7:const PLAN_KEY='vyron:youtube-quota-plan:v1';
.vyron-v110-audit/src/youtubeQuota.ts:8:const EVENT='vyron-youtube-quota';
.vyron-v110-audit/src/youtubeQuota.ts:9:export const DEFAULT_YOUTUBE_DAILY_QUOTA=10000;
.vyron-v110-audit/src/youtubeQuota.ts:10:export const ESTIMATED_VIDEO_WRITE_UNITS=52; // one videos.update (50) + normal read/verify overhead
.vyron-v110-audit/src/youtubeQuota.ts:12:export function nextYoutubeQuotaResetAt(now=new Date()){
.vyron-v110-audit/src/youtubeQuota.ts:23:export function youtubeQuotaResetLocalInfo(now=new Date()){
.vyron-v110-audit/src/youtubeQuota.ts:24: const at=nextYoutubeQuotaResetAt(now);const timeZone=Intl.DateTimeFormat().resolvedOptions().timeZone||'local';
.vyron-v110-audit/src/youtubeQuota.ts:31:export function isYoutubeQuotaError(error:unknown){const s=String(error||'').toLowerCase();return s.includes('quotaexceeded')||s.includes('exceeded your quota')||s.includes('dailylimitexceeded')||s.includes('youtube_quota_paused')}
.vyron-v110-audit/src/youtubeQuota.ts:32:export function youtubeQuotaState():YoutubeQuotaState{try{const raw=localStorage.getItem(KEY);if(!raw)return{blocked:false};const x=JSON.parse(raw) as YoutubeQuotaState;if(!x?.blocked)return{blocked:false};if(x.ptDate!==ptDate()){localStorage.removeItem(KEY);return{blocked:false}}return x}catch{return{blocked:false}}}
.vyron-v110-audit/src/youtubeQuota.ts:33:function readUsage():YoutubeQuotaUsage{const today=ptDate();try{const raw=localStorage.getItem(USAGE_KEY);if(raw){const x=JSON.parse(raw) as YoutubeQuotaUsage;if(x.ptDate===today){return{ptDate:today,limit:Math.max(1,safeInt(x.limit,DEFAULT_YOUTUBE_DAILY_QUOTA)),used:safeInt(x.used,0),calls:safeInt(x.calls,0),updatedAt:x.updatedAt,lastAction:x.lastAction}}const previousLimit=Math.max(1,safeInt(x.limit,DEFAULT_YOUTUBE_DAILY_QUOTA));return{ptDate:today,limit:previousLimit,used:0,calls:0}}}catch{}return{ptDate:today,limit:DEFAULT_YOUTUBE_DAILY_QUOTA,used:0,calls:0}}
.vyron-v110-audit/src/youtubeQuota.ts:34:function saveUsage(x:YoutubeQuotaUsage){localStorage.setItem(USAGE_KEY,JSON.stringify(x));emit();return x}
.vyron-v110-audit/src/youtubeQuota.ts:35:export function youtubeQuotaUsage(){const x=readUsage();const guard=youtubeQuotaState();if(guard.blocked&&x.used<x.limit){x.used=x.limit;return saveUsage(x)}return x}
.vyron-v110-audit/src/youtubeQuota.ts:36:export function youtubeQuotaRemaining(){const x=youtubeQuotaUsage();return Math.max(0,x.limit-x.used)}
.vyron-v110-audit/src/youtubeQuota.ts:37:export function setYoutubeQuotaLimit(limit:number){const x=readUsage();x.limit=Math.max(1,Math.floor(limit||DEFAULT_YOUTUBE_DAILY_QUOTA));x.used=Math.min(x.used,x.limit);x.updatedAt=new Date().toISOString();return saveUsage(x)}
.vyron-v110-audit/src/youtubeQuota.ts:38:export function recordYoutubeQuota(units:number,action:string){const n=Math.max(0,Math.ceil(Number(units)||0));if(!n)return youtubeQuotaUsage();const x=readUsage();x.used=Math.min(x.limit,Math.max(0,x.used+n));x.calls+=1;x.updatedAt=new Date().toISOString();x.lastAction=action;return saveUsage(x)}
.vyron-v110-audit/src/youtubeQuota.ts:39:export function youtubeQuotaCost(command:string,args?:Record<string,unknown>,result?:any){
.vyron-v110-audit/src/youtubeQuota.ts:40: if(command==='youtube_oauth_profile_health'||command==='youtube_channel_stats')return 1;
.vyron-v110-audit/src/youtubeQuota.ts:41: if(command==='youtube_list_existing_videos'){
.vyron-v110-audit/src/youtubeQuota.ts:42:  const received=Math.max(0,Number(result?.received||0));const found=Math.max(received,Number(result?.youtubeFound||0));const requested=Math.max(1,Number(result?.requested||args?.maxResults||received||1));const n=Math.min(found,requested);return 1+Math.max(1,Math.ceil(n/50))+Math.max(1,Math.ceil(received/50));
.vyron-v110-audit/src/youtubeQuota.ts:44: if(command==='youtube_backup_existing_videos'){const n=Array.isArray(args?.videos)?(args?.videos as unknown[]).length:Number(result?.count||0);return Math.max(1,Math.ceil(Math.max(1,n)/50));}
.vyron-v110-audit/src/youtubeQuota.ts:45: if(command==='youtube_update_existing_video'){
.vyron-v110-audit/src/youtubeQuota.ts:48:  const metadata=Boolean(result?.metadataAccepted);const scheduleRequested=Boolean(result?.scheduleRequested);const scheduleAccepted=Boolean(result?.scheduleAccepted);
.vyron-v110-audit/src/youtubeQuota.ts:49:  if(metadata&&scheduleRequested&&!scheduleAccepted)cost+=100; // combined update + metadata fallback
.vyron-v110-audit/src/youtubeQuota.ts:50:  else if(metadata||scheduleAccepted)cost+=50;
.vyron-v110-audit/src/youtubeQuota.ts:51:  if(result?.metadataVerifyPending||result?.scheduleVerifyPending)cost+=2;
.vyron-v110-audit/src/youtubeQuota.ts:54: // Analytics API has a separate quota model. Search/insert moved to granular buckets in 2026.
.vyron-v110-audit/src/youtubeQuota.ts:55: if(command==='youtube_channel_analytics')return 0;
.vyron-v110-audit/src/youtubeQuota.ts:56: if(command==='youtube_discover_competitors')return 1;
.vyron-v110-audit/src/youtubeQuota.ts:57: if(command==='youtube_competitor_snapshot')return 4;
.vyron-v110-audit/src/youtubeQuota.ts:58: if(command==='youtube_upload_video')return 0;
.vyron-v110-audit/src/youtubeQuota.ts:61:export function recordYoutubeCommand(command:string,args?:Record<string,unknown>,result?:any){return recordYoutubeQuota(youtubeQuotaCost(command,args,result),command)}
.vyron-v110-audit/src/youtubeQuota.ts:62:export function markYoutubeQuotaExceeded(error:unknown){const state:YoutubeQuotaState={blocked:true,ptDate:ptDate(),detectedAt:new Date().toISOString(),reason:String(error||'quotaExceeded')};localStorage.setItem(KEY,JSON.stringify(state));const x=readUsage();x.used=x.limit;x.updatedAt=new Date().toISOString();x.lastAction='quotaExceeded';saveUsage(x);emit();return state}
.vyron-v110-audit/src/youtubeQuota.ts:63:export function clearYoutubeQuotaGuard(){localStorage.removeItem(KEY);emit()}
.vyron-v110-audit/src/youtubeQuota.ts:64:export function youtubeQuotaMessage(){const r=youtubeQuotaResetLocalInfo();return `Квота YouTube Data API исчерпана. VYRON поставил YouTube-запросы на паузу до ${r.time} (${r.date}) по вашему местному времени. Базовый сброс Google: 00:00 Pacific Time. Черновики и незавершённые пачки сохранены.`}
.vyron-v110-audit/src/youtubeQuota.ts:65:export function assertYoutubeQuotaAvailable(){const q=youtubeQuotaState();if(q.blocked)throw new Error(`YOUTUBE_QUOTA_PAUSED: ${youtubeQuotaMessage()}`)}
.vyron-v110-audit/src/youtubeQuota.ts:66:export async function youtubeGuardedCall<T>(fn:()=>Promise<T>):Promise<T>{assertYoutubeQuotaAvailable();try{return await fn()}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);throw new Error(`YOUTUBE_QUOTA_PAUSED: ${youtubeQuotaMessage()}`)}throw e}}
.vyron-v110-audit/src/youtubeQuota.ts:67:export function subscribeYoutubeQuota(cb:()=>void){window.addEventListener(EVENT,cb);return()=>window.removeEventListener(EVENT,cb)}
.vyron-v110-audit/src/youtubeQuota.ts:68:export function loadYoutubeQuotaPlan():YoutubeQuotaPlanState{try{const x=JSON.parse(localStorage.getItem(PLAN_KEY)||'{}');return{channels:Math.max(1,safeInt(x.channels,100)),videosPerChannel:Math.max(1,safeInt(x.videosPerChannel,30))}}catch{return{channels:100,videosPerChannel:30}}}
.vyron-v110-audit/src/youtubeQuota.ts:69:export function saveYoutubeQuotaPlan(plan:YoutubeQuotaPlanState){const x={channels:Math.max(1,Math.floor(plan.channels||1)),videosPerChannel:Math.max(1,Math.floor(plan.videosPerChannel||1))};localStorage.setItem(PLAN_KEY,JSON.stringify(x));emit();return x}
.vyron-v110-audit/src/youtubeQuota.ts:70:export function buildYoutubeQuotaPlan(channels:number,videosPerChannel:number,usage=youtubeQuotaUsage()):YoutubeQuotaPlan{
.vyron-v110-audit/src/channelSchedule.test.ts:2:import {deriveChannelScheduleState,generatePatternSchedule,isPatternPublishDate} from './channelSchedule';
.vyron-v110-audit/src/channelSchedule.test.ts:3:import type {Channel,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/channelSchedule.test.ts:5:const v=(id:string,publishAt?:string):YoutubeExistingVideo=>({id,position:0,title:id,description:'',tags:[],categoryId:'10',privacyStatus:'private',publishAt,selected:false});
.vyron-v110-audit/src/channelSchedule.test.ts:6:const pattern=(extra:Partial<Channel>={}):Channel=>({...base,scheduleMode:'pattern',publishDays:3,pauseDays:1,patternAnchorDate:'2026-09-15',...extra});
.vyron-v110-audit/src/channelSchedule.test.ts:7:describe('VYRON calendar schedule strategies',()=>{
.vyron-v110-audit/src/channelSchedule.test.ts:8: it('keeps old cadence channels backward compatible as interval mode',()=>{const s=deriveChannelScheduleState(base,[v('x','2026-09-13T18:00:00+07:00')]);expect(s.scheduleMode).toBe('interval');expect(s.nextAvailableAt).toBe('2026-09-15T11:00:00.000Z')});
.vyron-v110-audit/src/channelSchedule.test.ts:10: it('preview shows pause days and creates exactly requested video dates',()=>{const c=pattern();const g=generatePatternSchedule(c,[],10);expect(g.dates).toHaveLength(10);expect(g.calendar.slice(0,5).map(x=>x.kind)).toEqual(['video','video','video','pause','video']);expect(g.dates[0]).toBe('2026-09-15T11:00:00.000Z');expect(g.dates[9]).toBe('2026-09-27T11:00:00.000Z')});
.vyron-v110-audit/src/channelSchedule.test.ts:11: it('continues in the middle of an existing 3/1 cycle',()=>{const c=pattern({patternAnchorDate:'2026-09-01'});const rows=[v('9','2026-09-09T18:00:00+07:00'),v('10','2026-09-10T18:00:00+07:00')];const g=generatePatternSchedule(c,rows,4);expect(g.dates.map(x=>x.slice(0,10))).toEqual(['2026-09-11','2026-09-13','2026-09-14','2026-09-15']);expect(g.calendar.some(x=>x.date==='2026-09-12'&&x.kind==='pause')).toBe(true)});
.vyron-v110-audit/src/channelSchedule.test.ts:12: it('occupied publish slot is skipped without moving pause day',()=>{const c=pattern();const rows=[v('busy','2026-09-16T18:00:00+07:00')];const g=generatePatternSchedule(c,rows,4);expect(g.dates.map(x=>x.slice(0,10))).toEqual(['2026-09-17','2026-09-19','2026-09-20','2026-09-21']);expect(g.calendar.some(x=>x.date==='2026-09-16')).toBe(false);expect(g.calendar.find(x=>x.date==='2026-09-18')?.kind).toBe('pause')});
.vyron-v110-audit/src/channelSchedule.test.ts:13: it('34 videos produce 34 slots without duplicate dates',()=>{const g=generatePatternSchedule(pattern(),[],34);expect(g.dates).toHaveLength(34);expect(new Set(g.dates.map(x=>x.slice(0,10))).size).toBe(34)});
.vyron-v110-audit/src/channelSchedule.test.ts:14: it('different channels keep different strategies',()=>{expect(deriveChannelScheduleState(base,[]).scheduleMode).toBe('interval');expect(deriveChannelScheduleState(pattern(),[]).scheduleMode).toBe('pattern')});
.vyron-v110-audit/src/QuotaMeter.tsx:2:import {buildYoutubeQuotaPlan,clearYoutubeQuotaGuard,loadYoutubeQuotaPlan,saveYoutubeQuotaPlan,setYoutubeQuotaLimit,subscribeYoutubeQuota,youtubeQuotaResetLocalInfo,youtubeQuotaState,youtubeQuotaUsage} from './youtubeQuota';
.vyron-v110-audit/src/QuotaMeter.tsx:4:export function QuotaMeter({compact=false,defaultChannels=100}:{compact?:boolean;defaultChannels?:number}){
.vyron-v110-audit/src/QuotaMeter.tsx:5: const [usage,setUsage]=useState(()=>youtubeQuotaUsage());
.vyron-v110-audit/src/QuotaMeter.tsx:6: const [guard,setGuard]=useState(()=>youtubeQuotaState());
.vyron-v110-audit/src/QuotaMeter.tsx:7: const initial=loadYoutubeQuotaPlan();
.vyron-v110-audit/src/QuotaMeter.tsx:10: useEffect(()=>{const refresh=()=>{setUsage(youtubeQuotaUsage());setGuard(youtubeQuotaState())};const off=subscribeYoutubeQuota(refresh);const id=window.setInterval(refresh,1000);return()=>{off();window.clearInterval(id)}},[]);
.vyron-v110-audit/src/QuotaMeter.tsx:11: const plan=useMemo(()=>buildYoutubeQuotaPlan(channels,videos,usage),[channels,videos,usage.ptDate,usage.limit,usage.used]);
.vyron-v110-audit/src/QuotaMeter.tsx:14: const reset=youtubeQuotaResetLocalInfo();
.vyron-v110-audit/src/QuotaMeter.tsx:15: const savePlan=()=>{saveYoutubeQuotaPlan({channels,videosPerChannel:videos});setUsage(youtubeQuotaUsage())};
.vyron-v110-audit/src/QuotaMeter.tsx:16: return <section className={`quotaMeter ${guard.blocked?'blocked':''} ${compact?'compact':''}`}>
.vyron-v110-audit/src/QuotaMeter.tsx:17:  <div className="quotaMeterHead"><div><small>YOUTUBE API QUOTA</small><h3>{guard.blocked?'Пауза по квоте':'Live estimate'}</h3><p>Мгновенный расчёт запросов VYRON. Google Cloud Monitoring может запаздывать на несколько минут.</p></div><span className={guard.blocked?'bad':'good'}>{guard.blocked?'QUOTA EXCEEDED':'TRACKING'}</span></div>
.vyron-v110-audit/src/QuotaMeter.tsx:18:  <div className="quotaMeterStats"><div><small>Использовано</small><b>{usage.used.toLocaleString('ru-RU')}</b><em>из {usage.limit.toLocaleString('ru-RU')}</em></div><div><small>Осталось</small><b>{remaining.toLocaleString('ru-RU')}</b><em>units</em></div><div><small>Сегодня можно</small><b>{plan.todayChannels}</b><em>каналов × {videos} видео</em></div><div><small>Сброс по вашему времени</small><b>{reset.time}</b><em>{reset.date} • 00:00 PT</em></div></div>
.vyron-v110-audit/src/QuotaMeter.tsx:19:  <div className="quotaMeterBar"><i style={{width:`${pct}%`}}/><span>{pct.toFixed(0)}%</span></div>
.vyron-v110-audit/src/QuotaMeter.tsx:21:   <div className="quotaPlannerControls"><label>Дневной лимит<input type="number" min="1000" step="1000" value={usage.limit} onChange={e=>setUsage(setYoutubeQuotaLimit(Math.max(1000,+e.target.value||10000)))}/></label><label>Каналов в плане<input type="number" min="1" max="1000" value={channels} onChange={e=>setChannels(Math.max(1,+e.target.value||1))}/></label><label>Видео / канал<input type="number" min="1" max="1000" value={videos} onChange={e=>setVideos(Math.max(1,+e.target.value||1))}/></label><button onClick={savePlan}>Сохранить план</button>{guard.blocked&&<button onClick={()=>{clearYoutubeQuotaGuard();setGuard(youtubeQuotaState());setUsage(youtubeQuotaUsage())}}>Проверить снова</button>}</div>
.vyron-v110-audit/src/QuotaMeter.tsx:22:   <div className="quotaPlannerSummary"><span><small>≈ на канал</small><b>{plan.perChannel.toLocaleString('ru-RU')} units</b></span><span><small>Нужно всего</small><b>{plan.totalUnits.toLocaleString('ru-RU')}</b></span><span><small>Полный день</small><b>{plan.fullDayChannels} каналов</b></span><span><small>Оценка срока</small><b>≈ {plan.days} дн.</b></span></div>
.vyron-v110-audit/src/QuotaMeter.tsx:23:   <div className="quotaDayPlan">{plan.rows.map(r=><div key={r.day} className={!r.channels?'emptyDay':''}><b>{r.day===0?'Сегодня':`День ${r.day+1}`}</b><span>{r.channels} каналов</span><small>≈ {r.units.toLocaleString('ru-RU')} units</small></div>)}</div>
.vyron-v110-audit/src/QuotaMeter.tsx:24:   <p className="quotaFinePrint">Планировщик использует консервативную оценку ~52 units на изменяемое видео: один videos.update (50) + обычный read/verify overhead. Уже совпадающие видео VYRON пропускает, поэтому фактический расход может быть ниже. Search/insert с 2026 года имеют отдельные granular buckets и здесь не смешиваются с основным write-пулом.</p>
.vyron-v110-audit/src/YouTubeCenter.tsx:3:import {MetadataPage} from './MetadataPage';
.vyron-v110-audit/src/YouTubeCenter.tsx:4:import {PublisherOS} from './PublisherOS';
.vyron-v110-audit/src/YouTubeCenter.tsx:6:import {YouTubeDataTools} from './YouTubeDataTools';
.vyron-v110-audit/src/YouTubeCenter.tsx:10:import {QuotaMeter} from './QuotaMeter';
.vyron-v110-audit/src/YouTubeCenter.tsx:12:type Tab='uploaded'|'metadata'|'queue'|'calendar'|'command'|'runway'|'data'|'accounts';
.vyron-v110-audit/src/YouTubeCenter.tsx:13:export function YouTubeCenter({initialTab='uploaded'}:{initialTab?:Tab}){
.vyron-v110-audit/src/YouTubeCenter.tsx:15: const calendar=useMemo(()=>jobs.filter(j=>j.publishAt&&['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)).sort((a,b)=>(a.publishAt||'').localeCompare(b.publishAt||'')),[jobs]);
.vyron-v110-audit/src/YouTubeCenter.tsx:16: const tabs:[Tab,string][]=[['uploaded','Загруженные'],['metadata','Метаданные'],['queue','Очередь'],['calendar','Календарь'],['command','Командный центр'],['runway','План каналов'],['data','Данные YouTube'],['accounts','Аккаунты']];
.vyron-v110-audit/src/YouTubeCenter.tsx:17: return <><div className="youtubeCenterHead"><div><small>YOUTUBE CENTER • API ZONE</small><h1>YouTube</h1><p>Единственная зона VYRON, где разрешены YouTube API-вызовы. Синхронизация и обновления запускаются только твоим действием.</p></div><div className="youtubeTabs">{tabs.map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div></div><QuotaMeter compact defaultChannels={Math.max(1,channels.length)}/>{tab!=='command'&&<div className="youtubePolicyNotice"><b>ZERO HIDDEN QUOTA</b><span>Нет startup sync, polling, background refresh или API-запросов при возврате на вкладку.</span></div>}{tab==='uploaded'&&<ExistingVideos/>}{tab==='metadata'&&<MetadataPage/>}{tab==='queue'&&<PublisherOS/>}{tab==='command'&&<CommandCenter/>}{tab==='runway'&&<ChannelRunway/>}{tab==='data'&&<YouTubeDataTools/>}{tab==='accounts'&&<AccountsPage/>}{tab==='calendar'&&<section className="panel calendarPanel"><div className="panelHead"><div><small>ALL CHANNELS</small><h3>Календарь публикаций</h3><p>Локальные даты из project state / YouTube queue.</p></div><span>{calendar.length} публикаций</span></div>{calendar.length===0?<div className="empty"><b>Публикаций пока нет</b><p>Когда у видео появится publishAt, оно будет показано здесь.</p></div>:<div className="calendarList">{calendar.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="calendarRow" key={j.id}><time>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})}</time><span><b>{c?.name||'Канал удалён'}</b><small>VIDEO_{String(j.number).padStart(3,'0')} • {j.title||'Без названия'}</small></span><em className={`publishState ${j.status.toLowerCase()}`}>{j.status}</em>{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div>})}</div>}</section>}</>
.vyron-v110-audit/src/MetadataPage.schedule.test.ts:2:import {resolveKratPublishAt} from "./MetadataPage";
.vyron-v110-audit/src/MetadataPage.schedule.test.ts:3:describe("KRAT schedule",()=>{it("combines PUBLISH TIME with planned date",()=>{expect(resolveKratPublishAt("04:00 KRAT","2026-09-02T18:00:00+07:00")).toBe("2026-09-02T04:00:00+07:00")});it("keeps explicit RFC3339",()=>{expect(resolveKratPublishAt("2026-09-05T04:00:00+07:00","2026-09-02T18:00:00+07:00")).toBe("2026-09-05T04:00:00+07:00")})});
.vyron-v110-audit/src/youtubeExisting.ts:1:import type { YoutubeExistingVideo } from './types';
.vyron-v110-audit/src/youtubeExisting.ts:2:import type { ImportedMetadata } from './metadata';
.vyron-v110-audit/src/youtubeExisting.ts:4:export function orderedExistingVideos(videos:YoutubeExistingVideo[],order:'oldest'|'newest'){
.vyron-v110-audit/src/youtubeExisting.ts:8:export function buildExistingSchedule(videos:YoutubeExistingVideo[],startIso:string,cadenceDays:number){
.vyron-v110-audit/src/youtubeExisting.ts:21:export function buildExistingScheduleFromLocal(videos:YoutubeExistingVideo[],startLocal:string,cadenceDays:number,meta:ImportedMetadata[]=[]){
.vyron-v110-audit/src/youtubeExisting.ts:34:export function overlayExistingMetadata(videos:YoutubeExistingVideo[],meta:ImportedMetadata[]){
.vyron-v110-audit/src/ProductionOS.tsx:14:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужно изображение',WAITING_MUSIC:'Нужно аудио',READY_RENDER:'Готов к ENDLUME',RENDERING:'Рендер',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загрузка YouTube',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/ProductionOS.tsx:15:const stageOrder:JobStatus[]=['NEED_IMAGE','WAITING_MUSIC','READY_RENDER','RENDERING','READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'];
.vyron-v110-audit/src/ProductionOS.tsx:18: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [prefs,patchPrefs]=useProductionPrefs();const tab=(prefs.tab||'queue') as Tab;const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';const setTab=(v:Tab)=>patchPrefs({tab:v});const setChannelId=(id:string)=>patchPrefs({selectedChannelId:id});const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false),[inbox,setInbox]=useState<any>(),[planOpen,setPlanOpen]=useState(false),[planCount,setPlanCount]=useState(30),[planScope,setPlanScope]=useState<'one'|'all'>('one');const [section,setSection]=useState<'overview'|'materials'|'builder'|'render'|'publish'>(()=>tab==='materials'?'materials':tab==='manager'?'builder':'overview');const scopedJobs=section==='render'?jobs.filter(j=>['READY_RENDER','RENDERING','ERROR'].includes(j.status)):section==='publish'?jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)):jobs;const filtered=scopedJobs.filter(j=>filter==='all'||j.channelId===filter||j.status===filter).sort((a,b)=>a.number-b.number);const c=channels.find(x=>x.id===channelId);const selectedJobIds=(prefs.selectedJobIds||[]).filter(id=>jobs.some(j=>j.id===id));const projectRoot=resolveProductionRootFromPrefs(prefs,channelId,settings.workspace);const [projectStorage,setProjectStorage]=useState<ProductionStorageStatus|null>(null);
.vyron-v110-audit/src/ProductionOS.tsx:26: async function refreshAll(){setBusy(true);try{for(const j of useApp.getState().jobs){const ch=channels.find(x=>x.id===j.channelId);if(!ch||!j.folder||j.status==='SCHEDULED')continue;try{patchJob(j.id,await api.refreshJob(j.folder,ch.minTracks))}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}toast('Статусы производства обновлены')}finally{setBusy(false)}}
.vyron-v110-audit/src/ProductionOS.tsx:34:  <div className="pageHeader"><div><small>PRODUCTION PIPELINE</small><h1>Производство</h1><p>Понятная цепочка от материалов до YouTube. Backend-очередь и ENDLUME остаются прежними — меняется только управление.</p></div><div className="headerActions"><button disabled={busy} onClick={refreshAll}>{busy?'Проверяю…':'↻ Обновить статусы'}</button><button onClick={maintainBuffer}>Поддержать буфер</button><button className="primary compactAction" onClick={()=>setPlanOpen(true)}>+ Создать проекты</button></div></div>
.vyron-v110-audit/src/ProductionOS.tsx:36:  <div className="youtubeTabs productionTabs productionNavFive"><button className={section==='overview'?'active':''} onClick={()=>{setSection('overview');setTab('queue');setFilter('all')}}>Обзор</button><button className={section==='materials'?'active':''} onClick={()=>{setSection('materials');setTab('materials')}}>Материалы</button><button className={section==='builder'?'active':''} onClick={()=>{setSection('builder');setTab('manager')}}>Сборка проектов</button><button className={section==='render'?'active':''} onClick={()=>{setSection('render');setTab('queue');setFilter('all')}}>Рендер</button><button className={section==='publish'?'active':''} onClick={()=>{setSection('publish');setTab('queue');setFilter('all')}}>Публикация</button></div>
.vyron-v110-audit/src/ProductionOS.tsx:37:  {tab==='queue'&&<>{section==='overview'&&<ProductionWorkspace/>}<div className="productionPipeline">{stageOrder.map(st=><button key={st} className={st==='ERROR'&&jobs.some(j=>j.status==='ERROR')?'warn':''} onClick={()=>setFilter(st)}><small>{statusLabel[st]}</small><b>{jobs.filter(j=>j.status===st).length}</b></button>)}</div><div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы / статусы</option>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}{stageOrder.map(st=><option key={st} value={st}>{statusLabel[st]}</option>)}</select><span>{filtered.length} задач</span></div>{filtered.length===0?<div className="empty"><b>Нет задач в этом фильтре</b><p>Создай точное число проектов или включи поддержание буфера.</p><button className="primary" onClick={()=>setPlanOpen(true)}>Создать проекты</button></div>:<div className="jobs virtualReady"><div className="pmBulkBar"><span>Выбрано <b>{selectedJobIds.filter(id=>visibleJobIds.includes(id)).length}</b> / {visibleJobIds.length}</span><button onClick={()=>patchPrefs({selectedJobIds:[...new Set([...selectedJobIds,...visibleJobIds])]})}>Выбрать всё</button><button onClick={()=>patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!visibleJobIds.includes(id))})}>Снять выделение</button><button className="danger" disabled={!selectedJobIds.some(id=>visibleJobIds.includes(id))} onClick={()=>void deleteJobs(selectedJobIds.filter(id=>visibleJobIds.includes(id)),'Удалить выбранные проекты?')}>Удалить выбранные</button><button className="danger" onClick={()=>void deleteJobs(visibleJobIds,'Удалить все проекты в текущем списке?')}>Удалить все</button></div>{filtered.map(j=>{const ch=channels.find(x=>x.id===j.channelId);return <article className="job productionJob" key={j.id}><label className="pmJobCheck"><input type="checkbox" checked={selectedJobIds.includes(j.id)} onChange={()=>toggleJob(j.id)}/></label><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{ch?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не готовы'}</b><span>Изображение {j.coverPath?'✓':'—'} • треков {j.tracksCount}/{j.minTracks} • final.mp4 {j.finalPath?'✓':'—'} • metadata {j.title&&j.description?'✓':'—'}</span><span>{j.publishAt?`Публикация: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата публикации не назначена'}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>Папка</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,ch?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ Аудио</button>}{j.status==='READY_RENDER'&&j.folder&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в ENDLUME RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('youtube')}>YouTube</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,ch?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>Повторить</button>}</div></article>})}</div>}</>}
.vyron-v110-audit/src/ProductionOS.tsx:40:  {planOpen&&<div className="modalBackdrop" onMouseDown={()=>setPlanOpen(false)}><section className="confirmModal planModal" onMouseDown={e=>e.stopPropagation()}><small>AUTOPILOT PLANNER</small><h2>Сколько проектов создать?</h2><div className="presetRow">{[10,30,50,100].map(n=><button className={planCount===n?'active':''} key={n} onClick={()=>setPlanCount(n)}>{n}</button>)}<label>Своё число<input type="number" min="1" max="1000" value={planCount} onChange={e=>setPlanCount(Math.max(1,Math.min(1000,+e.target.value||1)))}/></label></div><div className="metadataTargetSwitch"><button className={planScope==='one'?'active':''} onClick={()=>setPlanScope('one')}><b>Один канал</b><small>{c?.name||'выбери канал'}</small></button><button className={planScope==='all'?'active':''} onClick={()=>setPlanScope('all')}><b>Все активные</b><small>{planCount} на каждый канал</small></button></div>{planScope==='one'&&<label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label>}<div className="planPreview">{preview.map(x=><div key={x.ch.id}><b>{x.ch.name}</b><span>{x.count} проектов • VIDEO_{String(x.from).padStart(3,'0')} → VIDEO_{String(x.to).padStart(3,'0')}</span><small>по {x.ch.cadenceDays} дн. • {x.ch.minTracks} треков/проект</small></div>)}</div><footer><button onClick={()=>setPlanOpen(false)}>Отмена</button><button className="primary" onClick={createExact}>Создать {preview.reduce((n,x)=>n+x.count,0)} проектов</button></footer></section></div>}
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:2:import {buildYoutubeQuotaPlan,youtubeQuotaCost} from './youtubeQuota';
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:4:describe('YouTube quota planner',()=>{
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:5: it('prices one combined metadata+schedule write conservatively',()=>{
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:6:  expect(youtubeQuotaCost('youtube_update_existing_video',{}, {metadataAccepted:true,scheduleRequested:true,scheduleAccepted:true,skipped:false})).toBe(51);
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:7:  expect(youtubeQuotaCost('youtube_update_existing_video',{}, {skipped:true})).toBe(1);
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:9: it('splits 100 channels by the remaining daily quota',()=>{
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:10:  const plan=buildYoutubeQuotaPlan(100,30,{ptDate:'2026-09-01',limit:10000,used:0,calls:0});
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:16: it('shows zero capacity after quota is exhausted',()=>{
.vyron-v110-audit/src/youtubeQuotaPlanner.test.ts:17:  const plan=buildYoutubeQuotaPlan(30,30,{ptDate:'2026-09-01',limit:10000,used:10000,calls:1});
.vyron-v110-audit/src/channelRunway.test.ts:2:import type {Channel,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/channelRunway.test.ts:7:  quotaRiskCount,
.vyron-v110-audit/src/channelRunway.test.ts:17:  upsertChannelRunwayFromYoutube
.vyron-v110-audit/src/channelRunway.test.ts:33:const scheduled=(iso:string):YoutubeExistingVideo=>({
.vyron-v110-audit/src/channelRunway.test.ts:40:    const record=deriveRunwayRecord(channel(),[scheduled('2026-12-01T04:00:00+07:00')],new Date('2026-09-02T08:00:00Z'),'2026-09-02T08:00:00Z',true);
.vyron-v110-audit/src/channelRunway.test.ts:41:    expect(record.scheduledUntil).toBe('2026-12-01');
.vyron-v110-audit/src/channelRunway.test.ts:47:  it('counts only true YouTube Scheduled private + future publishAt',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:49:    const good=scheduled('2026-10-01T04:00:00+07:00');
.vyron-v110-audit/src/channelRunway.test.ts:50:    const malformedPublic={...scheduled('2026-12-01T04:00:00+07:00'),id:'public',privacyStatus:'public' as const};
.vyron-v110-audit/src/channelRunway.test.ts:51:    const malformedUnlisted={...scheduled('2026-11-01T04:00:00+07:00'),id:'unlisted',privacyStatus:'unlisted' as const};
.vyron-v110-audit/src/channelRunway.test.ts:52:    const past=scheduled('2026-09-01T04:00:00+07:00');
.vyron-v110-audit/src/channelRunway.test.ts:54:    expect(record.scheduledVideoCount).toBe(1);
.vyron-v110-audit/src/channelRunway.test.ts:55:    expect(record.scheduledUntil).toBe('2026-10-01');
.vyron-v110-audit/src/channelRunway.test.ts:68:  it('quota risk is calculated from local capacity only',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:71:      deriveRunwayRecord(channel({id:'a',name:'A'}),[scheduled('2026-09-03T04:00:00+07:00')],now,now.toISOString(),true),
.vyron-v110-audit/src/channelRunway.test.ts:72:      deriveRunwayRecord(channel({id:'b',name:'B'}),[scheduled('2026-09-04T04:00:00+07:00')],now,now.toISOString(),true),
.vyron-v110-audit/src/channelRunway.test.ts:73:      deriveRunwayRecord(channel({id:'c',name:'C'}),[scheduled('2026-09-20T04:00:00+07:00')],now,now.toISOString(),true)
.vyron-v110-audit/src/channelRunway.test.ts:75:    expect(quotaRiskCount(rows,0,1)).toBe(2);
.vyron-v110-audit/src/channelRunway.test.ts:79:describe('Channel Runway persistence and 06:00 KRAT scheduler',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:80:  it('persists scheduledUntil across restart-like reload',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:83:    upsertChannelRunwayFromYoutube(channel(),[scheduled('2026-12-01T04:00:00+07:00')],now,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:85:    expect(loaded.channels.neon.scheduledUntil).toBe('2026-12-01');
.vyron-v110-audit/src/channelRunway.test.ts:87:    expect(recalculated.channels.neon.scheduledUntil).toBe('2026-12-01');
.vyron-v110-audit/src/channelRunway.test.ts:97:    expect(result.channels.neon.scheduledUntil).toBeUndefined();
.vyron-v110-audit/src/channelRunway.test.ts:102:    const baseline=scheduled('2026-10-01T04:00:00+07:00');
.vyron-v110-audit/src/channelRunway.test.ts:103:    const draft=scheduled('2026-12-01T04:00:00+07:00');
.vyron-v110-audit/src/channelRunway.test.ts:108:    expect(result.channels.neon.scheduledUntil).toBe('2026-10-01');
.vyron-v110-audit/src/channelRunway.test.ts:112:  it('explicit Channel Runway YouTube sync stays authoritative over opaque Existing Videos cache',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:115:    upsertChannelRunwayFromYoutube(channel(),[scheduled('2026-12-01T04:00:00+07:00')],now,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:116:    const stale=scheduled('2026-10-01T04:00:00+07:00');
.vyron-v110-audit/src/channelRunway.test.ts:119:    expect(result.channels.neon.scheduledUntil).toBe('2026-12-01');
.vyron-v110-audit/src/youtubeQuota.test.ts:2:import {isYoutubeQuotaError} from './youtubeQuota';
.vyron-v110-audit/src/youtubeQuota.test.ts:4:describe('YouTube quota guard',()=>{
.vyron-v110-audit/src/youtubeQuota.test.ts:5:  it('detects real quotaExceeded errors',()=>{
.vyron-v110-audit/src/youtubeQuota.test.ts:6:    expect(isYoutubeQuotaError(new Error('The request cannot be completed because you have exceeded your quota. [quotaExceeded]'))).toBe(true);
.vyron-v110-audit/src/youtubeQuota.test.ts:7:    expect(isYoutubeQuotaError('YOUTUBE_QUOTA_PAUSED: daily quota')).toBe(true);
.vyron-v110-audit/src/youtubeQuota.test.ts:9:  it('does not classify ordinary YouTube errors as quota',()=>{
.vyron-v110-audit/src/youtubeQuota.test.ts:10:    expect(isYoutubeQuotaError(new Error('invalidPublishAt'))).toBe(false);
.vyron-v110-audit/src/youtubeQuota.test.ts:11:    expect(isYoutubeQuotaError(new Error('network timeout'))).toBe(false);
.vyron-v110-audit/src/api.ts:8:import type { AppState, ChannelAnalytics, Competitor, Diagnostics, InboxScan, LicenseStatus, VideoJob, YoutubeProfile } from './types';
.vyron-v110-audit/src/api.ts:9:import {recordYoutubeCommand,youtubeGuardedCall} from './youtubeQuota';
.vyron-v110-audit/src/api.ts:11:export type AiMetadata={title:string;description:string;tags:string[]};
.vyron-v110-audit/src/api.ts:12:export type YoutubeUploadResult={videoId?:string;channelId?:string;channelTitle?:string;scheduled:boolean};
.vyron-v110-audit/src/api.ts:14:export type YoutubeProfileHealth={ok:boolean;status:string;channelId?:string;channelTitle?:string;thumbnail?:string;expiresAt?:number;analyticsAuthorized?:boolean;monetaryAuthorized?:boolean;error?:string};
.vyron-v110-audit/src/api.ts:15:export type ExistingVideoSyncResult={channelId?:string;channelTitle?:string;youtubeFound:number;received:number;requested:number;privateCount:number;publicCount:number;scheduledCount:number;unlistedCount?:number;complete:boolean;videos:import('./types').YoutubeExistingVideo[]};
.vyron-v110-audit/src/api.ts:16:export type CompetitorCandidate={channelId:string;name:string;url:string;thumbnail?:string;subscribers?:number;views?:number;videos?:number;similarity:number};
.vyron-v110-audit/src/api.ts:17:const ytInvoke=<T>(command:string,args?:Record<string,unknown>)=>youtubeGuardedCall(async()=>{const result=await invoke<T>(command,args);recordYoutubeCommand(command,args,result);return result});
.vyron-v110-audit/src/api.ts:34:  ensureChannelInbox:(workspace:string,channelName:string)=>invoke<{root:string;music:string;images:string;metadata:string}>('ensure_channel_inbox',{workspace,channelName}),
.vyron-v110-audit/src/api.ts:38:  writeJobMetadata:(jobFolder:string,title:string,description:string,tags:string[],publishAt:string|undefined,source:string)=>invoke<void>('write_job_metadata',{jobFolder,title,description,tags,publishAt,source}),
.vyron-v110-audit/src/api.ts:44:  aiGenerateMetadata:(apiKey:string,model:string,channelName:string,genre:string,language:string,country:string,videoNumber:number,topic:string|undefined,prompt:string|undefined)=>invoke<AiMetadata>('ai_generate_metadata',{apiKey,model,channelName,genre,language,country,videoNumber,topic,prompt}),
.vyron-v110-audit/src/api.ts:45:  youtubeStats:(apiKey:string,channelId:string)=>ytInvoke<any>('youtube_channel_stats',{apiKey,channelId}),
.vyron-v110-audit/src/api.ts:46:  youtubeAnalytics:(profileId:string,days=28,offsetDays=0,allTime=false)=>ytInvoke<ChannelAnalytics&{publicStats?:any}>('youtube_channel_analytics',{profileId,days,offsetDays,allTime}),
.vyron-v110-audit/src/api.ts:47:  youtubeCompetitorSnapshot:(profileId:string,channelRef:string)=>ytInvoke<Partial<Competitor>&{channelId:string}>('youtube_competitor_snapshot',{profileId,channelRef}),
.vyron-v110-audit/src/api.ts:48:  youtubeDiscoverCompetitors:(profileId:string,maxResults=8)=>ytInvoke<CompetitorCandidate[]>('youtube_discover_competitors',{profileId,maxResults}),
.vyron-v110-audit/src/api.ts:49:  youtubeProfiles:()=>invoke<YoutubeProfile[]>('youtube_oauth_profiles'),
.vyron-v110-audit/src/api.ts:50:  youtubeGoogleConfig:()=>invoke<GoogleConfigStatus>('youtube_google_config_status'),
.vyron-v110-audit/src/api.ts:51:  youtubeImportGoogleConfig:(jsonText:string,apiKey='')=>invoke<GoogleConfigStatus>('youtube_google_config_import',{jsonText,apiKey}),
.vyron-v110-audit/src/api.ts:52:  youtubeOauthBrowsers:()=>invoke<{id:string;label:string;available:boolean}[]>('youtube_oauth_browsers'),
.vyron-v110-audit/src/api.ts:53:  youtubeConnectGlobal:(browser='default')=>invoke<YoutubeProfile>('youtube_oauth_connect_global',{browser}),
.vyron-v110-audit/src/api.ts:54:  youtubeProfileHealth:(profileId:string)=>ytInvoke<YoutubeProfileHealth>('youtube_oauth_profile_health',{profileId}),
.vyron-v110-audit/src/api.ts:55:  youtubeConnect:(clientId:string,clientSecret:string,browser='default')=>invoke<YoutubeProfile>('youtube_oauth_connect',{clientId,clientSecret,browser}),
.vyron-v110-audit/src/api.ts:56:  youtubeDisconnect:(profileId:string)=>invoke<void>('youtube_oauth_disconnect',{profileId}),
.vyron-v110-audit/src/api.ts:57:  youtubeUpload:(profileId:string,jobId:string,filePath:string,title:string,description:string,tags:string[],publishAt:string|undefined,categoryId:string)=>ytInvoke<YoutubeUploadResult>('youtube_upload_video',{profileId,jobId,filePath,title,description,tags,publishAt,categoryId}),
.vyron-v110-audit/src/api.ts:58:  youtubeListExisting:(profileId:string,maxResults=30)=>ytInvoke<ExistingVideoSyncResult>('youtube_list_existing_videos',{profileId,maxResults}),
.vyron-v110-audit/src/api.ts:59:  youtubeBackupExisting:(profileId:string,videos:any[])=>ytInvoke<{path:string;count:number}>('youtube_backup_existing_videos',{profileId,videos}),
.vyron-v110-audit/src/api.ts:60:  youtubeCacheThumbnail:(videoId:string,primary?:string)=>invoke<string>('youtube_cache_thumbnail',{videoId,primary}),
.vyron-v110-audit/src/api.ts:61:  youtubeUpdateExisting:(profileId:string,videoId:string,title:string,description:string,tags:string[],publishAt?:string,privacyStatus?:string)=>ytInvoke<{id:string;verified:boolean;metadataAccepted?:boolean;metadataVerified:boolean;metadataVerifyPending?:boolean;scheduleRequested:boolean;scheduleAccepted?:boolean;scheduleVerified:boolean;scheduleVerifyPending?:boolean;scheduleError?:string|null;skipped?:boolean;appliedTags?:number}>('youtube_update_existing_video',{profileId,videoId,title,description,tags,publishAt,privacyStatus}),
.vyron-v110-audit/src/api.ts:62:  onYoutubeProgress:(cb:(data:{jobId:string;progress:number})=>void)=>listen<{jobId:string;progress:number}>('youtube-upload-progress',e=>cb(e.payload)),
.vyron-v110-audit/src/PublisherOS.tsx:5:import {parseMetadataFile,type ImportedMetadata} from './metadata';
.vyron-v110-audit/src/PublisherOS.tsx:9:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужна картинка',WAITING_MUSIC:'Ждёт музыку',READY_RENDER:'Готов к рендеру',RENDERING:'Рендерится',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загружается',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/PublisherOS.tsx:11:export function PublisherOS(){
.vyron-v110-audit/src/PublisherOS.tsx:13: const [rows,setRows]=useState<ImportedMetadata[]>([]),[files,setFiles]=useState<string[]>([]),[metaChannel,setMetaChannel]=useState(channels[0]?.id||''),[busy,setBusy]=useState(false);const input=useRef<HTMLInputElement>(null);
.vyron-v110-audit/src/PublisherOS.tsx:14: const ready=jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status));
.vyron-v110-audit/src/PublisherOS.tsx:15: async function read(fl:FileList|null){if(!fl?.length)return;const out:ImportedMetadata[]=[],names:string[]=[];for(const f of Array.from(fl)){const docx=f.name.toLowerCase().endsWith('.docx');const text=docx?(await mammoth.extractRawText({arrayBuffer:await f.arrayBuffer()})).value:await f.text();out.push(...parseMetadataFile(f.name,text));names.push(f.name)}setRows(out);setFiles(names);toast(`Метаданные: ${out.length} записей`)}
.vyron-v110-audit/src/PublisherOS.tsx:16: async function apply(){const used=new Set<string>();let ok=0,miss=0;for(let i=0;i<rows.length;i++){const r=rows[i],named=r.channel?channels.find(c=>c.name.toLowerCase()===r.channel?.toLowerCase()||c.id===r.channel):undefined,channelId=named?.id||metaChannel;const pool=useApp.getState().jobs.filter(j=>(!channelId||j.channelId===channelId)&&!used.has(j.id)).sort((a,b)=>a.number-b.number);const j=r.number?pool.find(x=>x.number===r.number):pool[0];if(!j){miss++;continue}used.add(j.id);const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}toast(`Применено: ${ok}${miss?` • не найдено ${miss}`:''}`)}
.vyron-v110-audit/src/PublisherOS.tsx:17: async function upload(job:VideoJob){const c=channels.find(x=>x.id===job.channelId);if(!c?.youtubeProfileId){toast(`У ${c?.name||'канала'} не привязан YouTube OAuth`);return false}if(!job.finalPath){toast('final.mp4 не найден');return false}patchJob(job.id,{status:'UPLOADING',uploadProgress:0,error:undefined});try{const r=await api.youtubeUpload(c.youtubeProfileId,job.id,job.finalPath,job.title,job.description,job.tags,job.publishAt,settings.youtubeCategoryId);if(!r.videoId)throw new Error('YouTube не вернул video ID');patchJob(job.id,{status:'SCHEDULED',youtubeVideoId:r.videoId,uploadProgress:100,uploadedAt:new Date().toISOString()});return true}catch(e){patchJob(job.id,{status:'ERROR',error:String(e)});toast(String(e));return false}}
.vyron-v110-audit/src/PublisherOS.tsx:18: async function uploadAll(){setBusy(true);let n=0;try{for(const j of useApp.getState().jobs.filter(j=>j.status==='READY_UPLOAD'&&j.finalPath).sort((a,b)=>(a.publishAt||'').localeCompare(b.publishAt||''))){if(await upload(j))n++}toast(`YouTube: загружено ${n}`)}finally{setBusy(false)}}
.vyron-v110-audit/src/PublisherOS.tsx:20:  <input ref={input} hidden type="file" multiple accept=".docx,.json,.txt,.csv,application/json,text/plain,text/csv" onChange={e=>{void read(e.target.files);e.currentTarget.value=''}}/>
.vyron-v110-audit/src/PublisherOS.tsx:21:  <div className="queueHeader"><div><small>UPLOAD QUEUE</small><h2>Очередь YouTube</h2><p>Новые final.mp4, загрузка, обработка, расписание и ошибки.</p></div><div className="headerActions"><button onClick={()=>input.current?.click()}>✦ Метаданные</button><button className="primary" disabled={busy||!jobs.some(j=>j.status==='READY_UPLOAD')} onClick={uploadAll}>{busy?'Загружаю…':'Загрузить все готовые'}</button></div></div>
.vyron-v110-audit/src/PublisherOS.tsx:22:  <div className="queueKpis"><span>Готовы <b>{jobs.filter(j=>j.status==='READY_UPLOAD').length}</b></span><span>Uploading <b>{jobs.filter(j=>j.status==='UPLOADING').length}</b></span><span>Scheduled <b>{jobs.filter(j=>j.status==='SCHEDULED').length}</b></span><span className={jobs.some(j=>j.status==='ERROR')?'warn':''}>Ошибки <b>{jobs.filter(j=>j.status==='ERROR').length}</b></span></div>
.vyron-v110-audit/src/PublisherOS.tsx:23:  {files.length>0&&<section className="panel metadataInline"><div><b>Metadata Inbox</b><p>{files.length} файлов • {rows.length} записей</p></div><select value={metaChannel} onChange={e=>setMetaChannel(e.target.value)}><option value="">Все каналы</option>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="primary" disabled={!rows.length} onClick={apply}>Применить</button><button onClick={()=>{setRows([]);setFiles([])}}>Очистить</button></section>}
.vyron-v110-audit/src/PublisherOS.tsx:24:  {ready.length===0?<div className="empty"><b>Нет готовых MP4</b><p>После появления final.mp4 ролик автоматически попадёт сюда.</p></div>:<div className="publishList compactPublish">{ready.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <article className="publishCard compact" key={j.id}><div className="thumb"><span>▶</span><small>VIDEO_{String(j.number).padStart(3,'0')}</small></div><div className="publishMeta"><small>{c?.name} • {j.metadataSource||'template'}</small><input value={j.title} maxLength={100} onChange={e=>patchJob(j.id,{title:e.target.value,metadataLocked:true})}/><details><summary>Описание и теги</summary><textarea value={j.description} maxLength={5000} onChange={e=>patchJob(j.id,{description:e.target.value,metadataLocked:true})}/><input value={j.tags.join(', ')} onChange={e=>patchJob(j.id,{tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean),metadataLocked:true})}/></details>{j.status==='UPLOADING'&&<div className="progress"><i style={{width:`${j.uploadProgress||0}%`}}/><span>{Math.round(j.uploadProgress||0)}%</span></div>}{j.error&&<div className="errorBox">{j.error}</div>}</div><div className="publishSide"><label>Дата<input type="datetime-local" value={j.publishAt?new Date(new Date(j.publishAt).getTime()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16):''} onChange={e=>patchJob(j.id,{publishAt:e.target.value?new Date(e.target.value).toISOString():undefined,metadataLocked:true})}/></label><span className={`publishState ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</span>{j.status==='READY_UPLOAD'&&<button className="primary" onClick={()=>upload(j)}>Загрузить</button>}{j.status==='ERROR'&&<button onClick={()=>patchJob(j.id,{status:j.finalPath?'READY_UPLOAD':'ERROR',error:undefined})}>Повторить</button>}{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div></article>})}</div>}
.vyron-v110-audit/src/types.ts:1:export type Page='dashboard'|'autopilot'|'accounts'|'channels'|'production'|'content'|'youtube'|'competitors'|'analytics'|'metadata'|'existing'|'publisher'|'settings';
.vyron-v110-audit/src/types.ts:3:export type JobStatus='NEED_IMAGE'|'WAITING_MUSIC'|'READY_RENDER'|'RENDERING'|'READY_UPLOAD'|'UPLOADING'|'SCHEDULED'|'ERROR';
.vyron-v110-audit/src/types.ts:5:export type MetadataSource='template'|'import'|'ai';
.vyron-v110-audit/src/types.ts:9:  scheduleMode?:'interval'|'pattern'; publishIntervalDays?:number; publishDays?:number; pauseDays?:number; patternAnchorDate?:string;
.vyron-v110-audit/src/types.ts:12:  youtubeProfileId?:string; youtubeChannelId?:string;
.vyron-v110-audit/src/types.ts:22:  metadataSource?:MetadataSource; metadataLocked?:boolean;
.vyron-v110-audit/src/types.ts:23:  youtubeVideoId?:string; uploadProgress?:number; uploadedAt?:string;
.vyron-v110-audit/src/types.ts:28:export type AnalyticsTopVideo={id:string;title:string;thumbnail?:string;publishedAt?:string;views:number;engagedViews?:number;watchMinutes:number;averageViewDuration:number;averageViewPercentage:number;likes:number;comments:number;shares:number;subscribersGained?:number;estimatedRevenue?:number;rpm?:number};
.vyron-v110-audit/src/types.ts:30:export type ChannelAnalytics={periodDays:number;offsetDays?:number;allTime?:boolean;updatedAt:string;views:number;engagedViews?:number;watchMinutes:number;averageViewDuration:number;averageViewPercentage:number;subscribersGained:number;subscribersLost:number;likes:number;comments:number;shares:number;monetaryAuthorized?:boolean;estimatedRevenue?:number;estimatedAdRevenue?:number;estimatedRedPartnerRevenue?:number;monetizedPlaybacks?:number;adImpressions?:number;cpm?:number;playbackBasedCpm?:number;monetaryError?:string;impressions?:number;impressionCtr?:number;daily:AnalyticsPoint[];topVideos:AnalyticsTopVideo[];trafficSources:AnalyticsBreakdown[];countries:AnalyticsBreakdown[];audience?:AnalyticsBreakdown[];channelPublishedAt?:string;channelCountry?:string;channelLanguage?:string;channelThumbnail?:string;totalVideos?:number};
.vyron-v110-audit/src/types.ts:32:export type CompetitorVideo={id:string;title:string;thumbnail?:string;publishedAt?:string;views:number;likes:number;comments:number};
.vyron-v110-audit/src/types.ts:33:export type Competitor={id:string;channelId:string;name:string;url:string;youtubeChannelId?:string;thumbnail?:string;subscribers?:number;views?:number;videos?:number;recentAverageViews?:number;lastVideoAt?:string;latestVideos?:CompetitorVideo[];history?:CompetitorSnapshot[];updatedAt?:string;note?:string;similarity?:number;source?:'auto'|'manual'};
.vyron-v110-audit/src/types.ts:35:  workspace:string; endlumePath:string; youtubeApiKey:string; autoCheckUpdates:boolean; reduceMotion:boolean; fpsMonitor:boolean;
.vyron-v110-audit/src/types.ts:36:  autopilotMode:AutopilotMode; autopilotEnabled:boolean; autoCreatePlan:boolean; autoAssignMusic:boolean; autoAssignImages:boolean; autoGenerateMetadata:boolean;
.vyron-v110-audit/src/types.ts:37:  autoQueueRender:boolean; autoOpenEndlume:boolean; autoUploadYoutube:boolean; autopilotIntervalSec:number; tracksPerVideo:number;
.vyron-v110-audit/src/types.ts:38:  openaiApiKey:string; openaiModel:string; youtubeOAuthClientId:string; youtubeCategoryId:string;
.vyron-v110-audit/src/types.ts:39:  youtubeIntelligenceAutoRefresh:boolean; youtubeIntelligenceRefreshMin:number;
.vyron-v110-audit/src/types.ts:46:export type InboxScan={root:string;music:string[];images:string[];metadata:string[]};
.vyron-v110-audit/src/types.ts:47:export type YoutubeProfile={id:string;channelId?:string;channelTitle?:string;connectedAt?:string;clientIdMasked?:string;scopes?:string[];analyticsAuthorized?:boolean;monetaryAuthorized?:boolean;preferredBrowser?:string};
.vyron-v110-audit/src/types.ts:48:export type AutopilotSummary={prepared:number;tracksMoved:number;imagesMoved:number;metadataGenerated:number;renderQueued:number;uploads:number;errors:number;notes:string[]};
.vyron-v110-audit/src/types.ts:50:export type YoutubeExistingVideo={
.vyron-v110-audit/src/types.ts:52:  publishedAt?:string; privacyStatus:string; publishAt?:string; thumbnail?:string; duration?:string; views?:number; likes?:number; comments?:number; selected:boolean;
.vyron-v110-audit/src/youtubeWorkflow.ts:1:import type {YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/youtubeWorkflow.ts:3:export type ExistingFilter='private'|'scheduled'|'public'|'unlisted'|'all';
.vyron-v110-audit/src/youtubeWorkflow.ts:5:export function existingBucket(v:Pick<YoutubeExistingVideo,'privacyStatus'|'publishAt'>):Exclude<ExistingFilter,'all'> {
.vyron-v110-audit/src/youtubeWorkflow.ts:6:  if(v.privacyStatus==='private' && Boolean(v.publishAt)) return 'scheduled';
.vyron-v110-audit/src/youtubeWorkflow.ts:12:export function matchesExistingFilter(v:YoutubeExistingVideo,filter:ExistingFilter){
.vyron-v110-audit/src/youtubeWorkflow.ts:16:export function selectedVisibleIds(videos:YoutubeExistingVideo[],filter:ExistingFilter){
.vyron-v110-audit/src/youtubeWorkflow.ts:20:export function latestPrivateIds(videos:YoutubeExistingVideo[],count:number){
.vyron-v110-audit/src/youtubeWorkflow.ts:27:export function countBuckets(videos:YoutubeExistingVideo[]){
.vyron-v110-audit/src/youtubeWorkflow.ts:28:  const out={private:0,scheduled:0,public:0,unlisted:0};
.vyron-v110-audit/src/MetadataPage.tsx:5:import {parseMetadataFile,validateSequentialMetadata,type ImportedMetadata} from './metadata';
.vyron-v110-audit/src/MetadataPage.tsx:6:import {buildExistingScheduleFromLocal,orderedExistingVideos} from './youtubeExisting';
.vyron-v110-audit/src/MetadataPage.tsx:7:import {existingBucket,latestPrivateIds,matchesExistingFilter,selectedVisibleIds,type ExistingFilter} from './youtubeWorkflow';
.vyron-v110-audit/src/MetadataPage.tsx:8:import type {VideoJob,YoutubeExistingVideo} from './types';
.vyron-v110-audit/src/MetadataPage.tsx:9:import {isYoutubeQuotaError,markYoutubeQuotaExceeded,youtubeQuotaMessage,youtubeQuotaState} from './youtubeQuota';
.vyron-v110-audit/src/MetadataPage.tsx:10:import {dateKeyLabel,generatePatternSchedule,getChannelScheduleState,krasDateKey,mergeExistingCacheVideos,replaceExistingCacheFromSync,scheduleDateLabel,scheduleDescription,scheduleModeFor,toKratLocalInput} from './channelSchedule';
.vyron-v110-audit/src/MetadataPage.tsx:14:type Target='future'|'youtube';
.vyron-v110-audit/src/MetadataPage.tsx:15:type Match={row:ImportedMetadata;job?:VideoJob;video?:YoutubeExistingVideo;index:number};
.vyron-v110-audit/src/MetadataPage.tsx:16:type ApplyIssue={videoId:string;title:string;phase:'metadata'|'schedule';error:string};
.vyron-v110-audit/src/MetadataPage.tsx:17:type ApplyReport={metadataOk:number;total:number;scheduleOk:number;scheduleTotal:number;failed:number;issues:ApplyIssue[];pausedByQuota?:boolean;pending?:number;at?:string};
.vyron-v110-audit/src/MetadataPage.tsx:18:type MetadataHistory={at:string;metadataOk:number;total:number;scheduleOk:number;scheduleTotal:number;failed:number;pausedByQuota?:boolean};
.vyron-v110-audit/src/MetadataPage.tsx:19:type MetadataDraft={version:1;updatedAt:string;target:Target;rows:ImportedMetadata[];paste:string;order:'oldest'|'newest';filter:ExistingFilter;docxStrict:boolean;start:string;cadence:number;scheduleMode?:'auto'|'manual';selectedVideos:YoutubeExistingVideo[];syncStatus:string;history:MetadataHistory[]};
.vyron-v110-audit/src/MetadataPage.tsx:23:const metaCount=(r:ImportedMetadata)=>Number(Boolean(r.title))+Number(Boolean(r.description))+Number(Boolean(r.tags?.length))+Number(Boolean(r.publishAt||r.publishTime));
.vyron-v110-audit/src/MetadataPage.tsx:25:const draftKey=(channelId:string)=>`vyron:metadata-draft:v1:${channelId}`;
.vyron-v110-audit/src/MetadataPage.tsx:26:function readDraft(channelId:string):MetadataDraft|undefined{if(!channelId)return;try{const x=JSON.parse(localStorage.getItem(draftKey(channelId))||'null');return x?.version===1?x:undefined}catch{return}}
.vyron-v110-audit/src/MetadataPage.tsx:27:function writeDraft(channelId:string,d:MetadataDraft){if(!channelId)return;try{localStorage.setItem(draftKey(channelId),JSON.stringify(d))}catch{}}
.vyron-v110-audit/src/MetadataPage.tsx:28:function compactSelected(videos:YoutubeExistingVideo[]){return videos.filter(v=>v.selected).map(v=>({...v,thumbnail:undefined,applyState:'idle' as const,error:undefined,selected:true}))}
.vyron-v110-audit/src/MetadataPage.tsx:31:export function MetadataPage(){
.vyron-v110-audit/src/MetadataPage.tsx:33: const [channelId,setChannelId]=useState(channels[0]?.id||''),[target,setTarget]=useState<Target>('youtube'),[rows,setRows]=useState<ImportedMetadata[]>([]),[paste,setPaste]=useState(''),[yt,setYt]=useState<YoutubeExistingVideo[]>([]),[busy,setBusy]=useState(false),[order,setOrder]=useState<'oldest'|'newest'>('newest'),[limit,setLimit]=useState(1000),[filter,setFilter]=useState<ExistingFilter>('private'),[docxStrict,setDocxStrict]=useState(false),[start,setStart]=useState(()=>{const d=new Date();d.setDate(d.getDate()+1);d.setHours(18,0,0,0);return local(d.toISOString())}),[cadence,setCadence]=useState(2),[scheduleMode,setScheduleMode]=useState<'auto'|'manual'>('auto'),[customPattern,setCustomPattern]=useState(false),[scheduleRevision,setScheduleRevision]=useState(0),[syncStatus,setSyncStatus]=useState(''),[applyProgress,setApplyProgress]=useState(''),[applyReport,setApplyReport]=useState<ApplyReport|null>(null),[history,setHistory]=useState<MetadataHistory[]>([]),[draftSavedAt,setDraftSavedAt]=useState('');const file=useRef<HTMLInputElement>(null),restoring=useRef(false);
.vyron-v110-audit/src/MetadataPage.tsx:34: const channel=channels.find(c=>c.id===channelId),profileId=channel?.youtubeProfileId||'';
.vyron-v110-audit/src/MetadataPage.tsx:35: useEffect(()=>{restoring.current=true;const d=readDraft(channelId);setCadence(d?.cadence||channel?.cadenceDays||2);setScheduleMode(d?.scheduleMode||'auto');setTarget(d?.target||'youtube');setScheduleRevision(x=>x+1);setRows(d?.rows||[]);setPaste(d?.paste||'');setYt((d?.selectedVideos||[]).map(v=>({...v,selected:true})));setOrder(d?.order||'newest');setFilter(d?.filter||'private');setDocxStrict(Boolean(d?.docxStrict));if(d?.start)setStart(d.start);setSyncStatus(d?`↻ Черновик восстановлен • ${new Date(d.updatedAt).toLocaleString('ru-RU')}`:'');setHistory(d?.history||[]);setDraftSavedAt(d?.updatedAt||'');setApplyProgress('');setApplyReport(null);queueMicrotask(()=>{restoring.current=false})},[channelId]);
.vyron-v110-audit/src/MetadataPage.tsx:36: useEffect(()=>{if(restoring.current||!channelId)return;const updatedAt=new Date().toISOString();const d:MetadataDraft={version:1,updatedAt,target,rows,paste,order,filter,docxStrict,start,cadence,scheduleMode,selectedVideos:compactSelected(yt),syncStatus,history};const t=window.setTimeout(()=>{writeDraft(channelId,d);setDraftSavedAt(updatedAt)},180);return()=>window.clearTimeout(t)},[channelId,target,rows,paste,order,filter,docxStrict,start,cadence,scheduleMode,yt,syncStatus,history]);
.vyron-v110-audit/src/MetadataPage.tsx:37: async function readFiles(fl:FileList|null){if(!fl?.length)return;const out:ImportedMetadata[]=[];let strict=false;for(const f of Array.from(fl)){const docx=f.name.toLowerCase().endsWith('.docx');strict=strict||docx;const text=docx?(await mammoth.extractRawText({arrayBuffer:await f.arrayBuffer()})).value:await f.text();out.push(...parseMetadataFile(f.name,text))}out.sort((a,b)=>(a.number??Number.MAX_SAFE_INTEGER)-(b.number??Number.MAX_SAFE_INTEGER));if(strict&&target==='youtube'){const count=yt.filter(v=>v.selected).length;if(count>0){const check=validateSequentialMetadata(out,count);if(!check.ok){notifyWarning('DOCX не подходит к выбранным видео',out.length<count?`В Word ${out.length} записей, а выбрано ${count} видео. Нужно минимум ${count} записей.`:`Проверь нумерацию VIDEO 001–${String(count).padStart(3,'0')}${check.missing.length?`. Нет: ${check.missing.join(', ')}`:''}${check.duplicates.length?`. Дубли: ${check.duplicates.join(', ')}`:''}`);return}}}setRows(out);setDocxStrict(strict);if(strict){const count=yt.filter(v=>v.selected).length;notifySuccess('Word-файл принят',count?`${out.length} записей в DOCX • выбрано ${count} видео.${out.length>count?` Лишние ${out.length-count} записей пока не применяются.`:''}`:`Распознано ${out.length} записей. Теперь выберите до ${out.length} видео.`,{operationId:`docx-loaded:${channelId}:${out.length}:${Date.now()}`})}else toast(`Метаданные: распознано ${out.length}`)}
.vyron-v110-audit/src/MetadataPage.tsx:38: function parsePaste(){const out=parseMetadataFile('GPT.txt',paste);setRows(out);setDocxStrict(false);toast(out.length?`GPT: распознано ${out.length} записей`:'Не удалось распознать метаданные')}
.vyron-v110-audit/src/MetadataPage.tsx:39: async function loadYoutube(){if(!profileId){setSyncStatus('OAuth не подключён');return}if(youtubeQuotaState().blocked){setSyncStatus(youtubeQuotaMessage());return}setBusy(true);try{const selectedIds=new Set(yt.filter(v=>v.selected).map(v=>v.id));const r=await api.youtubeListExisting(profileId,Math.max(1,Math.min(5000,limit)));const videos=(r.videos||[]).map(v=>({...v,selected:selectedIds.has(v.id)}));setYt(videos);replaceExistingCacheFromSync(channelId,videos,r);setScheduleRevision(x=>x+1);setSyncStatus(r.complete?`✓ ${r.received}/${r.youtubeFound} получено точно`:`⚠ ${r.received}/${Math.min(r.youtubeFound,r.requested)} — синхронизация неполная`);notifySuccess('Данные YouTube обновлены',`Получено ${r.received}/${Math.min(r.youtubeFound,r.requested)} видео.`,{operationId:`youtube-sync:${channelId}:${Date.now()}`})}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setSyncStatus(youtubeQuotaMessage());notifyWarning('YouTube quota исчерпана','Синхронизация поставлена на паузу.')}else{setSyncStatus(String(e));notifyError('Не удалось обновить данные YouTube',String(e))}}finally{setBusy(false)}}
.vyron-v110-audit/src/MetadataPage.tsx:40: const future=useMemo(()=>jobs.filter(j=>j.channelId===channelId&&j.status!=='SCHEDULED').sort((a,b)=>a.number-b.number),[jobs,channelId]);
.vyron-v110-audit/src/MetadataPage.tsx:42: const scheduleState=useMemo(()=>channel?getChannelScheduleState(channelId,channel):undefined,[channelId,channel?.id,channel?.cadenceDays,channel?.scheduleMode,channel?.publishIntervalDays,channel?.publishDays,channel?.pauseDays,channel?.patternAnchorDate,channel?.publishHour,channel?.publishMinute,scheduleRevision,yt.length]);
.vyron-v110-audit/src/MetadataPage.tsx:43: const strategy=channel?scheduleModeFor(channel):'interval';
.vyron-v110-audit/src/MetadataPage.tsx:45: const schedulePairs=useMemo(()=>selectedYt.map((v,i)=>({v,row:rows[i]})).filter(x=>scheduleMode==='manual'||!x.v.publishAt),[selectedYt,rows,scheduleMode]);
.vyron-v110-audit/src/MetadataPage.tsx:46: const schedulePairIds=useMemo(()=>schedulePairs.map(x=>x.v.id),[schedulePairs]);
.vyron-v110-audit/src/MetadataPage.tsx:47: const effectiveStart=scheduleMode==='auto'?(scheduleState?.nextAvailableAt?toKratLocalInput(scheduleState.nextAvailableAt):''):start;
.vyron-v110-audit/src/MetadataPage.tsx:48: const patternGenerated=useMemo(()=>strategy==='pattern'&&channel?.patternAnchorDate?generatePatternSchedule(channel,yt,schedulePairs.length,schedulePairIds):{dates:[],calendar:[]},[strategy,channel?.id,channel?.patternAnchorDate,channel?.publishDays,channel?.pauseDays,channel?.publishHour,channel?.publishMinute,yt,schedulePairs.length,schedulePairIds.join('|'),scheduleRevision]);
.vyron-v110-audit/src/MetadataPage.tsx:49: const schedulePreview=useMemo(()=>strategy==='pattern'?schedulePairs.map((x,i)=>({...x.v,publishAt:patternGenerated.dates[i]})).filter(v=>Boolean(v.publishAt)):effectiveStart?buildExistingScheduleFromLocal(schedulePairs.map(x=>x.v),effectiveStart,cadence,schedulePairs.map(x=>x.row)):[],[strategy,schedulePairs,effectiveStart,cadence,patternGenerated.dates]);
.vyron-v110-audit/src/MetadataPage.tsx:50: const previewFirst=schedulePreview[0]?.publishAt,previewLast=schedulePreview.length?schedulePreview[schedulePreview.length-1]?.publishAt:undefined;
.vyron-v110-audit/src/MetadataPage.tsx:51: const defaultPublishTime=scheduleState?.defaultPublishTime||`${String(channel?.publishHour||0).padStart(2,'0')}:${String(channel?.publishMinute||0).padStart(2,'0')}`;
.vyron-v110-audit/src/MetadataPage.tsx:52: function changeCadence(value:number){const next=Math.max(1,Math.min(30,Math.floor(value||1)));setCadence(next);if(channel)updateChannel(channel.id,{scheduleMode:'interval',publishIntervalDays:next,cadenceDays:next});setCustomPattern(false);setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:53: function selectInterval(days:number){if(!channel)return;setCadence(days);setCustomPattern(false);updateChannel(channel.id,{scheduleMode:'interval',publishIntervalDays:days,cadenceDays:days});setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:54: function selectPattern(publishDays=3,pauseDays=1,custom=false){if(!channel)return;const before=getChannelScheduleState(channelId,channel);const anchor=channel.patternAnchorDate||krasDateKey(before.nextAvailableAt)||start.slice(0,10);updateChannel(channel.id,{scheduleMode:'pattern',publishDays:Math.max(1,publishDays),pauseDays:Math.max(1,pauseDays),patternAnchorDate:anchor});setCustomPattern(custom);setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:55: function changePattern(publishDays:number,pauseDays:number){if(!channel)return;updateChannel(channel.id,{scheduleMode:'pattern',publishDays:Math.max(1,Math.min(30,Math.floor(publishDays||1))),pauseDays:Math.max(1,Math.min(30,Math.floor(pauseDays||1))),patternAnchorDate:channel.patternAnchorDate||start.slice(0,10)});setCustomPattern(true);setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:56: function changePatternAnchor(value:string){if(!channel||!value)return;updateChannel(channel.id,{scheduleMode:'pattern',patternAnchorDate:value});setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:57: function changeDefaultTime(value:string){const m=value.match(/^(\d{2}):(\d{2})$/);if(!m||!channel)return;updateChannel(channel.id,{publishHour:+m[1],publishMinute:+m[2]});setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:66: async function applyFuture(){setBusy(true);let ok=0;try{for(const m of matches){const j=m.job,r=m.row;if(!j)continue;const p:Partial<VideoJob>={metadataSource:'import',metadataLocked:true};if(r.title)p.title=r.title;if(r.description)p.description=r.description;if(r.tags?.length)p.tags=r.tags;if(r.publishAt)p.publishAt=r.publishAt;patchJob(j.id,p);const after={...j,...p};if(j.folder)await api.writeJobMetadata(j.folder,after.title,after.description,after.tags,after.publishAt,'import');ok++}notifySuccess('Метаданные сохранены',`${ok} из ${rows.length} будущих видео обновлены.`,{operationId:`future-metadata:${channelId}:${Date.now()}`})}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/MetadataPage.tsx:67: async function applyYoutube(){
.vyron-v110-audit/src/MetadataPage.tsx:68:  if(!profileId||!selectedYt.length)return;if(docxStrict&&rows.length<selectedYt.length){notifyWarning('Недостаточно записей в Word',`В DOCX ${rows.length} записей, а выбрано ${selectedYt.length} видео. Уменьшите выбор видео или загрузите DOCX минимум на ${selectedYt.length} записей.`);return}if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}
.vyron-v110-audit/src/MetadataPage.tsx:69:  if(strategy==='pattern'&&schedulePairs.length>0&&!channel?.patternAnchorDate){notifyWarning('Не задано начало цикла','Выберите дату начала графика публикаций.');return}if(strategy==='interval'&&scheduleMode==='auto'&&schedulePairs.length>0&&!effectiveStart){notifyWarning('Нет будущего расписания','На канале нет будущих отложенных публикаций. Выберите первую дату вручную.');return}
.vyron-v110-audit/src/MetadataPage.tsx:70:  setBusy(true);setApplyReport(null);let metadataOk=0,scheduleOk=0,scheduleTotal=0,failed=0,pausedByQuota=false;const issues:ApplyIssue[]=[];const completedIds=new Set<string>();const cacheUpdates:YoutubeExistingVideo[]=[];
.vyron-v110-audit/src/MetadataPage.tsx:72:    const scheduled=strategy==='pattern'?schedulePairs.map((x,i)=>({...x.v,publishAt:patternGenerated.dates[i]})).filter(v=>Boolean(v.publishAt)):effectiveStart?buildExistingScheduleFromLocal(schedulePairs.map(x=>x.v),effectiveStart,cadence,schedulePairs.map(x=>x.row)):[];const scheduleById=new Map(scheduled.map(v=>[v.id,v.publishAt]));await api.youtubeBackupExisting(profileId,selectedYt);let done=0;
.vyron-v110-audit/src/MetadataPage.tsx:74:      const basePublishAt=scheduleById.get(v.id)||v.publishAt;const publishAt=v.privacyStatus==='private'?resolveKratPublishAt(r.publishAt,basePublishAt):v.publishAt;
.vyron-v110-audit/src/MetadataPage.tsx:75:      const title=r.title||v.title,description=r.description||v.description,tags=r.tags?.length?r.tags:v.tags;const result=await api.youtubeUpdateExisting(profileId,v.id,title,description,tags,publishAt,v.privacyStatus);
.vyron-v110-audit/src/MetadataPage.tsx:76:      if(result.metadataAccepted===false){failed++;issues.push({videoId:v.id,title:v.title,phase:'metadata',error:'YouTube не принял title/description/tags'});continue}metadataOk++;let updated:YoutubeExistingVideo={...v,title,description,tags};
.vyron-v110-audit/src/MetadataPage.tsx:77:      if(result.scheduleRequested){scheduleTotal++;if(result.scheduleAccepted!==false){scheduleOk++;completedIds.add(v.id);updated={...updated,publishAt}}else{failed++;const err=result.scheduleError||'YouTube не принял расписание';if(isYoutubeQuotaError(err)){markYoutubeQuotaExceeded(err);pausedByQuota=true;issues.push({videoId:v.id,title:v.title,phase:'schedule',error:youtubeQuotaMessage()});cacheUpdates.push(updated);break}issues.push({videoId:v.id,title:v.title,phase:'schedule',error:err})}}
.vyron-v110-audit/src/MetadataPage.tsx:79:    }catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);pausedByQuota=true;issues.push({videoId:v.id,title:v.title,phase:'metadata',error:youtubeQuotaMessage()});break}failed++;issues.push({videoId:v.id,title:v.title,phase:'metadata',error:String(e)})}}
.vyron-v110-audit/src/MetadataPage.tsx:80:    if(cacheUpdates.length){mergeExistingCacheVideos(channelId,cacheUpdates);const byId=new Map(cacheUpdates.map(v=>[v.id,v]));setYt(prev=>prev.map(v=>byId.get(v.id)?{...byId.get(v.id)!,selected:v.selected}:v));setScheduleRevision(x=>x+1)}
.vyron-v110-audit/src/MetadataPage.tsx:81:    let pending=0;if(pausedByQuota){const remainingMatches=matches.filter(x=>x.video&&!completedIds.has(x.video.id));const pendingIds=new Set(remainingMatches.map(x=>x.video!.id));const pendingRows=remainingMatches.map(x=>x.row);setRows(pendingRows);setYt(prev=>prev.map(v=>({...v,selected:pendingIds.has(v.id)})));pending=pendingIds.size;setSyncStatus(`⏸ Квота YouTube исчерпана • сохранено ${completedIds.size} • осталось ${pending}`)}
.vyron-v110-audit/src/MetadataPage.tsx:82:    const report:ApplyReport={metadataOk,total:matched,scheduleOk,scheduleTotal,failed,issues,pausedByQuota,pending,at:new Date().toISOString()};setApplyReport(report);const h:MetadataHistory={at:report.at!,metadataOk,total:matched,scheduleOk,scheduleTotal,failed,pausedByQuota};setHistory(x=>[h,...x].slice(0,20));
.vyron-v110-audit/src/MetadataPage.tsx:83:    if(pausedByQuota)notifyWarning('Выполнение приостановлено',`Квота YouTube закончилась. Осталось ${pending}; выбранная пачка сохранена.`);
.vyron-v110-audit/src/MetadataPage.tsx:84:    else if(metadataOk===matched&&scheduleOk===scheduleTotal&&failed===0){const after=channel?getChannelScheduleState(channelId,channel):undefined;const graph=channel?scheduleDescription(channel):'';notifySuccess(scheduleTotal?'Расписание применено':'Метаданные применены',`${metadataOk} из ${matched} видео успешно обновлены.${scheduleTotal&&graph?` Режим: ${graph}.`:''}${previewFirst?` Первая: ${scheduleDateLabel(previewFirst)}.`:''}${previewLast?` Последняя: ${scheduleDateLabel(previewLast)}.`:''}${after?.scheduledUntil?` Канал запланирован до ${scheduleDateLabel(after.lastScheduledAt)}.`:''}`,{operationId:`metadata-apply:${channelId}:${report.at}`})}
.vyron-v110-audit/src/MetadataPage.tsx:85:    else notifyWarning('Выполнено частично',`Обновлено ${metadataOk} из ${matched}. Не удалось: ${Math.max(failed,matched-metadataOk)}.`,{operationId:`metadata-partial:${channelId}:${report.at}`});
.vyron-v110-audit/src/MetadataPage.tsx:86:  }catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setSyncStatus(youtubeQuotaMessage());notifyWarning('YouTube quota исчерпана','Черновик сохранён для продолжения.')}else{const issue={videoId:'BATCH',title:'Пакетная операция',phase:'metadata' as const,error:String(e)};setApplyReport({metadataOk,total:matched,scheduleOk,scheduleTotal,failed:failed+1,issues:[...issues,issue],at:new Date().toISOString()});notifyError('Не удалось применить метаданные',String(e))}}
.vyron-v110-audit/src/MetadataPage.tsx:89: async function apply(){if(!rows.length){toast('Сначала вставь или загрузи метаданные');return}if(target==='youtube'&&!selectedYt.length){toast('Сначала выбери видео');return}if(target==='youtube'&&rows.length<selectedYt.length){notifyWarning('Недостаточно метаданных',`Записей: ${rows.length}. Выбрано видео: ${selectedYt.length}. Метаданных должно быть не меньше количества выбранных видео.`);return}if(target==='future')await applyFuture();else await applyYoutube()}
.vyron-v110-audit/src/MetadataPage.tsx:91:  <div className="pageHeader"><div><small>METADATA HUB</small><h1>Метаданные</h1><p>DOCX, JSON, TXT или вставка из GPT → точное сопоставление с выбранными роликами. Никаких бинарных символов Word и никаких скрытых массовых изменений.</p></div><div className="headerActions"><button className="primary" disabled={busy||!rows.length||!matched||(target==='youtube'&&rows.length<selectedYt.length)} onClick={apply}>{busy?`Применяю ${applyProgress||'…'}`:`Применить ${matched}`}</button></div></div>
.vyron-v110-audit/src/MetadataPage.tsx:92:  <div className="metadataDraftBar"><span><b>Черновик сохраняется автоматически</b><small>{draftSavedAt?` • ${new Date(draftSavedAt).toLocaleString('ru-RU')}`:''}</small></span><span>{rows.length} записей • {selectedYt.length} выбранных</span>{youtubeQuotaState().blocked&&<strong>⏸ YouTube quota pause</strong>}</div>
.vyron-v110-audit/src/MetadataPage.tsx:93:  {history.length>0&&<details className="panel metadataHistory"><summary>История операций • {history.length}</summary><div>{history.slice(0,10).map((h,i)=><span key={i}><b>{new Date(h.at).toLocaleString('ru-RU')}</b><small>Метаданные {h.metadataOk}/{h.total} • расписание {h.scheduleOk}/{h.scheduleTotal}{h.pausedByQuota?' • пауза квоты':''}</small></span>)}</div></details>}
.vyron-v110-audit/src/MetadataPage.tsx:94:  {applyReport&&<section className="panel"><div className="panelHead"><div><small>YOUTUBE WRITE REPORT</small><h3>{applyReport.failed===0&&applyReport.issues.length===0?'✓ Запись подтверждена':'Результат применения'}</h3></div><button onClick={()=>setApplyReport(null)}>Закрыть</button></div><div className="syncAudit"><span>Метаданные <b>{applyReport.metadataOk}/{applyReport.total}</b></span>{applyReport.scheduleTotal>0&&<span>Расписание <b>{applyReport.scheduleOk}/{applyReport.scheduleTotal}</b></span>}<span>Полностью не записано <b>{applyReport.failed}</b></span>{applyReport.pausedByQuota&&<span className="warn">Пауза квоты • осталось <b>{applyReport.pending||0}</b></span>}</div>{applyReport.issues.length>0&&<div className="metadataPreviewList">{applyReport.issues.map((x,i)=><div className="metadataPreviewRow missing" key={`${x.videoId}-${i}`}><span><b>{x.phase==='schedule'?'РАСПИСАНИЕ':'МЕТАДАННЫЕ'} • {x.videoId}</b><small>{short(x.title,70)}</small></span><strong>{x.phase==='schedule'?'Метаданные сохранены, расписание не применено':'Видео не обновлено'}</strong><em>{x.error}</em></div>)}</div>}</section>}
.vyron-v110-audit/src/MetadataPage.tsx:95:  <input ref={file} hidden type="file" multiple accept=".docx,.json,.txt,.csv,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={e=>{void readFiles(e.target.files);e.currentTarget.value=''}}/>
.vyron-v110-audit/src/MetadataPage.tsx:96:  <div className="metadataHubGrid">
.vyron-v110-audit/src/MetadataPage.tsx:97:   <section className="panel metadataSourcePanel"><div className="panelHead"><div><small>01 • ИСТОЧНИК</small><h3>SEO pack</h3></div><button className="primary compactAction" onClick={()=>file.current?.click()}>Загрузить DOCX / файл</button></div><textarea className="metadataPaste" value={paste} onChange={e=>setPaste(e.target.value)} placeholder={'VIDEO 1\nTITLE\n...\nDESCRIPTION\n...\nTAGS\n...\nPUBLISH TIME\n04:00 KRAT\n\nVIDEO 2\n...'}/><div className="metadataPasteActions"><button disabled={!paste.trim()} onClick={parsePaste}>Разобрать текст GPT</button><button onClick={()=>{setPaste('');setRows([]);setDocxStrict(false)}}>Очистить</button><span>DOCX / JSON / TXT / CSV</span></div></section>
.vyron-v110-audit/src/MetadataPage.tsx:98:   <section className="panel metadataTargetPanel"><div className="panelHead"><div><small>02 • НАЗНАЧЕНИЕ</small><h3>Куда применить</h3></div></div><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><div className="metadataTargetSwitch"><button className={target==='future'?'active':''} onClick={()=>setTarget('future')}><b>Будущие VYRON</b><small>проекты до загрузки</small></button><button className={target==='youtube'?'active':''} onClick={()=>setTarget('youtube')}><b>Уже на YouTube</b><small>выбранные private/scheduled</small></button></div>{target==='future'?<div className="metadataTargetInfo"><strong>{future.length}</strong><span>будущих проектов</span></div>:<><div className={`profileAutoBadge ${profileId?'good':'warn'}`}><small>OAuth</small><b>{profileId?'✓ привязан автоматически':'не подключён'}</b></div><div className="metadataYoutubeLoad"><label>Лимит<input type="number" min="1" max="5000" value={limit} onChange={e=>setLimit(+e.target.value||1000)}/></label><label>Порядок<select value={order} onChange={e=>setOrder(e.target.value as any)}><option value="newest">новые → старые</option><option value="oldest">старые → новые</option></select></label><button disabled={busy||!profileId} onClick={loadYoutube}>↻ Синхронизировать</button></div><div className="metadataTargetInfo"><strong>{selectedYt.length}</strong><span>выбрано из {yt.length}</span><small>{syncStatus||'YouTube будет загружен автоматически'}</small></div></>}</section>
.vyron-v110-audit/src/MetadataPage.tsx:100:  {target==='youtube'&&yt.length>0&&<section className="panel metadataVideoPicker"><div className="panelHead"><div><small>03 • ВЫБОР ВИДЕО</small><h3>Точно видно, куда пойдёт DOCX</h3></div><div className="batchScopeActions"><button onClick={selectVisible}>Выбрать все здесь</button><button onClick={selectLatest30}>Последние 30 Private</button><button onClick={selectNone}>Снять всё</button></div></div><div className="periodTabs"><button className={filter==='private'?'active':''} onClick={()=>setFilter('private')}>PRIVATE</button><button className={filter==='scheduled'?'active':''} onClick={()=>setFilter('scheduled')}>SCHEDULED</button><button className={filter==='public'?'active':''} onClick={()=>setFilter('public')}>PUBLIC</button><button className={filter==='unlisted'?'active':''} onClick={()=>setFilter('unlisted')}>UNLISTED</button></div><div className="metadataVideoRows">{shown.slice(0,500).map(v=><button key={v.id} className={v.selected?'selected':''} onClick={()=>toggle(v.id)}><i>{v.selected?'✓':''}</i><span><b>{v.title}</b><small>{existingBucket(v).toUpperCase()} • {v.id}</small></span></button>)}</div><div className="scheduleMini scheduleSmart"><div className="scheduleSmartHead"><div><b>Расписание канала</b><small>{scheduleState?.updatedAt?`Локальные данные обновлены ${new Date(scheduleState.updatedAt).toLocaleString('ru-RU')}`:'Используются сохранённые данные канала'}</small></div></div><div className="scheduleStrategy"><small>ГРАФИК ПУБЛИКАЦИЙ</small><div className="schedulePresetButtons"><button className={strategy==='interval'&&cadence===1?'active':''} onClick={()=>selectInterval(1)}>Каждый день</button><button className={strategy==='interval'&&cadence===2?'active':''} onClick={()=>selectInterval(2)}>Через день</button><button className={strategy==='pattern'&&patternPublishDays===3&&patternPauseDays===1&&!customPattern?'active':''} onClick={()=>selectPattern(3,1,false)}>3 дня / 1 пауза</button><button className={strategy==='pattern'&&customPattern?'active':''} onClick={()=>selectPattern(patternPublishDays,patternPauseDays,true)}>Настроить</button></div>{strategy==='pattern'&&<><p>{patternPublishDays} дня подряд публикуем видео, {patternPauseDays} день без публикаций, затем цикл повторяется.</p><div className="patternSettings">{customPattern&&<><label>Дней с видео<input type="number" min="1" max="30" value={patternPublishDays} onChange={e=>changePattern(+e.target.value||1,patternPauseDays)}/></label><label>Дней паузы<input type="number" min="1" max="30" value={patternPauseDays} onChange={e=>changePattern(patternPublishDays,+e.target.value||1)}/></label></>}<label>Начало цикла<input type="date" value={channel?.patternAnchorDate||''} onChange={e=>changePatternAnchor(e.target.value)}/></label></div></>}</div><div className="scheduleModeButtons"><button className={scheduleMode==='auto'?'active':''} onClick={()=>setScheduleMode('auto')}>Продолжить расписание автоматически</button><button className={scheduleMode==='manual'?'active':''} onClick={()=>setScheduleMode('manual')}>Указать первую дату вручную</button></div>{scheduleMode==='auto'?scheduleState?.lastScheduledAt?<><div className="scheduleFacts"><span><small>Запланировано до</small><b>{scheduleDateLabel(scheduleState.lastScheduledAt)}</b></span><span><small>Следующая дата</small><b>{scheduleDateLabel(scheduleState.nextAvailableAt)}</b></span><span><small>График</small><b>{channel?scheduleDescription(channel):`каждые ${cadence} дн.`}</b></span><span><small>Время</small><b>{defaultPublishTime}</b></span></div>{schedulePreview.length>0&&<div className="scheduleBatchPreview"><b>Новая партия: {scheduleDateLabel(previewFirst)} → {scheduleDateLabel(previewLast)}</b><small>{schedulePreview.length} видео • первая свободная дата выбрана автоматически</small><div>{schedulePreview.slice(0,6).map((v,i)=><span key={v.id}>{String(i+1).padStart(3,'0')} → {scheduleDateLabel(v.publishAt)}</span>)}{schedulePreview.length>6&&<span>… ещё {schedulePreview.length-6}</span>}</div></div>}{strategy==='pattern'&&patternGenerated.calendar.length>0&&<div className="patternCalendarPreview"><b>Календарь цикла</b><div>{patternGenerated.calendar.slice(0,20).map((d,i)=><span key={`${d.date}-${i}`} className={d.kind}>{dateKeyLabel(d.date)} — {d.kind==='pause'?'ПАУЗА':d.kind==='occupied'?'ЗАНЯТО':`VIDEO ${String(patternGenerated.calendar.slice(0,i+1).filter(x=>x.kind==='video').length).padStart(3,'0')}`}</span>)}{patternGenerated.calendar.length>20&&<span>… ещё {patternGenerated.calendar.length-20} дней</span>}</div></div>}</>:<div className="scheduleNoFuture"><b>На канале нет будущих отложенных публикаций</b><span>Выберите «Указать первую дату вручную», чтобы задать начало расписания.</span></div>:<label>Первая дата<input type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/></label>}<label>Публиковать каждые N дней<input type="number" min="1" max="30" value={cadence} onChange={e=>changeCadence(+e.target.value||1)}/></label><label>Время по умолчанию<input type="time" value={defaultPublishTime} onChange={e=>changeDefaultTime(e.target.value)}/></label><small>Если DOCX содержит PUBLISH TIME, для конкретного видео используется его время. Даты продолжают расписание текущего канала.</small></div></section>}
.vyron-v110-audit/src/MetadataPage.tsx:101:  <section className="panel metadataPreviewPanel"><div className="panelHead"><div><small>{target==='youtube'?'04':'03'} • ПРЕДПРОСМОТР</small><h3>{rows.length?`${rows.length} записей • сопоставлено ${matched}`:'Метаданные ещё не загружены'}</h3><p>{target==='youtube'?`Выбрано ${selectedYt.length}. Для применения должно быть точное 1:1.`:'Ничего не изменяется до нажатия «Применить».'}</p></div>{rows.length>0&&<div className="metadataStats"><span><b>{rows.filter(r=>r.title).length}</b> названий</span><span><b>{rows.filter(r=>r.description).length}</b> описаний</span><span><b>{rows.filter(r=>r.tags?.length).length}</b> тегов</span><span><b>{rows.filter(r=>r.publishAt||r.publishTime).length}</b> времени/дат</span></div>}</div>{rows.length===0?<div className="empty"><b>Загрузи SEO DOCX</b><p>После импорта здесь будет видно `VIDEO N → конкретный YouTube ролик`.</p></div>:<div className="metadataPreviewList">{matches.slice(0,200).map((m,i)=>{const dest=target==='future'?m.job:m.video;return <div className={`metadataPreviewRow ${dest?'matched':'missing'}`} key={i}><span><b>{m.row.number?`VIDEO_${String(m.row.number).padStart(3,'0')}`:`СТРОКА ${i+1}`}</b><small>{dest?target==='future'?`→ VIDEO_${String(m.job!.number).padStart(3,'0')}`:`→ ${short(m.video!.title,50)} • ${m.video!.id}`:'→ НЕ НАЙДЕНО'}</small></span><strong>{m.row.title||'Название не задано'}</strong><em>{metaCount(m.row)}/4 поля</em></div>})}</div>}</section>
.vyron-v110-audit/src/YouTubeDataTools.tsx:4:import {refreshChannelAnalytics,refreshCompetitor} from './youtubeIntelligence';
.vyron-v110-audit/src/YouTubeDataTools.tsx:5:import {isYoutubeQuotaError,markYoutubeQuotaExceeded,youtubeQuotaMessage,youtubeQuotaState} from './youtubeQuota';
.vyron-v110-audit/src/YouTubeDataTools.tsx:8:export function YouTubeDataTools(){
.vyron-v110-audit/src/YouTubeDataTools.tsx:10: const [channelId,setChannelId]=useState(channels.find(c=>c.youtubeProfileId)?.id||channels[0]?.id||''),[busy,setBusy]=useState(''),[note,setNote]=useState('');
.vyron-v110-audit/src/YouTubeDataTools.tsx:12: const guard=()=>{if(youtubeQuotaState().blocked){setNote(youtubeQuotaMessage());toast(youtubeQuotaMessage());return false}return true};
.vyron-v110-audit/src/YouTubeDataTools.tsx:13: async function analytics(){if(!channel?.youtubeProfileId){toast('Сначала подключи YouTube OAuth');return}if(!guard())return;setBusy('analytics');try{const r=await refreshChannelAnalytics(channel.id,90,true);update(channel.id,{analytics:r});setNote(`✓ Аналитика обновлена: ${new Date(r.updatedAt).toLocaleString('ru-RU')}`);toast(`${channel.name}: аналитика обновлена`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setNote(youtubeQuotaMessage())}else setNote(String(e));toast(isYoutubeQuotaError(e)?youtubeQuotaMessage():String(e))}finally{setBusy('')}}
.vyron-v110-audit/src/YouTubeDataTools.tsx:14: async function discover(){if(!channel?.youtubeProfileId){toast('Сначала подключи YouTube OAuth');return}if(!guard())return;setBusy('discover');try{const found=await api.youtubeDiscoverCompetitors(channel.youtubeProfileId,Math.max(10,Math.min(50,useApp.getState().settings.competitorPoolSize||30)));const existing=new Set(list.map(c=>c.youtubeChannelId).filter(Boolean));let added=0;for(const x of found){if(existing.has(x.channelId)||x.channelId===channel.youtubeChannelId)continue;add({id:crypto.randomUUID(),channelId,name:x.name,url:x.url,youtubeChannelId:x.channelId,thumbnail:x.thumbnail,subscribers:x.subscribers,views:x.views,videos:x.videos,history:[],similarity:x.similarity,source:'auto'});existing.add(x.channelId);added++}setNote(`✓ Найдено ${found.length}, добавлено новых ${added}`);toast(`Конкуренты: +${added}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setNote(youtubeQuotaMessage())}else setNote(String(e));toast(isYoutubeQuotaError(e)?youtubeQuotaMessage():String(e))}finally{setBusy('')}}
.vyron-v110-audit/src/YouTubeDataTools.tsx:15: async function refreshCompetitors(){if(!guard())return;setBusy('competitors');let ok=0;try{for(const c of list){try{await refreshCompetitor(c.id,true);ok++}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);setNote(youtubeQuotaMessage());break}useApp.getState().log(`${c.name}: ${String(e)}`,'warn')}}if(!youtubeQuotaState().blocked){setNote(`✓ Обновлено конкурентов: ${ok}/${list.length}`);toast(`Конкуренты: ${ok}/${list.length}`)}}finally{setBusy('')}}
.vyron-v110-audit/src/YouTubeDataTools.tsx:16: return <section className="panel youtubeDataTools"><div className="panelHead"><div><small>EXPLICIT API ACTIONS</small><h3>Данные YouTube</h3><p>API вызывается только после нажатия кнопки на этом экране. Другие разделы используют сохранённый локальный кэш.</p></div><span className="localOnlyBadge">YOUTUBE ZONE</span></div><div className="youtubeDataToolbar"><label>Канал<select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><div className="youtubeActionGrid"><button disabled={!!busy||!channel?.youtubeProfileId} onClick={analytics}><b>{busy==='analytics'?'Обновляю…':'Обновить аналитику'}</b><small>90 дней → локальный cache</small></button><button disabled={!!busy||!channel?.youtubeProfileId} onClick={discover}><b>{busy==='discover'?'Ищу…':'Найти конкурентов'}</b><small>Только по явному действию</small></button><button disabled={!!busy||!list.length} onClick={refreshCompetitors}><b>{busy==='competitors'?'Обновляю…':'Обновить конкурентов'}</b><small>{list.length} сохранено локально</small></button></div>{note&&<div className="youtubeActionNote">{note}</div>}</section>
.vyron-v110-audit/src/core.ts:5:export function futureScheduledJobs(channel:Channel,jobs:VideoJob[],now=new Date()){
.vyron-v110-audit/src/core.ts:6:  return jobs.filter(j=>j.channelId===channel.id&&j.status==='SCHEDULED'&&j.publishAt&&new Date(j.publishAt)>=now).length;
.vyron-v110-audit/src/core.ts:9:  const scheduled=jobs.filter(j=>j.channelId===channel.id&&j.status==='SCHEDULED'&&j.publishAt).map(j=>new Date(j.publishAt!)).filter(d=>d>=now).sort((a,b)=>a.getTime()-b.getTime());
.vyron-v110-audit/src/core.ts:10:  if(!scheduled.length)return 0;
.vyron-v110-audit/src/core.ts:11:  const last=scheduled[scheduled.length-1];
.vyron-v110-audit/src/core.ts:15:  const existing=jobs.filter(j=>j.channelId===channel.id&&j.status!=='ERROR'&&(j.status!=='SCHEDULED'||!j.publishAt||new Date(j.publishAt)>=now)).length;
.vyron-v110-audit/src/core.ts:31:export function generateMetadata(channel:Channel,number:number,topic?:string){
.vyron-v110-audit/src/commandCenterCore.ts:16:  schedule:number;
.vyron-v110-audit/src/commandCenterCore.ts:17:  readyToYoutube:number;
.vyron-v110-audit/src/commandCenterCore.ts:49:export function buildLocalBatchPlan(channel:Channel,count:number,scheduledUntil?:string,now=new Date()):BatchPlan{
.vyron-v110-audit/src/commandCenterCore.ts:53:  const first=scheduledUntil?addCalendarDays(scheduledUntil,cadence)||today:today;
.vyron-v110-audit/src/commandCenterCore.ts:78:  const schedule=rows.filter(j=>Boolean(j.publishAt)).length;
.vyron-v110-audit/src/commandCenterCore.ts:79:  const readyToYoutube=rows.filter(j=>Boolean(j.finalPath&&j.title?.trim()&&j.description?.trim()&&j.tags?.length&&j.publishAt&&j.status!=='ERROR')).length;
.vyron-v110-audit/src/commandCenterCore.ts:81:  const dimensions=[covers,music,videos,seo,schedule].map(n=>Math.min(1,n/safeTarget));
.vyron-v110-audit/src/commandCenterCore.ts:83:  return{channelId:channel.id,target:safeTarget,covers,music,videos,seo,schedule,readyToYoutube,errors,progress};
.vyron-v110-audit/src/commandCenterCore.ts:89:  covers:number;music:number;seo:number;schedule:number;errors:number;
.vyron-v110-audit/src/commandCenterCore.ts:93:  const percent=Math.max(0,Math.min(100,Math.round((base.readyToYoutube/base.target)*100)));
.vyron-v110-audit/src/commandCenterCore.ts:94:  return{channelId:channel.id,targetProjects:base.target,createdProjects:rows.length,renderedVideos:base.videos,readyToPublish:base.readyToYoutube,percent,covers:base.covers,music:base.music,seo:base.seo,schedule:base.schedule,errors:base.errors};
.vyron-v110-audit/src/commandCenterCore.ts:108:      items.push({channelId:channel.id,channelName:channel.name,severity:'warning',title:'Нет подтверждённого расписания',detail:'Нужна ручная синхронизация расписания внутри YouTube.'});
.vyron-v110-audit/src/commandCenterCore.ts:119:    if(p.readyToYoutube>0){
.vyron-v110-audit/src/commandCenterCore.ts:120:      items.push({channelId:channel.id,channelName:channel.name,severity:'info',title:`Готово к YouTube: ${p.readyToYoutube}`,detail:'Файлы, SEO и локальное расписание уже заполнены.'});
.vyron-v110-audit/src/commandCenterCore.ts:136:  let dueNow=0,next7Days=0,critical=0,readyToYoutube=0,missingSeo=0;
.vyron-v110-audit/src/commandCenterCore.ts:148:    readyToYoutube+=p.readyToYoutube;
.vyron-v110-audit/src/commandCenterCore.ts:151:  return{channels:active.length,dueNow,next7Days,critical,readyToYoutube,missingSeo};
.vyron-v110-audit/src/autopilotCore.ts:2:import { deficit, generateMetadata, nextPublishSlots } from './core';
.vyron-v110-audit/src/autopilotCore.ts:5: const n=Math.max(0,Math.min(10000,Math.floor(count)));if(!n)return [] as VideoJob[];const max=Math.max(0,...jobs.filter(j=>j.channelId===channel.id).map(j=>j.number));const slots=nextPublishSlots(channel,jobs,n,now);return Array.from({length:n},(_,i)=>{const number=max+i+1,meta=generateMetadata(channel,number);return {id:crypto.randomUUID(),channelId:channel.id,number,folder:'',status:'NEED_IMAGE',createdAt:now.toISOString(),publishAt:slots[i],tracksCount:0,minTracks:channel.minTracks,title:meta.title,description:meta.description,tags:meta.tags,metadataSource:'template'} as VideoJob})
.vyron-v110-audit/src/ChannelsOS.tsx:8:function health(c:Channel,jobs:VideoJob[]){let n=100;if(!c.youtubeProfileId)n-=25;if(!c.analytics)n-=15;if(jobs.some(j=>j.channelId===c.id&&j.status==='ERROR'))n-=20;const b=bufferDays(c,jobs);if(b<7)n-=25;else if(b<14)n-=12;return Math.max(0,n)}
.vyron-v110-audit/src/ChannelsOS.tsx:15:  <div className="pageHeader"><div><small>CHANNEL NETWORK • LOCAL CACHE</small><h1>Каналы</h1><p>Только сохранённые данные последней ручной синхронизации. YouTube API здесь не вызывается.</p></div><div className="headerActions"><button onClick={()=>setPage('youtube')}>Обновить данные в YouTube</button><button className="primary" onClick={add}>+ Добавить канал</button></div></div>
.vyron-v110-audit/src/ChannelsOS.tsx:16:  <div className="cacheNotice"><b>ZERO QUOTA</b><span>Переходы по приложению не расходуют YouTube API quota.</span></div>
.vyron-v110-audit/src/ChannelsOS.tsx:17:  {channels.length===0?<div className="empty"><b>Каналов пока нет</b><p>Добавь локальный канал или подключи реальный аккаунт во вкладке YouTube.</p></div>:<div className="channelOSList">{channels.map(c=>{
.vyron-v110-audit/src/ChannelsOS.tsx:20:    <header><div className="channelAvatarOS">{a?.channelThumbnail?<img src={a.channelThumbnail} loading="lazy"/>:c.name.slice(0,2).toUpperCase()}</div><span className="channelIdentity"><b>{c.name}</b><small>{c.youtubeChannelId||'YouTube Channel ID —'}</small><em className={c.youtubeProfileId?'good':'warn'}>{c.youtubeProfileId?'OAuth сохранён локально':'YouTube не привязан'}</em></span><span className="channelHealth"><small>HEALTH</small><b>{health(c,jobs)}</b><em>Reserve {bufferDays(c,jobs)} days</em></span><button onClick={()=>setEditing(editing===c.id?null:c.id)}>✎</button></header>
.vyron-v110-audit/src/ChannelsOS.tsx:22:    <div className="channelMetricsGrid"><span><small>Subscribers</small><b>{c.stats?.subscribers!=null?formatNumber(c.stats.subscribers):'—'}</b></span><span><small>Total Views</small><b>{c.stats?.views!=null?formatNumber(c.stats.views):'—'}</b></span><span><small>Views 28d</small><b>{a?.periodDays===28?formatNumber(a.views):'—'}</b></span><span><small>Revenue 28d</small><b>{money(rev)}</b></span><span><small>RPM 28d</small><b>{a?.periodDays===28?rpm(rev,a.views):'—'}</b></span><span><small>Scheduled local</small><b>{jobs.filter(j=>j.channelId===c.id&&j.status==='SCHEDULED').length}</b></span></div>
.vyron-v110-audit/src/ChannelsOS.tsx:23:    <div className="channelActions"><button onClick={()=>setPage('youtube')}>Открыть YouTube</button><button onClick={()=>setEditing(editing===c.id?null:c.id)}>{editing===c.id?'Закрыть настройки':'Настроить канал'}</button></div>
.vyron-v110-audit/src/commandCenter.test.ts:29:  id:`job-${n}`,channelId:'neon',number:n,folder:`VIDEO_${String(n).padStart(3,'0')}`,status:'READY_UPLOAD',createdAt:'2026-09-02T00:00:00Z',
.vyron-v110-audit/src/commandCenter.test.ts:36:  channelId:'neon',channelName:'NEON',scheduledUntil:'2026-12-01',scheduledVideoCount:30,averagePublishIntervalDays:2,
.vyron-v110-audit/src/commandCenter.test.ts:37:  lastScheduleSync:'2026-09-02T08:00:00Z',lastLocalCalculation:'2026-09-02T08:00:00Z',runwayDays:days,nextProductionDate:'2026-10-17',
.vyron-v110-audit/src/commandCenter.test.ts:61:    expect(ready.readyToYoutube).toBe(1);
.vyron-v110-audit/src/commandCenter.test.ts:76:    expect(result.readyToYoutube).toBe(1);
.vyron-v110-audit/src/youtubeIntelligence.ts:4:import {isYoutubeQuotaError,youtubeQuotaState} from './youtubeQuota';
.vyron-v110-audit/src/youtubeIntelligence.ts:8:export function competitorChannelRef(c:Competitor){return c.youtubeChannelId||c.url||''}
.vyron-v110-audit/src/youtubeIntelligence.ts:11: const s=useApp.getState(),channel=s.channels.find(c=>c.id===channelId);if(!channel?.youtubeProfileId)throw new Error('У канала не привязан YouTube OAuth');
.vyron-v110-audit/src/youtubeIntelligence.ts:12: if(!force&&channel.analytics?.periodDays===days&&Number(channel.analytics?.offsetDays||0)===offsetDays&&!stale(channel.analytics?.updatedAt,s.settings.youtubeIntelligenceRefreshMin))return channel.analytics;
.vyron-v110-audit/src/youtubeIntelligence.ts:13: const r=await api.youtubeAnalytics(channel.youtubeProfileId,days,offsetDays,allTime),ps=(r as any).publicStats||{};
.vyron-v110-audit/src/youtubeIntelligence.ts:14: s.updateChannel(channel.id,{name:ps.title||channel.name,youtubeChannelId:ps.channelId||channel.youtubeChannelId,stats:{subscribers:ps.subscribers??channel.stats?.subscribers,views:ps.views??channel.stats?.views,videos:ps.videos??channel.stats?.videos,updatedAt:r.updatedAt},analytics:r});
.vyron-v110-audit/src/youtubeIntelligence.ts:19: const s=useApp.getState(),c=s.competitors.find(x=>x.id===competitorId);if(!c)throw new Error('Конкурент не найден');const own=s.channels.find(x=>x.id===c.channelId);if(!own?.youtubeProfileId)throw new Error(`Для ${own?.name||'канала'} сначала привяжи YouTube OAuth`);if(!force&&!stale(c.updatedAt,Math.max(60,s.settings.youtubeIntelligenceRefreshMin)))return c;
.vyron-v110-audit/src/youtubeIntelligence.ts:20: const r=await api.youtubeCompetitorSnapshot(own.youtubeProfileId,competitorChannelRef(c)),now=r.updatedAt||new Date().toISOString(),snapshot={at:now,subscribers:Number(r.subscribers||0),views:Number(r.views||0),videos:Number(r.videos||0),recentAverageViews:Number(r.recentAverageViews||0)},history=[...(c.history||[]),snapshot].slice(-720);
.vyron-v110-audit/src/youtubeIntelligence.ts:21: s.patchCompetitor(c.id,{youtubeChannelId:r.channelId||c.youtubeChannelId,name:r.name||c.name,thumbnail:r.thumbnail||c.thumbnail,subscribers:r.subscribers??c.subscribers,views:r.views??c.views,videos:r.videos??c.videos,recentAverageViews:r.recentAverageViews??c.recentAverageViews,lastVideoAt:r.lastVideoAt||c.lastVideoAt,latestVideos:r.latestVideos||c.latestVideos,updatedAt:now,history});return r;
.vyron-v110-audit/src/youtubeIntelligence.ts:25: const s=useApp.getState(),own=s.channels.find(c=>c.id===channelId);if(!own?.youtubeProfileId)return 0;const key=`vyron:competitor-discovery:${channelId}`,last=Number(localStorage.getItem(key)||0);if(!force&&Date.now()-last<24*3600_000)return 0;
.vyron-v110-audit/src/youtubeIntelligence.ts:26: const pool=Math.max(10,Math.min(50,s.settings.competitorPoolSize||30));const found=await api.youtubeDiscoverCompetitors(own.youtubeProfileId,Math.min(50,pool));const current=useApp.getState().competitors.filter(c=>c.channelId===channelId);const existing=new Set(current.map(c=>c.youtubeChannelId).filter(Boolean));let added=0;
.vyron-v110-audit/src/youtubeIntelligence.ts:27: for(const x of found){if(existing.has(x.channelId)||x.channelId===own.youtubeChannelId)continue;if(current.length+added>=pool)break;useApp.getState().addCompetitor({id:crypto.randomUUID(),channelId,name:x.name,url:x.url,youtubeChannelId:x.channelId,thumbnail:x.thumbnail,subscribers:x.subscribers,views:x.views,videos:x.videos,history:[],similarity:x.similarity,source:'auto'});existing.add(x.channelId);added++}
.vyron-v110-audit/src/youtubeIntelligence.ts:31:export async function refreshYoutubeIntelligence(force=false){
.vyron-v110-audit/src/youtubeIntelligence.ts:32: if(running||youtubeQuotaState().blocked)return;running=true;const errors:string[]=[];try{const state=useApp.getState();for(const channel of state.channels.filter(c=>c.enabled&&c.youtubeProfileId)){try{await refreshChannelAnalytics(channel.id,28,force)}catch(e){errors.push(`${channel.name}: ${String(e)}`)}try{await discoverCompetitorsForChannel(channel.id,force)}catch(e){errors.push(`${channel.name} discovery: ${String(e)}`)}const candidates=useApp.getState().competitors.filter(c=>c.channelId===channel.id).sort((a,b)=>(Date.parse(a.updatedAt||'')||0)-(Date.parse(b.updatedAt||'')||0)).filter(c=>force||stale(c.updatedAt,Math.max(60,useApp.getState().settings.youtubeIntelligenceRefreshMin))).slice(0,force?10:3);for(const c of candidates){try{await refreshCompetitor(c.id,true)}catch(e){errors.push(`${c.name}: ${String(e)}`)}}}
.vyron-v110-audit/src/youtubeIntelligence.ts:33: if(errors.length&&!errors.some(isYoutubeQuotaError))useApp.getState().log(`YouTube Intelligence: ${errors.slice(0,8).join(' • ')}`,'warn');else if(force&&!youtubeQuotaState().blocked)useApp.getState().log('YouTube Intelligence обновлён')}finally{running=false}
.vyron-v110-audit/src/metadata.ts:1:export type ImportedMetadata={
.vyron-v110-audit/src/metadata.ts:38:  if(!value)return {} as Pick<ImportedMetadata,'publishTime'|'publishTimezone'|'publishUtcOffsetMinutes'>;
.vyron-v110-audit/src/metadata.ts:41:  if(!tm)return {} as Pick<ImportedMetadata,'publishTime'|'publishTimezone'|'publishUtcOffsetMinutes'>;
.vyron-v110-audit/src/metadata.ts:55:function objectRow(raw:any,source:string,key=''):ImportedMetadata|null{
.vyron-v110-audit/src/metadata.ts:60:  const row:ImportedMetadata={
.vyron-v110-audit/src/metadata.ts:86:  },source)}).filter(Boolean) as ImportedMetadata[]
.vyron-v110-audit/src/metadata.ts:104:function parseBlock(block:string,source:string,forcedNumber?:number):ImportedMetadata|null{
.vyron-v110-audit/src/metadata.ts:128:  const row:ImportedMetadata={number,channel,title,description:cleanMultiline(descriptionParts),tags,publishAt,publishTime,publishTimezone,publishUtcOffsetMinutes,source};
.vyron-v110-audit/src/metadata.ts:137:    const out:ImportedMetadata[]=[];
.vyron-v110-audit/src/metadata.ts:152:export function validateSequentialMetadata(rows:ImportedMetadata[],expected:number){
.vyron-v110-audit/src/metadata.ts:161:export function parseMetadataFile(name:string,text:string):ImportedMetadata[]{
.vyron-v110-audit/src/metadata.ts:171:      const rows=list.map((r:any)=>objectRow(r,name,r.__key||'')).filter(Boolean) as ImportedMetadata[];if(rows.length)return rows
.vyron-v110-audit/src/metadataCoverage.test.ts:2:import {validateSequentialMetadata,type ImportedMetadata} from './metadata';
.vyron-v110-audit/src/metadataCoverage.test.ts:3:const rows=(n:number):ImportedMetadata[]=>Array.from({length:n},(_,i)=>({number:i+1,title:`Title ${i+1}`,source:'test.docx'}));
.vyron-v110-audit/src/metadataCoverage.test.ts:4:describe('DOCX capacity follows actual selected videos, not Production plan',()=>{
.vyron-v110-audit/src/metadataCoverage.test.ts:5: it('accepts 30 metadata rows when only 19 real videos are selected',()=>{const r=validateSequentialMetadata(rows(30),19);expect(r.ok).toBe(true);expect(r.surplus).toBe(11)});
.vyron-v110-audit/src/metadataCoverage.test.ts:6: it('accepts exact actual video count regardless of a larger Production plan',()=>expect(validateSequentialMetadata(rows(19),19).ok).toBe(true));
.vyron-v110-audit/src/metadataCoverage.test.ts:7: it('rejects one metadata row when more than one video is selected',()=>expect(validateSequentialMetadata(rows(1),19).ok).toBe(false));
.vyron-v110-audit/src/metadataCoverage.test.ts:8: it('rejects missing VIDEO number inside the selected coverage',()=>{const x=rows(30).filter(r=>r.number!==7);expect(validateSequentialMetadata(x,19).ok).toBe(false)});
.vyron-v110-audit/src/ProductionStatusBridge.tsx:23:              else if(p.renderStatus==='Completed'&&p.outputFile&&(job.finalPath!==p.outputFile||job.status!=='READY_UPLOAD'))useApp.getState().patchJob(job.id,{status:'READY_UPLOAD',finalPath:p.outputFile,error:undefined});
.vyron-v110-audit/src/DashboardOS.tsx:11: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),competitors=useApp(s=>s.competitors),settings=useApp(s=>s.settings),setPage=useApp(s=>s.setPage),logs=useApp(s=>s.logs);const enabled=channels.filter(c=>c.enabled),errors=jobs.filter(j=>j.status==='ERROR'),needImage=jobs.filter(j=>j.status==='NEED_IMAGE'),needMusic=jobs.filter(j=>j.status==='WAITING_MUSIC');const analytics=enabled.map(c=>c.analytics).filter(Boolean);const todayViews=analytics.reduce((n,a)=>n+(a?.daily?.at(-1)?.views||0),0);const rev28=enabled.reduce((n,c)=>n+(c.analytics?.periodDays===28?Number(c.analytics.estimatedRevenue||0):0),0);const revenueKnown=enabled.some(c=>c.analytics?.periodDays===28&&c.analytics.estimatedRevenue!=null);const netSubs=enabled.reduce((n,c)=>n+(c.analytics?.periodDays===28?(c.analytics.subscribersGained-c.analytics.subscribersLost):0),0);const next=[...jobs].filter(j=>j.publishAt&&Date.parse(j.publishAt)>Date.now()).sort((a,b)=>Date.parse(a.publishAt!)-Date.parse(b.publishAt!)).slice(0,5);const issues=[...enabled.filter(c=>!c.youtubeProfileId).map(c=>({type:'OAuth',text:`${c.name}: YouTube не подключён`,page:'settings' as const})),...enabled.filter(c=>c.youtubeProfileId&&!c.analytics).map(c=>({type:'Analytics',text:`${c.name}: ждёт первую аналитику`,page:'analytics' as const})),...errors.slice(0,6).map(j=>({type:'Ошибка',text:`VIDEO_${String(j.number).padStart(3,'0')}: ${j.error||'ошибка производства'}`,page:'production' as const})),...(needImage.length?[{type:'Материалы',text:`${needImage.length} проектов ждут изображения`,page:'production' as const}]:[]),...(needMusic.length?[{type:'Материалы',text:`${needMusic.length} проектов ждут аудио`,page:'production' as const}]:[])].slice(0,10);const competitorPulse=[...competitors].map(c=>({c,v:velocity(c)})).filter(x=>x.v!=null).sort((a,b)=>(b.v||0)-(a.v||0)).slice(0,4);const systemOk=!errors.length&&enabled.every(c=>c.youtubeProfileId);
.vyron-v110-audit/src/DashboardOS.tsx:13:  <div className="pageHeader morningHeader"><div><small>MORNING CONTROL CENTER</small><h1>{systemOk?'Система работает':'Есть действия, требующие внимания'}</h1><p>Один экран: каналы, публикации, производство, рост и конкуренты. Технический шум спрятан в Диагностике.</p></div><div className="headerActions"><button onClick={()=>setPage('production')}>Производство</button><button className="primary compactAction" onClick={()=>setPage('youtube')}>Открыть YouTube</button></div></div>
.vyron-v110-audit/src/DashboardOS.tsx:14:  <div className="morningMetrics"><span><small>VIEWS TODAY*</small><b>{analytics.length?formatNumber(todayViews):'—'}</b><em>*последняя доступная строка YouTube</em></span><span><small>REVENUE 28D</small><b>{revenueKnown?money(rev28):'—'}</b><em>{revenueKnown?'реальные monetary analytics':'нет monetary data'}</em></span><span><small>NET SUBS 28D</small><b>{analytics.length?`${netSubs>=0?'+':''}${formatNumber(netSubs)}`:'—'}</b><em>{enabled.length} каналов</em></span><span><small>NEXT PUBLISH</small><b>{next[0]?.publishAt?new Date(next[0].publishAt).toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'}):'—'}</b><em>{next[0]?.publishAt?new Date(next[0].publishAt).toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'}):'нет в плане'}</em></span><span><small>AUTOPILOT</small><b>{settings.autopilotMode.toUpperCase()}</b><em>{settings.autopilotEnabled?'цикл активен':'ручной режим'}</em></span></div>
.vyron-v110-audit/src/DashboardOS.tsx:15:  <div className="morningGrid"><section className="panel morningAttention"><div className="panelHead"><div><small>ATTENTION INBOX</small><h3>{issues.length?`${issues.length} пунктов`:'Ничего срочного'}</h3></div></div>{issues.length?issues.map((x,i)=><button key={i} onClick={()=>setPage(x.page)}><i>!</i><span><small>{x.type}</small><b>{x.text}</b></span><em>→</em></button>):<div className="successBox">OAuth, очередь и основные проверки без критических ошибок.</div>}</section><section className="panel nextPublications"><div className="panelHead"><div><small>NEXT 5</small><h3>Ближайшие публикации</h3></div><button onClick={()=>setPage('youtube')}>Календарь</button></div>{next.length?next.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div key={j.id}><span><b>{c?.name||'Канал'} • VIDEO_{String(j.number).padStart(3,'0')}</b><small>{j.title||'без названия'}</small></span><em>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})}</em></div>}):<p>Запланированных будущих публикаций нет.</p>}</section></div>
.vyron-v110-audit/src/DashboardOS.tsx:16:  <section className="panel channelPulse"><div className="panelHead"><div><small>CHANNEL PULSE</small><h3>Все каналы</h3></div><button onClick={()=>setPage('channels')}>Открыть каналы</button></div><div className="channelPulseRows">{enabled.map(c=>{const a=c.analytics,net=a?(a.subscribersGained-a.subscribersLost):undefined;return <button key={c.id} onClick={()=>setPage('analytics')}><span><b>{c.name}</b><small>{c.youtubeProfileId?'OAuth ✓':'OAuth —'} • sync {rel(a?.updatedAt)}</small></span><em><small>Views 28d</small><b>{a?.periodDays===28?formatNumber(a.views):'—'}</b></em><em><small>Revenue</small><b>{a?.periodDays===28?money(a.estimatedRevenue):'—'}</b></em><em><small>Net subs</small><b>{net==null?'—':`${net>=0?'+':''}${formatNumber(net)}`}</b></em></button>})}</div></section>
.vyron-v110-audit/src/CompetitorsPage.tsx:15: return <><div className="pageHeader"><div><small>COMPETITOR RADAR • LOCAL CACHE</small><h1>Конкуренты</h1><p>Экран читает только сохранённые публичные метрики. Поиск и обновление через YouTube API запускаются вручную только во вкладке YouTube.</p></div><div className="headerActions"><select value={channelId} onChange={e=>setChannelId(e.target.value)}>{sortChannelsAlphabetically(channels).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="primary" onClick={()=>setPage('youtube')}>Обновить через YouTube</button></div></div><div className="cacheNotice"><b>ZERO QUOTA</b><span>Автопоиск и фоновые snapshots отключены.</span></div><details className="panel advancedPanel"><summary>Ручное добавление без API</summary><div className="competitorAdd"><input placeholder="Название конкурента" value={name} onChange={e=>setName(e.target.value)}/><input placeholder="YouTube URL / channel ID" value={url} onChange={e=>setUrl(e.target.value)}/><button disabled={!channelId||!name||!url} onClick={()=>{add({id:crypto.randomUUID(),channelId,name,url,history:[],source:'manual'});setName('');setUrl('')}}>Добавить</button></div></details>{list.length===0?<div className="empty"><b>Конкурентов в кэше нет</b><p>Открой YouTube → Данные YouTube → Найти конкурентов.</p></div>:<div className="competitorOSList">{list.map(c=>{const vv=velocity(c,'views'),sv=velocity(c,'subscribers');return <article className="competitorOSCard" key={c.id}><header>{c.thumbnail?<img src={c.thumbnail} loading="lazy"/>:<i className="compAvatar">YT</i>}<span><b>{c.name}</b><small>{c.youtubeChannelId||c.url}</small>{c.similarity!=null&&<em className="similarityBadge">Similarity {c.similarity}%</em>}</span><button className="danger mini" onClick={()=>remove(c.id)}>×</button></header><div className="competitorMetrics"><span><small>Subscribers</small><b>{c.subscribers==null?'—':formatNumber(c.subscribers)}</b></span><span><small>Total Views</small><b>{c.views==null?'—':formatNumber(c.views)}</b></span><span><small>Video Count</small><b>{c.videos==null?'—':formatNumber(c.videos)}</b></span><span><small>Recent Avg Views</small><b>{c.recentAverageViews==null?'—':formatNumber(c.recentAverageViews)}</b></span><span><small>Subscriber velocity</small><b>{signed(sv)}</b></span><span><small>View velocity</small><b>{signed(vv)}</b></span><span><small>Last upload</small><b>{miniDate(c.lastVideoAt)}</b></span><span><small>Last sync</small><b>{c.updatedAt?new Date(c.updatedAt).toLocaleString('ru-RU'):'—'}</b></span></div>{c.latestVideos?.length?<div className="competitorVideos os">{c.latestVideos.slice(0,5).map(v=><button key={v.id} onClick={()=>api.openWeb(`https://youtube.com/watch?v=${v.id}`)}>{v.thumbnail?<img src={v.thumbnail} loading="lazy"/>:<i/>}<span><b>{v.title}</b><small>{formatNumber(v.views)} views • {miniDate(v.publishedAt)}</small></span></button>)}</div>:<div className="competitorPlaceholder">Нет сохранённого snapshot</div>}<footer><small>Revenue / RPM / CTR / retention конкурента не показываются: YouTube не раскрывает private analytics.</small><button onClick={()=>api.openWeb(c.url)}>Открыть YouTube</button></footer></article>})}</div>}</>
.vyron-v110-audit/src/store.ts:4:import { generateMetadata, slugify } from './core';
.vyron-v110-audit/src/store.ts:8:  workspace:'',endlumePath:'',youtubeApiKey:'',autoCheckUpdates:true,reduceMotion:false,fpsMonitor:true,
.vyron-v110-audit/src/store.ts:9:  autopilotMode:'off',autopilotEnabled:false,autoCreatePlan:true,autoAssignMusic:true,autoAssignImages:true,autoGenerateMetadata:false,
.vyron-v110-audit/src/store.ts:10:  autoQueueRender:true,autoOpenEndlume:false,autoUploadYoutube:false,autopilotIntervalSec:30,tracksPerVideo:10,
.vyron-v110-audit/src/store.ts:11:  openaiApiKey:'',openaiModel:'',youtubeOAuthClientId:'',youtubeCategoryId:'10',
.vyron-v110-audit/src/store.ts:12:  youtubeIntelligenceAutoRefresh:false,youtubeIntelligenceRefreshMin:30,
.vyron-v110-audit/src/store.ts:29:function scheduleSave(){window.clearTimeout(saveTimer);saveTimer=window.setTimeout(()=>{void useApp.getState().persist()},180)}
.vyron-v110-audit/src/store.ts:30:function normalizeJob(j:VideoJob):VideoJob{return {...j,tags:Array.isArray(j.tags)?j.tags:[],metadataSource:j.metadataSource||'template',uploadProgress:j.uploadProgress||0}}
.vyron-v110-audit/src/store.ts:35:  hydrate:s=>set({...EMPTY_STATE,...s,version:7,channels:(s.channels||[]).map(normalizeChannel),jobs:(s.jobs||[]).map(normalizeJob),competitors:s.competitors||[],settings:{...DEFAULT_SETTINGS,...s.settings,autoUploadYoutube:false,youtubeIntelligenceAutoRefresh:false},logs:s.logs||[],booted:true}),
.vyron-v110-audit/src/store.ts:40:    const channel:Channel={id,name,slug:slugify(name),cadenceDays:p.cadenceDays||2,targetBufferDays:p.targetBufferDays||60,publishHour:p.publishHour??18,publishMinute:p.publishMinute??0,language:p.language||'RU',genre:p.genre||'Music',country:p.country||'Россия',minTracks:p.minTracks||defaultTracks,targetDurationMin:p.targetDurationMin||get().settings.endlumeTargetDurationMin||120,enabled:p.enabled??true,youtubeProfileId:p.youtubeProfileId,youtubeChannelId:p.youtubeChannelId,seo:p.seo||{titlePatterns:['{topic} • Session {number}','{genre} — {topic} | Mix {number}'],descriptionTemplate:'{title}\n\nНовая подборка в стиле {genre}.',tags:[p.genre||'music','mix','playlist'],banned:[],aiPrompt:''}};
.vyron-v110-audit/src/store.ts:41:    set(s=>({channels:[...s.channels,channel]}));scheduleSave();return channel;
.vyron-v110-audit/src/store.ts:43:  updateChannel:(id,p)=>{set(s=>({channels:s.channels.map(c=>c.id===id?normalizeChannel({...c,...p}):c)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:44:  removeChannel:id=>{set(s=>({channels:s.channels.filter(c=>c.id!==id),jobs:s.jobs.filter(j=>j.channelId!==id),competitors:s.competitors.filter(c=>c.channelId!==id)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:45:  setJobs:jobs=>{set({jobs:jobs.map(normalizeJob)});scheduleSave()},
.vyron-v110-audit/src/store.ts:46:  patchJob:(id,p)=>{set(s=>({jobs:s.jobs.map(j=>j.id===id?normalizeJob({...j,...p}):j)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:47:  addJobs:jobs=>{set(s=>({jobs:[...s.jobs,...jobs.map(normalizeJob)]}));scheduleSave()},
.vyron-v110-audit/src/store.ts:48:  addCompetitor:c=>{set(s=>({competitors:[...s.competitors,c]}));scheduleSave()},
.vyron-v110-audit/src/store.ts:49:  patchCompetitor:(id,p)=>{set(s=>({competitors:s.competitors.map(c=>c.id===id?{...c,...p}:c)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:50:  removeCompetitor:id=>{set(s=>({competitors:s.competitors.filter(c=>c.id!==id)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:51:  patchSettings:p=>{set(s=>({settings:{...s.settings,...p}}));scheduleSave()},
.vyron-v110-audit/src/store.ts:52:  log:(message,level='info')=>{set(s=>({logs:[{at:new Date().toISOString(),level,message},...s.logs].slice(0,500)}));scheduleSave()},
.vyron-v110-audit/src/store.ts:57:  const meta=generateMetadata(channel,number);
.vyron-v110-audit/src/store.ts:58:  return {id:crypto.randomUUID(),channelId:channel.id,number,folder,status,createdAt:new Date().toISOString(),tracksCount:0,minTracks:channel.minTracks,title:meta.title,description:meta.description,tags:meta.tags,metadataSource:'template'} satisfies VideoJob;
.vyron-v110-audit/src/ChannelRunwayScheduler.tsx:5:export function ChannelRunwayScheduler(){
.vyron-v110-audit/src/ChannelRunwayScheduler.tsx:7:  const signature=channels.map(c=>`${c.id}:${c.name}:${c.enabled}:${c.cadenceDays}:${c.youtubeProfileId||''}`).join('|');
.vyron-v110-audit/src-tauri/src/updater_bridge.rs:3:use std::os::unix::fs::MetadataExt;
.vyron-v110-audit/src-tauri/src/updater_bridge.rs:17:        let app_dev=fs::metadata(parent).map_err(|e|format!("Updater: app volume: {e}"))?.dev();
.vyron-v110-audit/src-tauri/src/updater_bridge.rs:18:        let tmp_dev=fs::metadata(&system_tmp).map_err(|e|format!("Updater: temp volume: {e}"))?.dev();
.vyron-v110-audit/src-tauri/src/ai.rs:7:pub async fn ai_generate_metadata(api_key:String,model:String,channel_name:String,genre:String,language:String,country:String,number:u32,publish_at:Option<String>,previous_titles:Vec<String>,custom_prompt:String)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/ai.rs:10: let prompt=format!("Ты SEO-редактор музыкального YouTube-канала. Верни ТОЛЬКО JSON без markdown: {{\"title\":\"...\",\"description\":\"...\",\"tags\":[\"...\"]}}. Title максимум 100 символов. Description естественный, без выдуманных фактов. До 30 релевантных tags без #. Не повторяй прошлые заголовки. Канал: {channel_name}. Жанр: {genre}. Язык контента: {language}. Страна/рынок: {country}. Номер будущего ролика: {number}. Плановая дата: {}. Прошлые заголовки: {}. Дополнительные правила канала: {}",publish_at.unwrap_or_default(),previous_titles.into_iter().take(30).collect::<Vec<_>>().join(" | "),custom_prompt);
.vyron-v110-audit/src-tauri/src/youtube.rs:16:fn store_path(app:&AppHandle)->Result<PathBuf,String>{let dir=app.path().app_data_dir().map_err(|e|e.to_string())?;fs::create_dir_all(&dir).map_err(|e|e.to_string())?;Ok(dir.join("youtube-oauth.json"))}
.vyron-v110-audit/src-tauri/src/youtube.rs:59:pub fn youtube_google_config_status(app:AppHandle)->Result<Value,String>{Ok(google_config_status_value(&load_or_migrate_google_config(&app)?))}
.vyron-v110-audit/src-tauri/src/youtube.rs:61:pub fn youtube_google_config_import(app:AppHandle,json_text:String,api_key:String)->Result<Value,String>{let v:Value=serde_json::from_str(&json_text).map_err(|e|format!("credentials.json: {e}"))?;let root=v.get("installed").or_else(||v.get("web")).unwrap_or(&v);let client_id=root.get("client_id").and_then(|x|x.as_str()).unwrap_or("").trim().to_string();if client_id.is_empty(){return Err("В credentials.json не найден client_id".into())}let client_secret=root.get("client_secret").and_then(|x|x.as_str()).unwrap_or("").trim().to_string();let project_id=root.get("project_id").and_then(|x|x.as_str()).or_else(||v.get("project_id").and_then(|x|x.as_str())).unwrap_or("").trim().to_string();let old=load_google_config(&app).unwrap_or_default();let c=GoogleConfig{client_id,client_secret,project_id,api_key:if api_key.trim().is_empty(){old.api_key}else{api_key.trim().to_string()}};save_google_config(&app,&c)?;Ok(google_config_status_value(&c))}
.vyron-v110-audit/src-tauri/src/youtube.rs:63:pub async fn youtube_oauth_connect_global(app:AppHandle,browser:Option<String>)->Result<Value,String>{let c=load_or_migrate_google_config(&app)?;if c.client_id.trim().is_empty(){return Err("Нет Google OAuth Client. Импортируй credentials.json один раз или подключи существующий OAuth профиль.".into())}youtube_oauth_connect(app,c.client_id,c.client_secret,browser).await}
.vyron-v110-audit/src-tauri/src/youtube.rs:65:pub async fn youtube_oauth_profile_health(app:AppHandle,profile_id:String)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:67: let r=reqwest::Client::new().get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","snippet"),("mine","true")]).send().await.map_err(|e|format!("YouTube health: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:68: let st=r.status();let v:Value=r.json().await.unwrap_or_else(|_|json!({}));if !st.is_success(){return Err(youtube_error(&v,"YouTube OAuth health check failed"))}
.vyron-v110-audit/src-tauri/src/youtube.rs:69: let item=v.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).cloned().unwrap_or_else(||json!({}));let sn=item.get("snippet").cloned().unwrap_or_else(||json!({}));let thumb=sn.pointer("/thumbnails/high/url").or_else(||sn.pointer("/thumbnails/medium/url")).or_else(||sn.pointer("/thumbnails/default/url")).and_then(|x|x.as_str());
.vyron-v110-audit/src-tauri/src/youtube.rs:71: Ok(json!({"ok":true,"status":"TOKEN_HEALTHY","channelId":item.get("id").and_then(|x|x.as_str()).or(p.channel_id.as_deref()),"channelTitle":sn.get("title").and_then(|x|x.as_str()).or(p.channel_title.as_deref()),"thumbnail":thumb,"expiresAt":p.expires_at,"analyticsAuthorized":analytics,"monetaryAuthorized":monetary,"preferredBrowser":p.preferred_browser}))
.vyron-v110-audit/src-tauri/src/youtube.rs:74:pub async fn youtube_cache_thumbnail(app:AppHandle,video_id:String,primary:Option<String>)->Result<String,String>{let id=video_id.trim();if id.is_empty(){return Err("Video ID пуст".into())}let dir=app.path().app_cache_dir().map_err(|e|e.to_string())?.join("thumbnails");fs::create_dir_all(&dir).map_err(|e|format!("Thumbnail cache: {e}"))?;let safe=id.chars().filter(|c|c.is_ascii_alphanumeric()||*c=='_'||*c=='-').collect::<String>();let path=dir.join(format!("{safe}.jpg"));if path.exists()&&fs::metadata(&path).map(|m|m.len()>900).unwrap_or(false){return Ok(path.to_string_lossy().to_string())}let mut urls=Vec::<String>::new();if let Some(x)=primary.filter(|x|!x.trim().is_empty()){urls.push(x)}for q in ["maxresdefault","sddefault","hqdefault","mqdefault","default"]{urls.push(format!("https://i.ytimg.com/vi/{id}/{q}.jpg"))}let client=reqwest::Client::builder().timeout(Duration::from_secs(12)).build().map_err(|e|e.to_string())?;for url in urls{for attempt in 0..3{match client.get(&url).send().await{Ok(r) if r.status().is_success()=>{let b=r.bytes().await.map_err(|e|e.to_string())?;if b.len()>900{let tmp=path.with_extension("tmp");fs::write(&tmp,&b).map_err(|e|e.to_string())?;fs::rename(&tmp,&path).map_err(|e|e.to_string())?;return Ok(path.to_string_lossy().to_string())}},Ok(_)=>{},Err(_)=>{if attempt==0{tokio::time::sleep(Duration::from_millis(250)).await;}}}}}Err("Thumbnail недоступна после fallback".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:80:pub fn youtube_oauth_browsers()->Value{
.vyron-v110-audit/src-tauri/src/youtube.rs:93:pub fn youtube_oauth_profiles(app:AppHandle)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:103:pub fn youtube_oauth_disconnect(app:AppHandle,profile_id:String)->Result<(),String>{let mut s=load_store(&app)?;s.profiles.retain(|p|p.id!=profile_id);save_store(&app,&s)}
.vyron-v110-audit/src-tauri/src/youtube.rs:106:pub async fn youtube_oauth_connect(app:AppHandle,client_id:String,client_secret:String,browser:Option<String>)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:111: let scope="https://www.googleapis.com/auth/youtube.force-ssl https://www.googleapis.com/auth/yt-analytics.readonly https://www.googleapis.com/auth/yt-analytics-monetary.readonly";let scopes=scope.split_whitespace().map(str::to_string).collect::<Vec<_>>();
.vyron-v110-audit/src-tauri/src/youtube.rs:113: let expected_state=state.clone();let code=tauri::async_runtime::spawn_blocking(move||->Result<String,String>{listener.set_nonblocking(false).map_err(|e|e.to_string())?;let (mut stream,_)=listener.accept().map_err(|e|format!("OAuth callback: {e}"))?;let _=stream.set_read_timeout(Some(Duration::from_secs(300)));let mut buf=[0u8;8192];let n=stream.read(&mut buf).map_err(|e|format!("OAuth callback read: {e}"))?;let req=String::from_utf8_lossy(&buf[..n]);let first=req.lines().next().unwrap_or("");let target=first.split_whitespace().nth(1).unwrap_or("");let query=target.split_once('?').map(|x|x.1).unwrap_or("");let got_state=query_param(query,"state").unwrap_or_default();let code=query_param(query,"code");let err=query_param(query,"error");let ok=got_state==expected_state&&code.is_some();let html=if ok{"<html><body style='font-family:-apple-system;padding:40px;background:#07111d;color:white'><h2>Google подтвердил доступ ✅</h2><p>VYRON завершает проверку токена и YouTube-канала. Вернитесь в приложение — окончательный статус будет показан там.</p></body></html>"}else{"<html><body><h2>VYRON OAuth error</h2><p>Вернитесь в приложение.</p></body></html>"};let resp=format!("HTTP/1.1 200 OK\r\nContent-Type: text/html; charset=utf-8\r\nContent-Length: {}\r\nConnection: close\r\n\r\n{}",html.as_bytes().len(),html);let _=stream.write_all(resp.as_bytes());if let Some(e)=err{return Err(format!("Google OAuth: {e}"))}if got_state!=expected_state{return Err("OAuth state mismatch".into())}code.ok_or_else(||"Google не вернул authorization code".into())}).await.map_err(|e|e.to_string())??;
.vyron-v110-audit/src-tauri/src/youtube.rs:115: if refresh.is_empty(){return Err("Google не вернул refresh_token. Отключи доступ VYRON в Google Account и подключи YouTube заново.".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:116: let me=reqwest::Client::new().get("https://www.googleapis.com/youtube/v3/channels?part=snippet&mine=true").bearer_auth(&access).send().await.map_err(|e|format!("YouTube account network: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:118: let mv:Value=me.json().await.map_err(|e|format!("YouTube account JSON: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:119: if !me_status.is_success(){return Err(youtube_error(&mv,"Не удалось получить YouTube-канал. Проверь, что YouTube Data API v3 включён именно в проекте VYRON."))}
.vyron-v110-audit/src-tauri/src/youtube.rs:120: let item=mv.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).ok_or_else(||"На выбранном Google-аккаунте YouTube-канал не найден".to_string())?;
.vyron-v110-audit/src-tauri/src/youtube.rs:121: let channel_id=item.get("id").and_then(|x|x.as_str()).map(str::to_string).ok_or_else(||"YouTube не вернул Channel ID".to_string())?;
.vyron-v110-audit/src-tauri/src/youtube.rs:129:async fn valid_access_token(app:&AppHandle,profile_id:&str)->Result<(String,OAuthProfile),String>{let mut s=load_store(app)?;let idx=s.profiles.iter().position(|p|p.id==profile_id).ok_or_else(||"YouTube профиль не найден. Подключи канал заново.".to_string())?;if s.profiles[idx].expires_at>now_ts()+60{return Ok((s.profiles[idx].access_token.clone(),s.profiles[idx].clone()))}let refresh=s.profiles[idx].refresh_token.clone();let client_id=s.profiles[idx].client_id.clone();let client_secret=s.profiles[idx].client_secret.clone();if refresh.is_empty(){return Err("Нет refresh_token. Переподключи YouTube с доступом offline.".into())}let mut refresh_form=vec![("client_id",client_id.as_str()),("refresh_token",refresh.as_str()),("grant_type","refresh_token")];if !client_secret.is_empty(){refresh_form.push(("client_secret",client_secret.as_str()));}let r=reqwest::Client::new().post("https://oauth2.googleapis.com/token").form(&refresh_form).send().await.map_err(|e|format!("OAuth refresh network: {e}"))?;let status=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !status.is_success(){return Err(v.get("error_description").and_then(|x|x.as_str()).unwrap_or("Не удалось обновить YouTube token").to_string())}let token=v.get("access_token").and_then(|x|x.as_str()).ok_or_else(||"Нет нового access_token".to_string())?.to_string();let expires=v.get("expires_in").and_then(|x|x.as_i64()).unwrap_or(3600);s.profiles[idx].access_token=token.clone();s.profiles[idx].expires_at=now_ts()+expires;let profile=s.profiles[idx].clone();save_store(app,&s)?;Ok((token,profile))}
.vyron-v110-audit/src-tauri/src/youtube.rs:133:async fn upload_offset(client:&reqwest::Client,url:&str,token:&str,total:u64)->Result<u64,String>{let r=client.put(url).bearer_auth(token).header("Content-Length","0").header("Content-Range",format!("bytes */{total}")).send().await.map_err(|e|e.to_string())?;if r.status().as_u16()==308{if let Some(range)=r.headers().get("Range").and_then(|x|x.to_str().ok()){if let Some(end)=range.split('-').last().and_then(|x|x.parse::<u64>().ok()){return Ok(end+1)}}return Ok(0)}if r.status().is_success(){return Ok(total)}Err(format!("YouTube resume status: {}",r.status()))}
.vyron-v110-audit/src-tauri/src/youtube.rs:136:pub async fn youtube_upload_video(app:AppHandle,profile_id:String,job_id:String,file_path:String,title:String,description:String,tags:Vec<String>,publish_at:Option<String>,category_id:String)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:137: let path=PathBuf::from(&file_path);if !path.is_file(){return Err(format!("Видео не найдено: {}",path.display()))}if title.trim().is_empty(){return Err("Название видео пустое".into())}let total=fs::metadata(&path).map_err(|e|e.to_string())?.len();if total==0{return Err("Видео пустое".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:139: let client=reqwest::Client::new();let init=client.post("https://www.googleapis.com/upload/youtube/v3/videos?uploadType=resumable&part=snippet,status").bearer_auth(&token).header("Content-Type","application/json; charset=UTF-8").header("X-Upload-Content-Length",total.to_string()).header("X-Upload-Content-Type","video/mp4").json(&body).send().await.map_err(|e|format!("YouTube session: {e}"))?;let init_status=init.status();if !init_status.is_success(){let t=init.text().await.unwrap_or_default();return Err(format!("YouTube upload init {init_status}: {}",t.chars().take(500).collect::<String>()))}let session=init.headers().get("Location").and_then(|x|x.to_str().ok()).ok_or_else(||"YouTube не вернул resumable Location".to_string())?.to_string();
.vyron-v110-audit/src-tauri/src/youtube.rs:140: let mut file=tokio::fs::File::open(&path).await.map_err(|e|e.to_string())?;let chunk_size:usize=8*1024*1024;let mut offset:u64=0;let mut final_json=json!({});while offset<total{file.seek(std::io::SeekFrom::Start(offset)).await.map_err(|e|e.to_string())?;let wanted=std::cmp::min(chunk_size as u64,total-offset) as usize;let mut buf=vec![0u8;wanted];file.read_exact(&mut buf).await.map_err(|e|e.to_string())?;let end=offset+wanted as u64-1;let mut completed=false;for attempt in 0..5{let sent=client.put(&session).bearer_auth(&token).header("Content-Type","video/mp4").header("Content-Length",wanted.to_string()).header("Content-Range",format!("bytes {offset}-{end}/{total}")).body(buf.clone()).send().await;match sent{Ok(r)=>{let st=r.status();if st.is_success(){final_json=r.json().await.unwrap_or_else(|_|json!({}));offset=total;completed=true;break}if st.as_u16()==308{offset=r.headers().get("Range").and_then(|x|x.to_str().ok()).and_then(|s|s.split('-').last()).and_then(|x|x.parse::<u64>().ok()).map(|x|x+1).unwrap_or(end+1);let pct=(offset as f64/total as f64*100.0).min(100.0);let _=app.emit("youtube-upload-progress",json!({"jobId":job_id,"progress":pct}));completed=true;break}if st.is_server_error(){tokio::time::sleep(Duration::from_secs(1u64<<attempt)).await;offset=upload_offset(&client,&session,&token,total).await.unwrap_or(offset);continue}let text=r.text().await.unwrap_or_default();return Err(format!("YouTube upload {st}: {}",text.chars().take(500).collect::<String>()))},Err(e)=>{if attempt==4{return Err(format!("YouTube upload network: {e}"))}tokio::time::sleep(Duration::from_secs(1u64<<attempt)).await;offset=upload_offset(&client,&session,&token,total).await.unwrap_or(offset)}}}if !completed{return Err("YouTube upload не удалось продолжить".into())}}
.vyron-v110-audit/src-tauri/src/youtube.rs:141: let video_id=final_json.get("id").and_then(|x|x.as_str()).unwrap_or("").to_string();let _=app.emit("youtube-upload-progress",json!({"jobId":job_id,"progress":100.0}));Ok(json!({"videoId":video_id,"channelId":profile.channel_id,"channelTitle":profile.channel_title,"scheduled":publish_at.is_some()}))
.vyron-v110-audit/src-tauri/src/youtube.rs:145:fn youtube_error(v:&Value,fallback:&str)->String{
.vyron-v110-audit/src-tauri/src/youtube.rs:152:pub async fn youtube_list_existing_videos(app:AppHandle,profile_id:String,max_results:Option<u32>)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:154: let r=client.get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","contentDetails"),("mine","true")]).send().await.map_err(|e|format!("YouTube channel: {e}"))?;let st=r.status();let cv:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(youtube_error(&cv,"Не удалось получить канал YouTube"))}let uploads=cv.pointer("/items/0/contentDetails/relatedPlaylists/uploads").and_then(|x|x.as_str()).ok_or_else(||"YouTube не вернул playlist загрузок".to_string())?;
.vyron-v110-audit/src-tauri/src/youtube.rs:155: let mut ids=Vec::<String>::new();let mut page:Option<String>=None;let mut youtube_found:usize=0;
.vyron-v110-audit/src-tauri/src/youtube.rs:157:  let mut q=client.get("https://www.googleapis.com/youtube/v3/playlistItems").bearer_auth(&token).query(&[("part","contentDetails"),("playlistId",uploads),("maxResults","50")]);if let Some(ref t)=page{q=q.query(&[("pageToken",t.as_str())]);}
.vyron-v110-audit/src-tauri/src/youtube.rs:158:  let r=q.send().await.map_err(|e|format!("YouTube uploads: {e}"))?;let st=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(youtube_error(&v,"Не удалось получить список загрузок"))}
.vyron-v110-audit/src-tauri/src/youtube.rs:159:  if youtube_found==0{youtube_found=v.pointer("/pageInfo/totalResults").and_then(|x|x.as_u64()).unwrap_or(0) as usize;}
.vyron-v110-audit/src-tauri/src/youtube.rs:163: if youtube_found==0{youtube_found=ids.len()}
.vyron-v110-audit/src-tauri/src/youtube.rs:164: let mut by_id=std::collections::HashMap::<String,Value>::new();for chunk in ids.chunks(50){let joined=chunk.join(",");let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,status,contentDetails,statistics"),("id",joined.as_str())]).send().await.map_err(|e|format!("YouTube videos: {e}"))?;let st=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(youtube_error(&v,"Не удалось получить данные видео"))}for item in v.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=item.get("id").and_then(|x|x.as_str()){by_id.insert(id.to_string(),item);}}}
.vyron-v110-audit/src-tauri/src/youtube.rs:165: let mut out=Vec::new();for (position,id) in ids.iter().enumerate(){if let Some(item)=by_id.get(id){let sn=item.get("snippet").cloned().unwrap_or_else(||json!({}));let st=item.get("status").cloned().unwrap_or_else(||json!({}));out.push(json!({"id":id,"channelId":sn.get("channelId").and_then(|x|x.as_str()),"position":position,"title":sn.get("title").and_then(|x|x.as_str()).unwrap_or(""),"description":sn.get("description").and_then(|x|x.as_str()).unwrap_or(""),"tags":sn.get("tags").cloned().unwrap_or_else(||json!([])),"categoryId":sn.get("categoryId").and_then(|x|x.as_str()).unwrap_or("10"),"publishedAt":sn.get("publishedAt").and_then(|x|x.as_str()),"privacyStatus":st.get("privacyStatus").and_then(|x|x.as_str()).unwrap_or("unknown"),"publishAt":st.get("publishAt").and_then(|x|x.as_str()),"thumbnail":sn.pointer("/thumbnails/maxres/url").or_else(||sn.pointer("/thumbnails/standard/url")).or_else(||sn.pointer("/thumbnails/high/url")).or_else(||sn.pointer("/thumbnails/medium/url")).or_else(||sn.pointer("/thumbnails/default/url")).and_then(|x|x.as_str()),"duration":item.pointer("/contentDetails/duration").and_then(|x|x.as_str()),"views":item.pointer("/statistics/viewCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()),"likes":item.pointer("/statistics/likeCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()),"comments":item.pointer("/statistics/commentCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()),"selected":false}));}}
.vyron-v110-audit/src-tauri/src/youtube.rs:166: let scheduled_count=out.iter().filter(|x|x.get("privacyStatus").and_then(|v|v.as_str())==Some("private")&&x.get("publishAt").and_then(|v|v.as_str()).is_some()).count();let private_count=out.iter().filter(|x|x.get("privacyStatus").and_then(|v|v.as_str())==Some("private")&&x.get("publishAt").and_then(|v|v.as_str()).is_none()).count();let public_count=out.iter().filter(|x|x.get("privacyStatus").and_then(|v|v.as_str())==Some("public")).count();let unlisted_count=out.iter().filter(|x|x.get("privacyStatus").and_then(|v|v.as_str())==Some("unlisted")).count();let expected=std::cmp::min(youtube_found,limit);let complete=out.len()==expected;
.vyron-v110-audit/src-tauri/src/youtube.rs:167: Ok(json!({"channelId":profile.channel_id,"channelTitle":profile.channel_title,"youtubeFound":youtube_found,"received":out.len(),"requested":limit,"privateCount":private_count,"publicCount":public_count,"scheduledCount":scheduled_count,"unlistedCount":unlisted_count,"complete":complete,"videos":out}))
.vyron-v110-audit/src-tauri/src/youtube.rs:171:pub async fn youtube_backup_existing_videos(app:AppHandle,profile_id:String,videos:Value)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:174: for chunk in ids.chunks(50){let joined=chunk.join(",");let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet"),("id",joined.as_str())]).send().await.map_err(|e|format!("Backup ownership check: {e}"))?;let st=r.status();let v:Value=r.json().await.unwrap_or_else(|_|json!({}));if !st.is_success(){return Err(youtube_error(&v,"Backup: не удалось проверить владельца видео"))}for item in v.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){let actual=item.pointer("/snippet/channelId").and_then(|x|x.as_str()).unwrap_or("");if actual!=channel_id{return Err(format!("BLOCK: video {} принадлежит другому channelId",item.get("id").and_then(|x|x.as_str()).unwrap_or("?")))}}}
.vyron-v110-audit/src-tauri/src/youtube.rs:175: let safe=channel_id.chars().filter(|c|c.is_ascii_alphanumeric()||*c=='_'||*c=='-').collect::<String>();let dir=app.path().app_data_dir().map_err(|e|e.to_string())?.join("Backup").join("Youtube").join(safe);fs::create_dir_all(&dir).map_err(|e|format!("Backup dir: {e}"))?;let path=dir.join(format!("{}.json",Utc::now().format("%Y%m%d-%H%M%S-%3f")));
.vyron-v110-audit/src-tauri/src/youtube.rs:183:fn youtube_truncate_utf8_bytes(input:&str,max_bytes:usize)->String{
.vyron-v110-audit/src-tauri/src/youtube.rs:188:fn youtube_truncate_utf8_bytes(input:&str,max_bytes:usize)->String{
.vyron-v110-audit/src-tauri/src/youtube.rs:192:fn youtube_clean_title(input:&str)->String{input.trim().replace(['<','>']," ").chars().take(100).collect::<String>().trim().to_string()}
.vyron-v110-audit/src-tauri/src/youtube.rs:193:fn youtube_tag_cost(tag:&str,has_previous:bool)->usize{tag.chars().count()+if tag.contains(char::is_whitespace){2}else{0}+if has_previous{1}else{0}}
.vyron-v110-audit/src-tauri/src/youtube.rs:194:fn youtube_sanitize_tags(tags:Vec<String>)->Vec<String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:196: for raw in tags{let tag=raw.trim().replace(['<','>']," ").split_whitespace().collect::<Vec<_>>().join(" ");if tag.is_empty()||out.iter().any(|x|x.eq_ignore_ascii_case(&tag)){continue}let cost=youtube_tag_cost(&tag,!out.is_empty());if used+cost>500{continue}used+=cost;out.push(tag)}out
.vyron-v110-audit/src-tauri/src/youtube.rs:198:fn youtube_norm_text(input:&str)->String{input.replace("\r\n","\n").replace('\r',"\n").lines().map(|x|x.trim_end()).collect::<Vec<_>>().join("\n").trim().to_string()}
.vyron-v110-audit/src-tauri/src/youtube.rs:199:fn youtube_norm_tags(tags:&[String])->Vec<String>{let mut out=tags.iter().map(|x|x.trim().split_whitespace().collect::<Vec<_>>().join(" ").to_lowercase()).filter(|x|!x.is_empty()).collect::<Vec<_>>();out.sort();out.dedup();out}
.vyron-v110-audit/src-tauri/src/youtube.rs:200:fn youtube_metadata_diff(sn:&Value,wanted_title:&str,wanted_desc:&str,wanted_tags:&[String])->Vec<String>{let mut diff=Vec::<String>::new();let got_title=sn.get("title").and_then(|x|x.as_str()).unwrap_or("");let got_desc=sn.get("description").and_then(|x|x.as_str()).unwrap_or("");let got_tags=sn.get("tags").and_then(|x|x.as_array()).cloned().unwrap_or_default().into_iter().filter_map(|x|x.as_str().map(str::to_string)).collect::<Vec<_>>();if youtube_norm_text(got_title)!=youtube_norm_text(wanted_title){diff.push("title".to_string())}if youtube_norm_text(got_desc)!=youtube_norm_text(wanted_desc){diff.push("description".to_string())}if youtube_norm_tags(&got_tags)!=youtube_norm_tags(wanted_tags){diff.push("tags".to_string())}diff}
.vyron-v110-audit/src-tauri/src/youtube.rs:201:fn youtube_status_body(old_status:&Value,target_privacy:&str,publish_at:Option<&str>)->Value{
.vyron-v110-audit/src-tauri/src/youtube.rs:206:pub async fn youtube_update_existing_video(app:AppHandle,profile_id:String,video_id:String,title:String,description:String,tags:Vec<String>,publish_at:Option<String>,privacy_status:Option<String>)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube.rs:208: if !profile.scopes.iter().any(|s|s=="https://www.googleapis.com/auth/youtube.force-ssl"||s=="https://www.googleapis.com/auth/youtube"){return Err("YouTube профиль подключён со старыми правами. Переподключи канал.".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:210: let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,status"),("id",video_id.as_str())]).send().await.map_err(|e|format!("YouTube video read: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:211: let st=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(youtube_error(&v,"Не удалось перечитать видео"))}
.vyron-v110-audit/src-tauri/src/youtube.rs:215: let wanted_title=youtube_clean_title(&title);if wanted_title.is_empty(){return Err("YouTube title не может быть пустым".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:216: let wanted_desc=youtube_truncate_utf8_bytes(&description,5000);let wanted_tags_vec=youtube_sanitize_tags(tags);let category=sn.get("categoryId").and_then(|x|x.as_str()).unwrap_or("10").to_string();
.vyron-v110-audit/src-tauri/src/youtube.rs:217: let metadata_needed=!youtube_metadata_diff(&sn,&wanted_title,&wanted_desc,&wanted_tags_vec).is_empty();
.vyron-v110-audit/src-tauri/src/youtube.rs:221: let schedule_requested=publish.is_some()||target_privacy!=old_privacy;
.vyron-v110-audit/src-tauri/src/youtube.rs:222: let mut schedule_pre_error:Option<String>=None;
.vyron-v110-audit/src-tauri/src/youtube.rs:223: if publish.is_some()&&target_privacy!="private"{schedule_pre_error=Some("Scheduled publishAt требует privacyStatus=private".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:224: if let Some(p)=publish{match chrono::DateTime::parse_from_rfc3339(p){Ok(dt)=>{if dt.with_timezone(&Utc)<=Utc::now(){schedule_pre_error=Some("Дата публикации уже в прошлом".into())}},Err(_)=>schedule_pre_error=Some("Некорректный publishAt: ожидается RFC3339".into())}}
.vyron-v110-audit/src-tauri/src/youtube.rs:226: let schedule_needed=schedule_requested&&status_changed&&schedule_pre_error.is_none();
.vyron-v110-audit/src-tauri/src/youtube.rs:227: if !metadata_needed&&!schedule_needed{
.vyron-v110-audit/src-tauri/src/youtube.rs:228:  return Ok(json!({"id":video_id,"verified":true,"metadataAccepted":true,"metadataVerified":true,"metadataVerifyPending":false,"scheduleRequested":schedule_requested,"scheduleAccepted":schedule_pre_error.is_none(),"scheduleVerified":schedule_pre_error.is_none(),"scheduleVerifyPending":false,"scheduleError":schedule_pre_error,"skipped":true,"appliedTags":wanted_tags_vec.len()}))
.vyron-v110-audit/src-tauri/src/youtube.rs:231: let desired_status=youtube_status_body(&old_status,&target_privacy,publish);
.vyron-v110-audit/src-tauri/src/youtube.rs:232: let mut metadata_accepted=!metadata_needed;let mut schedule_accepted=!schedule_needed&&schedule_pre_error.is_none();let mut response=Value::Null;let mut schedule_error=schedule_pre_error.clone();
.vyron-v110-audit/src-tauri/src/youtube.rs:233: if metadata_needed&&schedule_needed{
.vyron-v110-audit/src-tauri/src/youtube.rs:235:  let u=client.put("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,status")]).json(&body).send().await.map_err(|e|format!("YouTube combined update: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:237:  if ust.is_success(){metadata_accepted=true;schedule_accepted=true;response=uv}else{
.vyron-v110-audit/src-tauri/src/youtube.rs:238:   let err=youtube_error(&uv,"YouTube не принял metadata/schedule");if err.to_lowercase().contains("quotaexceeded")||err.to_lowercase().contains("daily limit"){return Err(err)}
.vyron-v110-audit/src-tauri/src/youtube.rs:240:   let mu=client.put("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet")]).json(&sb).send().await.map_err(|e|format!("YouTube metadata fallback: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube.rs:241:   let mst=mu.status();let mv:Value=mu.json().await.unwrap_or_else(|_|json!({}));if !mst.is_success(){return Err(format!("METADATA: {}",youtube_error(&mv,"YouTube не принял title/description/tags")))}
.vyron-v110-audit/src-tauri/src/youtube.rs:242:   metadata_accepted=true;response=mv;schedule_error=Some(err);
.vyron-v110-audit/src-tauri/src/youtube.rs:244: }else if metadata_needed{
.vyron-v110-audit/src-tauri/src/youtube.rs:245:  let body=json!({"id":video_id,"snippet":snippet});let u=client.put("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet")]).json(&body).send().await.map_err(|e|format!("YouTube metadata update: {e}"))?;let ust=u.status();let uv:Value=u.json().await.unwrap_or_else(|_|json!({}));if !ust.is_success(){return Err(format!("METADATA: {}",youtube_error(&uv,"YouTube не принял title/description/tags")))}metadata_accepted=true;response=uv;
.vyron-v110-audit/src-tauri/src/youtube.rs:246: }else if schedule_needed{
.vyron-v110-audit/src-tauri/src/youtube.rs:247:  let body=json!({"id":video_id,"status":desired_status});let u=client.put("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","status")]).json(&body).send().await.map_err(|e|format!("YouTube schedule update: {e}"))?;let ust=u.status();let uv:Value=u.json().await.unwrap_or_else(|_|json!({}));if !ust.is_success(){let err=youtube_error(&uv,"YouTube не принял расписание/privacy");if err.to_lowercase().contains("quotaexceeded")||err.to_lowercase().contains("daily limit"){return Err(err)}schedule_error=Some(err)}else{schedule_accepted=true;response=uv}
.vyron-v110-audit/src-tauri/src/youtube.rs:249: if metadata_needed&&!metadata_accepted{return Err("YouTube metadata write не подтверждён".into())}
.vyron-v110-audit/src-tauri/src/youtube.rs:250: let mut metadata_verified=!metadata_needed;let mut schedule_verified=!schedule_needed&&schedule_error.is_none();
.vyron-v110-audit/src-tauri/src/youtube.rs:251: if metadata_accepted&&metadata_needed{metadata_verified=youtube_metadata_diff(response.get("snippet").unwrap_or(&Value::Null),&wanted_title,&wanted_desc,&wanted_tags_vec).is_empty()}
.vyron-v110-audit/src-tauri/src/youtube.rs:252: if schedule_accepted&&schedule_needed{let rs=response.get("status").unwrap_or(&Value::Null);let time_ok=publish.map(|w|same_publish_time(Some(w),rs.get("publishAt").and_then(|x|x.as_str()))).unwrap_or(true);let privacy_ok=rs.get("privacyStatus").and_then(|x|x.as_str())==Some(target_privacy.as_str());schedule_verified=time_ok&&privacy_ok}
.vyron-v110-audit/src-tauri/src/youtube.rs:253: if (metadata_accepted&&!metadata_verified)||(schedule_accepted&&!schedule_verified){for delay in [300u64,1200]{tokio::time::sleep(std::time::Duration::from_millis(delay)).await;let q=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,status"),("id",video_id.as_str())]).send().await;if let Ok(q)=q{let qs=q.status();let qv:Value=q.json().await.unwrap_or_else(|_|json!({}));if !qs.is_success(){continue}if let Some(g)=qv.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()){if metadata_accepted{metadata_verified=youtube_metadata_diff(g.get("snippet").unwrap_or(&Value::Null),&wanted_title,&wanted_desc,&wanted_tags_vec).is_empty()}if schedule_accepted&&schedule_needed{let gs=g.get("status").unwrap_or(&Value::Null);let time_ok=publish.map(|w|same_publish_time(Some(w),gs.get("publishAt").and_then(|x|x.as_str()))).unwrap_or(true);let privacy_ok=gs.get("privacyStatus").and_then(|x|x.as_str())==Some(target_privacy.as_str());schedule_verified=time_ok&&privacy_ok}if metadata_verified&&(!schedule_accepted||schedule_verified){break}}}}
.vyron-v110-audit/src-tauri/src/youtube.rs:255: Ok(json!({"id":video_id,"verified":metadata_accepted&&(!schedule_requested||schedule_accepted),"metadataAccepted":metadata_accepted,"metadataVerified":metadata_verified,"metadataVerifyPending":metadata_accepted&&!metadata_verified,"scheduleRequested":schedule_requested,"scheduleAccepted":schedule_accepted,"scheduleVerified":schedule_verified,"scheduleVerifyPending":schedule_accepted&&schedule_needed&&!schedule_verified,"scheduleError":schedule_error,"skipped":false,"appliedTags":wanted_tags_vec.len()}))
.vyron-v110-audit/src-tauri/src/youtube.rs:259:mod youtube_write_tests{use super::*;#[test]fn same_publish_time_normalizes_offsets(){assert!(same_publish_time(Some("2026-09-04T04:00:00+07:00"),Some("2026-09-03T21:00:00Z")));}#[test]fn metadata_verify_ignores_tag_order_and_line_endings(){let sn=json!({"title":"  Hello  ","description":"A\r\nB","tags":["Paris Night","Deep House"]});let wanted=vec!["deep house".to_string(),"paris night".to_string()];assert!(youtube_metadata_diff(&sn,"Hello","A\nB",&wanted).is_empty());}#[test]fn utf8_description_respects_5000_bytes(){let s="я".repeat(3000);let out=youtube_truncate_utf8_bytes(&s,5000);assert!(out.len()<=5000);assert!(out.is_char_boundary(out.len()));}#[test]fn tags_respect_youtube_500_budget(){let tags=(0..100).map(|i|format!("long tag number {} abcdefghijklmnop",i)).collect::<Vec<_>>();let out=youtube_sanitize_tags(tags);let mut used=0;for (i,t) in out.iter().enumerate(){used+=youtube_tag_cost(t,i>0);}assert!(used<=500);assert!(!out.is_empty());}#[test]fn tags_dedupe_and_strip_angle_brackets(){let out=youtube_sanitize_tags(vec![" alpha ".into(),"ALPHA".into(),"<beta tag>".into()]);assert_eq!(out.len(),2);assert!(!out[1].contains('<'));assert!(!out[1].contains('>'));}}
.vyron-v110-audit/src-tauri/src/youtube.rs:262:pub async fn youtube_channel_stats(api_key:String,channel_id:String)->Result<Value,String>{if api_key.trim().is_empty(){return Err("YouTube API Key не указан".into())}if channel_id.trim().is_empty(){return Err("Channel ID не указан".into())}let url=format!("https://www.googleapis.com/youtube/v3/channels?part=snippet,statistics&id={}&key={}",urlencoding::encode(channel_id.trim()),urlencoding::encode(api_key.trim()));let r=reqwest::Client::new().get(url).send().await.map_err(|e|format!("Сеть: {e}"))?;let status=r.status();let v:Value=r.json().await.map_err(|e|format!("Ответ YouTube: {e}"))?;if !status.is_success(){return Err(v.get("error").and_then(|e|e.get("message")).and_then(|x|x.as_str()).unwrap_or("YouTube API вернул ошибку").to_string())}let item=v.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).ok_or_else(||"Канал не найден. Проверь Channel ID.".to_string())?;let stat=item.get("statistics").cloned().unwrap_or(json!({}));let num=|k:&str|stat.get(k).and_then(|x|x.as_str()).and_then(|s|s.parse::<u64>().ok());Ok(json!({"title":item.pointer("/snippet/title").and_then(|x|x.as_str()).unwrap_or(""),"subscribers":num("subscriberCount"),"views":num("viewCount"),"videos":num("videoCount")}))}
.vyron-v110-audit/src-tauri/src/lib.rs:7:mod youtube;
.vyron-v110-audit/src-tauri/src/lib.rs:8:mod youtube_intelligence;
.vyron-v110-audit/src-tauri/src/lib.rs:23:            files::import_images,files::add_tracks,files::refresh_job,files::prepare_job_folder,files::ensure_channel_inbox,files::scan_channel_inbox,files::ingest_tracks,files::ingest_cover,files::write_job_metadata,files::enqueue_render,files::reveal_path,files::open_endlume,
.vyron-v110-audit/src-tauri/src/lib.rs:25:            youtube::youtube_channel_stats,youtube::youtube_oauth_browsers,youtube_intelligence::youtube_channel_analytics,youtube_intelligence::youtube_competitor_snapshot,youtube_intelligence::youtube_discover_competitors,youtube::youtube_oauth_profiles,youtube::youtube_google_config_status,youtube::youtube_google_config_import,youtube::youtube_oauth_connect_global,youtube::youtube_oauth_profile_health,youtube::youtube_cache_thumbnail,youtube::youtube_oauth_connect,youtube::youtube_oauth_disconnect,youtube::youtube_upload_video,youtube::youtube_list_existing_videos,youtube::youtube_backup_existing_videos,youtube::youtube_update_existing_video,
.vyron-v110-audit/src-tauri/src/lib.rs:27:            ai::ai_generate_metadata
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:4:use crate::youtube;
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:8:fn thumb_url(sn:&Value)->Option<String>{for p in ["/thumbnails/maxres/url","/thumbnails/standard/url","/thumbnails/high/url","/thumbnails/medium/url","/thumbnails/default/url"]{if let Some(x)=sn.pointer(p).and_then(|x|x.as_str()){return Some(x.to_string())}}None}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:15: let r=reqwest::Client::new().get("https://youtubeanalytics.googleapis.com/v2/reports").bearer_auth(token).query(&params).send().await.map_err(|e|format!("YouTube Analytics network: {e}"))?;
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:16: let st=r.status();let v:Value=r.json().await.map_err(|e|format!("YouTube Analytics JSON: {e}"))?;if !st.is_success(){return Err(api_error(&v,"YouTube Analytics API вернул ошибку"))}Ok(v)
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:20:pub async fn youtube_channel_analytics(app:AppHandle,profile_id:String,days:Option<u32>,offset_days:Option<u32>,all_time:Option<bool>)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:21: let (token,scopes)=youtube::access_token_and_scopes(&app,&profile_id).await?;if !scopes.iter().any(|s|s=="https://www.googleapis.com/auth/yt-analytics.readonly"||s=="https://www.googleapis.com/auth/yt-analytics-monetary.readonly"){return Err("YouTube Analytics не разрешён. Переподключи YouTube OAuth.".into())}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:22: let client=reqwest::Client::new();let cr=client.get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","snippet,statistics,contentDetails"),("mine","true")]).send().await.map_err(|e|format!("YouTube channel stats: {e}"))?;let cst=cr.status();let cv:Value=cr.json().await.unwrap_or_else(|_|json!({}));if !cst.is_success(){return Err(api_error(&cv,"YouTube Data API не вернул канал"))}let ci=cv.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).cloned().unwrap_or_else(||json!({}));let stat=ci.get("statistics").cloned().unwrap_or_else(||json!({}));let sn=ci.get("snippet").cloned().unwrap_or_else(||json!({}));let offset=offset_days.unwrap_or(0).min(7300);let end=Utc::now().date_naive()-ChronoDuration::days(offset as i64);let req_days=days.unwrap_or(28).clamp(1,7300);let published=sn.get("publishedAt").and_then(|x|x.as_str()).and_then(|x|x.get(..10)).and_then(|x|chrono::NaiveDate::parse_from_str(x,"%Y-%m-%d").ok());let start=if all_time.unwrap_or(false){published.unwrap_or(end-ChronoDuration::days(req_days.saturating_sub(1) as i64))}else{end-ChronoDuration::days(req_days.saturating_sub(1) as i64)};let actual_days=(end-start).num_days().max(0) as u32+1;let start_s=start.format("%Y-%m-%d").to_string();let end_s=end.format("%Y-%m-%d").to_string();
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:28: let top=analytics_report(&token,&start_s,&end_s,"views,estimatedMinutesWatched,averageViewDuration,averageViewPercentage,likes,comments,shares,subscribersGained",Some("video"),Some("-views"),Some(10)).await.unwrap_or_else(|_|json!({"rows":[]}));let top_rows=report_rows(&top);let top_ids=top_rows.iter().filter_map(|r|r.first().and_then(|x|x.as_str())).collect::<Vec<_>>();let mut video_meta=std::collections::HashMap::<String,Value>::new();if !top_ids.is_empty(){let joined=top_ids.join(",");let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet"),("id",joined.as_str())]).send().await.map_err(|e|format!("Top videos metadata: {e}"))?;let st=r.status();let v:Value=r.json().await.unwrap_or_else(|_|json!({}));if st.is_success(){for item in v.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=item.get("id").and_then(|x|x.as_str()){video_meta.insert(id.to_string(),item);}}}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:30: let top_videos=top_rows.into_iter().map(|r|{let id=r.first().and_then(|x|x.as_str()).unwrap_or("").to_string();let meta=video_meta.get(&id).cloned().unwrap_or_else(||json!({}));let vsn=meta.get("snippet").cloned().unwrap_or_else(||json!({}));let views=r.get(1).map(value_u64).unwrap_or(0);let rev=video_rev.get(&id).copied();let rpm=if views>0{rev.map(|x|x/views as f64*1000.0)}else{None};json!({"id":id,"title":vsn.get("title").and_then(|x|x.as_str()).unwrap_or("Видео"),"publishedAt":vsn.get("publishedAt").and_then(|x|x.as_str()),"thumbnail":thumb_url(&vsn),"views":views,"watchMinutes":r.get(2).map(value_f64).unwrap_or(0.0),"averageViewDuration":r.get(3).map(value_f64).unwrap_or(0.0),"averageViewPercentage":r.get(4).map(value_f64).unwrap_or(0.0),"likes":r.get(5).map(value_u64).unwrap_or(0),"comments":r.get(6).map(value_u64).unwrap_or(0),"shares":r.get(7).map(value_u64).unwrap_or(0),"subscribersGained":r.get(8).map(value_u64).unwrap_or(0),"estimatedRevenue":rev,"rpm":rpm})}).collect::<Vec<_>>();
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:33: let total_videos=if let Some(uploads)=ci.pointer("/contentDetails/relatedPlaylists/uploads").and_then(|x|x.as_str()){let r=client.get("https://www.googleapis.com/youtube/v3/playlistItems").bearer_auth(&token).query(&[("part","id"),("playlistId",uploads),("maxResults","1")]).send().await.ok();if let Some(r)=r{let v:Value=r.json().await.unwrap_or_else(|_|json!({}));v.pointer("/pageInfo/totalResults").and_then(|x|x.as_u64())}else{None}}else{None};
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:34: let payload=json!({"source":"youtube-analytics-v2","snapshotDate":end_s,"periodDays":actual_days,"offsetDays":offset,"allTime":all_time.unwrap_or(false),"updatedAt":Utc::now().to_rfc3339(),"views":get(0) as u64,"engagedViews":engaged,"watchMinutes":get(1),"averageViewDuration":get(2),"averageViewPercentage":get(3),"subscribersGained":get(4) as u64,"subscribersLost":get(5) as u64,"likes":get(6) as u64,"comments":get(7) as u64,"shares":get(8) as u64,"monetaryAuthorized":monetary,"estimatedRevenue":mget(0),"estimatedAdRevenue":mget(1),"estimatedRedPartnerRevenue":mget(2),"monetizedPlaybacks":mget(3),"adImpressions":mget(4),"cpm":mget(5),"playbackBasedCpm":mget(6),"monetaryError":monetary_error,"impressions":Value::Null,"impressionCtr":Value::Null,"daily":daily_rows,"topVideos":top_videos,"trafficSources":traffic_sources,"countries":countries_rows,"audience":[],"channelPublishedAt":sn.get("publishedAt").and_then(|x|x.as_str()),"channelCountry":sn.get("country").and_then(|x|x.as_str()),"channelLanguage":sn.get("defaultLanguage").and_then(|x|x.as_str()).or_else(||sn.get("defaultAudioLanguage").and_then(|x|x.as_str())),"channelThumbnail":thumb_url(&sn),"totalVideos":total_videos,"publicStats":{"channelId":ci.get("id").and_then(|x|x.as_str()),"title":sn.get("title").and_then(|x|x.as_str()),"thumbnail":thumb_url(&sn),"subscribers":stat.get("subscriberCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()),"views":stat.get("viewCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()),"videos":stat.get("videoCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok())}});
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:39:async fn resolve_channel_ref(token:&str,channel_ref:&str)->Result<String,String>{let raw=channel_ref.trim();if raw.starts_with("UC"){return Ok(raw.split(|c:char|c=='?'||c=='/'||c=='&').next().unwrap_or(raw).to_string())}if let Some(pos)=raw.find("/channel/"){let rest=&raw[pos+9..];let id=rest.split(|c:char|c=='/'||c=='?'||c=='&').next().unwrap_or("");if id.starts_with("UC"){return Ok(id.to_string())}}if let Some(pos)=raw.find("/@"){let rest=&raw[pos+2..];let handle=rest.split(|c:char|c=='/'||c=='?'||c=='&').next().unwrap_or("");if !handle.is_empty(){let r=reqwest::Client::new().get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(token).query(&[("part","id"),("forHandle",handle)]).send().await.map_err(|e|format!("YouTube handle: {e}"))?;let v:Value=r.json().await.unwrap_or_else(|_|json!({}));if let Some(id)=v.pointer("/items/0/id").and_then(|x|x.as_str()){return Ok(id.to_string())}}}Err("Не удалось определить Channel ID. Используй URL /channel/UC… или ссылку @handle.".into())}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:44:pub async fn youtube_discover_competitors(app:AppHandle,profile_id:String,max_results:Option<u32>)->Result<Value,String>{let (token,_)=youtube::access_token_and_scopes(&app,&profile_id).await?;let client=reqwest::Client::new();let cr=client.get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","snippet,contentDetails"),("mine","true")]).send().await.map_err(|e|format!("Own channel: {e}"))?;let cv:Value=cr.json().await.unwrap_or_else(|_|json!({}));let own=cv.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).ok_or_else(||"Собственный YouTube-канал не найден".to_string())?;let own_id=own.get("id").and_then(|x|x.as_str()).unwrap_or("");let mut corpus=String::new();corpus.push_str(own.pointer("/snippet/title").and_then(|x|x.as_str()).unwrap_or(""));corpus.push(' ');corpus.push_str(own.pointer("/snippet/description").and_then(|x|x.as_str()).unwrap_or(""));if let Some(uploads)=own.pointer("/contentDetails/relatedPlaylists/uploads").and_then(|x|x.as_str()){let pr=client.get("https://www.googleapis.com/youtube/v3/playlistItems").bearer_auth(&token).query(&[("part","snippet"),("playlistId",uploads),("maxResults","12")]).send().await.map_err(|e|format!("Own uploads: {e}"))?;let pv:Value=pr.json().await.unwrap_or_else(|_|json!({}));for x in pv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(t)=x.pointer("/snippet/title").and_then(|x|x.as_str()){corpus.push(' ');corpus.push_str(t)}}}let own_keys=keywords(&corpus);let query=own_keys.iter().take(6).cloned().collect::<Vec<_>>().join(" ");if query.is_empty(){return Err("Недостаточно данных канала для автоматического поиска конкурентов".into())}let max=max_results.unwrap_or(12).clamp(3,50);let max_s=max.to_string();let sr=client.get("https://www.googleapis.com/youtube/v3/search").bearer_auth(&token).query(&[("part","snippet"),("type","channel"),("q",query.as_str()),("maxResults",max_s.as_str())]).send().await.map_err(|e|format!("Competitor discovery: {e}"))?;let ss=sr.status();let sv:Value=sr.json().await.unwrap_or_else(|_|json!({}));if !ss.is_success(){return Err(api_error(&sv,"YouTube Search API не выполнил поиск"))}let mut ids=sv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default().into_iter().filter_map(|x|x.pointer("/id/channelId").and_then(|v|v.as_str()).map(str::to_string)).filter(|x|x!=own_id).collect::<Vec<_>>();ids.sort();ids.dedup();if ids.is_empty(){return Ok(json!([]))}let joined=ids.join(",");let rr=client.get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","snippet,statistics"),("id",joined.as_str())]).send().await.map_err(|e|format!("Competitor details: {e}"))?;let rv:Value=rr.json().await.unwrap_or_else(|_|json!({}));let mut out=Vec::<Value>::new();for item in rv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){let id=item.get("id").and_then(|x|x.as_str()).unwrap_or("");if id.is_empty()||id==own_id{continue}let sn=item.get("snippet").cloned().unwrap_or_else(||json!({}));let stat=item.get("statistics").cloned().unwrap_or_else(||json!({}));let text=format!("{} {}",sn.get("title").and_then(|x|x.as_str()).unwrap_or(""),sn.get("description").and_then(|x|x.as_str()).unwrap_or(""));let mut similarity=overlap_score(&own_keys,&text);if similarity<15{similarity=15}let num=|k:&str|stat.get(k).and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0);out.push(json!({"channelId":id,"name":sn.get("title").and_then(|x|x.as_str()).unwrap_or("YouTube"),"url":format!("https://www.youtube.com/channel/{id}"),"thumbnail":thumb_url(&sn),"subscribers":num("subscriberCount"),"views":num("viewCount"),"videos":num("videoCount"),"similarity":similarity}))}out.sort_by(|a,b|b.get("similarity").and_then(|x|x.as_u64()).cmp(&a.get("similarity").and_then(|x|x.as_u64())));out.truncate(max as usize);Ok(json!(out))}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:47:pub async fn youtube_competitor_snapshot(app:AppHandle,profile_id:String,channel_ref:String)->Result<Value,String>{
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:48: let (token,_)=youtube::access_token_and_scopes(&app,&profile_id).await?;let channel_id=resolve_channel_ref(&token,&channel_ref).await?;let client=reqwest::Client::new();let r=client.get("https://www.googleapis.com/youtube/v3/channels").bearer_auth(&token).query(&[("part","snippet,statistics,contentDetails"),("id",channel_id.as_str())]).send().await.map_err(|e|format!("Competitor channel: {e}"))?;let st=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(api_error(&v,"Не удалось получить конкурента"))}let item=v.get("items").and_then(|x|x.as_array()).and_then(|a|a.first()).ok_or_else(||"Канал конкурента не найден".to_string())?;let sn=item.get("snippet").cloned().unwrap_or_else(||json!({}));let stat=item.get("statistics").cloned().unwrap_or_else(||json!({}));let uploads=item.pointer("/contentDetails/relatedPlaylists/uploads").and_then(|x|x.as_str()).unwrap_or("");
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:49: let mut ids=Vec::<String>::new();if !uploads.is_empty(){let pr=client.get("https://www.googleapis.com/youtube/v3/playlistItems").bearer_auth(&token).query(&[("part","contentDetails"),("playlistId",uploads),("maxResults","10")]).send().await.map_err(|e|format!("Competitor uploads: {e}"))?;let pv:Value=pr.json().await.unwrap_or_else(|_|json!({}));for x in pv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=x.pointer("/contentDetails/videoId").and_then(|x|x.as_str()){ids.push(id.to_string())}}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:50: let mut latest=Vec::<Value>::new();if !ids.is_empty(){let joined=ids.join(",");let vr=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,statistics"),("id",joined.as_str())]).send().await.map_err(|e|format!("Competitor videos: {e}"))?;let vv:Value=vr.json().await.unwrap_or_else(|_|json!({}));let mut map=std::collections::HashMap::<String,Value>::new();for it in vv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=it.get("id").and_then(|x|x.as_str()){map.insert(id.to_string(),it);}}for id in ids.iter(){if let Some(it)=map.get(id){let s=it.get("snippet").cloned().unwrap_or_else(||json!({}));let st=it.get("statistics").cloned().unwrap_or_else(||json!({}));latest.push(json!({"id":id,"title":s.get("title").and_then(|x|x.as_str()).unwrap_or(""),"thumbnail":thumb_url(&s),"publishedAt":s.get("publishedAt").and_then(|x|x.as_str()),"views":st.get("viewCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0),"likes":st.get("likeCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0),"comments":st.get("commentCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0)}));}}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:52: Ok(json!({"channelId":channel_id,"name":sn.get("title").and_then(|x|x.as_str()).unwrap_or("YouTube"),"thumbnail":thumb_url(&sn),"subscribers":subscribers,"views":views,"videos":videos,"recentAverageViews":recent_avg,"lastVideoAt":latest.first().and_then(|x|x.get("publishedAt")).and_then(|x|x.as_str()),"latestVideos":latest,"updatedAt":Utc::now().to_rfc3339()}))
.vyron-v110-audit/src-tauri/src/files.rs:136:        "READY_UPLOAD"
.vyron-v110-audit/src-tauri/src/files.rs:195:    let metadata = root.join("Metadata");
.vyron-v110-audit/src-tauri/src/files.rs:196:    for p in [&music, &images, &metadata] {
.vyron-v110-audit/src-tauri/src/files.rs:203:        "metadata": metadata
.vyron-v110-audit/src-tauri/src/files.rs:234:                    .metadata()
.vyron-v110-audit/src-tauri/src/files.rs:247:        "metadata": collect("Metadata", "metadata")
.vyron-v110-audit/src-tauri/src/files.rs:321:pub fn write_job_metadata(
.vyron-v110-audit/src-tauri/src/files.rs:342:    v["metadataSource"] = json!(source);
.vyron-v110-audit/src-tauri/src/storage.rs:12:    json!({"version":4,"channels":[],"jobs":[],"competitors":[],"settings":{"workspace":"","endlumePath":"","youtubeApiKey":"","autoCheckUpdates":true,"reduceMotion":false,"fpsMonitor":true},"logs":[]})
.vyron-v110-audit/src-tauri/src/production_manager.rs:32:fn nonempty_image(p:&Path)->bool{p.is_file()&&is_image(p)&&fs::metadata(p).map(|m|m.len()>0).unwrap_or(false)}
.vyron-v110-audit/src-tauri/src/production_manager.rs:43:fn modified_ms(meta: &fs::Metadata) -> u64 {
.vyron-v110-audit/src-tauri/src/production_manager.rs:106:    out.sort_by_key(|p|fs::metadata(p).ok().and_then(|m|m.modified().ok()).unwrap_or(UNIX_EPOCH));Ok(out)
.vyron-v110-audit/src-tauri/src/production_manager.rs:298:    let meta=match fs::metadata(src){Ok(m)=>m,Err(_)=>{pending.remove(&key);return false;}};
.vyron-v110-audit/src-tauri/src/production_manager.rs:337:                let before=match fs::metadata(&src){Ok(x) if x.len()>0=>x,_=>continue};
.vyron-v110-audit/src-tauri/src/production_manager.rs:349:                let after=match fs::metadata(&src){Ok(x)=>x,Err(_)=>{let _=fs::remove_file(&tmp);continue;}};
.vyron-v110-audit/src-tauri/src/production_manager.rs:350:                let copied=fs::metadata(&tmp).map(|m|m.len()).unwrap_or(0);
.vyron-v110-audit/src-tauri/src/production_manager.rs:422:        let m=fs::metadata(&p).map_err(|e|e.to_string())?; if m.len()==0{continue;}
.vyron-v110-audit/src-tauri/src/production_manager.rs:511:    if !folder.is_dir(){return false;} let image=folder.join(&plan.image_name);if !image.is_file()||fs::metadata(&image).map(|m|m.len()==0).unwrap_or(true){return false;}
.vyron-v110-audit/src-tauri/src/production_manager.rs:512:    plan.tracks.iter().all(|t|{let p=folder.join(&t.dest_name);p.is_file()&&fs::metadata(p).map(|m|m.len()>0).unwrap_or(false)})
.vyron-v110-audit/src-tauri/src/production_manager.rs:641:        for t in &p.tracks{let q=Path::new(&t.path);if !q.is_file(){errs.push(format!("нет {}",q.file_name().and_then(|x|x.to_str()).unwrap_or("track")));}else if fs::metadata(q).map(|x|x.len()==0).unwrap_or(true){errs.push("найден 0-byte аудиофайл".into());}if !is_audio(q){errs.push("неподдерживаемый аудиоформат".into());}}

## ENDLUME / project handoff / status symbols
.vyron-v110-audit/src/RecoveryGate.tsx:2:import {productionManagerApi,type RecoveryState} from './productionManagerApi';
.vyron-v110-audit/src/RecoveryGate.tsx:12:  useEffect(()=>{if(!booted||checked.current||!roots.length)return;checked.current=true;productionManagerApi.findRecovery(roots).then(rows=>{const first=rows.find(x=>x.recoverable);if(first){decided.current=false;setRecovery(first);setDeadline(Date.now()+60_000);notifyInfo('Предыдущая работа была прервана',`${first.channelName}: готово ${first.completedProjects} / ${first.totalProjects}.`,{operationId:`recovery-found:${first.batchId}`,durationMs:8000})}}).catch(e=>notifyError('Не удалось проверить восстановление',String(e),{operationId:'recovery-scan-error'}))},[booted,roots.join('|')]);
.vyron-v110-audit/src/RecoveryGate.tsx:15:  async function resumeNow(){if(!recovery||decided.current)return;decided.current=true;setBusy(true);setDeadline(null);try{const done=await productionManagerApi.resume(recovery.rootPath);notifySuccess('Производство восстановлено',`${done.channelName}: ${done.projectCount} проектов готовы.`,{operationId:`recovery-resumed:${done.batchId}`});setRecovery(null);setPage('production')}catch(e){decided.current=false;setBusy(false);notifyError('Не удалось продолжить производство',String(e),{operationId:`recovery-resume-error:${recovery.batchId}`})}}
.vyron-v110-audit/src/RecoveryGate.tsx:17:  async function restartNow(){if(!recovery||busy)return;decided.current=true;setBusy(true);try{const done=await productionManagerApi.restartRecovery(recovery.rootPath);notifySuccess('Новая сборка создана',`${done.channelName}: ${done.projectCount} проектов. Старая незавершённая сборка сохранена.`,{operationId:`recovery-restarted:${done.batchId}`});setRecovery(null);setPage('production')}catch(e){decided.current=false;setBusy(false);notifyError('Не удалось начать новую сборку',String(e),{operationId:`recovery-restart-error:${recovery.batchId}`})}}
.vyron-v110-audit/src/App.tsx:52:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужна картинка',WAITING_MUSIC:'Ждёт музыку',READY_RENDER:'Готов к рендеру',RENDERING:'Рендерится',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загружается',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/App.tsx:71:function Activation({onActivated}:{onActivated:(x:LicenseStatus)=>void}){const version=useRuntimeVersion();const [key,setKey]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');return <div className="activation"><div className="activationCard"><div className="brand big"><div className="brandMark">▶</div><div><b>VYRON</b><small>CONTENT OS</small></div></div><h1>Активация VYRON</h1><p>Используется совместимая лицензия владельца ENDLUME Studio.</p><input autoFocus value={key} placeholder="Ключ ENDLUME / VYRON" onChange={e=>setKey(e.target.value)} onKeyDown={e=>e.key==='Enter'&&document.getElementById('activate')?.click()}/>{error&&<div className="errorBox">{error}</div>}<button id="activate" className="primary full" disabled={busy||!key.trim()} onClick={async()=>{setBusy(true);setError('');try{onActivated(await api.activate(key))}catch(e){setError(String(e))}finally{setBusy(false)}}}>{busy?'ПРОВЕРЯЮ…':'АКТИВИРОВАТЬ →'}</button><small>VYRON {version||'…'} • macOS Apple Silicon</small></div></div>}
.vyron-v110-audit/src/App.tsx:79:<div className="metrics six"><Metric label="КАНАЛОВ" value={channels.length} note="в системе"/><Metric label="СРОЧНО" value={urgent} note="буфер < 14 дней" tone="red"/><Metric label="ЖДУТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note="папки готовы" tone="orange"/><Metric label="ГОТОВО РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="ENDLUME queue" tone="purple"/><Metric label="ГОТОВО MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="YouTube queue" tone="green"/><Metric label="YOUTUBE" value={jobs.filter(j=>j.status==='SCHEDULED').length} note="запланировано" tone="blue"/></div>
.vyron-v110-audit/src/App.tsx:82:function SystemCard(){const s=useApp(x=>x.settings),channels=useApp(x=>x.channels);return <div className="panel systemCard"><div className="panelHead"><div><small>СИСТЕМА</small><h3>Автономный контур</h3></div></div><div className="flow"><b>SUNO</b><i>→</i><b>VYRON</b><i>→</i><b>ENDLUME</b><i>→</i><b>YouTube</b></div><div className="sysRows"><span>Workspace <b>{s.workspace?'готов':'нет'}</b></span><span>Метаданные <b>{s.openaiApiKey?'AI API подключён':'ChatGPT / Inbox'}</b></span><span>ENDLUME <b>{s.endlumePath?'подключён':'не выбран'}</b></span><span>YouTube OAuth <b>{channels.some(c=>c.youtubeProfileId)?'привязан':'не привязан'}</b></span></div></div>}
.vyron-v110-audit/src/App.tsx:87:<div className="metrics six"><Metric label="НУЖНА КАРТИНКА" value={jobs.filter(j=>j.status==='NEED_IMAGE').length} note="Inbox/Images"/><Metric label="ЖДЁТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note={`${settings.tracksPerVideo} треков`} tone="orange"/><Metric label="РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="RenderQueue" tone="purple"/><Metric label="MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="готово" tone="green"/><Metric label="UPLOAD" value={jobs.filter(j=>j.status==='UPLOADING').length} note="resumable" tone="blue"/><Metric label="ОШИБКИ" value={jobs.filter(j=>j.status==='ERROR').length} note="не блокируют очередь" tone="red"/></div>
.vyron-v110-audit/src/App.tsx:88:<div className="autoSteps">{[['01','План 60 дней',settings.autoCreatePlan,'autoCreatePlan'],['02','Музыка → по проектам',settings.autoAssignMusic,'autoAssignMusic'],['03','Изображение → 1 проект',settings.autoAssignImages,'autoAssignImages'],['04','AI title/description/tags',settings.autoGenerateMetadata,'autoGenerateMetadata'],['05','Очередь ENDLUME',settings.autoQueueRender,'autoQueueRender'],['06','YouTube upload + schedule',settings.autoUploadYoutube,'autoUploadYoutube']].map(([n,label,val,key]:any)=><button key={key} className={`autoStep ${val?'active':''}`} onClick={()=>patch({[key]:!val} as any)}><span>{n}</span><b>{label}</b><em>{val?'AUTO':'OFF'}</em></button>)}</div>
.vyron-v110-audit/src/App.tsx:89:{last&&<div className="panel autoResult"><div className="panelHead"><div><small>ПОСЛЕДНИЙ РУЧНОЙ ЦИКЛ</small><h3>Результат</h3></div></div><div className="resultGrid"><span>Проекты <b>{last.prepared}</b></span><span>Треки <b>{last.tracksMoved}</b></span><span>Картинки <b>{last.imagesMoved}</b></span><span>AI <b>{last.metadataGenerated}</b></span><span>RenderQueue <b>{last.renderQueued}</b></span><span>YouTube <b>{last.uploads}</b></span><span>Ошибки <b>{last.errors}</b></span></div>{last.notes.slice(0,8).map((n,i)=><p key={i}>{n}</p>)}</div>}
.vyron-v110-audit/src/App.tsx:91:<div className="panel warningPanel"><b>Что ещё требуется один раз</b><p>Для полностью автоматической публикации: подключить Google OAuth в Настройках и привязать профиль к каналу. Метаданные можно бесплатно импортировать из обычного ChatGPT Plus через Metadata Inbox; платный OpenAI API не обязателен. Для нулевого ручного рендера ENDLUME должен читать RenderQueue — VYRON уже создаёт эту очередь.</p><button onClick={()=>setPage('settings')}>ОТКРЫТЬ НАСТРОЙКИ</button></div>
.vyron-v110-audit/src/App.tsx:101:{filtered.length===0?<Empty title="Очередь пустая" text="VYRON рассчитает нужное число видео по графику каждого канала." action="СОЗДАТЬ ПЛАН" onClick={createPlan}/>:<div className="jobs">{filtered.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="job" key={j.id}><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{c?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не созданы'}</b><span>{j.publishAt?`План: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата не назначена'} • треков {j.tracksCount}/{j.minTracks}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>ПАПКА</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,c?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ МУЗЫКА</button>}{j.status==='READY_RENDER'&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('publisher')}>YOUTUBE</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,c?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>ПОВТОРИТЬ</button>}</div></div>})}</div>}</>}
.vyron-v110-audit/src/App.tsx:123:{tab==='general'&&<div className="settingsStack"><div className="settingsCard"><h3>Workspace</h3><p>VYRON хранит Inbox, проекты Video_XXX и RenderQueue.</p><div className="pathLine"><input readOnly value={s.workspace} placeholder="создаётся автоматически"/><button onClick={async()=>{const p=await api.chooseWorkspace();if(p)patch({workspace:p})}}>ВЫБРАТЬ</button><button onClick={()=>s.workspace&&api.openLocal(s.workspace)}>ОТКРЫТЬ</button></div></div><div className="settingsCard"><div className="toggleRows"><Toggle label="FPS-монитор" text="Контроль плавности интерфейса." value={s.fpsMonitor} onChange={v=>patch({fpsMonitor:v})}/><Toggle label="Уменьшить анимации" text="Отключить декоративные переходы." value={s.reduceMotion} onChange={v=>patch({reduceMotion:v})}/></div></div></div>}
.vyron-v110-audit/src/App.tsx:124:{tab==='autopilot'&&<div className="settingsStack"><div className="settingsCard"><h3>Autonomous Pipeline</h3><div className="toggleRows"><Toggle label="Автопилот" text="Запускать цикл автоматически." value={s.autopilotEnabled} onChange={v=>patch({autopilotEnabled:v})}/><Toggle label="Поддерживать план 60 дней" text="Автоматически создавать недостающие Video_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Брать треки из Inbox/Music." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 Video_XXX." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI-метаданные (опционально)" text="Только если вручную подключён отдельный API. Для бесплатного режима используй ChatGPT Plus → Metadata Inbox." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="Очередь ENDLUME" text="Создавать RenderQueue marker для готовых проектов." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Запускать приложение при появлении новых render-задач." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/><Toggle label="YouTube Autoupload" text="Загружать по одному готовому видео за цикл и ставить publishAt." value={s.autoUploadYoutube} onChange={v=>patch({autoUploadYoutube:v})}/></div></div><div className="settingsCard"><h3>Параметры</h3><div className="editGrid"><label>Цикл, секунд<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label><label>Треков по умолчанию<input type="number" min="1" max="100" value={s.tracksPerVideo} onChange={e=>patch({tracksPerVideo:+e.target.value||10})}/></label></div></div></div>}
.vyron-v110-audit/src/App.tsx:125:{tab==='integrations'&&<div className="settingsStack"><div className="settingsCard"><h3>ENDLUME Studio</h3><p>VYRON отправляет готовые проекты в RenderQueue.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>ВЫБРАТЬ</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>ОТКРЫТЬ</button>}</div><small className="note">Полностью нулевой ручной рендер требует companion-update ENDLUME, который будет читать RenderQueue.</small></div><div className="settingsCard"><h3>OpenAI API — опционально</h3><p>Не требуется для бесплатного режима. Оставь пустым и используй ChatGPT Plus → Metadata Inbox. API нужен только если позже захочешь полностью фоновую генерацию без открытия ChatGPT.</p><input className="wideInput" type="password" placeholder="sk-…" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/><div className="pathLine"><input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/><button onClick={()=>patch({openaiModel:''})}>ОЧИСТИТЬ</button></div></div><div className="settingsCard"><h3>YouTube OAuth</h3><p>Desktop OAuth открывает Google в браузере один раз. После этого токен обновляется локально автоматически.</p><input className="wideInput" placeholder="Google OAuth Client ID …apps.googleusercontent.com" value={s.youtubeOAuthClientId} onChange={e=>patch({youtubeOAuthClientId:e.target.value.trim()})}/><input className="wideInput" type="password" autoComplete="off" placeholder="Google OAuth Client Secret — вводится один раз" value={oauthSecret} onChange={e=>setOauthSecret(e.target.value.trim())}/><small className="note">Client Secret не сохраняется в общих настройках и не выводится в логах. После успешного OAuth он хранится только внутри локального OAuth-профиля.</small><div className="cardActions"><button className="primary" disabled={connecting||!s.youtubeOAuthClientId||!oauthSecret} onClick={async()=>{setConnecting(true);setOauthError(false);setOauthMessage('OAuth: ожидаю подтверждение Google…');try{const p=await api.youtubeConnect(s.youtubeOAuthClientId,oauthSecret);setOauthSecret('');const m=`YouTube подключён: ${p.channelTitle||p.channelId||'канал'}`;setOauthMessage(m);toast(m);log(m);await refreshProfiles()}catch(e){const m=`OAuth: ${String(e)}`;setOauthError(true);setOauthMessage(m);toast(m);log(m,'error')}finally{setConnecting(false)}}}>{connecting?'ЖДУ GOOGLE…':'ПОДКЛЮЧИТЬ YOUTUBE'}</button><button onClick={async()=>{try{const p=await refreshProfiles();toast(p.length?`OAuth профилей: ${p.length}`:'OAuth профили не найдены')}catch(e){toast(String(e))}}}>↻ ОБНОВИТЬ</button></div>{oauthMessage&&<div className={oauthError?'errorBox':profiles.length?'successBox':'publisherNotice'}>{oauthMessage}</div>}{profiles.map(p=><div className="oauthProfile" key={p.id}><span><b>{p.channelTitle||'YouTube канал'}</b><small>{p.channelId||p.clientIdMasked}</small></span><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refreshProfiles()}}>ОТКЛЮЧИТЬ</button></div>)}<label className="compactLabel">YouTube category ID<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div><div className="settingsCard"><h3>YouTube Intelligence</h3><p>Автоматически обновляет приватную аналитику своих каналов и публичные показатели конкурентов, пока VYRON запущен.</p><Toggle label="Автообновление аналитики" text="Обновлять подключённые каналы и список конкурентов без ручной кнопки." value={s.youtubeIntelligenceAutoRefresh} onChange={v=>patch({youtubeIntelligenceAutoRefresh:v})}/><label className="compactLabel">Интервал, минут<input type="number" min="15" max="1440" value={s.youtubeIntelligenceRefreshMin} onChange={e=>patch({youtubeIntelligenceRefreshMin:Math.max(15,+e.target.value||30)})}/></label><small className="note">Для приватной аналитики включи YouTube Analytics API в Google Cloud и один раз переподключи OAuth после установки 0.6.0. Конкуренты используют только публичные данные.</small></div><div className="settingsCard"><h3>YouTube Data API Key</h3><p>Только публичная статистика своих каналов и конкурентов.</p><input className="wideInput" type="password" placeholder="AIza…" value={s.youtubeApiKey} onChange={e=>patch({youtubeApiKey:e.target.value.trim()})}/></div><div className="settingsCard"><h3>MusicVault AI</h3><p>VYRON принимает музыку через Inbox каждого канала. Следующий шаг — кнопка «Отправить в VYRON» прямо в MusicVault AI, чтобы не переносить файлы вручную.</p></div></div>}
.vyron-v110-audit/src/App.tsx:129:{tab==='about'&&<div className="settingsCard about"><div className="aboutMark">▶</div><h2>VYRON</h2><p>Autonomous Content Operating System</p><div className="aboutRows"><span>Версия <b>0.9.9</b></span><span>macOS <b>Apple Silicon M1+</b></span><span>UI <b>Tauri 2 + React</b></span><span>Core <b>Rust</b></span><span>Pipeline <b>SUNO → ENDLUME → YouTube</b></span><span>Target UI <b>60 FPS</b></span></div></div>}</>}
.vyron-v110-audit/src/main.tsx:12:createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
.vyron-v110-audit/src/ProductionWorkspace.tsx:28:  <div className="simpleFlow"><span className={images&&music?'done':''}>Материалы</span><i>→</i><span className={videos?'done':''}>ENDLUME / Рендер</span><i>→</i><span className={seo?'done':''}>SEO</span><i>→</i><button className={pref.reviewed?'done':''} onClick={()=>patch({reviewed:!pref.reviewed})}>{pref.reviewed?'Проверено ✓':'Проверить'}</button><i>→</i><span className={ready?'done':''}>Можно публиковать</span></div>
.vyron-v110-audit/src/productionPrefs.test.ts:32:    patchChannelProductionPrefs('neon',{projectCount:40,tracksPerProject:30,mode:'alphabetical',allowImageReuse:true,lastBatchId:'BATCH-14',selectedProjectIds:['001','003']});
.vyron-v110-audit/src/productionPrefs.test.ts:39:    expect(p.selectedProjectIds).toEqual(['001','003']);
.vyron-v110-audit/src/SettingsOS.tsx:7:type Tab='general'|'youtube'|'autopilot'|'endlume'|'updates'|'diagnostics';
.vyron-v110-audit/src/SettingsOS.tsx:17: async function runDiagnostics(){setChecking(true);try{let ws=s.workspace;if(!ws){ws=await api.defaultWorkspace();patch({workspace:ws})}const core=await api.diagnostics(ws);setDiag([{label:'Local Core',status:core.ok?'Готово':'Требует внимания',ok:core.ok},{label:'Workspace',status:core.workspaceWritable?'Доступен для записи':'Проблема записи',ok:core.workspaceWritable},{label:'ENDLUME',status:s.endlumePath?'Путь настроен':'Не выбран',ok:Boolean(s.endlumePath)},{label:'Updater',status:'Tauri signed updater подключён',ok:true},{label:'YouTube',status:'Проверяется только во вкладке YouTube',detail:'Zero Quota Policy: настройки не вызывают YouTube Data API.'}])}catch(e){setDiag([{label:'Диагностика',status:'Ошибка',ok:false,detail:String(e)}])}finally{setChecking(false)}}
.vyron-v110-audit/src/SettingsOS.tsx:20: const label=(t:Tab)=>t==='general'?'Общие':t==='youtube'?'YouTube':t==='autopilot'?'Автопилот':t==='endlume'?'ENDLUME':t==='updates'?'Обновления':'Диагностика';
.vyron-v110-audit/src/SettingsOS.tsx:21: return <><div className="pageHeader"><div><small>SETTINGS</small><h1>Настройки</h1><p>Локальная конфигурация VYRON. Ни один переход сюда не расходует YouTube API quota.</p></div></div><div className="settingsTabs osTabs">{(['general','youtube','autopilot','endlume','updates','diagnostics'] as Tab[]).map(t=><button className={tab===t?'active':''} key={t} onClick={()=>setTab(t)}>{label(t)}</button>)}</div>
.vyron-v110-audit/src/SettingsOS.tsx:24: {tab==='autopilot'&&<div className="settingsStack"><section className="settingsCard"><h3>Режим Autopilot</h3><div className="modeSwitch large"><button className={mode==='off'?'active':''} onClick={()=>setMode('off')}>OFF</button><button className={mode==='assisted'?'active':''} onClick={()=>setMode('assisted')}>ASSISTED</button><button className={mode==='full'?'active':''} onClick={()=>setMode('full')}>FULL LOCAL</button></div><p>{mode==='off'?'Автоматизация выключена.':'Autopilot обслуживает только локальный Production pipeline и ENDLUME. YouTube API всегда остаётся ручным.'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced Autopilot</summary><div className="toggleRows"><Toggle label="Поддерживать план" text="Создавать недостающие VIDEO_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Inbox/Music → проекты." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 проект." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI metadata" text="Только при подключённом OpenAI API." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="ENDLUME queue" text="Создавать render marker." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Открывать приложение при render задачах." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/></div><label>Интервал локального цикла, сек<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:25: {tab==='endlume'&&<div className="settingsStack"><section className="settingsCard"><h3>ENDLUME Studio</h3><p>Готовые локальные проекты отправляются в RenderQueue без обращения к YouTube.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>Выбрать</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>Открыть</button>}</div></section></div>}
.vyron-v110-audit/src/autopilotRuntime.ts:2:import {productionManagerApi} from './productionManagerApi';
.vyron-v110-audit/src/autopilotRuntime.ts:9:let lastEndlumeLaunch=0;
.vyron-v110-audit/src/autopilotRuntime.ts:10:const RENDER_REQUEUE_MS=10*60*1000;
.vyron-v110-audit/src/autopilotRuntime.ts:11:const ENDLUME_LAUNCH_COOLDOWN_MS=5*60*1000;
.vyron-v110-audit/src/autopilotRuntime.ts:13:function blankSummary():AutopilotSummary{return {prepared:0,tracksMoved:0,imagesMoved:0,metadataGenerated:0,renderQueued:0,uploads:0,errors:0,notes:[]}}
.vyron-v110-audit/src/autopilotRuntime.ts:18:  const storage=await productionManagerApi.storageStatus(projectRoot);
.vyron-v110-audit/src/autopilotRuntime.ts:85:  if(s.autoQueueRender){
.vyron-v110-audit/src/autopilotRuntime.ts:86:    jobs=useApp.getState().jobs.filter(j=>j.channelId===channel.id&&j.status==='READY_RENDER'&&j.folder);
.vyron-v110-audit/src/autopilotRuntime.ts:88:      const last=j.renderQueuedAt?new Date(j.renderQueuedAt).getTime():0;
.vyron-v110-audit/src/autopilotRuntime.ts:89:      if(last&&Date.now()-last<RENDER_REQUEUE_MS)continue;
.vyron-v110-audit/src/autopilotRuntime.ts:90:      try{await api.enqueueRender(workspace,j.folder);useApp.getState().patchJob(j.id,{renderQueuedAt:new Date().toISOString(),lastAutomationAt:new Date().toISOString()});summary.renderQueued++}catch(e){logError(summary,`${channel.name} Video_${j.number}: очередь ENDLUME`,e)}
.vyron-v110-audit/src/autopilotRuntime.ts:92:    if(s.autoOpenEndlume&&summary.renderQueued>0&&s.endlumePath&&Date.now()-lastEndlumeLaunch>ENDLUME_LAUNCH_COOLDOWN_MS){try{await api.openEndlume(s.endlumePath);lastEndlumeLaunch=Date.now()}catch(e){logError(summary,'ENDLUME запуск',e)}}
.vyron-v110-audit/src/autopilotRuntime.ts:105:    const moved=summary.tracksMoved+summary.imagesMoved+summary.renderQueued+summary.metadataGenerated+summary.prepared;
.vyron-v110-audit/src/autopilotRuntime.ts:106:    if(moved||summary.errors)useApp.getState().log(`Autopilot: папки ${summary.prepared}, треки ${summary.tracksMoved}, изображения ${summary.imagesMoved}, AI ${summary.metadataGenerated}, рендер ${summary.renderQueued}, ошибок ${summary.errors}`,summary.errors?'warn':'info');
.vyron-v110-audit/src/ProductionOS.tsx:8:import {ProductionManager} from './ProductionManager';
.vyron-v110-audit/src/ProductionOS.tsx:9:import {productionManagerApi,type ProductionStorageStatus} from './productionManagerApi';
.vyron-v110-audit/src/ProductionOS.tsx:14:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужно изображение',WAITING_MUSIC:'Нужно аудио',READY_RENDER:'Готов к ENDLUME',RENDERING:'Рендер',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загрузка YouTube',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/ProductionOS.tsx:15:const stageOrder:JobStatus[]=['NEED_IMAGE','WAITING_MUSIC','READY_RENDER','RENDERING','READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'];
.vyron-v110-audit/src/ProductionOS.tsx:18: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [prefs,patchPrefs]=useProductionPrefs();const tab=(prefs.tab||'queue') as Tab;const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';const setTab=(v:Tab)=>patchPrefs({tab:v});const setChannelId=(id:string)=>patchPrefs({selectedChannelId:id});const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false),[inbox,setInbox]=useState<any>(),[planOpen,setPlanOpen]=useState(false),[planCount,setPlanCount]=useState(30),[planScope,setPlanScope]=useState<'one'|'all'>('one');const [section,setSection]=useState<'overview'|'materials'|'builder'|'render'|'publish'>(()=>tab==='materials'?'materials':tab==='manager'?'builder':'overview');const scopedJobs=section==='render'?jobs.filter(j=>['READY_RENDER','RENDERING','ERROR'].includes(j.status)):section==='publish'?jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)):jobs;const filtered=scopedJobs.filter(j=>filter==='all'||j.channelId===filter||j.status===filter).sort((a,b)=>a.number-b.number);const c=channels.find(x=>x.id===channelId);const selectedJobIds=(prefs.selectedJobIds||[]).filter(id=>jobs.some(j=>j.id===id));const projectRoot=resolveProductionRootFromPrefs(prefs,channelId,settings.workspace);const [projectStorage,setProjectStorage]=useState<ProductionStorageStatus|null>(null);
.vyron-v110-audit/src/ProductionOS.tsx:20: useEffect(()=>{let live=true;if(!projectRoot){setProjectStorage(null);return}productionManagerApi.storageStatus(projectRoot).then(x=>{if(live)setProjectStorage(x)}).catch(e=>{if(live)setProjectStorage({path:projectRoot,exists:false,writable:false,external:projectRoot.startsWith('/Volumes/'),freeBytes:null,error:String(e)})});return()=>{live=false}},[projectRoot]);
.vyron-v110-audit/src/ProductionOS.tsx:22: async function chooseProjectRoot(scope:'global'|'channel'){const initial=scope==='channel'?(c?prefs.byChannel[c.id]?.productionRoot:undefined)||projectRoot:(prefs.productionRoot||settings.workspace||projectRoot);const requested=await productionManagerApi.chooseProductionRoot(initial||undefined);if(!requested)return;const st=await productionManagerApi.storageStatus(requested);if(!st.exists||!st.writable){toast(st.error||'Выбранная папка проектов недоступна для записи');return}const path=st.path||requested;if(scope==='channel'&&c){patchChannelProductionPrefs(c.id,{productionRoot:path});toast(`Отдельная папка для ${c.name} сохранена`)}else{patchPrefs({productionRoot:path});toast('Основная папка проектов сохранена')}setProjectStorage(st)}
.vyron-v110-audit/src/ProductionOS.tsx:25: async function deleteJobs(ids:string[],label:string){const unique=[...new Set(ids)].filter(id=>useApp.getState().jobs.some(j=>j.id===id));if(!unique.length)return;if(!confirm(label))return;setBusy(true);try{const rows=useApp.getState().jobs.filter(j=>unique.includes(j.id));for(const j of rows){if(!j.folder)continue;const roots=[resolveProductionRootFromPrefs(prefs,j.channelId,settings.workspace),inferJobRoot(j.folder),settings.workspace].filter((x,i,a)=>x&&a.indexOf(x)===i);let done=false,last:unknown;for(const root of roots){try{await productionManagerApi.deleteJobFolder(root,j.folder);done=true;break}catch(e){last=e}}if(!done)throw last||new Error('Не удалось определить хранилище проекта')}setJobs(useApp.getState().jobs.filter(j=>!unique.includes(j.id)));patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!unique.includes(id))});toast(`Удалено проектов: ${unique.length}`)}catch(e){toast(`Не удалось удалить проекты: ${String(e)}`)}finally{setBusy(false)}}
.vyron-v110-audit/src/ProductionOS.tsx:32: async function importImages(){if(!c)return;let root=projectRoot;if(!root){root=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:root})}const storage=await productionManagerApi.storageStatus(root);if(!storage.exists||!storage.writable){toast(storage.error||`Папка проектов недоступна: ${root}`);return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);if(!nums.length){toast('Нет проектов, ожидающих изображение');return}const imported=await api.importImages(root,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено по VIDEO_${String(nums[0]).padStart(3,'0')}…`)}
.vyron-v110-audit/src/ProductionOS.tsx:34:  <div className="pageHeader"><div><small>PRODUCTION PIPELINE</small><h1>Производство</h1><p>Понятная цепочка от материалов до YouTube. Backend-очередь и ENDLUME остаются прежними — меняется только управление.</p></div><div className="headerActions"><button disabled={busy} onClick={refreshAll}>{busy?'Проверяю…':'↻ Обновить статусы'}</button><button onClick={maintainBuffer}>Поддержать буфер</button><button className="primary compactAction" onClick={()=>setPlanOpen(true)}>+ Создать проекты</button></div></div>
.vyron-v110-audit/src/ProductionOS.tsx:35:  <section className="panel pmStorageUnified"><div className="pmStorageUnifiedMain"><small>ХРАНИЛИЩЕ ПРОЕКТОВ</small><h3>{projectRoot||'Папка не выбрана'}</h3><p>{projectStorage?.external?'Внешний диск':'Внутренний диск'} • {projectStorage?.writable?'запись доступна':'нужно проверить доступ'}{typeof projectStorage?.freeBytes==='number'?` • свободно ${(projectStorage.freeBytes/1024/1024/1024).toFixed(1)} GB`:''}</p></div><div className="pmActions"><button className="primary" onClick={()=>void chooseProjectRoot('global')}>ИЗМЕНИТЬ ПАПКУ</button>{c&&<button onClick={()=>void chooseProjectRoot('channel')}>ОТДЕЛЬНАЯ ПАПКА ДЛЯ КАНАЛА</button>}{projectRoot&&<button onClick={()=>void productionManagerApi.openFolder(projectRoot)}>ОТКРЫТЬ В FINDER</button>}</div>{projectStorage?.error&&<div className="pmStorageError">{projectStorage.error}</div>}</section>
.vyron-v110-audit/src/ProductionOS.tsx:36:  <div className="youtubeTabs productionTabs productionNavFive"><button className={section==='overview'?'active':''} onClick={()=>{setSection('overview');setTab('queue');setFilter('all')}}>Обзор</button><button className={section==='materials'?'active':''} onClick={()=>{setSection('materials');setTab('materials')}}>Материалы</button><button className={section==='builder'?'active':''} onClick={()=>{setSection('builder');setTab('manager')}}>Сборка проектов</button><button className={section==='render'?'active':''} onClick={()=>{setSection('render');setTab('queue');setFilter('all')}}>Рендер</button><button className={section==='publish'?'active':''} onClick={()=>{setSection('publish');setTab('queue');setFilter('all')}}>Публикация</button></div>
.vyron-v110-audit/src/ProductionOS.tsx:37:  {tab==='queue'&&<>{section==='overview'&&<ProductionWorkspace/>}<div className="productionPipeline">{stageOrder.map(st=><button key={st} className={st==='ERROR'&&jobs.some(j=>j.status==='ERROR')?'warn':''} onClick={()=>setFilter(st)}><small>{statusLabel[st]}</small><b>{jobs.filter(j=>j.status===st).length}</b></button>)}</div><div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы / статусы</option>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}{stageOrder.map(st=><option key={st} value={st}>{statusLabel[st]}</option>)}</select><span>{filtered.length} задач</span></div>{filtered.length===0?<div className="empty"><b>Нет задач в этом фильтре</b><p>Создай точное число проектов или включи поддержание буфера.</p><button className="primary" onClick={()=>setPlanOpen(true)}>Создать проекты</button></div>:<div className="jobs virtualReady"><div className="pmBulkBar"><span>Выбрано <b>{selectedJobIds.filter(id=>visibleJobIds.includes(id)).length}</b> / {visibleJobIds.length}</span><button onClick={()=>patchPrefs({selectedJobIds:[...new Set([...selectedJobIds,...visibleJobIds])]})}>Выбрать всё</button><button onClick={()=>patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!visibleJobIds.includes(id))})}>Снять выделение</button><button className="danger" disabled={!selectedJobIds.some(id=>visibleJobIds.includes(id))} onClick={()=>void deleteJobs(selectedJobIds.filter(id=>visibleJobIds.includes(id)),'Удалить выбранные проекты?')}>Удалить выбранные</button><button className="danger" onClick={()=>void deleteJobs(visibleJobIds,'Удалить все проекты в текущем списке?')}>Удалить все</button></div>{filtered.map(j=>{const ch=channels.find(x=>x.id===j.channelId);return <article className="job productionJob" key={j.id}><label className="pmJobCheck"><input type="checkbox" checked={selectedJobIds.includes(j.id)} onChange={()=>toggleJob(j.id)}/></label><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{ch?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не готовы'}</b><span>Изображение {j.coverPath?'✓':'—'} • треков {j.tracksCount}/{j.minTracks} • final.mp4 {j.finalPath?'✓':'—'} • metadata {j.title&&j.description?'✓':'—'}</span><span>{j.publishAt?`Публикация: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата публикации не назначена'}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>Папка</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,ch?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ Аудио</button>}{j.status==='READY_RENDER'&&j.folder&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в ENDLUME RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('youtube')}>YouTube</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,ch?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>Повторить</button>}</div></article>})}</div>}</>}
.vyron-v110-audit/src/ProductionOS.tsx:38:  {tab==='materials'&&<><section className="panel materialToolbar"><select value={channelId} onChange={e=>{setChannelId(e.target.value);setInbox(undefined)}}><option value="">— канал —</option>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="primary" disabled={!c} onClick={importImages}>+ Импортировать изображения</button></section><ProductionManager view="materials"/>{inbox&&<div className="panel"><b>Папка материалов готова</b><p>{inbox.root}</p></div>}</>}
.vyron-v110-audit/src/ProductionOS.tsx:39:  {tab==='manager'&&<ProductionManager view="builder"/>}
.vyron-v110-audit/src/api.ts:27:  chooseEndlume:async()=>{const r=await open({directory:false,multiple:false,title:'Выберите ENDLUME Studio.app'});return typeof r==='string'?r:null},
.vyron-v110-audit/src/api.ts:39:  enqueueRender:(workspace:string,jobFolder:string)=>invoke<{queueFile:string}>('enqueue_render',{workspace,jobFolder}),
.vyron-v110-audit/src/api.ts:41:  openEndlume:(path:string)=>invoke<void>('open_endlume',{path}),
.vyron-v110-audit/src/PublisherOS.tsx:9:const statusLabel:Record<JobStatus,string>={NEED_IMAGE:'Нужна картинка',WAITING_MUSIC:'Ждёт музыку',READY_RENDER:'Готов к рендеру',RENDERING:'Рендерится',READY_UPLOAD:'Готов к YouTube',UPLOADING:'Загружается',SCHEDULED:'Запланирован',ERROR:'Ошибка'};
.vyron-v110-audit/src/types.ts:3:export type JobStatus='NEED_IMAGE'|'WAITING_MUSIC'|'READY_RENDER'|'RENDERING'|'READY_UPLOAD'|'UPLOADING'|'SCHEDULED'|'ERROR';
.vyron-v110-audit/src/types.ts:24:  renderQueuedAt?:string; lastAutomationAt?:string;
.vyron-v110-audit/src/types.ts:35:  workspace:string; endlumePath:string; youtubeApiKey:string; autoCheckUpdates:boolean; reduceMotion:boolean; fpsMonitor:boolean;
.vyron-v110-audit/src/types.ts:37:  autoQueueRender:boolean; autoOpenEndlume:boolean; autoUploadYoutube:boolean; autopilotIntervalSec:number; tracksPerVideo:number;
.vyron-v110-audit/src/types.ts:41:  endlumeTargetDurationMin:number; endlumeTargetRenderSec:number; endlumeTargetFileMinMb:number; endlumeTargetFileMaxMb:number; endlumePreserveImageQuality:boolean; endlumeProjectNaming:string;
.vyron-v110-audit/src/types.ts:48:export type AutopilotSummary={prepared:number;tracksMoved:number;imagesMoved:number;metadataGenerated:number;renderQueued:number;uploads:number;errors:number;notes:string[]};
.vyron-v110-audit/src/productionManagerApi.ts:13:export type Validation={batchId:string;ready:number;errors:number;endlumeExists:boolean;items:{projectId:string;ok:boolean;error?:string|null}[]};
.vyron-v110-audit/src/productionManagerApi.ts:14:export type BatchStatus={batchId:string;status:string;updatedAt?:string;projects:{projectId:string;jobId?:string|null;videoNumber?:number|null;renderStatus:string;outputFile?:string|null;duration?:number|null;fileSize?:number|null;error?:string|null}[]};
.vyron-v110-audit/src/productionManagerApi.ts:15:export type DeleteResult={deletedProjectIds:string[];deletedJobIds:string[];batch?:BatchSummary|null};
.vyron-v110-audit/src/productionManagerApi.ts:16:export type HandoffReceipt={batchId:string;manifestPath:string;requestPath:string};
.vyron-v110-audit/src/productionManagerApi.ts:19:export const productionManagerApi={
.vyron-v110-audit/src/productionManagerApi.ts:35:  validate:(manifestPath:string,endlumePath:string)=>invoke<Validation>('validate_production_batch',{manifestPath,endlumePath}),
.vyron-v110-audit/src/productionManagerApi.ts:36:  deleteBatchProjects:(manifestPath:string,projectIds:string[])=>invoke<DeleteResult>('delete_production_batch_projects',{manifestPath,projectIds}),
.vyron-v110-audit/src/productionManagerApi.ts:38:  openInEndlume:(endlumePath:string,manifestPath:string)=>invoke<HandoffReceipt>('open_production_batch_in_endlume',{endlumePath,manifestPath}),
.vyron-v110-audit/src/productionManagerApi.ts:39:  handoffConsumed:(requestPath:string)=>invoke<boolean>('production_endlume_handoff_consumed',{requestPath}),
.vyron-v110-audit/src/commandCenterCore.ts:88:  channelId:string;targetProjects:number;createdProjects:number;renderedVideos:number;readyToPublish:number;percent:number;
.vyron-v110-audit/src/commandCenterCore.ts:94:  return{channelId:channel.id,targetProjects:base.target,createdProjects:rows.length,renderedVideos:base.videos,readyToPublish:base.readyToYoutube,percent,covers:base.covers,music:base.music,seo:base.seo,schedule:base.schedule,errors:base.errors};
.vyron-v110-audit/src/productionPrefs.ts:2:import type {DistributionMode} from './productionManagerApi';
.vyron-v110-audit/src/productionPrefs.ts:5:export type ChannelProductionPrefs={projectCount:number;tracksPerProject:number;mode:DistributionMode;allowImageReuse:boolean;lastBatchId?:string;selectedProjectIds:string[];productionRoot?:string};
.vyron-v110-audit/src/productionPrefs.ts:10:export const defaultChannelProductionPrefs=():ChannelProductionPrefs=>({projectCount:30,tracksPerProject:15,mode:'even',allowImageReuse:false,selectedProjectIds:[]});
.vyron-v110-audit/src/ProductionManager.tsx:3:import {productionManagerApi,type BatchStatus,type BatchSummary,type BuildResult,type ChannelProductionState,type DistributionMode,type ImportSession,type MusicSummary,type ProductionStorageStatus,type Validation} from './productionManagerApi';
.vyron-v110-audit/src/ProductionManager.tsx:19:export function ProductionManager({view='all'}:{view?:'all'|'materials'|'builder'}){
.vyron-v110-audit/src/ProductionManager.tsx:34:  const selectedProjectIds=channelPrefs.selectedProjectIds||[];
.vyron-v110-audit/src/ProductionManager.tsx:50:  const endlumePath=settings.endlumePath||'';
.vyron-v110-audit/src/ProductionManager.tsx:60:      const state:ChannelProductionState=await productionManagerApi.state(workspace,channelId);
.vyron-v110-audit/src/ProductionManager.tsx:63:      try{base=await productionManagerApi.batches(workspace,channelId)}catch{}
.vyron-v110-audit/src/ProductionManager.tsx:65:    if(productionRoot&&productionRoot!==workspace){try{base=[...base,...await productionManagerApi.batches(productionRoot,channelId)]}catch{}}
.vyron-v110-audit/src/ProductionManager.tsx:70:  useEffect(()=>{let live=true;if(!productionRoot){setStorageStatus(null);return}productionManagerApi.storageStatus(productionRoot).then(s=>{if(live)setStorageStatus(s)}).catch(e=>{if(live)setStorageStatus({path:productionRoot,exists:false,writable:false,external:productionRoot.startsWith('/Volumes/'),freeBytes:null,error:String(e)})});return()=>{live=false}},[productionRoot]);
.vyron-v110-audit/src/ProductionManager.tsx:71:  useEffect(()=>{if(result?.batch){productionManagerApi.status(result.batch.manifestPath).then(setBatchStatus).catch(()=>setBatchStatus(null));return}const last=channelPrefs.lastBatchId&&batches.find(b=>b.batchId===channelPrefs.lastBatchId);if(last)setResult({status:'ready',availableImages:collected,requestedProjects:last.projectCount,batch:last})},[batches,result?.batch?.batchId,channelPrefs.lastBatchId]);
.vyron-v110-audit/src/ProductionManager.tsx:76:    offs.push(productionManagerApi.onImportProgress(p=>{if(live&&p.channelId===channelId)void refreshState()}));
.vyron-v110-audit/src/ProductionManager.tsx:77:    offs.push(productionManagerApi.onImportError(p=>{if(live&&p.channelId===channelId)setImportError(p.message||'Ошибка сбора изображений')}));
.vyron-v110-audit/src/ProductionManager.tsx:78:    offs.push(productionManagerApi.onBatchProgress(p=>{
.vyron-v110-audit/src/ProductionManager.tsx:88:    const timer=window.setInterval(()=>{productionManagerApi.importStatus(workspace,channelId).then(next=>{setSession(next);if(!next.active)setImportError('')}).catch(e=>setImportError(String(e)))},1200);
.vyron-v110-audit/src/ProductionManager.tsx:99:      const next=session?.active?await productionManagerApi.stopImport(workspace,channel.id):await productionManagerApi.startImport(workspace,channel.id,channel.name);
.vyron-v110-audit/src/ProductionManager.tsx:107:    const path=await productionManagerApi.chooseMusicFolder(music?.libraryPath||undefined);if(!path)return;
.vyron-v110-audit/src/ProductionManager.tsx:110:      await productionManagerApi.setMusicLibrary(workspace,channel.id,channel.name,path);
.vyron-v110-audit/src/ProductionManager.tsx:111:      const indexed=await productionManagerApi.indexMusic(workspace,channel.id);
.vyron-v110-audit/src/ProductionManager.tsx:119:    try{const indexed=await productionManagerApi.indexMusic(workspace,channelId);setMusic(indexed);notifySuccess('Музыкальная библиотека обновлена',`${indexed.tracks.toLocaleString('ru-RU')} треков доступно.`,{operationId:`music-reindex:${channelId}:${indexed.indexedAt}`})}
.vyron-v110-audit/src/ProductionManager.tsx:144:      const built=await productionManagerApi.build({requestId:crypto.randomUUID(),workspace,outputWorkspace:productionRoot||workspace,channelId:channel.id,channelName:channel.name,projectCount,tracksPerProject,mode,allowImageReuse:reuse,jobLinks:links.map(j=>({jobId:j.id,number:j.number}))});
.vyron-v110-audit/src/ProductionManager.tsx:147:        setBatches(prev=>[built.batch!,...prev.filter(x=>x.batchId!==built.batch!.batchId)]);patchChannelProductionPrefs(channel.id,{lastBatchId:built.batch.batchId,selectedProjectIds:[]});
.vyron-v110-audit/src/ProductionManager.tsx:148:        notifySuccess('Проекты созданы',`${built.batch.projectCount.toLocaleString('ru-RU')} проектов готовы для ENDLUME.`,{operationId:`batch-built:${built.batch.batchId}`});
.vyron-v110-audit/src/ProductionManager.tsx:149:        if(endlumePath){try{setValidation(await productionManagerApi.validate(built.batch.manifestPath,endlumePath))}catch{}}
.vyron-v110-audit/src/ProductionManager.tsx:155:    if(!batch)return;if(!endlumePath){toast('Укажите путь к ENDLUME в настройках VYRON');return}
.vyron-v110-audit/src/ProductionManager.tsx:157:    try{setValidation(await productionManagerApi.validate(batch.manifestPath,endlumePath))}
.vyron-v110-audit/src/ProductionManager.tsx:161:  async function waitForHandoff(requestPath:string){for(let i=0;i<40;i++){if(await productionManagerApi.handoffConsumed(requestPath))return true;await new Promise(r=>window.setTimeout(r,500))}return false}
.vyron-v110-audit/src/ProductionManager.tsx:162:  async function openEndlume(batch=result?.batch||null){
.vyron-v110-audit/src/ProductionManager.tsx:163:    if(!batch)return;if(!endlumePath){toast('Укажите путь к ENDLUME в настройках VYRON');return}
.vyron-v110-audit/src/ProductionManager.tsx:164:    setBusy('endlume');
.vyron-v110-audit/src/ProductionManager.tsx:166:      const checked=await productionManagerApi.validate(batch.manifestPath,endlumePath);setValidation(checked);
.vyron-v110-audit/src/ProductionManager.tsx:168:      const receipt=await productionManagerApi.openInEndlume(endlumePath,batch.manifestPath);
.vyron-v110-audit/src/ProductionManager.tsx:169:      const consumed=await waitForHandoff(receipt.requestPath);
.vyron-v110-audit/src/ProductionManager.tsx:170:      if(!consumed){toast('ENDLUME открыт, но batch не был принят. Проверьте, что установлен ENDLUME 1.0.0-alpha.8.44 или новее.');return}
.vyron-v110-audit/src/ProductionManager.tsx:171:      notifySuccess('Передано в ENDLUME',`${batch.projectCount.toLocaleString('ru-RU')} проектов отправлены на рендер.`,{operationId:`endlume-handoff:${receipt.batchId}`});await refreshState();setBatchStatus(await productionManagerApi.status(batch.manifestPath));
.vyron-v110-audit/src/ProductionManager.tsx:172:    }catch(e){toast(`Не удалось передать batch в ENDLUME: ${String(e)}`)}finally{setBusy('')}
.vyron-v110-audit/src/ProductionManager.tsx:174:  const setSelectedProjects=(ids:string[])=>patchChannelProductionPrefs(channelId,{selectedProjectIds:[...new Set(ids)]});
.vyron-v110-audit/src/ProductionManager.tsx:175:  const toggleProject=(id:string)=>setSelectedProjects(selectedProjectIds.includes(id)?selectedProjectIds.filter(x=>x!==id):[...selectedProjectIds,id]);
.vyron-v110-audit/src/ProductionManager.tsx:178:    setBusy('delete');try{const r=await productionManagerApi.deleteBatchProjects(batch.manifestPath,ids);if(r.deletedJobIds.length)setJobs(useApp.getState().jobs.filter(j=>!r.deletedJobIds.includes(j.id)));setSelectedProjects([]);await refreshState();if(r.batch){setResult({status:'ready',availableImages:collected,requestedProjects:r.batch.projectCount,batch:r.batch});patchChannelProductionPrefs(channelId,{lastBatchId:r.batch.batchId});setBatchStatus(await productionManagerApi.status(r.batch.manifestPath))}else{setResult(null);setBatchStatus(null);patchChannelProductionPrefs(channelId,{lastBatchId:undefined})}toast(`Удалено проектов: ${r.deletedProjectIds.length}`)}catch(e){toast(`Не удалось удалить проекты: ${String(e)}`)}finally{setBusy('')}
.vyron-v110-audit/src/ProductionManager.tsx:184:      const resumed=await productionManagerApi.resume(batch.manifestPath);
.vyron-v110-audit/src/ProductionManager.tsx:186:      await refreshState();setBatchStatus(await productionManagerApi.status(resumed.manifestPath));toast(`Batch восстановлен: ${resumed.completedProjects}/${resumed.projectCount}`);
.vyron-v110-audit/src/ProductionManager.tsx:192:    const requested=await productionManagerApi.chooseProductionRoot(initial||undefined);if(!requested)return;
.vyron-v110-audit/src/ProductionManager.tsx:193:    const status=await productionManagerApi.storageStatus(requested);if(!status.exists||!status.writable){toast(status.error||'Папка проектов недоступна для записи');return}
.vyron-v110-audit/src/ProductionManager.tsx:201:  return <div className="productionManager">
.vyron-v110-audit/src/ProductionManager.tsx:202:    <section className="panel pmHero"><div><small>ПОДГОТОВКА ДЛЯ ENDLUME</small><h2>Материалы и сборка проектов</h2><p>Соберите изображения и музыку, затем создайте готовые папки проектов для ENDLUME.</p></div><div className="pmChannelBadge"><small>ТЕКУЩИЙ КАНАЛ</small><b>{channel?.name||'—'}</b><span>Берётся из общего Production workspace</span></div></section>
.vyron-v110-audit/src/ProductionManager.tsx:208:    {view!=='materials'&&<><section className="panel pmBuilder"><div className="pmBuilderHead"><div><small>03</small><h3>Собрать проекты для ENDLUME</h3><p>Укажите количество проектов и песен. В каждой папке будет одно изображение и выбранная музыка.</p></div><div className="pmFormula"><b>{projectCount}</b><span>×</span><b>{tracksPerProject}</b><span>=</span><strong>{requiredTracks}</strong><small>назначений</small></div></div>
.vyron-v110-audit/src/ProductionManager.tsx:211:      <div className="pmReadiness"><span className={collected>=projectCount?'ok':'warn'}>Изображения<b>{collected} / {projectCount}</b></span><span className={music?.tracks?'ok':'warn'}>Музыка<b>{music?.tracks||0} треков</b></span><span className={productionRoot?'ok':'warn'}>Папка проектов<b>{productionRoot?'готова':'не выбрана'}</b></span><span className={endlumePath?'ok':'warn'}>ENDLUME<b>{endlumePath?'настроен':'путь не задан'}</b></span></div>
.vyron-v110-audit/src/ProductionManager.tsx:217:    {result?.batch&&<section className="panel pmResult"><div className="pmResultHead"><div><small>ГОТОВО ДЛЯ ENDLUME</small><h3>{batchLabel(result.batch)}</h3><p>{result.batch.projectCount} проектов • {result.batch.tracksAssigned} музыкальных назначений • manifest готов</p><code className="pmPath">Batch: {result.batch.rootPath}</code><div className="pmActions"><button onClick={()=>void productionManagerApi.openFolder(result.batch!.rootPath)}>ОТКРЫТЬ ПАПКУ BATCH</button></div></div>{validation&&<div className={`pmValidation ${validation.errors?'bad':'good'}`}>{validation.errors?`ОШИБОК ${validation.errors}`:`ГОТОВО ${validation.ready}/${result.batch.projectCount}`}</div>}</div><div className="pmChecklist"><span>Папки<b>{result.batch.projectCount}</b></span><span>Изображения<b>{result.batch.projectCount}</b></span><span>Музыка<b>{result.batch.tracksAssigned}</b></span><span>Manifest<b>готов</b></span><span>ENDLUME<b>{validation?.endlumeExists?'найден':endlumePath?'проверить':'не задан'}</b></span></div>{validation&&validation.errors>0&&<div className="pmErrors">{validation.items.filter(x=>!x.ok).slice(0,8).map(x=><div key={x.projectId}><b>{x.projectId}</b><span>{x.error||'Ошибка проверки'}</span></div>)}</div>}{batchStatus?.projects?.length?<div className="pmProjectSelection"><div className="pmBulkBar"><span>Проекты batch • выбрано <b>{selectedProjectIds.filter(id=>batchStatus.projects.some(p=>p.projectId===id)).length}</b> / {batchStatus.projects.length}</span><button onClick={()=>setSelectedProjects(batchStatus.projects.map(p=>p.projectId))}>Выбрать всё</button><button onClick={()=>setSelectedProjects([])}>Снять выделение</button><button className="danger" disabled={!selectedProjectIds.length||busy==='delete'} onClick={()=>void deleteBatchProjects(selectedProjectIds,false)}>Удалить выбранные</button><button className="danger" disabled={busy==='delete'} onClick={()=>void deleteBatchProjects(batchStatus.projects.map(p=>p.projectId),true)}>Удалить все</button></div><div className="pmProjectRows">{batchStatus.projects.map(p=><label key={p.projectId}><input type="checkbox" checked={selectedProjectIds.includes(p.projectId)} onChange={()=>toggleProject(p.projectId)}/><b>{p.projectId}</b><span>{p.renderStatus}</span><small>{p.outputFile?'MP4 готов':p.error||'ожидает'}</small></label>)}</div></div>:null}<div className="pmActions resultActions"><button disabled={busy==='validate'} onClick={()=>void validateBatch()}>ПРОВЕРИТЬ</button><button className="primary" disabled={busy==='endlume'||!!validation?.errors} onClick={()=>void openEndlume()}>ОТКРЫТЬ В ENDLUME</button></div></section>}
.vyron-v110-audit/src/ProductionManager.tsx:219:    <section className="panel pmHistory"><div className="pmHistoryHead"><div><small>ИСТОРИЯ СБОРОК</small><h3>{channel?.name||'Канал'}</h3></div><button onClick={()=>void refreshState()}>↻ ОБНОВИТЬ</button></div>{batches.length?<div className="pmBatchRows">{batches.slice(0,20).map(batch=><button key={batch.batchId} className={result?.batch?.batchId===batch.batchId?'active':''} onClick={()=>{setResult({status:'ready',availableImages:collected,requestedProjects:batch.projectCount,batch});patchChannelProductionPrefs(channelId,{lastBatchId:batch.batchId,selectedProjectIds:[]});void productionManagerApi.status(batch.manifestPath).then(setBatchStatus).catch(()=>setBatchStatus(null))}}><span><b>{batch.batchId}</b><small>{new Date(batch.createdAt).toLocaleString('ru-RU')}</small></span><em>{batch.projectCount} проектов</em><em>{batch.completedProjects}/{batch.projectCount}</em><small>{batch.status}</small><strong onClick={e=>{e.stopPropagation();void resume(batch)}}>{busy===batch.batchId?'…':'ОТКРЫТЬ'}</strong></button>)}</div>:<div className="pmEmpty">Для этого канала ещё нет Production batches.</div>}</section></>}
.vyron-v110-audit/src/ProductionStatusBridge.tsx:2:import {productionManagerApi} from './productionManagerApi';
.vyron-v110-audit/src/ProductionStatusBridge.tsx:14:          let batches=[] as Awaited<ReturnType<typeof productionManagerApi.batches>>;
.vyron-v110-audit/src/ProductionStatusBridge.tsx:15:          try{batches=await productionManagerApi.batches(workspace,channel.id)}catch{continue}
.vyron-v110-audit/src/ProductionStatusBridge.tsx:18:            let status;try{status=await productionManagerApi.status(batch.manifestPath)}catch{continue}
.vyron-v110-audit/src/ProductionStatusBridge.tsx:22:              if(p.renderStatus==='Rendering'&&job.status!=='RENDERING')useApp.getState().patchJob(job.id,{status:'RENDERING',error:undefined});
.vyron-v110-audit/src/ProductionStatusBridge.tsx:23:              else if(p.renderStatus==='Completed'&&p.outputFile&&(job.finalPath!==p.outputFile||job.status!=='READY_UPLOAD'))useApp.getState().patchJob(job.id,{status:'READY_UPLOAD',finalPath:p.outputFile,error:undefined});
.vyron-v110-audit/src/ProductionStatusBridge.tsx:24:              else if(p.renderStatus==='Error'&&job.status!=='ERROR')useApp.getState().patchJob(job.id,{status:'ERROR',error:p.error||'ENDLUME: ошибка рендера'});
.vyron-v110-audit/src/store.ts:8:  workspace:'',endlumePath:'',youtubeApiKey:'',autoCheckUpdates:true,reduceMotion:false,fpsMonitor:true,
.vyron-v110-audit/src/store.ts:10:  autoQueueRender:true,autoOpenEndlume:false,autoUploadYoutube:false,autopilotIntervalSec:30,tracksPerVideo:10,
.vyron-v110-audit/src/store.ts:14:  endlumeTargetDurationMin:120,endlumeTargetRenderSec:35,endlumeTargetFileMinMb:700,endlumeTargetFileMaxMb:1000,endlumePreserveImageQuality:true,endlumeProjectNaming:'VIDEO_{number}'
.vyron-v110-audit/src/store.ts:40:    const channel:Channel={id,name,slug:slugify(name),cadenceDays:p.cadenceDays||2,targetBufferDays:p.targetBufferDays||60,publishHour:p.publishHour??18,publishMinute:p.publishMinute??0,language:p.language||'RU',genre:p.genre||'Music',country:p.country||'Россия',minTracks:p.minTracks||defaultTracks,targetDurationMin:p.targetDurationMin||get().settings.endlumeTargetDurationMin||120,enabled:p.enabled??true,youtubeProfileId:p.youtubeProfileId,youtubeChannelId:p.youtubeChannelId,seo:p.seo||{titlePatterns:['{topic} • Session {number}','{genre} — {topic} | Mix {number}'],descriptionTemplate:'{title}\n\nНовая подборка в стиле {genre}.',tags:[p.genre||'music','mix','playlist'],banned:[],aiPrompt:''}};
.vyron-v110-audit/src-tauri/src/lib.rs:23:            files::import_images,files::add_tracks,files::refresh_job,files::prepare_job_folder,files::ensure_channel_inbox,files::scan_channel_inbox,files::ingest_tracks,files::ingest_cover,files::write_job_metadata,files::enqueue_render,files::reveal_path,files::open_endlume,
.vyron-v110-audit/src-tauri/src/lib.rs:26:            production_manager::production_storage_status,production_manager::start_production_import,production_manager::stop_production_import,production_manager::production_import_status,production_manager::set_production_music_library,production_manager::index_production_music_library,production_manager::build_production_batch,production_manager::resume_production_batch,production_manager::find_production_recovery,production_manager::restart_production_batch,production_manager::read_production_batch_status,production_manager::list_production_batches,production_manager::production_channel_state,production_manager::validate_production_batch,production_manager::delete_production_batch_projects,production_manager::delete_production_job_folder,production_manager::open_production_batch_in_endlume,production_manager::production_endlume_handoff_consumed,
.vyron-v110-audit/src-tauri/src/files.rs:140:        "READY_RENDER"
.vyron-v110-audit/src-tauri/src/files.rs:353:pub fn enqueue_render(workspace: String, job_folder: String) -> Result<Value, String> {
.vyron-v110-audit/src-tauri/src/files.rs:358:    let queue = PathBuf::from(workspace).join("RenderQueue");
.vyron-v110-audit/src-tauri/src/files.rs:368:        "status": "READY_RENDER"
.vyron-v110-audit/src-tauri/src/files.rs:518:pub fn open_endlume(path: String) -> Result<(), String> {
.vyron-v110-audit/src-tauri/src/files.rs:521:        return Err("ENDLUME Studio не найден по сохранённому пути".into());
.vyron-v110-audit/src-tauri/src/storage.rs:12:    json!({"version":4,"channels":[],"jobs":[],"competitors":[],"settings":{"workspace":"","endlumePath":"","youtubeApiKey":"","autoCheckUpdates":true,"reduceMotion":false,"fpsMonitor":true},"logs":[]})
.vyron-v110-audit/src-tauri/src/production_manager.rs:33:fn project_has_renderable_image(folder:&Path,manifest_image:&Path)->bool{
.vyron-v110-audit/src-tauri/src/production_manager.rs:57:    let p = PathBuf::from(workspace).join("ProductionManager");
.vyron-v110-audit/src-tauri/src/production_manager.rs:199:    pub project_id:String,pub job_id:Option<String>,pub video_number:Option<u32>,pub render_status:String,pub output_file:Option<String>,
.vyron-v110-audit/src-tauri/src/production_manager.rs:219:pub struct Validation { pub batch_id:String,pub ready:usize,pub errors:usize,pub endlume_exists:bool,pub items:Vec<ValidationItem> }
.vyron-v110-audit/src-tauri/src/production_manager.rs:225:pub struct HandoffReceipt { pub batch_id:String,pub manifest_path:String,pub request_path:String }
.vyron-v110-audit/src-tauri/src/production_manager.rs:527:    let mut manifest=BatchManifest{schema_version:SCHEMA_VERSION,source:"VYRON Production Manager".into(),batch_id:plan.batch_id.clone(),channel_id:plan.request.channel_id.clone(),channel_name:plan.request.channel_name.clone(),created_at:plan.created_at.clone(),project_count:plan.projects.len(),tracks_per_project:plan.request.tracks_per_project,mode:plan.request.mode.clone(),root_path:plan.batch_root.clone(),output_dir:broot.join("Rendered").to_string_lossy().into_owned(),status_path:broot.join("status.json").to_string_lossy().into_owned(),status:"Готово".into(),projects:Vec::new()};
.vyron-v110-audit/src-tauri/src/production_manager.rs:533:    atomic_json(&broot.join("batch.json"),&manifest)?;
.vyron-v110-audit/src-tauri/src/production_manager.rs:540:    let status=BatchStatus{batch_id:plan.batch_id.clone(),status:"Готово".into(),updated_at:Utc::now().to_rfc3339(),projects:manifest.projects.iter().map(|p|BatchProjectStatus{project_id:p.project_id.clone(),job_id:p.job_id.clone(),video_number:p.video_number,render_status:"Waiting".into(),output_file:None,duration:None,file_size:None,error:None}).collect()};
.vyron-v110-audit/src-tauri/src/production_manager.rs:546:    let base=PathBuf::from(workspace).join("ProductionManager").join("Batches");
.vyron-v110-audit/src-tauri/src/production_manager.rs:589:    BatchSummary{batch_id:m.batch_id.clone(),channel_id:m.channel_id.clone(),channel_name:m.channel_name.clone(),created_at:m.created_at.clone(),project_count:m.project_count,tracks_assigned:m.projects.iter().map(|p|p.tracks.len()).sum(),status:s.status.clone(),manifest_path:Path::new(&m.root_path).join("batch.json").to_string_lossy().into_owned(),root_path:m.root_path.clone(),completed_projects:s.projects.iter().filter(|p|p.render_status=="Completed").count(),error_projects:s.projects.iter().filter(|p|p.render_status=="Error").count()}
.vyron-v110-audit/src-tauri/src/production_manager.rs:594:    let p=PathBuf::from(path_or_root);let mp=if p.is_dir(){p.join("batch.json")}else{p};let m:BatchManifest=serde_json::from_slice(&fs::read(&mp).map_err(|_|"batch.json не найден".to_string())?).map_err(|e|format!("batch.json: {e}"))?;Ok((m,mp))
.vyron-v110-audit/src-tauri/src/production_manager.rs:628:pub fn validate_production_batch(manifest_path:String,endlume_path:String)->Result<Validation,String>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:633:        // VYRON validates renderability, not the number of visual assets. ENDLUME owns
.vyron-v110-audit/src-tauri/src/production_manager.rs:637:        if !project_has_renderable_image(folder,Path::new(&p.image_path)){
.vyron-v110-audit/src-tauri/src/production_manager.rs:645:    let ready=items.iter().filter(|x|x.ok).count();let errors=items.len()-ready;Ok(Validation{batch_id:m.batch_id,ready,errors,endlume_exists:Path::new(&endlume_path).exists(),items})
.vyron-v110-audit/src-tauri/src/production_manager.rs:664:pub fn open_production_batch_in_endlume(endlume_path:String,manifest_path:String)->Result<HandoffReceipt,String>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:665:    let(m,_)=load_manifest(&manifest_path)?;let app=PathBuf::from(&endlume_path);if !app.exists(){return Err("ENDLUME Studio не найден".into());}
.vyron-v110-audit/src-tauri/src/production_manager.rs:668:        let home=std::env::var("HOME").map_err(|_|"HOME не найден".to_string())?;let inbox=PathBuf::from(home).join("Library/Application Support/studio.endlume.desktop/VYRON Inbox");fs::create_dir_all(&inbox).map_err(|e|e.to_string())?;
.vyron-v110-audit/src-tauri/src/production_manager.rs:670:        if !request.is_file(){return Err("Не удалось создать inbox-запрос ENDLUME".into())}Command::new("open").arg(&app).spawn().map_err(|e|format!("Не удалось открыть ENDLUME: {e}"))?;
.vyron-v110-audit/src-tauri/src/production_manager.rs:671:        return Ok(HandoffReceipt{batch_id:m.batch_id,manifest_path,request_path:request.to_string_lossy().into_owned()});
.vyron-v110-audit/src-tauri/src/production_manager.rs:674:    {Command::new(&app).spawn().map_err(|e|e.to_string())?;Ok(HandoffReceipt{batch_id:m.batch_id,manifest_path,request_path:String::new()})}
.vyron-v110-audit/src-tauri/src/production_manager.rs:677:pub fn production_endlume_handoff_consumed(request_path:String)->Result<bool,String>{if request_path.is_empty(){return Ok(true)}Ok(!Path::new(&request_path).exists())}
.vyron-v110-audit/src-tauri/src/production_manager.rs:696:        assert!(project_has_renderable_image(&d,&img));let _=fs::remove_dir_all(d);
.vyron-v110-audit/src-tauri/src/production_manager.rs:700:    fn multiple_images_are_valid_for_endlume(){
.vyron-v110-audit/src-tauri/src/production_manager.rs:702:        assert!(project_has_renderable_image(&d,&img));let _=fs::remove_dir_all(d);
.vyron-v110-audit/src-tauri/src/production_manager.rs:708:        assert!(project_has_renderable_image(&d,&d.join("missing.png")));let _=fs::remove_dir_all(d);
.vyron-v110-audit/src-tauri/src/production_manager.rs:714:        assert!(!project_has_renderable_image(&d,&d.join("missing.png")));let _=fs::remove_dir_all(d);
.vyron-v110-audit/src-tauri/src/license.rs:6:// Same owner key hash as ENDLUME Studio. The raw key is never embedded in the app.
.vyron-v110-audit/src-tauri/src/license.rs:13:pub fn activate_license(app:AppHandle,key:String)->Result<Value,String>{let key=key.trim();if key.is_empty(){return Err("Введите ключ VYRON".into())}let digest=hex::encode(Sha256::digest(key.as_bytes()));if digest!=OWNER_HASH{return Err("Ключ не найден. Используй тот же ключ владельца, что и в ENDLUME Studio.".into())}let v=json!({"valid":true,"type":"owner-lifetime","expiresAt":null,"maskedKey":mask(key),"activatedAt":chrono::Utc::now()});let p=file(&app)?;let tmp=p.with_extension("tmp");fs::write(&tmp,serde_json::to_vec_pretty(&v).map_err(|e|e.to_string())?).map_err(|e|e.to_string())?;if p.exists(){let _=fs::remove_file(&p);}fs::rename(tmp,p).map_err(|e|e.to_string())?;Ok(v)}
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:28:    let fake=ws.parent().unwrap().join("ENDLUME Studio.app");fs::create_dir_all(&fake).unwrap();let v=validate_production_batch(summary.manifest_path,fake.to_string_lossy().into_owned()).unwrap();assert_eq!(v.ready,30);assert_eq!(v.errors,0);assert!(v.endlume_exists);cleanup(&ws);

## Persistence / recovery / queue symbols
.vyron-v110-audit/src/channelSchedule.ts:9:export function readExistingCache(channelId:string):ExistingCache|undefined{if(!channelId)return;try{const x=JSON.parse(localStorage.getItem(existingCacheKey(channelId))||'null');return x?.version===1?x:undefined}catch{return}}
.vyron-v110-audit/src/channelSchedule.ts:10:export function writeExistingCache(channelId:string,x:ExistingCache){if(!channelId)return;try{localStorage.setItem(existingCacheKey(channelId),JSON.stringify(x));window.dispatchEvent(new CustomEvent(EVENT,{detail:{channelId,updatedAt:x.updatedAt}}))}catch{}}
.vyron-v110-audit/src/RecoveryGate.tsx:2:import {productionManagerApi,type RecoveryState} from './productionManagerApi';
.vyron-v110-audit/src/RecoveryGate.tsx:7:export function RecoveryGate(){
.vyron-v110-audit/src/RecoveryGate.tsx:9:  const [recovery,setRecovery]=useState<RecoveryState|null>(null),[deadline,setDeadline]=useState<number|null>(null),[now,setNow]=useState(Date.now()),[busy,setBusy]=useState(false),[confirmRestart,setConfirmRestart]=useState(false);
.vyron-v110-audit/src/RecoveryGate.tsx:12:  useEffect(()=>{if(!booted||checked.current||!roots.length)return;checked.current=true;productionManagerApi.findRecovery(roots).then(rows=>{const first=rows.find(x=>x.recoverable);if(first){decided.current=false;setRecovery(first);setDeadline(Date.now()+60_000);notifyInfo('Предыдущая работа была прервана',`${first.channelName}: готово ${first.completedProjects} / ${first.totalProjects}.`,{operationId:`recovery-found:${first.batchId}`,durationMs:8000})}}).catch(e=>notifyError('Не удалось проверить восстановление',String(e),{operationId:'recovery-scan-error'}))},[booted,roots.join('|')]);
.vyron-v110-audit/src/RecoveryGate.tsx:13:  useEffect(()=>{if(!recovery||deadline===null||confirmRestart)return;const tick=()=>{const t=Date.now();setNow(t);if(t>=deadline&&!decided.current)void resumeNow()};tick();const id=window.setInterval(tick,250);return()=>window.clearInterval(id)},[recovery?.batchId,deadline,confirmRestart]);
.vyron-v110-audit/src/RecoveryGate.tsx:15:  async function resumeNow(){if(!recovery||decided.current)return;decided.current=true;setBusy(true);setDeadline(null);try{const done=await productionManagerApi.resume(recovery.rootPath);notifySuccess('Производство восстановлено',`${done.channelName}: ${done.projectCount} проектов готовы.`,{operationId:`recovery-resumed:${done.batchId}`});setRecovery(null);setPage('production')}catch(e){decided.current=false;setBusy(false);notifyError('Не удалось продолжить производство',String(e),{operationId:`recovery-resume-error:${recovery.batchId}`})}}
.vyron-v110-audit/src/RecoveryGate.tsx:16:  function requestRestart(){if(!recovery||busy)return;setDeadline(null);setConfirmRestart(true)}
.vyron-v110-audit/src/RecoveryGate.tsx:17:  async function restartNow(){if(!recovery||busy)return;decided.current=true;setBusy(true);try{const done=await productionManagerApi.restartRecovery(recovery.rootPath);notifySuccess('Новая сборка создана',`${done.channelName}: ${done.projectCount} проектов. Старая незавершённая сборка сохранена.`,{operationId:`recovery-restarted:${done.batchId}`});setRecovery(null);setPage('production')}catch(e){decided.current=false;setBusy(false);notifyError('Не удалось начать новую сборку',String(e),{operationId:`recovery-restart-error:${recovery.batchId}`})}}
.vyron-v110-audit/src/RecoveryGate.tsx:18:  if(!recovery)return null;
.vyron-v110-audit/src/RecoveryGate.tsx:19:  return <div className="recoveryOverlay"><section className="recoveryDialog">
.vyron-v110-audit/src/RecoveryGate.tsx:20:    <small>ВОССТАНОВЛЕНИЕ VYRON</small><h2>Производство было прервано</h2><h3>{recovery.channelName}</h3>
.vyron-v110-audit/src/RecoveryGate.tsx:21:    <div className="recoveryFacts"><span>Уже готово<b>{recovery.completedProjects} / {recovery.totalProjects}</b></span><span>Текущий проект<b>{recovery.currentProject==='Финализация'?recovery.currentProject:`VIDEO_${recovery.currentProject}`}</b></span></div>
.vyron-v110-audit/src/RecoveryGate.tsx:22:    {!confirmRestart?<><p>Можно продолжить с последнего подтверждённого checkpoint. Готовые проекты не будут пересоздаваться.</p><div className="recoveryCountdown"><small>Автоматическое продолжение через</small><b>{seconds}</b><span>сек.</span></div><footer><button className="primary" disabled={busy} onClick={()=>void resumeNow()}>{busy?'ВОССТАНАВЛИВАЮ…':'ПРОДОЛЖИТЬ'}</button><button disabled={busy} onClick={requestRestart}>НАЧАТЬ ЗАНОВО</button></footer></>:<><div className="recoveryWarning"><b>Начать новую сборку?</b><p>Уже создано {recovery.completedProjects} проектов. Они не будут удалены. VYRON создаст новый batchId и сохранит старую незавершённую сборку.</p></div><footer><button className="danger" disabled={busy} onClick={()=>void restartNow()}>{busy?'СОЗДАЮ…':'НАЧАТЬ НОВУЮ СБОРКУ'}</button><button disabled={busy} onClick={()=>{setConfirmRestart(false);decided.current=false;setDeadline(Date.now()+60_000)}}>ОТМЕНА</button></footer></>}
.vyron-v110-audit/src/App.tsx:24:import {RecoveryGate} from './RecoveryGate';
.vyron-v110-audit/src/App.tsx:29:  if(localStorage.getItem(notifiedKey)===version)return;
.vyron-v110-audit/src/App.tsx:32:    if(!granted&&localStorage.getItem('vyron:notification-permission-requested')!=='1'){
.vyron-v110-audit/src/App.tsx:33:      localStorage.setItem('vyron:notification-permission-requested','1');
.vyron-v110-audit/src/App.tsx:38:      localStorage.setItem(notifiedKey,version);
.vyron-v110-audit/src/App.tsx:60:  useEffect(()=>{if(!booted)return;void api.appVersion().then(v=>{const expected=localStorage.getItem('vyron:update-installing-version');if(expected&&expected===v){localStorage.removeItem('vyron:update-installing-version');notifySuccess('Обновление установлено',`VYRON обновлён до версии ${v}.`,{operationId:`update-installed:${v}`})}})},[booted]);
.vyron-v110-audit/src/App.tsx:66:  const screen=page==='dashboard'||page==='autopilot'?<DashboardOS/>:page==='accounts'?<SettingsOS license={license}/>:page==='channels'?<ChannelsOS/>:page==='production'||page==='content'?<ProductionOS/>:page==='youtube'?<YouTubeCenter/>:page==='competitors'?<CompetitorsPage/>:page==='analytics'?<AnalyticsPage/>:page==='metadata'?<YouTubeCenter initialTab='metadata'/>:page==='existing'?<YouTubeCenter initialTab='uploaded'/>:page==='publisher'?<YouTubeCenter initialTab='queue'/>:<SettingsOS license={license}/>;
.vyron-v110-audit/src/App.tsx:67:  return <div className="appShell"><ChannelRunwayScheduler/><ProductionStatusBridge/><RecoveryGate/><Sidebar page={page} setPage={setPage}/><main className="main"><Topbar/><div className="pageWrap" key={page}>{screen}</div></main>{settings.fpsMonitor&&<FpsMonitor/>}<NotificationCenter/>{update&&<UpdateNotice update={update} onLater={()=>setUpdate(null)}/>}<div className="bgGlow a"/><div className="bgGlow b"/></div>
.vyron-v110-audit/src/App.tsx:79:<div className="metrics six"><Metric label="КАНАЛОВ" value={channels.length} note="в системе"/><Metric label="СРОЧНО" value={urgent} note="буфер < 14 дней" tone="red"/><Metric label="ЖДУТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note="папки готовы" tone="orange"/><Metric label="ГОТОВО РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="ENDLUME queue" tone="purple"/><Metric label="ГОТОВО MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="YouTube queue" tone="green"/><Metric label="YOUTUBE" value={jobs.filter(j=>j.status==='SCHEDULED').length} note="запланировано" tone="blue"/></div>
.vyron-v110-audit/src/App.tsx:87:<div className="metrics six"><Metric label="НУЖНА КАРТИНКА" value={jobs.filter(j=>j.status==='NEED_IMAGE').length} note="Inbox/Images"/><Metric label="ЖДЁТ МУЗЫКУ" value={jobs.filter(j=>j.status==='WAITING_MUSIC').length} note={`${settings.tracksPerVideo} треков`} tone="orange"/><Metric label="РЕНДЕР" value={jobs.filter(j=>j.status==='READY_RENDER').length} note="RenderQueue" tone="purple"/><Metric label="MP4" value={jobs.filter(j=>j.status==='READY_UPLOAD').length} note="готово" tone="green"/><Metric label="UPLOAD" value={jobs.filter(j=>j.status==='UPLOADING').length} note="resumable" tone="blue"/><Metric label="ОШИБКИ" value={jobs.filter(j=>j.status==='ERROR').length} note="не блокируют очередь" tone="red"/></div>
.vyron-v110-audit/src/App.tsx:88:<div className="autoSteps">{[['01','План 60 дней',settings.autoCreatePlan,'autoCreatePlan'],['02','Музыка → по проектам',settings.autoAssignMusic,'autoAssignMusic'],['03','Изображение → 1 проект',settings.autoAssignImages,'autoAssignImages'],['04','AI title/description/tags',settings.autoGenerateMetadata,'autoGenerateMetadata'],['05','Очередь ENDLUME',settings.autoQueueRender,'autoQueueRender'],['06','YouTube upload + schedule',settings.autoUploadYoutube,'autoUploadYoutube']].map(([n,label,val,key]:any)=><button key={key} className={`autoStep ${val?'active':''}`} onClick={()=>patch({[key]:!val} as any)}><span>{n}</span><b>{label}</b><em>{val?'AUTO':'OFF'}</em></button>)}</div>
.vyron-v110-audit/src/App.tsx:89:{last&&<div className="panel autoResult"><div className="panelHead"><div><small>ПОСЛЕДНИЙ РУЧНОЙ ЦИКЛ</small><h3>Результат</h3></div></div><div className="resultGrid"><span>Проекты <b>{last.prepared}</b></span><span>Треки <b>{last.tracksMoved}</b></span><span>Картинки <b>{last.imagesMoved}</b></span><span>AI <b>{last.metadataGenerated}</b></span><span>RenderQueue <b>{last.renderQueued}</b></span><span>YouTube <b>{last.uploads}</b></span><span>Ошибки <b>{last.errors}</b></span></div>{last.notes.slice(0,8).map((n,i)=><p key={i}>{n}</p>)}</div>}
.vyron-v110-audit/src/App.tsx:91:<div className="panel warningPanel"><b>Что ещё требуется один раз</b><p>Для полностью автоматической публикации: подключить Google OAuth в Настройках и привязать профиль к каналу. Метаданные можно бесплатно импортировать из обычного ChatGPT Plus через Metadata Inbox; платный OpenAI API не обязателен. Для нулевого ручного рендера ENDLUME должен читать RenderQueue — VYRON уже создаёт эту очередь.</p><button onClick={()=>setPage('settings')}>ОТКРЫТЬ НАСТРОЙКИ</button></div>
.vyron-v110-audit/src/App.tsx:101:{filtered.length===0?<Empty title="Очередь пустая" text="VYRON рассчитает нужное число видео по графику каждого канала." action="СОЗДАТЬ ПЛАН" onClick={createPlan}/>:<div className="jobs">{filtered.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="job" key={j.id}><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{c?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не созданы'}</b><span>{j.publishAt?`План: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата не назначена'} • треков {j.tracksCount}/{j.minTracks}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>ПАПКА</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,c?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ МУЗЫКА</button>}{j.status==='READY_RENDER'&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('publisher')}>YOUTUBE</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,c?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>ПОВТОРИТЬ</button>}</div></div>})}</div>}</>}
.vyron-v110-audit/src/App.tsx:103:function Content(){const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [channelId,setChannelId]=useState(channels[0]?.id||''),[inbox,setInbox]=useState<any>();useEffect(()=>{if(!channelId&&channels[0])setChannelId(channels[0].id)},[channels]);const c=channels.find(x=>x.id===channelId);async function ensure(){if(!c)return;const r=await api.ensureChannelInbox(settings.workspace,c.name);setInbox(r);return r}async function importImages(){if(!c){setPage('channels');return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);const imported=await api.importImages(settings.workspace,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено`)}return <>
.vyron-v110-audit/src/App.tsx:120:function Settings({license,setLicense}:{license:LicenseStatus;setLicense:(x:LicenseStatus)=>void}){const s=useApp(x=>x.settings),patch=useApp(x=>x.patchSettings),logs=useApp(x=>x.logs),toast=useApp(x=>x.toast),log=useApp(x=>x.log);const [tab,setTab]=useState<'general'|'autopilot'|'integrations'|'subscription'|'updates'|'diagnostics'|'about'>('general'),[profiles,setProfiles]=useState<YoutubeProfile[]>([]),[connecting,setConnecting]=useState(false),[oauthSecret,setOauthSecret]=useState(''),[oauthMessage,setOauthMessage]=useState(''),[oauthError,setOauthError]=useState(false),[update,setUpdate]=useState<any>(),[progress,setProgress]=useState<number|null>(null),[diag,setDiag]=useState<any>(),[key,setKey]=useState('');const refreshProfiles=async()=>{try{const p=await api.youtubeProfiles();setProfiles(p);setOauthError(false);setOauthMessage(p.length?`OAuth: подключено профилей — ${p.length}`:'OAuth: сохранённых YouTube-профилей пока нет');return p}catch(e){const m=`OAuth storage: ${String(e)}`;setProfiles([]);setOauthError(true);setOauthMessage(m);log(m,'error');throw e}};useEffect(()=>{void refreshProfiles().catch(()=>{})},[]);return <>
.vyron-v110-audit/src/App.tsx:123:{tab==='general'&&<div className="settingsStack"><div className="settingsCard"><h3>Workspace</h3><p>VYRON хранит Inbox, проекты Video_XXX и RenderQueue.</p><div className="pathLine"><input readOnly value={s.workspace} placeholder="создаётся автоматически"/><button onClick={async()=>{const p=await api.chooseWorkspace();if(p)patch({workspace:p})}}>ВЫБРАТЬ</button><button onClick={()=>s.workspace&&api.openLocal(s.workspace)}>ОТКРЫТЬ</button></div></div><div className="settingsCard"><div className="toggleRows"><Toggle label="FPS-монитор" text="Контроль плавности интерфейса." value={s.fpsMonitor} onChange={v=>patch({fpsMonitor:v})}/><Toggle label="Уменьшить анимации" text="Отключить декоративные переходы." value={s.reduceMotion} onChange={v=>patch({reduceMotion:v})}/></div></div></div>}
.vyron-v110-audit/src/App.tsx:124:{tab==='autopilot'&&<div className="settingsStack"><div className="settingsCard"><h3>Autonomous Pipeline</h3><div className="toggleRows"><Toggle label="Автопилот" text="Запускать цикл автоматически." value={s.autopilotEnabled} onChange={v=>patch({autopilotEnabled:v})}/><Toggle label="Поддерживать план 60 дней" text="Автоматически создавать недостающие Video_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Брать треки из Inbox/Music." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 Video_XXX." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI-метаданные (опционально)" text="Только если вручную подключён отдельный API. Для бесплатного режима используй ChatGPT Plus → Metadata Inbox." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="Очередь ENDLUME" text="Создавать RenderQueue marker для готовых проектов." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Запускать приложение при появлении новых render-задач." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/><Toggle label="YouTube Autoupload" text="Загружать по одному готовому видео за цикл и ставить publishAt." value={s.autoUploadYoutube} onChange={v=>patch({autoUploadYoutube:v})}/></div></div><div className="settingsCard"><h3>Параметры</h3><div className="editGrid"><label>Цикл, секунд<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label><label>Треков по умолчанию<input type="number" min="1" max="100" value={s.tracksPerVideo} onChange={e=>patch({tracksPerVideo:+e.target.value||10})}/></label></div></div></div>}
.vyron-v110-audit/src/App.tsx:125:{tab==='integrations'&&<div className="settingsStack"><div className="settingsCard"><h3>ENDLUME Studio</h3><p>VYRON отправляет готовые проекты в RenderQueue.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>ВЫБРАТЬ</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>ОТКРЫТЬ</button>}</div><small className="note">Полностью нулевой ручной рендер требует companion-update ENDLUME, который будет читать RenderQueue.</small></div><div className="settingsCard"><h3>OpenAI API — опционально</h3><p>Не требуется для бесплатного режима. Оставь пустым и используй ChatGPT Plus → Metadata Inbox. API нужен только если позже захочешь полностью фоновую генерацию без открытия ChatGPT.</p><input className="wideInput" type="password" placeholder="sk-…" value={s.openaiApiKey} onChange={e=>patch({openaiApiKey:e.target.value.trim()})}/><div className="pathLine"><input value={s.openaiModel} onChange={e=>patch({openaiModel:e.target.value.trim()})}/><button onClick={()=>patch({openaiModel:''})}>ОЧИСТИТЬ</button></div></div><div className="settingsCard"><h3>YouTube OAuth</h3><p>Desktop OAuth открывает Google в браузере один раз. После этого токен обновляется локально автоматически.</p><input className="wideInput" placeholder="Google OAuth Client ID …apps.googleusercontent.com" value={s.youtubeOAuthClientId} onChange={e=>patch({youtubeOAuthClientId:e.target.value.trim()})}/><input className="wideInput" type="password" autoComplete="off" placeholder="Google OAuth Client Secret — вводится один раз" value={oauthSecret} onChange={e=>setOauthSecret(e.target.value.trim())}/><small className="note">Client Secret не сохраняется в общих настройках и не выводится в логах. После успешного OAuth он хранится только внутри локального OAuth-профиля.</small><div className="cardActions"><button className="primary" disabled={connecting||!s.youtubeOAuthClientId||!oauthSecret} onClick={async()=>{setConnecting(true);setOauthError(false);setOauthMessage('OAuth: ожидаю подтверждение Google…');try{const p=await api.youtubeConnect(s.youtubeOAuthClientId,oauthSecret);setOauthSecret('');const m=`YouTube подключён: ${p.channelTitle||p.channelId||'канал'}`;setOauthMessage(m);toast(m);log(m);await refreshProfiles()}catch(e){const m=`OAuth: ${String(e)}`;setOauthError(true);setOauthMessage(m);toast(m);log(m,'error')}finally{setConnecting(false)}}}>{connecting?'ЖДУ GOOGLE…':'ПОДКЛЮЧИТЬ YOUTUBE'}</button><button onClick={async()=>{try{const p=await refreshProfiles();toast(p.length?`OAuth профилей: ${p.length}`:'OAuth профили не найдены')}catch(e){toast(String(e))}}}>↻ ОБНОВИТЬ</button></div>{oauthMessage&&<div className={oauthError?'errorBox':profiles.length?'successBox':'publisherNotice'}>{oauthMessage}</div>}{profiles.map(p=><div className="oauthProfile" key={p.id}><span><b>{p.channelTitle||'YouTube канал'}</b><small>{p.channelId||p.clientIdMasked}</small></span><button className="danger mini" onClick={async()=>{await api.youtubeDisconnect(p.id);await refreshProfiles()}}>ОТКЛЮЧИТЬ</button></div>)}<label className="compactLabel">YouTube category ID<input value={s.youtubeCategoryId} onChange={e=>patch({youtubeCategoryId:e.target.value.trim()||'10'})}/></label></div><div className="settingsCard"><h3>YouTube Intelligence</h3><p>Автоматически обновляет приватную аналитику своих каналов и публичные показатели конкурентов, пока VYRON запущен.</p><Toggle label="Автообновление аналитики" text="Обновлять подключённые каналы и список конкурентов без ручной кнопки." value={s.youtubeIntelligenceAutoRefresh} onChange={v=>patch({youtubeIntelligenceAutoRefresh:v})}/><label className="compactLabel">Интервал, минут<input type="number" min="15" max="1440" value={s.youtubeIntelligenceRefreshMin} onChange={e=>patch({youtubeIntelligenceRefreshMin:Math.max(15,+e.target.value||30)})}/></label><small className="note">Для приватной аналитики включи YouTube Analytics API в Google Cloud и один раз переподключи OAuth после установки 0.6.0. Конкуренты используют только публичные данные.</small></div><div className="settingsCard"><h3>YouTube Data API Key</h3><p>Только публичная статистика своих каналов и конкурентов.</p><input className="wideInput" type="password" placeholder="AIza…" value={s.youtubeApiKey} onChange={e=>patch({youtubeApiKey:e.target.value.trim()})}/></div><div className="settingsCard"><h3>MusicVault AI</h3><p>VYRON принимает музыку через Inbox каждого канала. Следующий шаг — кнопка «Отправить в VYRON» прямо в MusicVault AI, чтобы не переносить файлы вручную.</p></div></div>}
.vyron-v110-audit/src/App.tsx:134:function UpdateNotice({update,onLater}:{update:any;onLater:()=>void}){const [progress,setProgress]=useState<number|null>(null),[error,setError]=useState('');return <aside className="updateNotice"><div><i/><span><b>Новое обновление</b><small>VYRON {update.version}</small></span></div><p>{String(update.body||'Доступна новая версия VYRON.').split('\n').slice(0,2).join(' • ')}</p>{progress!==null&&<div className="progress"><i style={{width:`${progress}%`}}/><span>{progress.toFixed(0)}%</span></div>}{error&&<div className="errorBox">{error}</div>}<footer><button disabled={progress!==null} onClick={onLater}>ПОЗЖЕ</button><button className="primary" disabled={progress!==null} onClick={async()=>{setProgress(0);localStorage.setItem('vyron:update-installing-version',String(update.version));try{await update.install((p:number)=>setProgress(p))}catch(e){localStorage.removeItem('vyron:update-installing-version');setProgress(null);setError(String(e));notifyError('Не удалось установить обновление',String(e))}}}>ОБНОВИТЬ</button></footer></aside>}
.vyron-v110-audit/src/ExistingVideos.tsx:91: async function apply(){setConfirm(false);if(!selected.length||!profileId)return;if(youtubeQuotaState().blocked){toast(youtubeQuotaMessage());return}const backup=selected.map(v=>baseline[v.id]).filter(Boolean).map(v=>({...v,tags:[...v.tags]}));setBusy(true);let ok=0;const verifiedIds=new Set<string>();try{const saved=await api.youtubeBackupExisting(profileId,backup);setBackupPath(saved.path||'');for(let i=0;i<selected.length;i++){const v=selected[i];setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'saving',error:undefined}:z));try{const result=await api.youtubeUpdateExisting(profileId,v.id,v.title,v.description,v.tags,v.publishAt,v.privacyStatus);if(result?.metadataAccepted===false)throw new Error(`YouTube не принял metadata ${v.id}`);if(result.scheduleRequested&&result.scheduleAccepted===false){const err=result.scheduleError||'расписание не принято';if(isYoutubeQuotaError(err)){markYoutubeQuotaExceeded(err);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: метаданные сохранены; расписание: ${err}`}:z));ok++;verifiedIds.add(v.id);continue}ok++;verifiedIds.add(v.id);setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'done',verified:true,error:result.metadataVerifyPending||result.scheduleVerifyPending?'YouTube принял запись; контрольное чтение ещё догоняет':undefined}:z))}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);const pendingIds=new Set(selected.slice(i).map(x=>x.id));setVideos(x=>x.map(z=>({...z,selected:pendingIds.has(z.id),applyState:z.id===v.id?'error':z.applyState,error:z.id===v.id?youtubeQuotaMessage():z.error})));toast(`⏸ Квота закончилась. Осталось ${pendingIds.size}; выбор сохранён.`);break}setVideos(x=>x.map(z=>z.id===v.id?{...z,applyState:'error',verified:false,error:`${v.id}: ${String(e)}`}:z))}}if(ok){setLastUndo(backup.filter(v=>verifiedIds.has(v.id)));setBaseline(prev=>{const n={...prev};for(const v of selected.filter(x=>verifiedIds.has(x.id)))n[v.id]={...v,tags:[...v.tags]};return n})}if(!youtubeQuotaState().blocked)toast(ok===selected.length?`✓ ${ok} видео приняты YouTube`:`YouTube принял ${ok}/${selected.length}`)}catch(e){if(isYoutubeQuotaError(e)){markYoutubeQuotaExceeded(e);toast('⏸ YouTube quota: пачка сохранена для продолжения')}else toast(`Backup/Update: ${String(e)}`)}finally{setBusy(false)}}
.vyron-v110-audit/src/ProductionWorkspace.tsx:10:function read():State{try{const x=JSON.parse(localStorage.getItem(KEY)||'null');if(x?.version===1)return x}catch{}return{version:1,byChannel:{}}}
.vyron-v110-audit/src/ProductionWorkspace.tsx:11:function write(s:State){try{localStorage.setItem(KEY,JSON.stringify(s))}catch{}}
.vyron-v110-audit/src/productionPrefs.test.ts:4:class MemoryStorage {
.vyron-v110-audit/src/productionPrefs.test.ts:15:  Object.defineProperty(globalThis,'localStorage',{value:new MemoryStorage(),configurable:true});
.vyron-v110-audit/src/productionPrefs.test.ts:20:describe('Production Manager v2 persistence',()=>{
.vyron-v110-audit/src/productionPrefs.test.ts:21:  it('migrates current Production channel from v1 and persists tab/channel across reads',()=>{
.vyron-v110-audit/src/productionPrefs.test.ts:22:    localStorage.setItem('vyron:production-workspace:v1',JSON.stringify({selectedChannelId:'neon'}));
.vyron-v110-audit/src/productionPrefs.test.ts:30:  it('persists channel-specific builder settings and selected projects',()=>{
.vyron-v110-audit/src/channelRunwayStore.ts:3:  CHANNEL_RUNWAY_STORAGE_KEY,
.vyron-v110-audit/src/channelRunwayStore.ts:17:type StorageLike=Pick<Storage,'getItem'|'setItem'>;
.vyron-v110-audit/src/channelRunwayStore.ts:22:function browserStorage(){return typeof localStorage==='undefined'?undefined:localStorage}
.vyron-v110-audit/src/channelRunwayStore.ts:25:export function loadChannelRunwayStore(storage:StorageLike|undefined=browserStorage()):ChannelRunwayStore{
.vyron-v110-audit/src/channelRunwayStore.ts:26:  if(!storage)return defaultStore();
.vyron-v110-audit/src/channelRunwayStore.ts:28:    const raw=storage.getItem(CHANNEL_RUNWAY_STORAGE_KEY);
.vyron-v110-audit/src/channelRunwayStore.ts:36:export function saveChannelRunwayStore(value:ChannelRunwayStore,storage:StorageLike|undefined=browserStorage()){
.vyron-v110-audit/src/channelRunwayStore.ts:37:  if(storage)storage.setItem(CHANNEL_RUNWAY_STORAGE_KEY,JSON.stringify(value));
.vyron-v110-audit/src/channelRunwayStore.ts:50:export function readExistingRunwayCache(channelId:string,storage:StorageLike|undefined=browserStorage()):ExistingCache|undefined{
.vyron-v110-audit/src/channelRunwayStore.ts:51:  if(!storage||!channelId)return;
.vyron-v110-audit/src/channelRunwayStore.ts:53:    const parsed=JSON.parse(storage.getItem(existingCacheKey(channelId))||'null') as ExistingCache|null;
.vyron-v110-audit/src/channelRunwayStore.ts:73:  storage:StorageLike|undefined=browserStorage()
.vyron-v110-audit/src/channelRunwayStore.ts:75:  const previous=loadChannelRunwayStore(storage);
.vyron-v110-audit/src/channelRunwayStore.ts:88:    const cached=readExistingRunwayCache(channel.id,storage);
.vyron-v110-audit/src/channelRunwayStore.ts:101:  return saveChannelRunwayStore(next,storage);
.vyron-v110-audit/src/channelRunwayStore.ts:108:  storage:StorageLike|undefined=browserStorage()
.vyron-v110-audit/src/channelRunwayStore.ts:110:  const current=loadChannelRunwayStore(storage);
.vyron-v110-audit/src/channelRunwayStore.ts:118:  return saveChannelRunwayStore(next,storage);
.vyron-v110-audit/src/channelRunwayStore.ts:130:  storage:StorageLike|undefined=browserStorage()
.vyron-v110-audit/src/channelRunwayStore.ts:132:  const current=loadChannelRunwayStore(storage);
.vyron-v110-audit/src/channelRunwayStore.ts:134:  recalculateChannelRunway(channels,now,true,storage);
.vyron-v110-audit/src/SettingsOS.tsx:19: async function installUpdate(){setProgress(0);setStage('Загрузка');localStorage.setItem('vyron:update-installing-version',String(update.version||''));try{await update.install((p:number)=>{setProgress(p);if(p>=100)setStage('Проверка подписи и установка')})}catch(e){localStorage.removeItem('vyron:update-installing-version');setProgress(null);setStage('Обновление не установлено. Текущая версия сохранена.');notifyError('Не удалось установить обновление',String(e))}}
.vyron-v110-audit/src/SettingsOS.tsx:24: {tab==='autopilot'&&<div className="settingsStack"><section className="settingsCard"><h3>Режим Autopilot</h3><div className="modeSwitch large"><button className={mode==='off'?'active':''} onClick={()=>setMode('off')}>OFF</button><button className={mode==='assisted'?'active':''} onClick={()=>setMode('assisted')}>ASSISTED</button><button className={mode==='full'?'active':''} onClick={()=>setMode('full')}>FULL LOCAL</button></div><p>{mode==='off'?'Автоматизация выключена.':'Autopilot обслуживает только локальный Production pipeline и ENDLUME. YouTube API всегда остаётся ручным.'}</p></section><details className="settingsCard advancedPanel"><summary>Advanced Autopilot</summary><div className="toggleRows"><Toggle label="Поддерживать план" text="Создавать недостающие VIDEO_XXX." value={s.autoCreatePlan} onChange={v=>patch({autoCreatePlan:v})}/><Toggle label="Распределять музыку" text="Inbox/Music → проекты." value={s.autoAssignMusic} onChange={v=>patch({autoAssignMusic:v})}/><Toggle label="Распределять изображения" text="1 изображение = 1 проект." value={s.autoAssignImages} onChange={v=>patch({autoAssignImages:v})}/><Toggle label="AI metadata" text="Только при подключённом OpenAI API." value={s.autoGenerateMetadata} onChange={v=>patch({autoGenerateMetadata:v})}/><Toggle label="ENDLUME queue" text="Создавать render marker." value={s.autoQueueRender} onChange={v=>patch({autoQueueRender:v})}/><Toggle label="Открывать ENDLUME" text="Открывать приложение при render задачах." value={s.autoOpenEndlume} onChange={v=>patch({autoOpenEndlume:v})}/></div><label>Интервал локального цикла, сек<input type="number" min="10" max="3600" value={s.autopilotIntervalSec} onChange={e=>patch({autopilotIntervalSec:Math.max(10,+e.target.value||30)})}/></label></details></div>}
.vyron-v110-audit/src/SettingsOS.tsx:25: {tab==='endlume'&&<div className="settingsStack"><section className="settingsCard"><h3>ENDLUME Studio</h3><p>Готовые локальные проекты отправляются в RenderQueue без обращения к YouTube.</p><div className="pathLine"><input readOnly value={s.endlumePath} placeholder="ENDLUME Studio.app"/><button onClick={async()=>{const p=await api.chooseEndlume();if(p)patch({endlumePath:p})}}>Выбрать</button>{s.endlumePath&&<button onClick={()=>api.openEndlume(s.endlumePath)}>Открыть</button>}</div></section></div>}
.vyron-v110-audit/src/autopilotRuntime.ts:10:const RENDER_REQUEUE_MS=10*60*1000;
.vyron-v110-audit/src/autopilotRuntime.ts:13:function blankSummary():AutopilotSummary{return {prepared:0,tracksMoved:0,imagesMoved:0,metadataGenerated:0,renderQueued:0,uploads:0,errors:0,notes:[]}}
.vyron-v110-audit/src/autopilotRuntime.ts:18:  const storage=await productionManagerApi.storageStatus(projectRoot);
.vyron-v110-audit/src/autopilotRuntime.ts:19:  if(!storage.exists||!storage.writable)throw new Error(storage.error||`Папка проектов недоступна: ${projectRoot}`);
.vyron-v110-audit/src/autopilotRuntime.ts:85:  if(s.autoQueueRender){
.vyron-v110-audit/src/autopilotRuntime.ts:88:      const last=j.renderQueuedAt?new Date(j.renderQueuedAt).getTime():0;
.vyron-v110-audit/src/autopilotRuntime.ts:89:      if(last&&Date.now()-last<RENDER_REQUEUE_MS)continue;
.vyron-v110-audit/src/autopilotRuntime.ts:90:      try{await api.enqueueRender(workspace,j.folder);useApp.getState().patchJob(j.id,{renderQueuedAt:new Date().toISOString(),lastAutomationAt:new Date().toISOString()});summary.renderQueued++}catch(e){logError(summary,`${channel.name} Video_${j.number}: очередь ENDLUME`,e)}
.vyron-v110-audit/src/autopilotRuntime.ts:92:    if(s.autoOpenEndlume&&summary.renderQueued>0&&s.endlumePath&&Date.now()-lastEndlumeLaunch>ENDLUME_LAUNCH_COOLDOWN_MS){try{await api.openEndlume(s.endlumePath);lastEndlumeLaunch=Date.now()}catch(e){logError(summary,'ENDLUME запуск',e)}}
.vyron-v110-audit/src/autopilotRuntime.ts:105:    const moved=summary.tracksMoved+summary.imagesMoved+summary.renderQueued+summary.metadataGenerated+summary.prepared;
.vyron-v110-audit/src/autopilotRuntime.ts:106:    if(moved||summary.errors)useApp.getState().log(`Autopilot: папки ${summary.prepared}, треки ${summary.tracksMoved}, изображения ${summary.imagesMoved}, AI ${summary.metadataGenerated}, рендер ${summary.renderQueued}, ошибок ${summary.errors}`,summary.errors?'warn':'info');
.vyron-v110-audit/src/autopilotRuntime.ts:107:    await useApp.getState().persist();
.vyron-v110-audit/src/channelRunwayCore.ts:3:export const CHANNEL_RUNWAY_STORAGE_KEY='vyron:channel-runway:v1';
.vyron-v110-audit/src/AccountsPage.tsx:11: const [profiles,setProfiles]=useState<YoutubeProfile[]>([]),[health,setHealth]=useState<Record<string,YoutubeProfileHealth>>({}),[config,setConfig]=useState<GoogleConfigStatus|null>(null),[busy,setBusy]=useState(false),[checking,setChecking]=useState(false),[browserOpen,setBrowserOpen]=useState(false),[browsers,setBrowsers]=useState<BrowserOption[]>([]),[browser,setBrowser]=useState(localStorage.getItem('vyron:oauth-browser')||'default');const file=useRef<HTMLInputElement>(null);
.vyron-v110-audit/src/AccountsPage.tsx:17: async function connect(){setBrowserOpen(false);setBusy(true);try{localStorage.setItem('vyron:oauth-browser',browser);const p=await api.youtubeConnectGlobal(browser);bindProfile(p);await refresh();toast(`✓ ${p.channelTitle||p.channelId||'YouTube канал'} подключён и привязан автоматически`)}catch(e){toast(String(e))}finally{setBusy(false)}}
.vyron-v110-audit/src/youtubeQuota.ts:32:export function youtubeQuotaState():YoutubeQuotaState{try{const raw=localStorage.getItem(KEY);if(!raw)return{blocked:false};const x=JSON.parse(raw) as YoutubeQuotaState;if(!x?.blocked)return{blocked:false};if(x.ptDate!==ptDate()){localStorage.removeItem(KEY);return{blocked:false}}return x}catch{return{blocked:false}}}
.vyron-v110-audit/src/youtubeQuota.ts:33:function readUsage():YoutubeQuotaUsage{const today=ptDate();try{const raw=localStorage.getItem(USAGE_KEY);if(raw){const x=JSON.parse(raw) as YoutubeQuotaUsage;if(x.ptDate===today){return{ptDate:today,limit:Math.max(1,safeInt(x.limit,DEFAULT_YOUTUBE_DAILY_QUOTA)),used:safeInt(x.used,0),calls:safeInt(x.calls,0),updatedAt:x.updatedAt,lastAction:x.lastAction}}const previousLimit=Math.max(1,safeInt(x.limit,DEFAULT_YOUTUBE_DAILY_QUOTA));return{ptDate:today,limit:previousLimit,used:0,calls:0}}}catch{}return{ptDate:today,limit:DEFAULT_YOUTUBE_DAILY_QUOTA,used:0,calls:0}}
.vyron-v110-audit/src/youtubeQuota.ts:34:function saveUsage(x:YoutubeQuotaUsage){localStorage.setItem(USAGE_KEY,JSON.stringify(x));emit();return x}
.vyron-v110-audit/src/youtubeQuota.ts:51:  if(result?.metadataVerifyPending||result?.scheduleVerifyPending)cost+=2;
.vyron-v110-audit/src/youtubeQuota.ts:62:export function markYoutubeQuotaExceeded(error:unknown){const state:YoutubeQuotaState={blocked:true,ptDate:ptDate(),detectedAt:new Date().toISOString(),reason:String(error||'quotaExceeded')};localStorage.setItem(KEY,JSON.stringify(state));const x=readUsage();x.used=x.limit;x.updatedAt=new Date().toISOString();x.lastAction='quotaExceeded';saveUsage(x);emit();return state}
.vyron-v110-audit/src/youtubeQuota.ts:63:export function clearYoutubeQuotaGuard(){localStorage.removeItem(KEY);emit()}
.vyron-v110-audit/src/youtubeQuota.ts:68:export function loadYoutubeQuotaPlan():YoutubeQuotaPlanState{try{const x=JSON.parse(localStorage.getItem(PLAN_KEY)||'{}');return{channels:Math.max(1,safeInt(x.channels,100)),videosPerChannel:Math.max(1,safeInt(x.videosPerChannel,30))}}catch{return{channels:100,videosPerChannel:30}}}
.vyron-v110-audit/src/youtubeQuota.ts:69:export function saveYoutubeQuotaPlan(plan:YoutubeQuotaPlanState){const x={channels:Math.max(1,Math.floor(plan.channels||1)),videosPerChannel:Math.max(1,Math.floor(plan.videosPerChannel||1))};localStorage.setItem(PLAN_KEY,JSON.stringify(x));emit();return x}
.vyron-v110-audit/src/YouTubeCenter.tsx:12:type Tab='uploaded'|'metadata'|'queue'|'calendar'|'command'|'runway'|'data'|'accounts';
.vyron-v110-audit/src/YouTubeCenter.tsx:16: const tabs:[Tab,string][]=[['uploaded','Загруженные'],['metadata','Метаданные'],['queue','Очередь'],['calendar','Календарь'],['command','Командный центр'],['runway','План каналов'],['data','Данные YouTube'],['accounts','Аккаунты']];
.vyron-v110-audit/src/YouTubeCenter.tsx:17: return <><div className="youtubeCenterHead"><div><small>YOUTUBE CENTER • API ZONE</small><h1>YouTube</h1><p>Единственная зона VYRON, где разрешены YouTube API-вызовы. Синхронизация и обновления запускаются только твоим действием.</p></div><div className="youtubeTabs">{tabs.map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div></div><QuotaMeter compact defaultChannels={Math.max(1,channels.length)}/>{tab!=='command'&&<div className="youtubePolicyNotice"><b>ZERO HIDDEN QUOTA</b><span>Нет startup sync, polling, background refresh или API-запросов при возврате на вкладку.</span></div>}{tab==='uploaded'&&<ExistingVideos/>}{tab==='metadata'&&<MetadataPage/>}{tab==='queue'&&<PublisherOS/>}{tab==='command'&&<CommandCenter/>}{tab==='runway'&&<ChannelRunway/>}{tab==='data'&&<YouTubeDataTools/>}{tab==='accounts'&&<AccountsPage/>}{tab==='calendar'&&<section className="panel calendarPanel"><div className="panelHead"><div><small>ALL CHANNELS</small><h3>Календарь публикаций</h3><p>Локальные даты из project state / YouTube queue.</p></div><span>{calendar.length} публикаций</span></div>{calendar.length===0?<div className="empty"><b>Публикаций пока нет</b><p>Когда у видео появится publishAt, оно будет показано здесь.</p></div>:<div className="calendarList">{calendar.map(j=>{const c=channels.find(x=>x.id===j.channelId);return <div className="calendarRow" key={j.id}><time>{new Date(j.publishAt!).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'})}</time><span><b>{c?.name||'Канал удалён'}</b><small>VIDEO_{String(j.number).padStart(3,'0')} • {j.title||'Без названия'}</small></span><em className={`publishState ${j.status.toLowerCase()}`}>{j.status}</em>{j.youtubeVideoId&&<small>YT: {j.youtubeVideoId}</small>}</div>})}</div>}</section>}</>
.vyron-v110-audit/src/ProductionOS.tsx:9:import {productionManagerApi,type ProductionStorageStatus} from './productionManagerApi';
.vyron-v110-audit/src/ProductionOS.tsx:13:type Tab='queue'|'materials'|'manager';
.vyron-v110-audit/src/ProductionOS.tsx:18: const channels=useApp(s=>s.channels),jobs=useApp(s=>s.jobs),setJobs=useApp(s=>s.setJobs),patchJob=useApp(s=>s.patchJob),settings=useApp(s=>s.settings),toast=useApp(s=>s.toast),setPage=useApp(s=>s.setPage);const [prefs,patchPrefs]=useProductionPrefs();const tab=(prefs.tab||'queue') as Tab;const channelId=(prefs.selectedChannelId&&channels.some(c=>c.id===prefs.selectedChannelId)?prefs.selectedChannelId:channels[0]?.id)||'';const setTab=(v:Tab)=>patchPrefs({tab:v});const setChannelId=(id:string)=>patchPrefs({selectedChannelId:id});const [filter,setFilter]=useState('all'),[busy,setBusy]=useState(false),[inbox,setInbox]=useState<any>(),[planOpen,setPlanOpen]=useState(false),[planCount,setPlanCount]=useState(30),[planScope,setPlanScope]=useState<'one'|'all'>('one');const [section,setSection]=useState<'overview'|'materials'|'builder'|'render'|'publish'>(()=>tab==='materials'?'materials':tab==='manager'?'builder':'overview');const scopedJobs=section==='render'?jobs.filter(j=>['READY_RENDER','RENDERING','ERROR'].includes(j.status)):section==='publish'?jobs.filter(j=>['READY_UPLOAD','UPLOADING','SCHEDULED','ERROR'].includes(j.status)):jobs;const filtered=scopedJobs.filter(j=>filter==='all'||j.channelId===filter||j.status===filter).sort((a,b)=>a.number-b.number);const c=channels.find(x=>x.id===channelId);const selectedJobIds=(prefs.selectedJobIds||[]).filter(id=>jobs.some(j=>j.id===id));const projectRoot=resolveProductionRootFromPrefs(prefs,channelId,settings.workspace);const [projectStorage,setProjectStorage]=useState<ProductionStorageStatus|null>(null);
.vyron-v110-audit/src/ProductionOS.tsx:20: useEffect(()=>{let live=true;if(!projectRoot){setProjectStorage(null);return}productionManagerApi.storageStatus(projectRoot).then(x=>{if(live)setProjectStorage(x)}).catch(e=>{if(live)setProjectStorage({path:projectRoot,exists:false,writable:false,external:projectRoot.startsWith('/Volumes/'),freeBytes:null,error:String(e)})});return()=>{live=false}},[projectRoot]);
.vyron-v110-audit/src/ProductionOS.tsx:22: async function chooseProjectRoot(scope:'global'|'channel'){const initial=scope==='channel'?(c?prefs.byChannel[c.id]?.productionRoot:undefined)||projectRoot:(prefs.productionRoot||settings.workspace||projectRoot);const requested=await productionManagerApi.chooseProductionRoot(initial||undefined);if(!requested)return;const st=await productionManagerApi.storageStatus(requested);if(!st.exists||!st.writable){toast(st.error||'Выбранная папка проектов недоступна для записи');return}const path=st.path||requested;if(scope==='channel'&&c){patchChannelProductionPrefs(c.id,{productionRoot:path});toast(`Отдельная папка для ${c.name} сохранена`)}else{patchPrefs({productionRoot:path});toast('Основная папка проектов сохранена')}setProjectStorage(st)}
.vyron-v110-audit/src/ProductionOS.tsx:32: async function importImages(){if(!c)return;let root=projectRoot;if(!root){root=await api.defaultWorkspace();useApp.getState().patchSettings({workspace:root})}const storage=await productionManagerApi.storageStatus(root);if(!storage.exists||!storage.writable){toast(storage.error||`Папка проектов недоступна: ${root}`);return}const files=await api.chooseImages();if(!files.length)return;const pending=jobs.filter(j=>j.channelId===c.id&&j.status==='NEED_IMAGE').sort((a,b)=>a.number-b.number);const nums=pending.slice(0,files.length).map(j=>j.number);if(!nums.length){toast('Нет проектов, ожидающих изображение');return}const imported=await api.importImages(root,c.id,c.name,files,c.minTracks,nums);for(const x of imported){const old=useApp.getState().jobs.find(j=>j.channelId===c.id&&j.number===x.number);if(old)useApp.getState().patchJob(old.id,x);else useApp.getState().addJobs([x])}toast(`${files.length} изображений распределено по VIDEO_${String(nums[0]).padStart(3,'0')}…`)}
.vyron-v110-audit/src/ProductionOS.tsx:35:  <section className="panel pmStorageUnified"><div className="pmStorageUnifiedMain"><small>ХРАНИЛИЩЕ ПРОЕКТОВ</small><h3>{projectRoot||'Папка не выбрана'}</h3><p>{projectStorage?.external?'Внешний диск':'Внутренний диск'} • {projectStorage?.writable?'запись доступна':'нужно проверить доступ'}{typeof projectStorage?.freeBytes==='number'?` • свободно ${(projectStorage.freeBytes/1024/1024/1024).toFixed(1)} GB`:''}</p></div><div className="pmActions"><button className="primary" onClick={()=>void chooseProjectRoot('global')}>ИЗМЕНИТЬ ПАПКУ</button>{c&&<button onClick={()=>void chooseProjectRoot('channel')}>ОТДЕЛЬНАЯ ПАПКА ДЛЯ КАНАЛА</button>}{projectRoot&&<button onClick={()=>void productionManagerApi.openFolder(projectRoot)}>ОТКРЫТЬ В FINDER</button>}</div>{projectStorage?.error&&<div className="pmStorageError">{projectStorage.error}</div>}</section>
.vyron-v110-audit/src/ProductionOS.tsx:36:  <div className="youtubeTabs productionTabs productionNavFive"><button className={section==='overview'?'active':''} onClick={()=>{setSection('overview');setTab('queue');setFilter('all')}}>Обзор</button><button className={section==='materials'?'active':''} onClick={()=>{setSection('materials');setTab('materials')}}>Материалы</button><button className={section==='builder'?'active':''} onClick={()=>{setSection('builder');setTab('manager')}}>Сборка проектов</button><button className={section==='render'?'active':''} onClick={()=>{setSection('render');setTab('queue');setFilter('all')}}>Рендер</button><button className={section==='publish'?'active':''} onClick={()=>{setSection('publish');setTab('queue');setFilter('all')}}>Публикация</button></div>
.vyron-v110-audit/src/ProductionOS.tsx:37:  {tab==='queue'&&<>{section==='overview'&&<ProductionWorkspace/>}<div className="productionPipeline">{stageOrder.map(st=><button key={st} className={st==='ERROR'&&jobs.some(j=>j.status==='ERROR')?'warn':''} onClick={()=>setFilter(st)}><small>{statusLabel[st]}</small><b>{jobs.filter(j=>j.status===st).length}</b></button>)}</div><div className="filterBar"><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">Все каналы / статусы</option>{sortChannelsAlphabetically(channels).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}{stageOrder.map(st=><option key={st} value={st}>{statusLabel[st]}</option>)}</select><span>{filtered.length} задач</span></div>{filtered.length===0?<div className="empty"><b>Нет задач в этом фильтре</b><p>Создай точное число проектов или включи поддержание буфера.</p><button className="primary" onClick={()=>setPlanOpen(true)}>Создать проекты</button></div>:<div className="jobs virtualReady"><div className="pmBulkBar"><span>Выбрано <b>{selectedJobIds.filter(id=>visibleJobIds.includes(id)).length}</b> / {visibleJobIds.length}</span><button onClick={()=>patchPrefs({selectedJobIds:[...new Set([...selectedJobIds,...visibleJobIds])]})}>Выбрать всё</button><button onClick={()=>patchPrefs({selectedJobIds:selectedJobIds.filter(id=>!visibleJobIds.includes(id))})}>Снять выделение</button><button className="danger" disabled={!selectedJobIds.some(id=>visibleJobIds.includes(id))} onClick={()=>void deleteJobs(selectedJobIds.filter(id=>visibleJobIds.includes(id)),'Удалить выбранные проекты?')}>Удалить выбранные</button><button className="danger" onClick={()=>void deleteJobs(visibleJobIds,'Удалить все проекты в текущем списке?')}>Удалить все</button></div>{filtered.map(j=>{const ch=channels.find(x=>x.id===j.channelId);return <article className="job productionJob" key={j.id}><label className="pmJobCheck"><input type="checkbox" checked={selectedJobIds.includes(j.id)} onChange={()=>toggleJob(j.id)}/></label><div className={`jobStatus ${j.status.toLowerCase()}`}>{statusLabel[j.status]}</div><div className="jobMain"><small>{ch?.name||'Канал удалён'} • VIDEO_{String(j.number).padStart(3,'0')}</small><b>{j.title||'Метаданные ещё не готовы'}</b><span>Изображение {j.coverPath?'✓':'—'} • треков {j.tracksCount}/{j.minTracks} • final.mp4 {j.finalPath?'✓':'—'} • metadata {j.title&&j.description?'✓':'—'}</span><span>{j.publishAt?`Публикация: ${new Date(j.publishAt).toLocaleString('ru-RU')}`:'Дата публикации не назначена'}</span>{j.status==='UPLOADING'&&<div className="miniProgress"><i style={{width:`${j.uploadProgress||0}%`}}/></div>}{j.error&&<em className="jobError">{j.error}</em>}</div><div className="jobActions">{j.folder&&<button onClick={()=>api.reveal(j.folder)}>Папка</button>}{j.status==='WAITING_MUSIC'&&<button className="primary small" onClick={async()=>{const files=await api.chooseTracks();if(!files.length||!j.folder)return;try{const r=await api.addTracks(j.folder,files,ch?.minTracks||j.minTracks);patchJob(j.id,{tracksCount:r.tracksCount,status:r.status as JobStatus})}catch(e){patchJob(j.id,{status:'ERROR',error:String(e)})}}}>+ Аудио</button>}{j.status==='READY_RENDER'&&j.folder&&<button className="primary small" onClick={async()=>{try{await api.enqueueRender(settings.workspace,j.folder);patchJob(j.id,{renderQueuedAt:new Date().toISOString()});if(settings.endlumePath)await api.openEndlume(settings.endlumePath);toast('Проект отправлен в ENDLUME RenderQueue')}catch(e){toast(String(e))}}}>В ENDLUME</button>}{j.status==='READY_UPLOAD'&&<button className="primary small" onClick={()=>setPage('youtube')}>YouTube</button>}{j.status==='ERROR'&&<button onClick={async()=>{if(!j.folder)return;try{patchJob(j.id,{...(await api.refreshJob(j.folder,ch?.minTracks||j.minTracks)),error:undefined})}catch(e){patchJob(j.id,{error:String(e)})}}}>Повторить</button>}</div></article>})}</div>}</>}
.vyron-v110-audit/src/NotificationStack.tsx:16:  const [visible,setVisible]=useState<AppNotification[]>([]);const queue=useRef<AppNotification[]>([]);
.vyron-v110-audit/src/NotificationStack.tsx:17:  const fill=(rows:AppNotification[])=>{const next=[...rows];while(next.length<MAX_VISIBLE&&queue.current.length)next.push(queue.current.shift()!);return next};
.vyron-v110-audit/src/NotificationStack.tsx:18:  useEffect(()=>subscribeNotifications(n=>setVisible(rows=>{if(rows.some(x=>x.operationId&&x.operationId===n.operationId))return rows;if(rows.length<MAX_VISIBLE)return[...rows,n];queue.current.push(n);return rows})),[]);
.vyron-v110-audit/src/channelRunway.test.ts:20:class MemoryStorage{
.vyron-v110-audit/src/channelRunway.test.ts:79:describe('Channel Runway persistence and 06:00 KRAT scheduler',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:80:  it('persists scheduledUntil across restart-like reload',()=>{
.vyron-v110-audit/src/channelRunway.test.ts:81:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:83:    upsertChannelRunwayFromYoutube(channel(),[scheduled('2026-12-01T04:00:00+07:00')],now,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:84:    const loaded=loadChannelRunwayStore(storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:86:    const recalculated=recalculateChannelRunway([channel()],new Date('2026-09-03T08:00:00Z'),false,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:92:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:93:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({version:1,updatedAt:'2026-09-02T08:00:00Z',videos:[],baseline:{},syncInfo:null}));
.vyron-v110-audit/src/channelRunway.test.ts:94:    const result=recalculateChannelRunway([channel()],new Date('2026-09-02T08:00:00Z'),false,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:101:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:104:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({
.vyron-v110-audit/src/channelRunway.test.ts:107:    const result=recalculateChannelRunway([channel()],new Date('2026-09-02T08:00:00Z'),false,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:113:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:115:    upsertChannelRunwayFromYoutube(channel(),[scheduled('2026-12-01T04:00:00+07:00')],now,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:117:    storage.setItem('vyron:existing-cache:v1:neon',JSON.stringify({version:1,videos:[stale],baseline:{[stale.id]:stale},syncInfo:{complete:true}}));
.vyron-v110-audit/src/channelRunway.test.ts:118:    const result=recalculateChannelRunway([channel()],new Date('2026-09-03T08:00:00Z'),false,storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:124:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:127:    const initial=loadChannelRunwayStore(storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:129:    expect(maybeRunDailyChannelRunway([channel()],before,storage as any)).toBe(false);
.vyron-v110-audit/src/channelRunway.test.ts:130:    expect(maybeRunDailyChannelRunway([channel()],atSix,storage as any)).toBe(true);
.vyron-v110-audit/src/channelRunway.test.ts:131:    const after=loadChannelRunwayStore(storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:137:    const storage=new MemoryStorage();
.vyron-v110-audit/src/channelRunway.test.ts:138:    saveChannelRunwayStore({version:1,lastKrasnoyarskDate:'2026-09-01',channels:{}},storage as any);
.vyron-v110-audit/src/channelRunway.test.ts:140:    expect(maybeRunDailyChannelRunway([channel()],launch,storage as any)).toBe(true);
.vyron-v110-audit/src/channelRunway.test.ts:141:    expect(maybeRunDailyChannelRunway([channel()],launch,storage as any)).toBe(false);
.vyron-v110-audit/src/youtubeQuota.test.ts:6:    expect(isYoutubeQuotaError(new Error('The request cannot be completed because you have exceeded your quota. [quotaExceeded]'))).toBe(true);
.vyron-v110-audit/src/api.ts:39:  enqueueRender:(workspace:string,jobFolder:string)=>invoke<{queueFile:string}>('enqueue_render',{workspace,jobFolder}),
.vyron-v110-audit/src/api.ts:61:  youtubeUpdateExisting:(profileId:string,videoId:string,title:string,description:string,tags:string[],publishAt?:string,privacyStatus?:string)=>ytInvoke<{id:string;verified:boolean;metadataAccepted?:boolean;metadataVerified:boolean;metadataVerifyPending?:boolean;scheduleRequested:boolean;scheduleAccepted?:boolean;scheduleVerified:boolean;scheduleVerifyPending?:boolean;scheduleError?:string|null;skipped?:boolean;appliedTags?:number}>('youtube_update_existing_video',{profileId,videoId,title,description,tags,publishAt,privacyStatus}),
.vyron-v110-audit/src/PublisherOS.tsx:21:  <div className="queueHeader"><div><small>UPLOAD QUEUE</small><h2>Очередь YouTube</h2><p>Новые final.mp4, загрузка, обработка, расписание и ошибки.</p></div><div className="headerActions"><button onClick={()=>input.current?.click()}>✦ Метаданные</button><button className="primary" disabled={busy||!jobs.some(j=>j.status==='READY_UPLOAD')} onClick={uploadAll}>{busy?'Загружаю…':'Загрузить все готовые'}</button></div></div>
.vyron-v110-audit/src/PublisherOS.tsx:22:  <div className="queueKpis"><span>Готовы <b>{jobs.filter(j=>j.status==='READY_UPLOAD').length}</b></span><span>Uploading <b>{jobs.filter(j=>j.status==='UPLOADING').length}</b></span><span>Scheduled <b>{jobs.filter(j=>j.status==='SCHEDULED').length}</b></span><span className={jobs.some(j=>j.status==='ERROR')?'warn':''}>Ошибки <b>{jobs.filter(j=>j.status==='ERROR').length}</b></span></div>
.vyron-v110-audit/src/types.ts:24:  renderQueuedAt?:string; lastAutomationAt?:string;
.vyron-v110-audit/src/types.ts:37:  autoQueueRender:boolean; autoOpenEndlume:boolean; autoUploadYoutube:boolean; autopilotIntervalSec:number; tracksPerVideo:number;
.vyron-v110-audit/src/types.ts:48:export type AutopilotSummary={prepared:number;tracksMoved:number;imagesMoved:number;metadataGenerated:number;renderQueued:number;uploads:number;errors:number;notes:string[]};
.vyron-v110-audit/src/MetadataPage.tsx:17:type ApplyReport={metadataOk:number;total:number;scheduleOk:number;scheduleTotal:number;failed:number;issues:ApplyIssue[];pausedByQuota?:boolean;pending?:number;at?:string};
.vyron-v110-audit/src/MetadataPage.tsx:26:function readDraft(channelId:string):MetadataDraft|undefined{if(!channelId)return;try{const x=JSON.parse(localStorage.getItem(draftKey(channelId))||'null');return x?.version===1?x:undefined}catch{return}}
.vyron-v110-audit/src/MetadataPage.tsx:27:function writeDraft(channelId:string,d:MetadataDraft){if(!channelId)return;try{localStorage.setItem(draftKey(channelId),JSON.stringify(d))}catch{}}
.vyron-v110-audit/src/MetadataPage.tsx:35: useEffect(()=>{restoring.current=true;const d=readDraft(channelId);setCadence(d?.cadence||channel?.cadenceDays||2);setScheduleMode(d?.scheduleMode||'auto');setTarget(d?.target||'youtube');setScheduleRevision(x=>x+1);setRows(d?.rows||[]);setPaste(d?.paste||'');setYt((d?.selectedVideos||[]).map(v=>({...v,selected:true})));setOrder(d?.order||'newest');setFilter(d?.filter||'private');setDocxStrict(Boolean(d?.docxStrict));if(d?.start)setStart(d.start);setSyncStatus(d?`↻ Черновик восстановлен • ${new Date(d.updatedAt).toLocaleString('ru-RU')}`:'');setHistory(d?.history||[]);setDraftSavedAt(d?.updatedAt||'');setApplyProgress('');setApplyReport(null);queueMicrotask(()=>{restoring.current=false})},[channelId]);
.vyron-v110-audit/src/MetadataPage.tsx:70:  setBusy(true);setApplyReport(null);let metadataOk=0,scheduleOk=0,scheduleTotal=0,failed=0,pausedByQuota=false;const issues:ApplyIssue[]=[];const completedIds=new Set<string>();const cacheUpdates:YoutubeExistingVideo[]=[];
.vyron-v110-audit/src/MetadataPage.tsx:77:      if(result.scheduleRequested){scheduleTotal++;if(result.scheduleAccepted!==false){scheduleOk++;completedIds.add(v.id);updated={...updated,publishAt}}else{failed++;const err=result.scheduleError||'YouTube не принял расписание';if(isYoutubeQuotaError(err)){markYoutubeQuotaExceeded(err);pausedByQuota=true;issues.push({videoId:v.id,title:v.title,phase:'schedule',error:youtubeQuotaMessage()});cacheUpdates.push(updated);break}issues.push({videoId:v.id,title:v.title,phase:'schedule',error:err})}}
.vyron-v110-audit/src/MetadataPage.tsx:78:      else completedIds.add(v.id);cacheUpdates.push(updated);
.vyron-v110-audit/src/MetadataPage.tsx:81:    let pending=0;if(pausedByQuota){const remainingMatches=matches.filter(x=>x.video&&!completedIds.has(x.video.id));const pendingIds=new Set(remainingMatches.map(x=>x.video!.id));const pendingRows=remainingMatches.map(x=>x.row);setRows(pendingRows);setYt(prev=>prev.map(v=>({...v,selected:pendingIds.has(v.id)})));pending=pendingIds.size;setSyncStatus(`⏸ Квота YouTube исчерпана • сохранено ${completedIds.size} • осталось ${pending}`)}
.vyron-v110-audit/src/MetadataPage.tsx:82:    const report:ApplyReport={metadataOk,total:matched,scheduleOk,scheduleTotal,failed,issues,pausedByQuota,pending,at:new Date().toISOString()};setApplyReport(report);const h:MetadataHistory={at:report.at!,metadataOk,total:matched,scheduleOk,scheduleTotal,failed,pausedByQuota};setHistory(x=>[h,...x].slice(0,20));
.vyron-v110-audit/src/MetadataPage.tsx:83:    if(pausedByQuota)notifyWarning('Выполнение приостановлено',`Квота YouTube закончилась. Осталось ${pending}; выбранная пачка сохранена.`);
.vyron-v110-audit/src/MetadataPage.tsx:94:  {applyReport&&<section className="panel"><div className="panelHead"><div><small>YOUTUBE WRITE REPORT</small><h3>{applyReport.failed===0&&applyReport.issues.length===0?'✓ Запись подтверждена':'Результат применения'}</h3></div><button onClick={()=>setApplyReport(null)}>Закрыть</button></div><div className="syncAudit"><span>Метаданные <b>{applyReport.metadataOk}/{applyReport.total}</b></span>{applyReport.scheduleTotal>0&&<span>Расписание <b>{applyReport.scheduleOk}/{applyReport.scheduleTotal}</b></span>}<span>Полностью не записано <b>{applyReport.failed}</b></span>{applyReport.pausedByQuota&&<span className="warn">Пауза квоты • осталось <b>{applyReport.pending||0}</b></span>}</div>{applyReport.issues.length>0&&<div className="metadataPreviewList">{applyReport.issues.map((x,i)=><div className="metadataPreviewRow missing" key={`${x.videoId}-${i}`}><span><b>{x.phase==='schedule'?'РАСПИСАНИЕ':'МЕТАДАННЫЕ'} • {x.videoId}</b><small>{short(x.title,70)}</small></span><strong>{x.phase==='schedule'?'Метаданные сохранены, расписание не применено':'Видео не обновлено'}</strong><em>{x.error}</em></div>)}</div>}</section>}
.vyron-v110-audit/src/productionManagerApi.ts:8:export type BatchSummary={batchId:string;channelId:string;channelName:string;createdAt:string;projectCount:number;tracksAssigned:number;status:string;manifestPath:string;rootPath:string;completedProjects:number;errorProjects:number};
.vyron-v110-audit/src/productionManagerApi.ts:11:export type ProductionStorageStatus={path:string;exists:boolean;writable:boolean;external:boolean;freeBytes?:number|null;error?:string|null};
.vyron-v110-audit/src/productionManagerApi.ts:17:export type RecoveryState={batchId:string;channelId:string;channelName:string;rootPath:string;completedProjects:number;totalProjects:number;currentProject:string;status:string;updatedAt:string;recoverable:boolean};
.vyron-v110-audit/src/productionManagerApi.ts:22:  storageStatus:(path:string)=>invoke<ProductionStorageStatus>('production_storage_status',{path}),
.vyron-v110-audit/src/productionManagerApi.ts:32:  findRecovery:(workspaces:string[])=>invoke<RecoveryState[]>('find_production_recovery',{workspaces}),
.vyron-v110-audit/src/productionManagerApi.ts:33:  restartRecovery:(manifestOrRoot:string)=>invoke<BatchSummary>('restart_production_batch',{manifestOrRoot}),
.vyron-v110-audit/src/productionManagerApi.ts:43:  onBatchProgress:(cb:(p:{batchId:string;completed:number;total:number;stage:string})=>void):Promise<UnlistenFn>=>listen('production-batch-progress',e=>cb(e.payload as any)),
.vyron-v110-audit/src/commandCenterCore.ts:5:export const COMMAND_CENTER_STORAGE_KEY='vyron:command-center:v1';
.vyron-v110-audit/src/commandCenter.test.ts:14:class MemoryStorage{
.vyron-v110-audit/src/commandCenter.test.ts:84:describe('Command Center persistence',()=>{
.vyron-v110-audit/src/commandCenter.test.ts:85:  it('persists Smart Batch Builder plan in its own versioned store',()=>{
.vyron-v110-audit/src/commandCenter.test.ts:86:    const storage=new MemoryStorage();
.vyron-v110-audit/src/commandCenter.test.ts:88:    saveBatchPlan(plan,storage as any);
.vyron-v110-audit/src/commandCenter.test.ts:89:    const loaded=loadCommandCenterStore(storage as any);
.vyron-v110-audit/src/productionPrefs.ts:4:export type ProductionTab='queue'|'materials'|'manager';
.vyron-v110-audit/src/productionPrefs.ts:9:const defaults=():ProductionPrefs=>({version:2,tab:'queue',byChannel:{},selectedJobIds:[]});
.vyron-v110-audit/src/productionPrefs.ts:13:  try{const x=JSON.parse(localStorage.getItem(KEY)||'null');if(x?.version===2)return{...defaults(),...x,byChannel:x.byChannel||{},selectedJobIds:Array.isArray(x.selectedJobIds)?x.selectedJobIds:[]}}catch{}
.vyron-v110-audit/src/productionPrefs.ts:15:  try{const old=JSON.parse(localStorage.getItem('vyron:production-workspace:v1')||'null');if(old?.selectedChannelId)next.selectedChannelId=String(old.selectedChannelId)}catch{}
.vyron-v110-audit/src/productionPrefs.ts:16:  try{localStorage.setItem(KEY,JSON.stringify(next))}catch{}
.vyron-v110-audit/src/productionPrefs.ts:19:function emit(next:ProductionPrefs){try{localStorage.setItem(KEY,JSON.stringify(next))}catch{}window.dispatchEvent(new CustomEvent(EVENT,{detail:next}))}
.vyron-v110-audit/src/youtubeIntelligence.ts:25: const s=useApp.getState(),own=s.channels.find(c=>c.id===channelId);if(!own?.youtubeProfileId)return 0;const key=`vyron:competitor-discovery:${channelId}`,last=Number(localStorage.getItem(key)||0);if(!force&&Date.now()-last<24*3600_000)return 0;
.vyron-v110-audit/src/youtubeIntelligence.ts:28: localStorage.setItem(key,String(Date.now()));return added;
.vyron-v110-audit/src/ProductionManager.tsx:3:import {productionManagerApi,type BatchStatus,type BatchSummary,type BuildResult,type ChannelProductionState,type DistributionMode,type ImportSession,type MusicSummary,type ProductionStorageStatus,type Validation} from './productionManagerApi';
.vyron-v110-audit/src/ProductionManager.tsx:39:  const [progress,setProgress]=useState<{completed:number;total:number;stage:string}|null>(null);
.vyron-v110-audit/src/ProductionManager.tsx:44:  const [storageStatus,setStorageStatus]=useState<ProductionStorageStatus|null>(null);
.vyron-v110-audit/src/ProductionManager.tsx:70:  useEffect(()=>{let live=true;if(!productionRoot){setStorageStatus(null);return}productionManagerApi.storageStatus(productionRoot).then(s=>{if(live)setStorageStatus(s)}).catch(e=>{if(live)setStorageStatus({path:productionRoot,exists:false,writable:false,external:productionRoot.startsWith('/Volumes/'),freeBytes:null,error:String(e)})});return()=>{live=false}},[productionRoot]);
.vyron-v110-audit/src/ProductionManager.tsx:80:      setProgress({completed:p.completed,total:p.total,stage:p.stage});
.vyron-v110-audit/src/ProductionManager.tsx:81:      if(p.total>0&&p.completed>=p.total)void refreshState();
.vyron-v110-audit/src/ProductionManager.tsx:142:    setBusy('build');setProgress({completed:0,total:projectCount,stage:'Подготовка'});setValidation(null);
.vyron-v110-audit/src/ProductionManager.tsx:186:      await refreshState();setBatchStatus(await productionManagerApi.status(resumed.manifestPath));toast(`Batch восстановлен: ${resumed.completedProjects}/${resumed.projectCount}`);
.vyron-v110-audit/src/ProductionManager.tsx:193:    const status=await productionManagerApi.storageStatus(requested);if(!status.exists||!status.writable){toast(status.error||'Папка проектов недоступна для записи');return}
.vyron-v110-audit/src/ProductionManager.tsx:195:    if(scope==='global')patchPrefs({productionRoot:path});else patchChannelProductionPrefs(channelId,{productionRoot:path});setStorageStatus(status);notifySuccess('Папка проектов изменена',path,{operationId:`storage:${scope}:${channelId}:${path}`});
.vyron-v110-audit/src/ProductionManager.tsx:213:      {progress&&<div className="pmBuildProgress"><div><span>{progress.stage}</span><b>{progress.completed} / {progress.total}</b></div><i><em style={{width:`${progress.total?Math.min(100,progress.completed/progress.total*100):0}%`}}/></i><small>Файловые операции выполняются native backend и не блокируют UI.</small></div>}
.vyron-v110-audit/src/ProductionManager.tsx:214:      <div className="pmActions"><button className="primary pmBuildButton" disabled={!!busy||!workspace||!channel||!music?.tracks||Boolean(customProductionRoot&&storageStatus&&(!storageStatus.exists||!storageStatus.writable))} onClick={()=>void buildBatch()}>{busy==='build'?'СОЗДАЮ…':`СОЗДАТЬ ${projectCount} ПРОЕКТОВ`}</button></div>
.vyron-v110-audit/src/ProductionManager.tsx:219:    <section className="panel pmHistory"><div className="pmHistoryHead"><div><small>ИСТОРИЯ СБОРОК</small><h3>{channel?.name||'Канал'}</h3></div><button onClick={()=>void refreshState()}>↻ ОБНОВИТЬ</button></div>{batches.length?<div className="pmBatchRows">{batches.slice(0,20).map(batch=><button key={batch.batchId} className={result?.batch?.batchId===batch.batchId?'active':''} onClick={()=>{setResult({status:'ready',availableImages:collected,requestedProjects:batch.projectCount,batch});patchChannelProductionPrefs(channelId,{lastBatchId:batch.batchId,selectedProjectIds:[]});void productionManagerApi.status(batch.manifestPath).then(setBatchStatus).catch(()=>setBatchStatus(null))}}><span><b>{batch.batchId}</b><small>{new Date(batch.createdAt).toLocaleString('ru-RU')}</small></span><em>{batch.projectCount} проектов</em><em>{batch.completedProjects}/{batch.projectCount}</em><small>{batch.status}</small><strong onClick={e=>{e.stopPropagation();void resume(batch)}}>{busy===batch.batchId?'…':'ОТКРЫТЬ'}</strong></button>)}</div>:<div className="pmEmpty">Для этого канала ещё нет Production batches.</div>}</section></>}
.vyron-v110-audit/src/ProductionStatusBridge.tsx:17:            if(['Completed','Завершено'].includes(batch.status)&&batch.completedProjects>=batch.projectCount)continue;
.vyron-v110-audit/src/ProductionStatusBridge.tsx:23:              else if(p.renderStatus==='Completed'&&p.outputFile&&(job.finalPath!==p.outputFile||job.status!=='READY_UPLOAD'))useApp.getState().patchJob(job.id,{status:'READY_UPLOAD',finalPath:p.outputFile,error:undefined});
.vyron-v110-audit/src/commandCenterStore.ts:1:import {COMMAND_CENTER_STORAGE_KEY,type BatchPlan} from './commandCenterCore';
.vyron-v110-audit/src/commandCenterStore.ts:3:type StorageLike=Pick<Storage,'getItem'|'setItem'|'removeItem'>;
.vyron-v110-audit/src/commandCenterStore.ts:7:const browserStorage=()=>typeof localStorage==='undefined'?undefined:localStorage;
.vyron-v110-audit/src/commandCenterStore.ts:10:export function loadCommandCenterStore(storage:StorageLike|undefined=browserStorage()):CommandCenterStore{
.vyron-v110-audit/src/commandCenterStore.ts:11:  if(!storage)return defaultStore();
.vyron-v110-audit/src/commandCenterStore.ts:13:    const x=JSON.parse(storage.getItem(COMMAND_CENTER_STORAGE_KEY)||'null') as CommandCenterStore|null;
.vyron-v110-audit/src/commandCenterStore.ts:19:export function saveCommandCenterStore(value:CommandCenterStore,storage:StorageLike|undefined=browserStorage()){
.vyron-v110-audit/src/commandCenterStore.ts:21:  if(storage)storage.setItem(COMMAND_CENTER_STORAGE_KEY,JSON.stringify(next));
.vyron-v110-audit/src/commandCenterStore.ts:26:export function saveBatchPlan(plan:BatchPlan,storage:StorageLike|undefined=browserStorage()){
.vyron-v110-audit/src/commandCenterStore.ts:27:  const current=loadCommandCenterStore(storage);
.vyron-v110-audit/src/commandCenterStore.ts:28:  return saveCommandCenterStore({...current,selectedChannelId:plan.channelId,batchPlans:{...current.batchPlans,[plan.channelId]:plan}},storage);
.vyron-v110-audit/src/commandCenterStore.ts:31:export function clearBatchPlan(channelId:string,storage:StorageLike|undefined=browserStorage()){
.vyron-v110-audit/src/commandCenterStore.ts:32:  const current=loadCommandCenterStore(storage);
.vyron-v110-audit/src/commandCenterStore.ts:34:  return saveCommandCenterStore({...current,batchPlans},storage);
.vyron-v110-audit/src/commandCenterStore.ts:37:export function selectCommandCenterChannel(channelId:string,storage:StorageLike|undefined=browserStorage()){
.vyron-v110-audit/src/commandCenterStore.ts:38:  const current=loadCommandCenterStore(storage);
.vyron-v110-audit/src/commandCenterStore.ts:39:  return saveCommandCenterStore({...current,selectedChannelId:channelId},storage);
.vyron-v110-audit/src/store.ts:10:  autoQueueRender:true,autoOpenEndlume:false,autoUploadYoutube:false,autopilotIntervalSec:30,tracksPerVideo:10,
.vyron-v110-audit/src/store.ts:21:  hydrate:(s:AppState)=>void; setPage:(p:Page)=>void; persist:()=>Promise<void>;
.vyron-v110-audit/src/store.ts:29:function scheduleSave(){window.clearTimeout(saveTimer);saveTimer=window.setTimeout(()=>{void useApp.getState().persist()},180)}
.vyron-v110-audit/src/store.ts:37:  persist:async()=>{const s=get();const state:AppState={version:7,channels:s.channels,jobs:s.jobs,competitors:s.competitors,settings:s.settings,logs:s.logs};await api.saveState(state)},
.vyron-v110-audit/src-tauri/src/youtube.rs:140: let mut file=tokio::fs::File::open(&path).await.map_err(|e|e.to_string())?;let chunk_size:usize=8*1024*1024;let mut offset:u64=0;let mut final_json=json!({});while offset<total{file.seek(std::io::SeekFrom::Start(offset)).await.map_err(|e|e.to_string())?;let wanted=std::cmp::min(chunk_size as u64,total-offset) as usize;let mut buf=vec![0u8;wanted];file.read_exact(&mut buf).await.map_err(|e|e.to_string())?;let end=offset+wanted as u64-1;let mut completed=false;for attempt in 0..5{let sent=client.put(&session).bearer_auth(&token).header("Content-Type","video/mp4").header("Content-Length",wanted.to_string()).header("Content-Range",format!("bytes {offset}-{end}/{total}")).body(buf.clone()).send().await;match sent{Ok(r)=>{let st=r.status();if st.is_success(){final_json=r.json().await.unwrap_or_else(|_|json!({}));offset=total;completed=true;break}if st.as_u16()==308{offset=r.headers().get("Range").and_then(|x|x.to_str().ok()).and_then(|s|s.split('-').last()).and_then(|x|x.parse::<u64>().ok()).map(|x|x+1).unwrap_or(end+1);let pct=(offset as f64/total as f64*100.0).min(100.0);let _=app.emit("youtube-upload-progress",json!({"jobId":job_id,"progress":pct}));completed=true;break}if st.is_server_error(){tokio::time::sleep(Duration::from_secs(1u64<<attempt)).await;offset=upload_offset(&client,&session,&token,total).await.unwrap_or(offset);continue}let text=r.text().await.unwrap_or_default();return Err(format!("YouTube upload {st}: {}",text.chars().take(500).collect::<String>()))},Err(e)=>{if attempt==4{return Err(format!("YouTube upload network: {e}"))}tokio::time::sleep(Duration::from_secs(1u64<<attempt)).await;offset=upload_offset(&client,&session,&token,total).await.unwrap_or(offset)}}}if !completed{return Err("YouTube upload не удалось продолжить".into())}}
.vyron-v110-audit/src-tauri/src/youtube.rs:164: let mut by_id=std::collections::HashMap::<String,Value>::new();for chunk in ids.chunks(50){let joined=chunk.join(",");let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,status,contentDetails,statistics"),("id",joined.as_str())]).send().await.map_err(|e|format!("YouTube videos: {e}"))?;let st=r.status();let v:Value=r.json().await.map_err(|e|e.to_string())?;if !st.is_success(){return Err(youtube_error(&v,"Не удалось получить данные видео"))}for item in v.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=item.get("id").and_then(|x|x.as_str()){by_id.insert(id.to_string(),item);}}}
.vyron-v110-audit/src-tauri/src/youtube.rs:228:  return Ok(json!({"id":video_id,"verified":true,"metadataAccepted":true,"metadataVerified":true,"metadataVerifyPending":false,"scheduleRequested":schedule_requested,"scheduleAccepted":schedule_pre_error.is_none(),"scheduleVerified":schedule_pre_error.is_none(),"scheduleVerifyPending":false,"scheduleError":schedule_pre_error,"skipped":true,"appliedTags":wanted_tags_vec.len()}))
.vyron-v110-audit/src-tauri/src/youtube.rs:255: Ok(json!({"id":video_id,"verified":metadata_accepted&&(!schedule_requested||schedule_accepted),"metadataAccepted":metadata_accepted,"metadataVerified":metadata_verified,"metadataVerifyPending":metadata_accepted&&!metadata_verified,"scheduleRequested":schedule_requested,"scheduleAccepted":schedule_accepted,"scheduleVerified":schedule_verified,"scheduleVerifyPending":schedule_accepted&&schedule_needed&&!schedule_verified,"scheduleError":schedule_error,"skipped":false,"appliedTags":wanted_tags_vec.len()}))
.vyron-v110-audit/src-tauri/src/lib.rs:4:mod storage;
.vyron-v110-audit/src-tauri/src/lib.rs:22:            storage::load_state,storage::save_state,
.vyron-v110-audit/src-tauri/src/lib.rs:23:            files::import_images,files::add_tracks,files::refresh_job,files::prepare_job_folder,files::ensure_channel_inbox,files::scan_channel_inbox,files::ingest_tracks,files::ingest_cover,files::write_job_metadata,files::enqueue_render,files::reveal_path,files::open_endlume,
.vyron-v110-audit/src-tauri/src/lib.rs:26:            production_manager::production_storage_status,production_manager::start_production_import,production_manager::stop_production_import,production_manager::production_import_status,production_manager::set_production_music_library,production_manager::index_production_music_library,production_manager::build_production_batch,production_manager::resume_production_batch,production_manager::find_production_recovery,production_manager::restart_production_batch,production_manager::read_production_batch_status,production_manager::list_production_batches,production_manager::production_channel_state,production_manager::validate_production_batch,production_manager::delete_production_batch_projects,production_manager::delete_production_job_folder,production_manager::open_production_batch_in_endlume,production_manager::production_endlume_handoff_consumed,
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:26: let money_daily=if monetary{analytics_report(&token,&start_s,&end_s,"estimatedRevenue",Some("day"),Some("day"),Some(400)).await.unwrap_or_else(|_|json!({"rows":[]}))}else{json!({"rows":[]})};let mut daily_revenue=std::collections::HashMap::<String,f64>::new();for r in report_rows(&money_daily){if let Some(d)=r.first().and_then(|x|x.as_str()){daily_revenue.insert(d.to_string(),r.get(1).map(value_f64).unwrap_or(0.0));}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:28: let top=analytics_report(&token,&start_s,&end_s,"views,estimatedMinutesWatched,averageViewDuration,averageViewPercentage,likes,comments,shares,subscribersGained",Some("video"),Some("-views"),Some(10)).await.unwrap_or_else(|_|json!({"rows":[]}));let top_rows=report_rows(&top);let top_ids=top_rows.iter().filter_map(|r|r.first().and_then(|x|x.as_str())).collect::<Vec<_>>();let mut video_meta=std::collections::HashMap::<String,Value>::new();if !top_ids.is_empty(){let joined=top_ids.join(",");let r=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet"),("id",joined.as_str())]).send().await.map_err(|e|format!("Top videos metadata: {e}"))?;let st=r.status();let v:Value=r.json().await.unwrap_or_else(|_|json!({}));if st.is_success(){for item in v.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=item.get("id").and_then(|x|x.as_str()){video_meta.insert(id.to_string(),item);}}}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:29: let video_money=if monetary{analytics_report(&token,&start_s,&end_s,"views,estimatedRevenue",Some("video"),Some("-views"),Some(200)).await.unwrap_or_else(|_|json!({"rows":[]}))}else{json!({"rows":[]})};let mut video_rev=std::collections::HashMap::<String,f64>::new();for r in report_rows(&video_money){if let Some(id)=r.first().and_then(|x|x.as_str()){video_rev.insert(id.to_string(),r.get(2).map(value_f64).unwrap_or(0.0));}}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:32: let countries=analytics_report(&token,&start_s,&end_s,"views,estimatedMinutesWatched",Some("country"),Some("-views"),Some(12)).await.unwrap_or_else(|_|json!({"rows":[]}));let country_money=if monetary{analytics_report(&token,&start_s,&end_s,"views,estimatedRevenue",Some("country"),Some("-views"),Some(50)).await.unwrap_or_else(|_|json!({"rows":[]}))}else{json!({"rows":[]})};let mut crev=std::collections::HashMap::<String,f64>::new();for r in report_rows(&country_money){if let Some(k)=r.first().and_then(|x|x.as_str()){crev.insert(k.to_string(),r.get(2).map(value_f64).unwrap_or(0.0));}}let countries_rows=report_rows(&countries).into_iter().map(|r|{let key=r.first().and_then(|x|x.as_str()).unwrap_or("—").to_string();let views=r.get(1).map(value_u64).unwrap_or(0);let rev=crev.get(&key).copied();json!({"key":key,"views":views,"watchMinutes":r.get(2).map(value_f64).unwrap_or(0.0),"estimatedRevenue":rev,"rpm":if views>0{rev.map(|x|x/views as f64*1000.0)}else{None}})}).collect::<Vec<_>>();
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:41:fn keywords(text:&str)->Vec<String>{let stop=["the","and","for","with","from","this","that","your","you","radio","music","mix","playlist","official","channel","video","songs","song","new","best","для","это","как","что","или","канал","музыка"];let mut counts=std::collections::HashMap::<String,u32>::new();for raw in text.split(|c:char|!c.is_alphanumeric()){let w=raw.to_lowercase();if w.chars().count()<3||stop.contains(&w.as_str()){continue}*counts.entry(w).or_insert(0)+=1}let mut v=counts.into_iter().collect::<Vec<_>>();v.sort_by(|a,b|b.1.cmp(&a.1).then(a.0.cmp(&b.0)));v.into_iter().take(8).map(|x|x.0).collect()}
.vyron-v110-audit/src-tauri/src/youtube_intelligence.rs:50: let mut latest=Vec::<Value>::new();if !ids.is_empty(){let joined=ids.join(",");let vr=client.get("https://www.googleapis.com/youtube/v3/videos").bearer_auth(&token).query(&[("part","snippet,statistics"),("id",joined.as_str())]).send().await.map_err(|e|format!("Competitor videos: {e}"))?;let vv:Value=vr.json().await.unwrap_or_else(|_|json!({}));let mut map=std::collections::HashMap::<String,Value>::new();for it in vv.get("items").and_then(|x|x.as_array()).cloned().unwrap_or_default(){if let Some(id)=it.get("id").and_then(|x|x.as_str()){map.insert(id.to_string(),it);}}for id in ids.iter(){if let Some(it)=map.get(id){let s=it.get("snippet").cloned().unwrap_or_else(||json!({}));let st=it.get("statistics").cloned().unwrap_or_else(||json!({}));latest.push(json!({"id":id,"title":s.get("title").and_then(|x|x.as_str()).unwrap_or(""),"thumbnail":thumb_url(&s),"publishedAt":s.get("publishedAt").and_then(|x|x.as_str()),"views":st.get("viewCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0),"likes":st.get("likeCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0),"comments":st.get("commentCount").and_then(|x|x.as_str()).and_then(|x|x.parse::<u64>().ok()).unwrap_or(0)}));}}}
.vyron-v110-audit/src-tauri/src/files.rs:353:pub fn enqueue_render(workspace: String, job_folder: String) -> Result<Value, String> {
.vyron-v110-audit/src-tauri/src/files.rs:358:    let queue = PathBuf::from(workspace).join("RenderQueue");
.vyron-v110-audit/src-tauri/src/files.rs:359:    fs::create_dir_all(&queue).map_err(|e| e.to_string())?;
.vyron-v110-audit/src-tauri/src/files.rs:364:    let marker = queue.join(format!("{}.json", number));
.vyron-v110-audit/src-tauri/src/files.rs:367:        "queuedAt": Utc::now().to_rfc3339(),
.vyron-v110-audit/src-tauri/src/files.rs:375:    Ok(json!({"queueFile": marker}))
.vyron-v110-audit/src-tauri/src/production_manager.rs:6:    collections::{HashMap, HashSet},
.vyron-v110-audit/src-tauri/src/production_manager.rs:22:static IMPORT_STOPS: OnceLock<Mutex<HashMap<String, Arc<AtomicBool>>>> = OnceLock::new();
.vyron-v110-audit/src-tauri/src/production_manager.rs:25:fn import_stops() -> &'static Mutex<HashMap<String, Arc<AtomicBool>>> {
.vyron-v110-audit/src-tauri/src/production_manager.rs:26:    IMPORT_STOPS.get_or_init(|| Mutex::new(HashMap::new()))
.vyron-v110-audit/src-tauri/src/production_manager.rs:81:fn hash_file(path: &Path) -> Result<String,String> {
.vyron-v110-audit/src-tauri/src/production_manager.rs:87:fn seq_hash(ids: &[String]) -> String {
.vyron-v110-audit/src-tauri/src/production_manager.rs:108:fn image_snapshot(dir:&Path)->HashSet<String>{recursive_images(dir).unwrap_or_default().into_iter().map(|p|p.to_string_lossy().into_owned()).collect()}
.vyron-v110-audit/src-tauri/src/production_manager.rs:166:pub struct MusicHistory { pub schema_version:u32,pub tracks:HashMap<String,TrackUsage>,pub sequence_fingerprints:Vec<String> }
.vyron-v110-audit/src-tauri/src/production_manager.rs:187:    pub tracks:Vec<ManifestTrack>,pub total_duration_sec:f64,pub status:String,pub sequence_fingerprint:String
.vyron-v110-audit/src-tauri/src/production_manager.rs:209:    pub status:String,pub manifest_path:String,pub root_path:String,pub completed_projects:usize,pub error_projects:usize
.vyron-v110-audit/src-tauri/src/production_manager.rs:234:struct PlanProject { project_id:String,job_id:Option<String>,video_number:Option<u32>,image_source:String,image_name:String,tracks:Vec<PlanTrack>,sequence_fingerprint:String }
.vyron-v110-audit/src-tauri/src/production_manager.rs:237:pub struct ProductionStorageStatus { pub path:String,pub exists:bool,pub writable:bool,pub external:bool,pub free_bytes:Option<u64>,pub error:Option<String> }
.vyron-v110-audit/src-tauri/src/production_manager.rs:239:fn storage_free_bytes(path:&Path)->Option<u64>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:246:fn storage_probe(path:&Path)->ProductionStorageStatus{
.vyron-v110-audit/src-tauri/src/production_manager.rs:248:    if !path.exists(){return ProductionStorageStatus{path:display,exists:false,writable:false,external,free_bytes:None,error:Some("Production-диск или папка недоступны".into())};}
.vyron-v110-audit/src-tauri/src/production_manager.rs:249:    if !path.is_dir(){return ProductionStorageStatus{path:display,exists:true,writable:false,external,free_bytes:None,error:Some("Production Workspace должен быть папкой".into())};}
.vyron-v110-audit/src-tauri/src/production_manager.rs:252:    ProductionStorageStatus{path:display,exists:true,writable,external,free_bytes:storage_free_bytes(path),error:if writable{None}else{Some("Нет доступа на запись в Production Workspace".into())}}
.vyron-v110-audit/src-tauri/src/production_manager.rs:257:    if chosen!=req.workspace.trim(){let st=storage_probe(Path::new(chosen));if !st.exists||!st.writable{return Err(st.error.unwrap_or_else(||format!("Production Workspace недоступен: {chosen}")));}}
.vyron-v110-audit/src-tauri/src/production_manager.rs:261:pub fn production_storage_status(path:String)->ProductionStorageStatus{let raw=PathBuf::from(path.trim());let exact=raw.canonicalize().unwrap_or(raw);storage_probe(&exact)}
.vyron-v110-audit/src-tauri/src/production_manager.rs:268:struct Checkpoint { completed_projects:usize,total_projects:usize,status:String,history_applied:bool,updated_at:String }
.vyron-v110-audit/src-tauri/src/production_manager.rs:271:pub struct RecoveryState {
.vyron-v110-audit/src-tauri/src/production_manager.rs:273:    pub completed_projects:usize,pub total_projects:usize,pub current_project:String,pub status:String,pub updated_at:String,pub recoverable:bool
.vyron-v110-audit/src-tauri/src/production_manager.rs:296:fn import_candidate_ready(src:&Path,pending:&mut HashMap<String,ImportProbe>)->bool{
.vyron-v110-audit/src-tauri/src/production_manager.rs:298:    let meta=match fs::metadata(src){Ok(m)=>m,Err(_)=>{pending.remove(&key);return false;}};
.vyron-v110-audit/src-tauri/src/production_manager.rs:299:    if meta.len()==0{pending.remove(&key);return false;}
.vyron-v110-audit/src-tauri/src/production_manager.rs:301:    match pending.get_mut(&key){
.vyron-v110-audit/src-tauri/src/production_manager.rs:306:        _=>{pending.insert(key,now);false}
.vyron-v110-audit/src-tauri/src/production_manager.rs:310:fn collector_seen_at_start(session:&ImportSession,_startup_snapshot:&HashSet<String>)->HashSet<String>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:311:    // Only files already persisted by THIS session are duplicates.
.vyron-v110-audit/src-tauri/src/production_manager.rs:316:fn spawn_import_watcher(app:AppHandle, workspace:String, channel_id:String, mut session:ImportSession, baseline:HashSet<String>) -> Result<(),String> {
.vyron-v110-audit/src-tauri/src/production_manager.rs:326:        let mut pending=HashMap::<String,ImportProbe>::new();
.vyron-v110-audit/src-tauri/src/production_manager.rs:336:                if !import_candidate_ready(&src,&mut pending){continue;}
.vyron-v110-audit/src-tauri/src/production_manager.rs:353:                    pending.insert(key.clone(),ImportProbe{size:after.len(),modified_ms:modified_ms(&after),stable_cycles:0});
.vyron-v110-audit/src-tauri/src/production_manager.rs:362:                pending.remove(&key);
.vyron-v110-audit/src-tauri/src/production_manager.rs:419:    let old:MusicIndex=read_json(&index_path(workspace,channel_id)?); let old_map=old.tracks.into_iter().map(|t|(t.path.clone(),t)).collect::<HashMap<_,_>>();
.vyron-v110-audit/src-tauri/src/production_manager.rs:425:        tracks.push(TrackIndex{track_id:hash_file(&p)?,path:ps,size:m.len(),modified_ms:mm,duration_sec:audio_duration(&p)});
.vyron-v110-audit/src-tauri/src/production_manager.rs:443:fn choose_sequence(mode:&str,tracks:&[TrackIndex],history:&MusicHistory,planned:&HashMap<String,u64>,global_pool:&mut Vec<usize>,cursor:&mut usize,count:usize,batch_id:&str,project_no:usize,seen:&HashSet<String>)->Result<Vec<usize>,String>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:456:                let mut local=HashSet::new();while picked.len()<count{if *cursor>=tracks.len(){*cursor=0;}let i=*cursor;*cursor+=1;if local.insert(i){picked.push(i);}}
.vyron-v110-audit/src-tauri/src/production_manager.rs:459:                let mut local=HashSet::new();
.vyron-v110-audit/src-tauri/src/production_manager.rs:469:        if mode=="alphabetical"&&seen.contains(&seq_hash(&ids))&&picked.len()>1{let n=picked.len();picked.rotate_left((project_no+attempt+1)%n);ids=picked.iter().map(|i|tracks[*i].track_id.clone()).collect();}
.vyron-v110-audit/src-tauri/src/production_manager.rs:470:        if !seen.contains(&seq_hash(&ids)){return Ok(picked);}
.vyron-v110-audit/src-tauri/src/production_manager.rs:490:    let mut seen=history.sequence_fingerprints.iter().cloned().collect::<HashSet<_>>();
.vyron-v110-audit/src-tauri/src/production_manager.rs:491:    let mut planned=HashMap::<String,u64>::new(); let mut pool=(0..idx.tracks.len()).collect::<Vec<_>>();shuffle(&mut pool,seed64(&bid));let mut cursor=0usize;
.vyron-v110-audit/src-tauri/src/production_manager.rs:496:        let ids=picks.iter().map(|x|idx.tracks[*x].track_id.clone()).collect::<Vec<_>>();let fp=seq_hash(&ids);seen.insert(fp.clone());
.vyron-v110-audit/src-tauri/src/production_manager.rs:504:        projects.push(PlanProject{project_id:format!("{:03}",i+1),job_id:link.map(|x|x.job_id.clone()),video_number:link.map(|x|x.number),image_source:image.path.clone(),image_name:format!("image.{}",ext(Path::new(&image.path))),tracks:pt,sequence_fingerprint:fp});
.vyron-v110-audit/src-tauri/src/production_manager.rs:507:    atomic_json(&broot.join("plan.json"),&plan)?;atomic_json(&broot.join("checkpoint.json"),&Checkpoint{completed_projects:0,total_projects:req.project_count,status:"Подготовка".into(),history_applied:false,updated_at:Utc::now().to_rfc3339()})?;
.vyron-v110-audit/src-tauri/src/production_manager.rs:524:        cp.completed_projects=i+1;cp.status="Подготовка".into();cp.updated_at=Utc::now().to_rfc3339();atomic_json(&cp_path,&cp)?;
.vyron-v110-audit/src-tauri/src/production_manager.rs:525:        if let Some(a)=app{let _=a.emit("production-batch-progress",json!({"batchId":plan.batch_id,"completed":i+1,"total":plan.projects.len(),"stage":"files"}));}
.vyron-v110-audit/src-tauri/src/production_manager.rs:531:        manifest.projects.push(ManifestProject{project_id:p.project_id.clone(),job_id:p.job_id.clone(),video_number:p.video_number,folder_path:folder.to_string_lossy().into_owned(),image_path:folder.join(&p.image_name).to_string_lossy().into_owned(),total_duration_sec:tracks.iter().map(|x|x.duration_sec).sum(),tracks,status:"Waiting".into(),sequence_fingerprint:p.sequence_fingerprint.clone()});
.vyron-v110-audit/src-tauri/src/production_manager.rs:536:        for p in &plan.projects{h.sequence_fingerprints.push(p.sequence_fingerprint.clone());for t in &p.tracks{let x=h.tracks.entry(t.track_id.clone()).or_insert(TrackUsage{track_id:t.track_id.clone(),original_path:t.source.clone(),times_used:0,last_used_at:None,batch_ids:Vec::new()});x.times_used+=1;x.last_used_at=Some(Utc::now().to_rfc3339());if !x.batch_ids.contains(&plan.batch_id){x.batch_ids.push(plan.batch_id.clone());}}}
.vyron-v110-audit/src-tauri/src/production_manager.rs:537:        if h.sequence_fingerprints.len()>10000{let n=h.sequence_fingerprints.len()-10000;h.sequence_fingerprints.drain(0..n);}atomic_json(&hp,&h)?;cp.history_applied=true;
.vyron-v110-audit/src-tauri/src/production_manager.rs:545:fn recovery_in_workspace(workspace:&str)->Vec<RecoveryState>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:560:            if actual!=cp.completed_projects{cp.completed_projects=actual;cp.updated_at=Utc::now().to_rfc3339();let _=atomic_json(&broot.join("checkpoint.json"),&cp);}
.vyron-v110-audit/src-tauri/src/production_manager.rs:562:            out.push(RecoveryState{batch_id:plan.batch_id.clone(),channel_id:plan.request.channel_id.clone(),channel_name:plan.request.channel_name.clone(),root_path:broot.to_string_lossy().into_owned(),completed_projects:actual,total_projects:plan.projects.len(),current_project:current,status:cp.status.clone(),updated_at:cp.updated_at.clone(),recoverable:true});
.vyron-v110-audit/src-tauri/src/production_manager.rs:569:pub fn find_production_recovery(workspaces:Vec<String>)->Result<Vec<RecoveryState>,String>{
.vyron-v110-audit/src-tauri/src/production_manager.rs:570:    let mut seen=HashSet::<String>::new();let mut out=Vec::new();
.vyron-v110-audit/src-tauri/src/production_manager.rs:571:    for raw in workspaces{let w=raw.trim();if w.is_empty()||!seen.insert(w.to_string()){continue;}out.extend(recovery_in_workspace(w));}
.vyron-v110-audit/src-tauri/src/production_manager.rs:589:    BatchSummary{batch_id:m.batch_id.clone(),channel_id:m.channel_id.clone(),channel_name:m.channel_name.clone(),created_at:m.created_at.clone(),project_count:m.project_count,tracks_assigned:m.projects.iter().map(|p|p.tracks.len()).sum(),status:s.status.clone(),manifest_path:Path::new(&m.root_path).join("batch.json").to_string_lossy().into_owned(),root_path:m.root_path.clone(),completed_projects:s.projects.iter().filter(|p|p.render_status=="Completed").count(),error_projects:s.projects.iter().filter(|p|p.render_status=="Error").count()}
.vyron-v110-audit/src-tauri/src/production_manager.rs:591:fn request_map(workspace:&str,channel_id:&str)->Result<HashMap<String,String>,String>{Ok(read_json(&requests_path(workspace,channel_id)?))}
.vyron-v110-audit/src-tauri/src/production_manager.rs:592:fn save_request_map(workspace:&str,channel_id:&str,m:&HashMap<String,String>)->Result<(),String>{atomic_json(&requests_path(workspace,channel_id)?,m)}
.vyron-v110-audit/src-tauri/src/production_manager.rs:629:    let(m,_)=load_manifest(&manifest_path)?;let mut items=Vec::new();let mut fps=HashSet::new();
.vyron-v110-audit/src-tauri/src/production_manager.rs:642:        if !fps.insert(p.sequence_fingerprint.clone()){errs.push("повтор музыкальной последовательности".into());}
.vyron-v110-audit/src-tauri/src/production_manager.rs:653:    let(mut m,mp)=load_manifest(&manifest_path)?;let wanted=project_ids.into_iter().collect::<HashSet<_>>();if wanted.is_empty(){return Ok(DeleteResult{deleted_project_ids:Vec::new(),deleted_job_ids:Vec::new(),batch:Some(summary_from(&m,&read_json(Path::new(&m.status_path))))})}
.vyron-v110-audit/src-tauri/src/production_manager.rs:658:    let broot=PathBuf::from(&m.root_path);let plan_path=broot.join("plan.json");if plan_path.exists(){let mut plan:BuildPlan=read_json(&plan_path);plan.projects.retain(|p|!wanted.contains(&p.project_id));plan.request.project_count=plan.projects.len();atomic_json(&plan_path,&plan)?;let cp_path=broot.join("checkpoint.json");let mut cp:Checkpoint=read_json(&cp_path);cp.total_projects=plan.projects.len();cp.completed_projects=cp.completed_projects.min(cp.total_projects);cp.updated_at=Utc::now().to_rfc3339();atomic_json(&cp_path,&cp)?;}
.vyron-v110-audit/src-tauri/src/license.rs:6:// Same owner key hash as ENDLUME Studio. The raw key is never embedded in the app.
.vyron-v110-audit/src-tauri/src/license.rs:7:const OWNER_HASH:&str="4b5631d4a5b7018be7c5237994ad4ba463a4fc1df33165beab0fedc901733ef9";
.vyron-v110-audit/src-tauri/src/license.rs:13:pub fn activate_license(app:AppHandle,key:String)->Result<Value,String>{let key=key.trim();if key.is_empty(){return Err("Введите ключ VYRON".into())}let digest=hex::encode(Sha256::digest(key.as_bytes()));if digest!=OWNER_HASH{return Err("Ключ не найден. Используй тот же ключ владельца, что и в ENDLUME Studio.".into())}let v=json!({"valid":true,"type":"owner-lifetime","expiresAt":null,"maskedKey":mask(key),"activatedAt":chrono::Utc::now()});let p=file(&app)?;let tmp=p.with_extension("tmp");fs::write(&tmp,serde_json::to_vec_pretty(&v).map_err(|e|e.to_string())?).map_err(|e|e.to_string())?;if p.exists(){let _=fs::remove_file(&p);}fs::rename(tmp,p).map_err(|e|e.to_string())?;Ok(v)}
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:27:    let mut seq=HashSet::new();for p in &m.projects{let dir=Path::new(&p.folder_path);let entries=fs::read_dir(dir).unwrap().filter_map(Result::ok).collect::<Vec<_>>();assert_eq!(entries.len(),16);assert!(entries.iter().all(|e|e.file_type().unwrap().is_file()));assert_eq!(p.tracks.len(),15);assert!(seq.insert(p.sequence_fingerprint.clone()));}
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:39:    let(ws,cid,name)=fixture(30,100);let plan=plan_build(&request(&ws,&cid,&name,30,15,"no-repeat",false)).unwrap();let mut seq=HashSet::new();let mut use_count=HashMap::<String,usize>::new();
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:40:    for p in &plan.projects{assert!(seq.insert(p.sequence_fingerprint.clone()));let mut local=HashSet::new();for t in &p.tracks{assert!(local.insert(t.track_id.clone()));*use_count.entry(t.track_id.clone()).or_insert(0)+=1;}}
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:61:    let image=root.join("download.png");fs::write(&image,Vec::<u8>::new()).unwrap();let mut pending=HashMap::<String,ImportProbe>::new();
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:62:    assert!(!import_candidate_ready(&image,&mut pending));
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:64:    assert!(!import_candidate_ready(&image,&mut pending));
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:65:    assert!(import_candidate_ready(&image,&mut pending));
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:70:fn acceptance_persisted_active_import_is_not_reported_alive_without_runtime_watcher(){
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:81:fn acceptance_alphabetical_mode_continues_and_sequences_differ(){let(ws,cid,name)=fixture(8,40);let plan=plan_build(&request(&ws,&cid,&name,8,10,"alphabetical",false)).unwrap();let mut seq=HashSet::new();for p in &plan.projects{assert_eq!(p.tracks.len(),10);assert!(seq.insert(p.sequence_fingerprint.clone()));}assert_eq!(seq.len(),8);cleanup(&ws);}
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:94:    let mut startup=HashSet::new();startup.insert(existing.clone());
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:128:    let cp_path=root.join("checkpoint.json");let mut cp:Checkpoint=read_json(&cp_path);cp.completed_projects=1;cp.updated_at=Utc::now().to_rfc3339();atomic_json(&cp_path,&cp).unwrap();
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:130:    let rows=find_production_recovery(vec![ws.to_string_lossy().into_owned()]).unwrap();assert_eq!(rows.len(),1);assert_eq!(rows[0].completed_projects,1);assert_eq!(rows[0].current_project,"002");
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:132:    assert!(find_production_recovery(vec![ws.to_string_lossy().into_owned()]).unwrap().is_empty());cleanup(&ws);
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:136:fn acceptance_recovery_scan_never_creates_missing_external_mount(){
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:137:    let missing=std::env::temp_dir().join(format!("vyron-missing-recovery-{}",Uuid::new_v4())).join("not-mounted");assert!(!missing.exists());
.vyron-v110-audit/src-tauri/src/production_manager_tests.rs:138:    let rows=find_production_recovery(vec![missing.to_string_lossy().into_owned()]).unwrap();assert!(rows.is_empty());assert!(!missing.exists());

## package scripts and dependencies
```json
{
  "scripts": {
    "dev": "vite --port 1421",
    "build": "tsc && vite build",
    "test": "vitest run",
    "tauri": "tauri",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.0",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "@tauri-apps/plugin-opener": "^2.5.0",
    "@tauri-apps/plugin-process": "^2.3.0",
    "@tauri-apps/plugin-updater": "^2.9.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zustand": "^5.0.7",
    "@tauri-apps/plugin-notification": "^2.3.3",
    "mammoth": "1.12.2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
```
